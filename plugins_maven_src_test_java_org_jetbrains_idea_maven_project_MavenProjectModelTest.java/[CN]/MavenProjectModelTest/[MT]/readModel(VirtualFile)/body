{
  model.read(Arrays.asList(files),Collections.<VirtualFile,Module>emptyMap(),Collections.<String>emptyList(),getMavenCoreSettings(),getMavenImporterSettings(),new MavenProcess(new EmptyProgressIndicator()));
}
