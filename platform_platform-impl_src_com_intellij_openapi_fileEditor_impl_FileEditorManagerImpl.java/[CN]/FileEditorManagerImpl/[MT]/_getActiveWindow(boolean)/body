{
  return getActiveSplitters(now).subResult(new Function<EditorsSplitters,EditorWindow>(){
    @Override public EditorWindow fun(    EditorsSplitters splitters){
      return splitters.getCurrentWindow();
    }
  }
);
}
