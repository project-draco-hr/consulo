{
  if (event instanceof MouseEvent && ((MouseEvent)event).isShiftDown()) {
    return true;
  }
  return event instanceof KeyEvent && ((KeyEvent)event).getKeyCode() == KeyEvent.VK_ENTER && ((KeyEvent)event).isShiftDown();
}
