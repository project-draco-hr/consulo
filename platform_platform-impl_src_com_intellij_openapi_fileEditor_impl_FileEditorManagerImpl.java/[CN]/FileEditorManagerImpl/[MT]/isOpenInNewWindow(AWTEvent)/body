{
  if (event instanceof MouseEvent && ((MouseEvent)event).isShiftDown()) {
    return true;
  }
  if (event instanceof KeyEvent && ((KeyEvent)event).getKeyCode() == KeyEvent.VK_ENTER && ((KeyEvent)event).isShiftDown()) {
    return true;
  }
  return false;
}
