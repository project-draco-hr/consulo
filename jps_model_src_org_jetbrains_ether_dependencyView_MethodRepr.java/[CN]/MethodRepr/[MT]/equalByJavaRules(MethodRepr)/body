{
  return new Predicate(){
    @Override public boolean satisfy(    MethodRepr that){
      if (me == that)       return true;
      return me.myName == that.myName && Arrays.equals(me.myArgumentTypes,that.myArgumentTypes);
    }
  }
;
}
