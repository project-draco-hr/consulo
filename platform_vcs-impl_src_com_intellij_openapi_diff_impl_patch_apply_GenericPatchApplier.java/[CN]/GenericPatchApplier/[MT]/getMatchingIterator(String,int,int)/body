{
  return new CompositeIterator<Integer>(Arrays.<Iterator<Integer>>asList(new WalkingIterator(line,originalStart,maxWalkFromBinding,true),new WalkingIterator(line,originalStart,maxWalkFromBinding,false)));
}
