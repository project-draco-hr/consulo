{
  if (myValue == null || myValue.getValue() != null) {
    final String value=myTfWithButton.getText();
    if (myValue == null && value.length() == 0) {
      return null;
    }
 else {
      return StringDescriptor.create(value);
    }
  }
 else {
    return myValue;
  }
}
