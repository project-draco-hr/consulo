{
  PsiFile[] files=FilenameIndex.getFilesByName(project,getOriginalScriptName(runtimeName) + "." + GantFileType.DEFAULT_EXTENSION,GlobalSearchScope.allScope(project));
  if (files.length == 1)   return files[0];
  return null;
}
