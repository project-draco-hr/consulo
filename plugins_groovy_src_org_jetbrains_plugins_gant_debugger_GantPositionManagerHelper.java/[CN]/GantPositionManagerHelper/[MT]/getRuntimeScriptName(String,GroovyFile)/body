{
  final String version=GantConfigUtils.getInstance().getSDKVersion(GantConfigUtils.getInstance().getSDKInstallPath(ModuleUtil.findModuleForPsiElement(groovyFile)));
  if (version.compareToIgnoreCase("1.5") >= 0) {
    return originalName;
  }
  return originalName + GANT_SUFFIX;
}
