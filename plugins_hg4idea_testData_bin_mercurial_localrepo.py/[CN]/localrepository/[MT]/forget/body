def forget(self, list):
    wlock = self.wlock()
    try:
        for f in list:
            if (self.dirstate[f] != 'a'):
                self.ui.warn((_('%s not added!\n') % f))
            else:
                self.dirstate.forget(f)
    finally:
        wlock.release()
