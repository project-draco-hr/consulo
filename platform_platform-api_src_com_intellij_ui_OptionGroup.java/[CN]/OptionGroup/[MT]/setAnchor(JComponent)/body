{
  this.anchor=anchor;
  for (  Object o : myOptions) {
    if (o instanceof Pair && ((Pair)o).getFirst() instanceof AnchorableComponent) {
      ((AnchorableComponent)((Pair)o).getFirst()).setAnchor(anchor);
    }
  }
}
