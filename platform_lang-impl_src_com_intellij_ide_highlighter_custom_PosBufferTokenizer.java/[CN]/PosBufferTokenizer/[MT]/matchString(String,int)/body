{
  for (int i=from; i < s.length(); i++) {
    int c=peekc;
    if (peekc == NEED_CHAR) {
      c=read();
    }
    if (c != s.charAt(i)) {
      peekc=c;
      return false;
    }
    peekc=NEED_CHAR;
  }
  return true;
}
