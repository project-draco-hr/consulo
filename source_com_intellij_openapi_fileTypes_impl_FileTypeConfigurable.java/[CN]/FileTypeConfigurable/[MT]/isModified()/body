{
  return !myTempExtension2TypeMap.equals(myManager.getExtensionMap()) || !myTempFileTypes.equals(new HashSet(Arrays.asList(myManager.getRegisteredFileTypes()))) || !myOriginalToEditedMap.isEmpty();
}
