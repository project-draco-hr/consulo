{
synchronized (this) {
    if (size() == 0) {
      myFirstIndex=0;
      myLastIndex=0;
    }
    System.arraycopy(myReadBuffer,0,myBuffer,myLastIndex,result);
    myLastIndex+=result;
  }
  notify();
}
