{
  PsiVariable previousVariable=findPreviousVariable();
  return variable != null && variable.isValid() && variable instanceof PsiLocalVariable && previousVariable != null && Comparing.equal(previousVariable.getType(),variable.getType()) && identifier != null && identifier.isValid() && variable.getManager().isInProject(variable);
}
