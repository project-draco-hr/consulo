{
  final String dirName=indexName.toString().toLowerCase(Locale.US);
  final File indexDir=indexName instanceof StubIndexKey ? new File(getIndexRootDir(StubUpdatingIndex.INDEX_ID),dirName) : new File(PathManager.getIndexRoot(),dirName);
  indexDir.mkdirs();
  return indexDir;
}
