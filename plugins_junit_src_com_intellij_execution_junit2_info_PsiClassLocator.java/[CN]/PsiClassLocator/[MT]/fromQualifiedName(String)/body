{
  final int lastDot=name.lastIndexOf('.');
  if (lastDot == -1 || lastDot == name.length())   return new PsiClassLocator(name,"");
 else   return new PsiClassLocator(name.substring(lastDot + 1),name.substring(0,lastDot));
}
