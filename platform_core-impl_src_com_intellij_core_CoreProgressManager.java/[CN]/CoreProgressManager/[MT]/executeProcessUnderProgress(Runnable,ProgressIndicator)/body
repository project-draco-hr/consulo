{
  ProgressIndicator old=null;
  if (progress != null) {
    old=getProgressIndicator();
    myIndicator.set(progress);
  }
  try {
    process.run();
  }
  finally {
    if (progress != null) {
      myIndicator.set(old);
    }
  }
}
