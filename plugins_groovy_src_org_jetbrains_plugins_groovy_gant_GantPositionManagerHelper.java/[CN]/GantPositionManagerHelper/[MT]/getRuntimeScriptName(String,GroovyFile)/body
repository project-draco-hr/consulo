{
  final String version=GantUtils.getGantVersion(GantUtils.getSDKInstallPath(ModuleUtil.findModuleForPsiElement(groovyFile),groovyFile.getProject()));
  if (version.compareToIgnoreCase("1.5") >= 0) {
    return originalName;
  }
  return originalName + GANT_SUFFIX;
}
