{
  Project project=holder.getProject();
  ProjectFileIndex fileIndex=ProjectRootManager.getInstance(project).getFileIndex();
  return file != null && fileIndex.isInContent(file) && fileMatcher(file,fileIndex) && matchesModule(myModuleGroupPattern,myModulePattern,file,fileIndex);
}
