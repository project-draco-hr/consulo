{
  final JpsAndroidDexCompilerConfiguration configuration=JpsAndroidExtensionService.getInstance().getDexCompilerConfiguration(element);
  if (configuration != null) {
    XmlSerializer.serializeInto(((JpsAndroidDexCompilerConfigurationImpl)configuration).getState(),componentTag,FILTERS);
  }
}
