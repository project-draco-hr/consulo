{
  myFileSystem=fileSystem;
  myBasePath=path;
  NewVirtualFile localJarFile=(NewVirtualFile)LocalFileSystem.getInstance().findFileByPath(path);
  if (localJarFile != null) {
    localJarFile.markDirty();
    localJarFile.refresh(true,false);
  }
}
