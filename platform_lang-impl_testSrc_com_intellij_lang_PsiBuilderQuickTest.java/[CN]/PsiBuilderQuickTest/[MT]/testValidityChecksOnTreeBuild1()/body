{
  doFailTest("aa",new Parser(){
    @Override public void parse(    PsiBuilder builder){
      while (!builder.eof())       builder.advanceLexer();
    }
  }
,"Assertion failed: Parser produced no markers. Text:\naa");
}
