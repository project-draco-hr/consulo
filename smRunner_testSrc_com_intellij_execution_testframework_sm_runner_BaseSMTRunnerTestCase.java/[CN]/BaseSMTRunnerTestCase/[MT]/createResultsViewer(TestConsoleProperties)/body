{
  final ExecutionEnvironment environment=new ExecutionEnvironment();
  return new SMTestRunnerResultsForm(consoleProperties.getConfiguration(),consoleProperties,environment.getRunnerSettings(),environment.getConfigurationSettings());
}
