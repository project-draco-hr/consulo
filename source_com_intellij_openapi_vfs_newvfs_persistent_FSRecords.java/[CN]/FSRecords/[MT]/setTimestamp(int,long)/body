{
  w.lock();
  try {
    incModCount(id);
    myConnection.getRecords().putLong(id * RECORD_SIZE + TIMESTAMP_OFFSET,value);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    w.unlock();
  }
}
