{
  w.lock();
  try {
    incModCount(id);
    myConnection.getRecords().putLong(id * RECORD_SIZE + CRC_OFFSET,crc);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    w.unlock();
  }
}
