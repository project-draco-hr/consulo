{
  r.lock();
  try {
    return myConnection.getRecords().getLong(id * RECORD_SIZE + CRC_OFFSET);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    r.unlock();
  }
}
