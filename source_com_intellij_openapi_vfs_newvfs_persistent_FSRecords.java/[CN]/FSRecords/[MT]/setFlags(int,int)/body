{
  w.lock();
  try {
    incModCount(id);
    myConnection.getRecords().putInt(id * RECORD_SIZE + FLAGS_OFFSET,flags);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    w.unlock();
  }
}
