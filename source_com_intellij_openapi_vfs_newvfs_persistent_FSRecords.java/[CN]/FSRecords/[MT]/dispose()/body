{
  w.lock();
  try {
    if (myConnection != null) {
      myConnection.dispose();
      myConnection=null;
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    w.unlock();
  }
}
