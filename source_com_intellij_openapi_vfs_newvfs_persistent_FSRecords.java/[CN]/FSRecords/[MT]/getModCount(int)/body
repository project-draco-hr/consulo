{
  r.lock();
  try {
    return myConnection.getRecords().getInt(id * RECORD_SIZE + MODCOUNT_OFFSET);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    r.unlock();
  }
}
