{
  w.lock();
  try {
    incModCount(id);
    myConnection.getRecords().putInt(id * RECORD_SIZE + LENGTH_OFFSET,len);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    w.unlock();
  }
}
