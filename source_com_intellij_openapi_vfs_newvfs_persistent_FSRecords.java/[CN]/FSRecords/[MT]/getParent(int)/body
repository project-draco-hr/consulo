{
  r.lock();
  try {
    return myConnection.getRecords().getInt(id * RECORD_SIZE + PARENT_OFFSET);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    r.unlock();
  }
}
