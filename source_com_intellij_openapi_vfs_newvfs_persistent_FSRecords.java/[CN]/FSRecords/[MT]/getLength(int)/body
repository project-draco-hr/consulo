{
  r.lock();
  try {
    return myConnection.getRecords().getInt(id * RECORD_SIZE + LENGTH_OFFSET);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    r.unlock();
  }
}
