{
  r.lock();
  try {
    return myConnection.getRecords().getLong(id * RECORD_SIZE + LENGTH_OFFSET);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    r.unlock();
  }
}
