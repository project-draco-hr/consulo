{
  r.lock();
  try {
    return myConnection.getRecords().getInt(HEADER_GLOBAL_MODCOUNT_OFFSET);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    r.unlock();
  }
}
