{
  DeleteTagDialog deleteTagDialog=new DeleteTagDialog(collectFiles(context),context.getProject());
  deleteTagDialog.show();
  if (!deleteTagDialog.isOK())   return CvsHandler.NULL;
  return CommandCvsHandler.createRemoveTagAction(context.getSelectedFiles(),deleteTagDialog.getTagName());
}
