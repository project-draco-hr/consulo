{
  PsiType type=expression.getType();
  if (type != null && TypeConversionUtil.isPrimitiveWrapper(type.getCanonicalText())) {
    addInstruction(new MethodCallInstruction(expression,myFactory));
  }
}
