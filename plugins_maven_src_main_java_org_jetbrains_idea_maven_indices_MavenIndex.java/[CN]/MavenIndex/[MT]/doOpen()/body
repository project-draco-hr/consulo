{
  try {
    if (myDataDirName == null) {
      myDataDirName=findAvailableDataDirName();
    }
    myData=openData(myDataDirName);
  }
 catch (  Exception e) {
    cleanupBrokenData();
    throw e;
  }
}
