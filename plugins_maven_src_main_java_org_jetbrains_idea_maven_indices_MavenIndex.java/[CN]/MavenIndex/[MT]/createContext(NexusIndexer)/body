{
  File targetDir=getContextDir();
  String id=targetDir.exists() ? null : myDir.getName();
  return indexer.addIndexingContext(id,id,getRepositoryFile(),getContextDir(),getRepositoryUrl(),null,NexusIndexer.FULL_INDEX);
}
