{
  String indexId=myDir.getName() + "-" + suffix;
  String repoId=contextDir.exists() ? null : myDir.getName();
  return myIndexer.createIndex(indexId,repoId,getRepositoryFile(),getRepositoryUrl(),contextDir);
}
