{
  String indexId=myDir.getName() + "-" + suffix;
  String repoId=contextDir.exists() ? null : myDir.getName();
  return myIndexer.addIndexingContext(indexId,repoId,getRepositoryFile(),contextDir,getRepositoryUrl(),null,NexusIndexer.FULL_INDEX);
}
