{
  final List<String> result=new ArrayList<String>();
  result.add("view");
  result.add("merge");
  result.addAll(AndroidDomUtil.removeUnambigiousNames(AndroidDomExtender.getViewClassMap(facet)));
  result.remove("View");
  return result;
}
