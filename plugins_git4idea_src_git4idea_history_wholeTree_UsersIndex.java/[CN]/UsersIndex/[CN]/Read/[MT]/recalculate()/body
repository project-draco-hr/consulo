{
  myKeys.clear();
  myKeys.addAll(myIndex.keySet());
  Collections.sort(myKeys);
  if (myIsDirty) {
    int i=0;
    for (    String key : myKeys) {
      myIndex.get(key).set(i);
      ++i;
    }
    myIsDirty=false;
  }
}
