{
  String name=(String)value;
  if (index < 9) {
    name=(index + 1) + ". " + name;
  }
 else   if (index == 9) {
    name=0 + ". " + name;
  }
 else {
    name=(char)('A' + index - 10) + ". " + name;
  }
  append(name,myAttributes);
}
