{
  final RuntimeException[] exception=new RuntimeException[1];
  Runnable executeUndoOrRedoAction=new Runnable(){
    public void run(){
      try {
        if (isUndoInProgress()) {
          myLastMerger.undoOrRedo(editor,true);
        }
 else {
          myLastMerger.undoOrRedo(editor,false);
        }
      }
 catch (      RuntimeException ex) {
        exception[0]=ex;
      }
 finally {
        myCurrentOperationState=NONE;
      }
    }
  }
;
  CommandProcessor.getInstance().executeCommand(myProject,executeUndoOrRedoAction,isUndoInProgress() ? "Undo" : "Redo",null,myLastMerger.getUndoConfirmationPolicy());
  if (exception[0] != null)   throw exception[0];
}
