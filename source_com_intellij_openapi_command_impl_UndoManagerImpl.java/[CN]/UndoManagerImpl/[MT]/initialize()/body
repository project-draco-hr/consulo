{
  Runnable initAction=new Runnable(){
    public void run(){
      runStartupActivity();
    }
  }
;
  if (myProject != null) {
    myStartupManager.registerStartupActivity(initAction);
  }
 else {
    initAction.run();
  }
}
