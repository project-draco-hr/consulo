{
  try {
    while (true) {
      String line=readLine();
      if (line == null)       return;
      if (line.contains(VERSION)) {
        myVersionString[0]=line;
      }
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
