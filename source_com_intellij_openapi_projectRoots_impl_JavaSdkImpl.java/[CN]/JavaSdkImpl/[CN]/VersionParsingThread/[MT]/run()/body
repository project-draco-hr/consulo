{
  try {
    while (true) {
      String line=readLine();
      if (line == null)       return;
      if (line.indexOf(VERSION) >= 0) {
        myVersionString[0]=line;
      }
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
