{
  final TestInfo info=testProxy.getInfo();
  if (info.getComment().length() == 0)   return false;
  final TestProxy parent=testProxy.getParent();
  return parent == null || !fullLabelOf(parent.getInfo()).equals(info.getComment());
}
