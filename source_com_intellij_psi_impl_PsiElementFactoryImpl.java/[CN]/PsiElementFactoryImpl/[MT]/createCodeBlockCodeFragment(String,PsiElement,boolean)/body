{
  final PsiCodeFragmentImpl result=new PsiCodeFragmentImpl(myManager,ElementType.STATEMENTS,isPhysical,"fragment.java",text.toCharArray(),0,text.length());
  result.setContext(context);
  return result;
}
