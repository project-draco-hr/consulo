{
  final OSProcessHandler processHandler=ansiColoring ? new ColoredProcessHandler(commandLine) : new DefaultJavaProcessHandler(commandLine);
  ProcessTerminatedListener.attach(processHandler);
  return processHandler;
}
