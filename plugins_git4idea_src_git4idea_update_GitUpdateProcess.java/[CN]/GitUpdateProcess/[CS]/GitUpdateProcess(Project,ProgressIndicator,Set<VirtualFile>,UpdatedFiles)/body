{
  myProject=project;
  myRoots=roots;
  myUpdatedFiles=updatedFiles;
  myProgressIndicator=progressIndicator;
  myProjectManager=ProjectManagerEx.getInstanceEx();
  myMerger=new GitMerger(myProject);
  mySaver=GitChangesSaver.getSaver(myProject,myProgressIndicator,"Uncommitted changes before update operation at " + DateFormatUtil.formatDateTime(Clock.getTime()));
  myGeneralSettings=GeneralSettings.getInstance();
  myUpdaters=new HashMap<VirtualFile,GitUpdater>();
  myRootsToSave=new HashSet<VirtualFile>(1);
}
