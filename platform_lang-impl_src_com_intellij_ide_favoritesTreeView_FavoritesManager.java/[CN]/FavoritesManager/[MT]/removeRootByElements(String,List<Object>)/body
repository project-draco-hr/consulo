{
  return findListToRemoveFrom(name,elements,new Convertor<Object,AbstractUrl>(){
    @Override public AbstractUrl convert(    Object o){
      return createUrlByElement(o,myProject);
    }
  }
);
}
