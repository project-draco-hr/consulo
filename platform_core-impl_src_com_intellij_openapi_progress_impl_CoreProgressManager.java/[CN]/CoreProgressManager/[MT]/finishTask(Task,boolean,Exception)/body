{
  try {
    if (exception != null) {
      task.onError(exception);
    }
 else     if (canceled) {
      task.onCancel();
    }
 else {
      task.onSuccess();
    }
  }
  finally {
    task.onFinished();
  }
}
