{
  FormatProcessor processor=new FormatProcessor(docModel,rootBlock,settings,indentOptions,affectedRanges,interestingOffset,FormattingProgressIndicator.EMPTY);
  while (!processor.iteration())   ;
  return processor;
}
