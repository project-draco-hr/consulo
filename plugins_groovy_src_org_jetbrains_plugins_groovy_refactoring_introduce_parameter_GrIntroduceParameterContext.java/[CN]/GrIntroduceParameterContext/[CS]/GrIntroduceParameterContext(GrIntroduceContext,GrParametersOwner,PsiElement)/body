{
  super(context.getProject(),context.getEditor(),context.getExpression(),context.getVar(),context.getOccurrences(),context.getScope());
  this.toReplaceIn=toReplaceIn;
  this.toSearchFor=toSearchFor;
}
