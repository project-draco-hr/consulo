{
  RootEntry original=new RootEntry();
  RootEntry result=original.apply(cs(new CreateFileChange(p("file"),null,null)));
  assertFalse(original.hasEntry(p("file")));
  assertTrue(result.hasEntry(p("file")));
}
