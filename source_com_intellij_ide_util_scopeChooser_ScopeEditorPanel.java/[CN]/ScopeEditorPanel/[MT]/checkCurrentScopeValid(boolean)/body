{
  if (myCurrentScope == null) {
    if (showMessage) {
      Messages.showErrorDialog(myPanel,IdeBundle.message("error.correct.pattern.syntax.errors.first"),IdeBundle.message("title.syntax.error"));
    }
    return false;
  }
  return true;
}
