{
  if (myLoaded) {
    return;
  }
  ApplicationManager.getApplication().runReadAction(new Runnable(){
    public void run(){
synchronized (LOCK) {
        if (myLoaded) {
          return;
        }
        loadTemplates();
      }
      myLoaded=true;
    }
  }
);
}
