{
  LOG.assertTrue(symbol != null);
  if (symbol.length() == 0) {
    return -1;
  }
  try {
    return myTrie.addString(symbol);
  }
 catch (  IOException e) {
    throw new CacheCorruptedException(e);
  }
}
