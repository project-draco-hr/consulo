{
  if (myRepositoryBrowser == null) {
    myRepositoryBrowser=new RepositoryBrowserComponent(SvnVcs17.getInstance(myProject));
    myRepositoryBrowser.setPreferredSize(new Dimension(300,300));
  }
  return myRepositoryBrowser;
}
