{
  updateProjectPom("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<dependencies>"+ "  <dependency>"+ "    <scope><error>xxx</error></scope>"+ "  </dependency>"+ "</dependencies>");
  checkHighlighting(myProjectPom);
}
