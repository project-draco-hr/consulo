{
  final PsiClass testCaseClass=PsiManager.getInstance(project).findClass(TESTCASE_CLASS,scope);
  if (testCaseClass == null)   throw new NoJUnitException(scope.getDisplayName());
  return testCaseClass;
}
