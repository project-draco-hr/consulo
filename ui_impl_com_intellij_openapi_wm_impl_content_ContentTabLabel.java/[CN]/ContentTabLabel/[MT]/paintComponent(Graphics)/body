{
  if (!isSelected() && myUi.isToDrawTabs()) {
    g.translate(0,TitlePanel.STRUT);
  }
  super.paintComponent(g);
  if (!isSelected() && myUi.isToDrawTabs()) {
    g.translate(0,-TitlePanel.STRUT);
  }
}
