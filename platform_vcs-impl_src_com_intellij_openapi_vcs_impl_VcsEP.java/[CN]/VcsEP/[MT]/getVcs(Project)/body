{
synchronized (LOCK) {
    if (myVcs != null) {
      return myVcs;
    }
  }
  AbstractVcs vcs=getInstance(project,vcsClass);
synchronized (LOCK) {
    if (myVcs == null && vcs != null) {
      vcs.setupEnvironments();
      myVcs=vcs;
    }
    return myVcs;
  }
}
