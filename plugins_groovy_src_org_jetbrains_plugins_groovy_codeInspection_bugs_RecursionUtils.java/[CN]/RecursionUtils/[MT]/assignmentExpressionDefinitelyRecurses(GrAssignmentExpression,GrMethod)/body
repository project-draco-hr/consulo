{
  final GrExpression rhs=assignmentExpression.getRValue();
  final GrExpression lhs=assignmentExpression.getLValue();
  return expressionDefinitelyRecurses(rhs,method) || expressionDefinitelyRecurses(lhs,method);
}
