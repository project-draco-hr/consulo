{
  String result=version != null ? MessageFormat.format("{0}:{1}:{2}",groupId,artifactId,version) : MessageFormat.format("{0}:{1}",groupId,artifactId);
  return classifier == null ? result : result + ":" + classifier;
}
