{
  String selectedVersion=baseVersion == null ? version : baseVersion;
  String result=selectedVersion != null ? MessageFormat.format("{0}:{1}:{2}",groupId,artifactId,selectedVersion) : MessageFormat.format("{0}:{1}",groupId,artifactId);
  return classifier == null ? result : result + ":" + classifier;
}
