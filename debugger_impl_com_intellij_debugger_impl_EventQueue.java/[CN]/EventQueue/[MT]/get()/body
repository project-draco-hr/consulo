{
  E event=getEvent();
  while (event == null) {
    myLock.lock();
    try {
      myEventsAvailable.await();
    }
 catch (    InterruptedException ignored) {
    }
 finally {
      myLock.unlock();
    }
    event=getEvent();
  }
  myCurrentEvent=event;
  return event;
}
