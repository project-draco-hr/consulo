{
  Rectangle bounds=getPatchedBounds(GraphicsEnvironment.getLocalGraphicsEnvironment().getDefaultScreenDevice().getDefaultConfiguration());
  window.setLocation(bounds.x + (bounds.width - window.getWidth()) / 2,bounds.y);
}
