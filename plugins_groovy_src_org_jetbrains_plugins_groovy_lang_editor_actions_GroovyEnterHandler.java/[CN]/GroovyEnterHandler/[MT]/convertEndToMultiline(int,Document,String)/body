{
  if (caretOffset < fileText.length() && fileText.charAt(caretOffset) == '\'' || caretOffset > 0 && fileText.charAt(caretOffset - 1) == '\'') {
    document.insertString(caretOffset,"''");
  }
 else {
    document.insertString(caretOffset,"'''");
  }
}
