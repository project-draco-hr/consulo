{
  for (  PsiClass anInterface : psiClass.getInterfaces()) {
    if (anInterface.getQualifiedName().matches("org.testng.(IReporter|ITestListener|internal.annotations.IAnnotationTransformer)"))     return true;
  }
  return false;
}
