{
  for (  PsiClass anInterface : psiClass.getInterfaces()) {
    if (anInterface.getQualifiedName().matches("org.testng.(IReporter|ITestListener|IMethodInterceptor|IInvokedMethodListener|internal.annotations.IAnnotationTransformer)"))     return true;
  }
  return false;
}
