{
  final XmlFile newFile=CreateResourceFileAction.createFileResource(myFacet,myResourceType,myResourceName + ".xml",null,null,myChooseResName,null);
  if (newFile != null) {
    UndoUtil.markPsiFileForUndo(myFile);
  }
}
