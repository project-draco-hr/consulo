{
  DataOutputStream stream=autoDetectedAttribute.writeAttribute(file);
  try {
    try {
      stream.writeByte(flags);
    }
  finally {
      stream.close();
    }
  }
 catch (  IOException e) {
    LOG.error(e);
  }
}
