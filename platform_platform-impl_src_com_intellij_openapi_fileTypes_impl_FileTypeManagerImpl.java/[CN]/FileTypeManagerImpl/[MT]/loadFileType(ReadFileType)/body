{
  ExternalInfo info=mySchemesManager.isShared(readFileType) ? readFileType.getExternalInfo() : null;
  return loadFileType(readFileType.getElement(),false,info,true,readFileType.getExternalInfo().getCurrentFileName());
}
