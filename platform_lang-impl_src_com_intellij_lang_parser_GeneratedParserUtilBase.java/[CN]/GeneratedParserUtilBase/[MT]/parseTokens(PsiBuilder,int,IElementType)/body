{
  PsiBuilder.Marker marker_=builder_.mark();
  boolean result_=consumeTokens(builder_,pin_,tokens_);
  if (!result_) {
    marker_.rollbackTo();
  }
 else {
    marker_.drop();
  }
  return result_;
}
