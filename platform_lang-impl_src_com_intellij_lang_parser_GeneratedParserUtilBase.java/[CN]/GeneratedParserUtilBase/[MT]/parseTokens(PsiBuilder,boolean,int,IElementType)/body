{
  PsiBuilder.Marker marker_=builder_.mark();
  boolean result_=consumeTokens(builder_,smart,pin_,tokens);
  if (!result_) {
    marker_.rollbackTo();
  }
 else {
    marker_.drop();
  }
  return result_;
}
