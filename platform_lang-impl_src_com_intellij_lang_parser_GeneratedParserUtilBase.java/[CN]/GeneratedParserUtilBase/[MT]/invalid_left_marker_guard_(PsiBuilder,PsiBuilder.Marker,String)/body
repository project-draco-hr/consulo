{
  boolean goodMarker=marker_ != null;
  if (!goodMarker)   return false;
  ErrorState state=ErrorState.get(builder_);
  Frame frame=state.frameStack.peekLast();
  return frame == null || frame.errorReportedAt <= builder_.rawTokenIndex();
}
