{
  JavaRefactoringSettings.getInstance().TURN_REFS_TO_SUPER_PREVIEW_USAGES=isPreviewUsages();
  final TurnRefsToSuperProcessor processor=new TurnRefsToSuperProcessor(getProject(),mySubClass,getSuperClass(),isUseInInstanceOf());
  invokeRefactoring(processor);
}
