{
  String scope=myArtifact.getScope();
  setNameAndTooltip(myArtifact.getDisplayStringForLibraryName(),null,MavenConstants.SCOPE_COMPILE.equals(scope) ? null : scope);
  setNodeErrorLevel(myArtifact.isResolved() ? ErrorLevel.NONE : ErrorLevel.ERROR);
}
