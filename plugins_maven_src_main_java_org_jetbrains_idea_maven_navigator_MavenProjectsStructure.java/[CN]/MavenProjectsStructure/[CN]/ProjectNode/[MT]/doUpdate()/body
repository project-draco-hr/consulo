{
  String hint=null;
  if (myProjectsManager.findAggregator(myMavenProject) == null && myProjectsManager.getProjects().size() > myProjectsManager.getRootProjects().size()) {
    hint="aggregator";
  }
  setNameAndTooltip(getName(),myTooltipCache,hint);
}
