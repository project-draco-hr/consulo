{
  updateNameAndDescription();
  if (getTreeViewSettings().showIgnored) {
    updateSubTree();
  }
 else {
    if (on) {
      removeFromParent();
    }
 else {
      myRoot.addToStructure(this);
    }
  }
}
