{
  final ModifiableRootModel model=getModel();
  final Sdk oldSdk=model.getSdk();
  if (newJDK != null) {
    model.setSdk(newJDK);
  }
 else {
    model.inheritSdk();
  }
  myPanel.forceInitFromModel();
  flushChangesToModel();
  return oldSdk;
}
