{
  if (myWidthInColumns < 0) {
    Document document=myEditor.getDocument();
    myWidthInColumns=myRepresentationHelper.toVisualColumnSymbolsNumber(document.getCharsSequence(),myFoldRegion.getStartOffset(),myFoldRegion.getEndOffset(),myStartX);
  }
  return myWidthInColumns;
}
