{
  return myMethodCall != null && myMethodCall.getElement() != null && myMethodCall.getElement().isValid() && getMethodExpression(myMethodCall.getElement()) != null && getMethodExpression(myMethodCall.getElement()).getQualifierExpression() == null && file.getManager().isInProject(file) && file.getManager().isInProject(file) && !getCandidates().isEmpty();
}
