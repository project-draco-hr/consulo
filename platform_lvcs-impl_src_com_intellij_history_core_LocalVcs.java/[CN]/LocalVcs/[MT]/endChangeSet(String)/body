{
  writeChanges();
  try {
    myChangeList.endChangeSet(name);
    myChangeSetDepth--;
    if (myChangeSetDepth == 0) {
      myStorage.saveContents();
    }
  }
  finally {
    unwriteChanges();
  }
}
