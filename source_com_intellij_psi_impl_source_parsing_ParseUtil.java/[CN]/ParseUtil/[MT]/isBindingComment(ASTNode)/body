{
  ASTNode prev=node.getTreePrev();
  if (prev != null) {
    if (prev.getElementType() != TokenType.WHITE_SPACE) {
      return false;
    }
 else {
      if (!prev.textContains('\n'))       return false;
    }
  }
  return true;
}
