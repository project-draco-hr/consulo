{
  m.selectLabels(1,1);
  assertTrue(m.canRevert());
  m.selectLabels(2,2);
  assertTrue(m.canRevert());
  m.selectLabels(0,0);
  assertFalse(m.canRevert());
  m.selectLabels(-1,-1);
  assertFalse(m.canRevert());
  m.selectLabels(1,2);
  assertFalse(m.canRevert());
}
