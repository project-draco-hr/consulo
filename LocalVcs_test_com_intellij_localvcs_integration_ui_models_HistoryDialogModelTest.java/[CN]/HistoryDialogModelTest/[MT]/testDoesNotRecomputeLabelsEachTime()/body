{
  vcs.createFile("f",null,null);
  vcs.apply();
  initModelFor("f");
  assertEquals(1,m.getLabels().size());
  vcs.changeFileContent("f",b("bbb"),null);
  vcs.apply();
  assertEquals(1,m.getLabels().size());
}
