{
  DefaultListModel model=new DefaultListModel();
  myList.setModel(model);
  for (Iterator each=myTags.iterator(); each.hasNext(); ) {
    model.addElement(each.next());
  }
  new PopupChooserBuilder(myList).setTitle(com.intellij.CvsBundle.message("list.popup.text.tags")).createPopup().showUnderneathOf(myMoreLabel);
}
