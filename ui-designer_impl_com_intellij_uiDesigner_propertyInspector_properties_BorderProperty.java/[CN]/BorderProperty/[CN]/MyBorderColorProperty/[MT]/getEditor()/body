{
  if (myEditor == null) {
    myEditor=new ColorEditor(myTitleColor ? UIDesignerBundle.message("border.title.editor.title") : UIDesignerBundle.message("border.color.editor.title"));
  }
  return myEditor;
}
