{
  final Iterator<Pair<ASTNode,Integer>> iterator=myOffsets.iterator();
  while (iterator.hasNext()) {
    Pair<ASTNode,Integer> pair=iterator.next();
    if (child == pair.getFirst())     return pair.getSecond().intValue();
  }
  return -1;
}
