{
  return getChildren().length == 0 ? String.valueOf(myId) : StringUtil.join(myChildren,new Function<Node,String>(){
    @Override public String fun(    Node node){
      return node.toString();
    }
  }
,"");
}
