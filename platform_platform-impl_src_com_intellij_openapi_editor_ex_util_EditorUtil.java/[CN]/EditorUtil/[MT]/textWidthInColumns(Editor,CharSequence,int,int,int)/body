{
  int result=0;
  for (int i=start; i < end; i++) {
    if (text.charAt(i) == '\t') {
      result+=tabWidthInColumns(editor,columnOffset + result);
    }
 else {
      result++;
    }
  }
  return result;
}
