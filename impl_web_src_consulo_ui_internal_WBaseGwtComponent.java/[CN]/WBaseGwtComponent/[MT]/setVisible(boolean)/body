{
  makeChange(new Consumer<UIComponent>(){
    @Override public void consume(    UIComponent component){
      myVisible=value;
      putIfNotDefault("visible",myVisible,true,component.getVariables());
    }
  }
);
}
