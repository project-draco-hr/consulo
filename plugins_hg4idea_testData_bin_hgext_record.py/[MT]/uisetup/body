def uisetup(ui):
    try:
        mq = extensions.find('mq')
    except KeyError:
        return
    qcmdtable = {'qrecord': (qrecord, [opt for opt in mq.cmdtable['qnew'][1] if (opt[1] != 'force')], _('hg qrecord [OPTION]... PATCH [FILE]...')), }
    cmdtable.update(qcmdtable)
