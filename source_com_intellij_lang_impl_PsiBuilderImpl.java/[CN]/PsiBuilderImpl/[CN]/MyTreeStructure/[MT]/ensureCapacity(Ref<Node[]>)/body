{
  Node[] old=into.get();
  if (old == null) {
    old=new Node[10];
    into.set(old);
  }
 else   if (count >= old.length) {
    Node[] newStore=new Node[(count * 3) / 2];
    System.arraycopy(old,0,newStore,0,count);
    into.set(newStore);
  }
}
