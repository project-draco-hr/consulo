{
  for (int i=0; i < nodes.size(); i++) {
    Node node=nodes.get(i);
    if (node instanceof Token) {
      final Token token=(Token)node;
      token.myHC=-1;
      myPool.push(token);
    }
  }
}
