{
  lastIdx=Math.min(lastIdx,myLexems.size());
  while (curToken < lastIdx) {
    Token lexem=myLexems.get(curToken++);
    if (lexem.myTokenStart < lexem.myTokenEnd) {
      curNode.addChild(lexem);
    }
  }
  return curToken;
}
