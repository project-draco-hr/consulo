{
  myProduction.add(getCurrentLexem());
  myCurrentLexem++;
  if (myCurrentLexem > myLexems.size()) {
    LOG.assertTrue(!eof());
    final Lexem lxm=advanceOriginalLexer();
    if (lxm == null)     return;
    myLexems.add(lxm);
  }
}
