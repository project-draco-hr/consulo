{
  final T first=first(node);
  if (first == null)   return JBIterable.empty();
  return JBIterable.generate(first,new Function<T,T>(){
    @Override public T fun(    T t){
      return next(t);
    }
  }
);
}
