{
  super(projectName);
  if (previous != null) {
    this.previous=previous;
    this.next=previous.next;
    previous.next=this;
    if (previous.previous == previous) {
      previous.previous=this;
    }
  }
 else {
    this.previous=this;
    this.next=this;
  }
}
