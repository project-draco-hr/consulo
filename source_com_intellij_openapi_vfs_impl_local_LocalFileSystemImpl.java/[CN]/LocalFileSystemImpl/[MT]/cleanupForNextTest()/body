{
  ApplicationManager.getApplication().runWriteAction(new Runnable(){
    public void run(){
      refresh(false);
    }
  }
);
  myRoots.clear();
  myDirtyFiles.clear();
  myDeletedFiles.clear();
}
