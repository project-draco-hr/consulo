{
  if (isCaseSensitive()) {
    return super.getCanonicallyCasedName(file);
  }
  try {
    return convertToIOFile(file).getCanonicalFile().getName();
  }
 catch (  IOException e) {
    return file.getName();
  }
}
