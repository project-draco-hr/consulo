{
  if (type == null) {
    return false;
  }
  for (Iterator iterator=s_obsoleteCollectionTypes.iterator(); iterator.hasNext(); ) {
    final String typeName=(String)iterator.next();
    if (type.equalsToText(typeName)) {
      return true;
    }
  }
  return false;
}
