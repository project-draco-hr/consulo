{
  final PsiFile containingFile=namedElement.getContainingFile();
  final Query<PsiReference> query=ReferencesSearch.search(namedElement,GlobalSearchScope.fileScope(containingFile));
  for (  PsiReference reference : query) {
    final PsiElement element=reference.getElement();
    final PsiElement parent=element.getParent();
    if (isObsoleteCollectionTypeElementArgumentOfLibraryMethod(parent)) {
      return true;
    }
  }
  return false;
}
