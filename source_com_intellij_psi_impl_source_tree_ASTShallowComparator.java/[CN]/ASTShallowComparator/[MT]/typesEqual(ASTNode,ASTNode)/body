{
  if (n1 instanceof LeafPsiElement && n2 instanceof LeafPsiElement) {
    return ((LeafPsiElement)n1).getTypeIndex() == ((LeafPsiElement)n2).getTypeIndex();
  }
  return n1.getElementType() == n2.getElementType();
}
