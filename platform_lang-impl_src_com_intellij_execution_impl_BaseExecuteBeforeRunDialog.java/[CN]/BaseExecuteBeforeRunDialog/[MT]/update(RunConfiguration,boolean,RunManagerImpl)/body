{
  T task=runManager.getBeforeRunTask(config,getTaskID());
  if (task == null)   return;
  if (enabled) {
    task.setEnabled(true);
    update(task);
  }
 else {
    if (isRunning(task)) {
      task.setEnabled(false);
      clear(task);
    }
  }
}
