{
  LOG.assertTrue(myHolderMap.containsKey(myCurrentVcs),"current vcs: " + myCurrentVcs + " file: "+ file.getPath()+ " file's vcs: "+ myVcsManager.getVcsFor(file));
  myHolderMap.get(myCurrentVcs).addFile(file);
}
