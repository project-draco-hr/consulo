{
  myCurrentVcs=vcs;
  if (!myHolderMap.containsKey(vcs)) {
    myHolderMap.put(vcs,vcs.reportsIgnoredDirectories() ? (IgnoredFilesHolder)new RecursiveFileHolder(myProject,HolderType.IGNORED) : new MapIgnoredFilesHolder(myProject));
  }
}
