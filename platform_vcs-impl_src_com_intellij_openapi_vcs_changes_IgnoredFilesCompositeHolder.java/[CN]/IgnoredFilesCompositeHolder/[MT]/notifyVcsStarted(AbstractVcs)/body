{
  myCurrentVcs=vcs;
  if (!myHolderMap.containsKey(vcs)) {
    myHolderMap.put(vcs,vcs.reportsIgnoredDirectories() ? new RecursiveFileHolder(myProject,HolderType.IGNORED) : new MapIgnoredFilesHolder(myProject));
  }
}
