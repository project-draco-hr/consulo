{
  PsiElement next=element.getNextSibling();
  while (next != null && (next instanceof PsiWhiteSpace || next instanceof PsiComment)) {
    next=next.getNextSibling();
  }
  return (next == null || SEPARATORS.contains(next.getNode().getElementType()));
}
