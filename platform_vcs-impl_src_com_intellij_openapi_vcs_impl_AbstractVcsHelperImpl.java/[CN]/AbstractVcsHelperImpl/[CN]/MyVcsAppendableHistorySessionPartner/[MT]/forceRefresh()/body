{
  ApplicationManager.getApplication().invokeLater(new Runnable(){
    public void run(){
      if (mySession == null) {
        return;
      }
      ensureHistoryPanelCreated().scheduleRefresh();
    }
  }
);
}
