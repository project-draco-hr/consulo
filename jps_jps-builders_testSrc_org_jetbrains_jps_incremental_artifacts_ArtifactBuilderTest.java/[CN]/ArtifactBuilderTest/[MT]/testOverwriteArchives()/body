{
  final Artifact included=addArtifact("included",root().archive("x.jar").fileCopy(createFile("aaa.class")));
  final Artifact a=addArtifact(root().artifact(included).archive("x.jar").fileCopy(createFile("bbb.class")));
  buildAll();
  assertOutput(a,fs().archive("x.jar").file("aaa.class"));
}
