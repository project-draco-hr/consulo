{
  final Artifact included=addArtifact("included",root().dir("dir").fileCopy(createFile("aaa.class")));
  final Artifact a=addArtifact(root().artifact(included).dir("dir").fileCopy(createFile("bbb.class")));
  buildAll();
  assertOutput(a,fs().dir("dir").file("aaa.class").file("bbb.class"));
}
