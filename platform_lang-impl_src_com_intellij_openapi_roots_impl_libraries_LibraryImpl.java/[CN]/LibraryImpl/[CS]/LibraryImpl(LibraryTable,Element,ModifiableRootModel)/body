{
  myLibraryTable=table;
  myRootModel=rootModel;
  mySource=null;
  readName(element);
  readProperties(element);
  readJarDirectories(element);
  myRoots=initRoots();
  readRoots(element);
  updateWatchedRoots();
}
