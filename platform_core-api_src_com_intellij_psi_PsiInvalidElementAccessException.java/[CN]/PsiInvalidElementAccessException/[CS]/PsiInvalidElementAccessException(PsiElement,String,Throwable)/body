{
  super((element != null ? "Element: " + element.getClass() + " because: "+ reason(element) : "Unknown psi element") + (message == null ? "" : "; " + message),cause);
  myElementReference=new SoftReference<PsiElement>(element);
}
