{
  Object trace=findInvalidationTrace(element.getNode());
  return "\ninvalidated at: " + (!isTrackingInvalidation() ? "disabled" : trace instanceof Throwable ? ExceptionUtil.getThrowableText((Throwable)trace) : trace != null ? trace : "no info");
}
