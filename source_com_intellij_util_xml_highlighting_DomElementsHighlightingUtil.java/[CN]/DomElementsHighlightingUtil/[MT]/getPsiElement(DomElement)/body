{
  if (domElement instanceof GenericValue && ((GenericValue)domElement).getStringValue() != null && domElement.getXmlTag().getValue().getTextElements().length > 0) {
    return domElement.getXmlTag().getValue().getTextElements()[0];
  }
  return domElement.getXmlTag();
}
