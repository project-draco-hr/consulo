{
  int oldInactiveTimeout=myInactiveTimeout;
  myInactiveTimeout=inactiveTimeout;
  myPropertyChangeSupport.firePropertyChange(PROP_INACTIVE_TIMEOUT,new Integer(oldInactiveTimeout),new Integer(inactiveTimeout));
}
