{
  return myDelegate.addFoldRegion(new FoldRegionImpl(region.getEditor(),myDocumentWindow.injectedToHost(region.getStartOffset()),myDocumentWindow.injectedToHost(region.getEndOffset()),region.getPlaceholderText(),region.getGroup()));
}
