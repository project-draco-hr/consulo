{
  return text.length() == 0 || text.contains("\"") || text.contains("'") || text.contains(">") || text.contains("<") || text.contains("/") || text.contains("\\") || Character.isDigit(text.charAt(0));
}
