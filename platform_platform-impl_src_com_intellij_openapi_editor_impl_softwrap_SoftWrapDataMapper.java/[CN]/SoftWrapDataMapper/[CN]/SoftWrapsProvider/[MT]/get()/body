{
  if (myIndex < 0) {
    return null;
  }
  while (myIndex < mySoftWraps.size()) {
    TextChange result=mySoftWraps.get(myIndex++);
    if (isVisible(result)) {
      return result;
    }
  }
  return null;
}
