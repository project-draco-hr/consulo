{
  if (myIndex < 0 || myIndex >= mySoftWraps.size()) {
    return null;
  }
  TextChange result=mySoftWraps.get(myIndex++);
  if (isVisible(result)) {
    return result;
  }
 else {
    return get();
  }
}
