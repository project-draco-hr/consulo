{
  int result=0;
  for (int i=start; i < end; i++) {
    if (text.charAt(i) == '\t') {
      result+=EditorUtil.tabWidthInColumns(myEditor,offsetInColumns + result);
    }
 else {
      result++;
    }
  }
  return result;
}
