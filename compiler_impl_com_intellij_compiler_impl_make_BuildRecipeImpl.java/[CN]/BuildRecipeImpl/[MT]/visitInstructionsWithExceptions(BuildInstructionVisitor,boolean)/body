{
  for (int i=reverseOrder ? myInstructions.size() - 1 : 0; reverseOrder ? i >= 0 : i < myInstructions.size(); i+=reverseOrder ? -1 : 1) {
    BuildInstruction instruction=myInstructions.get(i);
    if (!instruction.accept(visitor)) {
      return false;
    }
  }
  return true;
}
