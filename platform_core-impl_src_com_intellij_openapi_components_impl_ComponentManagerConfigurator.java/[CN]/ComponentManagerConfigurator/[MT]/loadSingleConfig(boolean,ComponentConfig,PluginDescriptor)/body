{
  if (defaultProject) {
    if (!config.isLoadForDefaultProject())     return;
  }
  if (!myComponentManager.isComponentSuitable(config.options))   return;
  myComponentManager.registerComponent(config,descriptor);
}
