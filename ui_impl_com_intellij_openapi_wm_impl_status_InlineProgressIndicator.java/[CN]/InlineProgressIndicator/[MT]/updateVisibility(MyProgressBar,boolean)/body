{
  if (holdsValue && !bar.isActive()) {
    bar.setActive(true);
    bar.repaint();
    myComponent.revalidate();
    myComponent.repaint();
  }
 else   if (!holdsValue && bar.isActive()) {
    bar.setActive(false);
    bar.repaint();
    myComponent.revalidate();
    myComponent.repaint();
  }
}
