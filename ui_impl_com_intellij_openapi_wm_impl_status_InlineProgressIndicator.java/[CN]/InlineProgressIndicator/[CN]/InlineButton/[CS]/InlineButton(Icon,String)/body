{
  super(icon);
  setBorder(null);
  addActionListener(new ActionListener(){
    public void actionPerformed(    ActionEvent e){
      if (!myActive)       return;
      onActionPerformed(e);
    }
  }
);
  setToolTipText(tooltip);
}
