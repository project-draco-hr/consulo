{
  super.setEnabled(enabled);
  myIsViewer=!enabled;
  if (myEditor == null) {
    return;
  }
  Editor editor=myEditor;
  myEditor=createEditor();
  setEditor();
  revalidate();
  releaseEditor(editor);
}
