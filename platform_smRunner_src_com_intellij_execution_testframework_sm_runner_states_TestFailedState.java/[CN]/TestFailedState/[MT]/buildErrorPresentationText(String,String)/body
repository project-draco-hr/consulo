{
  final String text=(StringUtil.isEmptyOrSpaces(localizedMessage) ? "" : appendLineEnd(localizedMessage)) + (StringUtil.isEmptyOrSpaces(stackTrace) ? "" : appendLineEnd(stackTrace));
  return StringUtil.isEmptyOrSpaces(text) ? null : text;
}
