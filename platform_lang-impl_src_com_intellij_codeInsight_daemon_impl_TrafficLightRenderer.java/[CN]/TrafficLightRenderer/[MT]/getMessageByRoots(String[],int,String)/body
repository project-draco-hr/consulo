{
  if (roots != null) {
    final int length=roots.length;
    if (length > 0) {
      return BR + (rootsNumber > 1 ? DaemonBundle.message(prefix + NO_PASS_FOR_MESSAGE_KEY_SUFFIX,StringUtil.join(roots,", ")) : DaemonBundle.message(prefix));
    }
  }
  return "";
}
