{
  String name=myName;
  if (name == null) {
synchronized (this) {
      name=myName;
      if (name == null) {
        myName=name=ProjectDetailsComponent.getInstance(this).getProjectName();
      }
    }
  }
  return name;
}
