{
  return ContainerUtil.filter(ourMap.values(),new Condition<XPath2Type>(){
    @Override public boolean value(    XPath2Type type){
      return type.getQName().getNamespaceURI().equals(XMLSCHEMA_NS) && !type.isAbstract();
    }
  }
);
}
