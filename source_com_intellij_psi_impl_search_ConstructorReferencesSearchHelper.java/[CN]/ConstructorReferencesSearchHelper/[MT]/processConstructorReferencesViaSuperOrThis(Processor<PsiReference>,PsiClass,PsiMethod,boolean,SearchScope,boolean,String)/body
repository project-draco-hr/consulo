{
  return ApplicationManager.getApplication().runReadAction(new Computable<Boolean>(){
    public Boolean compute(){
      return processInReadAction(inheritor,searchScope,superOrThisKeyword,isStrictSignatureSearch,constructor,constructorCanBeCalledImplicitly,processor);
    }
  }
);
}
