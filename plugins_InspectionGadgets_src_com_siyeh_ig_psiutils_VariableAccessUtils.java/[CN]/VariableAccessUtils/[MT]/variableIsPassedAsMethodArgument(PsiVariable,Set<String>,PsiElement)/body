{
  if (context == null) {
    return false;
  }
  final VariablePassedAsArgumentExcludedVisitor visitor=new VariablePassedAsArgumentExcludedVisitor(variable,excludes);
  context.accept(visitor);
  return visitor.isPassed();
}
