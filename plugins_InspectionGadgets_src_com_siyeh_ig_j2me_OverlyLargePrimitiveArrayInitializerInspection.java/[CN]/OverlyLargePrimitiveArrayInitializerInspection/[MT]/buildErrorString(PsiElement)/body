{
  final PsiExpression expression=(PsiExpression)location;
  final int numElements=calculateNumElements(expression);
  return "Primitive array initializer with too many elements (" + numElements + ") #loc";
}
