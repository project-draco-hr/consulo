{
  if (!(otherState instanceof AndroidBuildConfigState)) {
    return false;
  }
  final AndroidBuildConfigState otherState1=(AndroidBuildConfigState)otherState;
  return otherState1.myDebug == myDebug && otherState1.myPackage.equals(myPackage) && otherState1.myLibPackages.equals(myLibPackages);
}
