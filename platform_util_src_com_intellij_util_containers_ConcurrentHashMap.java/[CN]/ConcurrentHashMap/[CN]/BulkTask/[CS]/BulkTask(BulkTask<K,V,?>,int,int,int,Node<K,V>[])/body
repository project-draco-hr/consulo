{
  super(par);
  batch=b;
  index=baseIndex=i;
  if ((tab=t) == null) {
    baseSize=baseLimit=0;
  }
 else   if (par == null) {
    baseSize=baseLimit=t.length;
  }
 else {
    baseLimit=f;
    baseSize=par.baseSize;
  }
}
