{
  final List<String> result=new LinkedList<String>();
  final Set<String> keySet=myResources.keySet();
  for (  String key : keySet) {
    String resource=myResources.get(key);
    if (fileType != null) {
      String defaultExtension=fileType.getDefaultExtension();
      if (resource.endsWith(defaultExtension) && resource.length() > defaultExtension.length() && resource.charAt(resource.length() - defaultExtension.length() - 1) == '.') {
        result.add(key);
      }
    }
 else {
      result.add(key);
    }
  }
  return result.toArray(new String[result.size()]);
}
