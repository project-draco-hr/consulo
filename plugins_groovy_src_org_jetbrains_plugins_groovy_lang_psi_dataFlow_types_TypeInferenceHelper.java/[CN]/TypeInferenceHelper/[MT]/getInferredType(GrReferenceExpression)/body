{
  final GrControlFlowOwner scope=ControlFlowUtils.findControlFlowOwner(refExpr);
  if (scope == null)   return null;
  return getInferenceCache(scope).getInferredType(refExpr.getReferenceName(),ControlFlowUtils.findRWInstruction(refExpr,scope.getControlFlow()));
}
