{
  final Map<Module,Boolean> result=new HashMap<Module,Boolean>();
  for (  Pair<Module,Boolean> pair : myModulesChooser.getMarkedModules()) {
    result.put(pair.getFirst(),pair.getSecond());
  }
  return result;
}
