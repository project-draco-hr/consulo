{
  initPluginsPanel(myPanel,myPluginsPanel,myEditorPane);
  myNothingFoundToUpdateLabel.setVisible(myUploadedPlugins == null);
  myNothingFoundToUpdateLabel.setText("You already have the latest version of " + ApplicationInfo.getInstance().getVersionName() + " installed.");
}
