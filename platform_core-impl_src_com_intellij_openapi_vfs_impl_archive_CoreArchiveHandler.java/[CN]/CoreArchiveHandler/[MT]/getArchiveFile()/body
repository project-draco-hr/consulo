{
  ArchiveFile jar=myArchiveFile.get();
  if (jar == null) {
synchronized (lock) {
      jar=myArchiveFile.get();
      if (jar == null) {
        jar=createArchiveFile();
        if (jar != null) {
          myArchiveFile.set(jar);
        }
      }
    }
  }
  return jar;
}
