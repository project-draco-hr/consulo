{
synchronized (lock) {
    myRelPathsToEntries=null;
    ArchiveFile archiveFile=myArchiveFile.get();
    if (archiveFile != null) {
      archiveFile.close();
    }
    myArchiveFile.set(null);
  }
}
