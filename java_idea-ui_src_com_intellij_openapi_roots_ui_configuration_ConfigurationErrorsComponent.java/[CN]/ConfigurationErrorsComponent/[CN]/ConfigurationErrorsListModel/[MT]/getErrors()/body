{
  return ContainerUtil.filter(myErrorsList,new Condition<ConfigurationError>(){
    @Override public boolean value(    final ConfigurationError error){
      return !error.isIgnored();
    }
  }
);
}
