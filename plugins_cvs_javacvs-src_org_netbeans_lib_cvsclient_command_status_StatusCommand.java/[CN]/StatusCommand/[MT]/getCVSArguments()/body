{
  @NonNls final StringBuilder toReturn=new StringBuilder();
  if (isIncludeTags()) {
    toReturn.append("-v ");
  }
  if (!isRecursive()) {
    toReturn.append("-l ");
  }
  return toReturn.toString();
}
