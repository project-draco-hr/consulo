{
  @NonNls final StringBuffer toReturn=new StringBuffer("");
  if (isIncludeTags()) {
    toReturn.append("-v ");
  }
  if (!isRecursive()) {
    toReturn.append("-l ");
  }
  return toReturn.toString();
}
