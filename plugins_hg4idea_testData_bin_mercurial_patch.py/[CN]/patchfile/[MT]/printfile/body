def printfile(self, warn):
    if self.fileprinted:
        return
    if (warn or self.ui.verbose):
        self.fileprinted = True
    s = (_('patching file %s\n') % self.fname)
    if warn:
        self.ui.warn(s)
    else:
        self.ui.note(s)
