{
  for (  int oldInterface : oldInterfaces) {
    boolean found=false;
    for (    int newInterface : newInterfaces) {
      found=oldInterface == newInterface;
      if (found) {
        break;
      }
    }
    if (!found) {
      return true;
    }
  }
  return false;
}
