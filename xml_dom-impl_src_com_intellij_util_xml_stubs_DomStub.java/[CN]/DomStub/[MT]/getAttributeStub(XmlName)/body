{
  return (AttributeStub)ContainerUtil.find(getChildrenStubs(),new Condition<DomStub>(){
    @Override public boolean value(    DomStub o){
      return o instanceof AttributeStub && o.getName().equals(name.getLocalName());
    }
  }
);
}
