{
  final String creator=getCreator(editor);
  if (creator == null) {
    return "Editor of " + editor.getClass() + " and the following text hasn't been released:\n"+ editor.getDocument().getText();
  }
 else {
    return "Editor of " + editor.getClass() + " hasn't been released:\n"+ creator;
  }
}
