{
synchronized (ourLock) {
    if (!myClosed) {
      myClosed=true;
      try {
        if (myKeyStorage != null) {
          myKeyStorage.close();
        }
        flush();
      }
  finally {
        myStorage.close();
      }
    }
  }
}
