{
  if (!myClosed) {
    myClosed=true;
    try {
      myKeyStream.close();
      myKeyReadStream.close();
      myKeyRaf.close();
      flush();
    }
  finally {
      myStorage.close();
    }
  }
}
