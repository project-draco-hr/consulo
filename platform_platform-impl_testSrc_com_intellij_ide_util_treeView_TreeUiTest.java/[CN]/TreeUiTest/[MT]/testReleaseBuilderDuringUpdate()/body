{
  assertReleaseDuringBuilding("update","fabrique",new Runnable(){
    public void run(){
      try {
        select(new NodeElement("ide"),false);
      }
 catch (      Exception e) {
        myCancelRequest=e;
      }
    }
  }
);
}
