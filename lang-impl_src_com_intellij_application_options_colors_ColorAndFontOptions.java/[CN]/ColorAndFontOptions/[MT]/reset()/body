{
  super.reset();
  if (!myResetCompleted) {
    ensureSchemesPanel();
    try {
      mySomeSchemesDeleted=false;
      initAll();
      resetSchemesCombo(null);
    }
  finally {
      myResetCompleted=true;
    }
  }
}
