{
  Presentation presentation=e.getPresentation();
  Project project=e.getData(PlatformDataKeys.PROJECT);
  final String vcsName=getVcsName(project);
  presentation.setVisible(project != null && vcsName != null && ProjectLevelVcsManager.getInstance(project).checkVcsIsActive(vcsName));
  presentation.setEnabled(presentation.isVisible());
}
