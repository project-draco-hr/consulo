{
  StringBuilder sb=emitter.getTextBuffer();
  if (myEmit) {
    if (sb.length() > 0) {
      sb.append('\n');
      sb.append(myCommentRule.getExpandedCommentText());
    }
 else {
      sb.append(myCommentRule.getExpandedCommentText());
      sb.append('\n');
    }
  }
}
