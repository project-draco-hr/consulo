{
  try {
    String hostName=InetAddress.getLocalHost().getHostName();
    return myUser.equals(root.getCvsRoot().getUser()) && myEditLocation.equals(getFile(root).getParentFile()) && myHost.equals(hostName);
  }
 catch (  UnknownHostException e) {
    LOG.error(e);
    return false;
  }
}
