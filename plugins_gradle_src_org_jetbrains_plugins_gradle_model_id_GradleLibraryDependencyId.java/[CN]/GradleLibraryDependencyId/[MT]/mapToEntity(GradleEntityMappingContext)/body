{
switch (getOwner()) {
case GRADLE:
    return context.getProjectStructureHelper().findGradleLibraryDependency(getOwnerModuleName(),getDependencyName());
case INTELLIJ:
  return context.getProjectStructureHelper().findIntellijLibraryDependency(getOwnerModuleName(),getDependencyName());
}
throw new IllegalStateException(String.format("Can't map id to the target library dependency. Owner: %s, module: '%s', library: '%s'",getOwner(),getOwnerModuleName(),getDependencyName()));
}
