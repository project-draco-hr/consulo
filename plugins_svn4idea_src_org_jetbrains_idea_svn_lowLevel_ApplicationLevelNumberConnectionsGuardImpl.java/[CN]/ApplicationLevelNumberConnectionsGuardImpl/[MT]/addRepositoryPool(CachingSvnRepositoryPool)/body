{
synchronized (myLock) {
    mySet.add(pool);
    ++myInstanceCount;
    if (myFuture == null) {
      myFuture=myService.scheduleWithFixedDelay(myRecheck,DELAY,DELAY,TimeUnit.MILLISECONDS);
    }
  }
}
