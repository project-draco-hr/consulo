{
  if (!jarFile.exists()) {
    return null;
  }
  final String url=JarFileSystem.PROTOCOL_PREFIX + jarFile.getPath().replace(File.separatorChar,'/') + JarFileSystem.JAR_SEPARATOR+ relativePath;
  return VirtualFileManager.getInstance().findFileByUrl(url);
}
