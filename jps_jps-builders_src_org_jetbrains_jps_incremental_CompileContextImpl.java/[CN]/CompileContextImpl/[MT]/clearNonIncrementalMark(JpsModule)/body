{
  final Pair<JpsModule,FSOperations.DirtyMarkScope> pair=new Pair<JpsModule,FSOperations.DirtyMarkScope>(module,isCompilingTests() ? FSOperations.DirtyMarkScope.TESTS : FSOperations.DirtyMarkScope.PRODUCTION);
  myNonIncrementalModules.remove(pair);
}
