def getargmax(self):
    if ('_argmax' in self.__dict__):
        return self._argmax
    self._argmax = 4096
    try:
        self._argmax = os.sysconf('SC_ARG_MAX')
    except:
        pass
    self._argmax = ((self._argmax / 2) - 1)
    return self._argmax
