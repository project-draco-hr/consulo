{
  if (length > available()) {
    length=available();
  }
  int amountRead=-1;
  if (available() > 0) {
    raf.seek(cur);
    amountRead=raf.read(b,offset,length);
  }
  if (amountRead > 0) {
    cur+=amountRead;
  }
  return amountRead;
}
