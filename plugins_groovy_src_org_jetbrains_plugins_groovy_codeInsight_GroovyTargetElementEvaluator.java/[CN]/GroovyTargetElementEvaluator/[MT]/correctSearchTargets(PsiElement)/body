{
  if (target != null && !(target instanceof GrAccessorMethod) && !target.isPhysical() && target.getUserData(NAVIGATION_ELEMENT_IS_NOT_TARGET) == null) {
    return target.getNavigationElement();
  }
  return target;
}
