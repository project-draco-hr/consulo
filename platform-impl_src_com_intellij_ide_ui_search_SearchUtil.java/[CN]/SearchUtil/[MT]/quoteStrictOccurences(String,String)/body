{
  String cur="";
  for (  String part : filter.split(" ")) {
    if (textToMarkup.toLowerCase().indexOf(part) != -1) {
      cur+="\"" + part + "\" ";
    }
 else {
      cur+=part + " ";
    }
  }
  return cur;
}
