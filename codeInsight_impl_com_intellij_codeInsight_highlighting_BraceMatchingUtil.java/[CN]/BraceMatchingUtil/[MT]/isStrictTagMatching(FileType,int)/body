{
switch (tokenGroup) {
case XML_TAG_TOKEN_GROUP:
    return fileType instanceof LanguageFileType && ((LanguageFileType)fileType).getLanguage() instanceof XMLLanguage;
case JSP_TOKEN_GROUP:
  return true;
default :
return false;
}
}
