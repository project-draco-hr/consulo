{
switch (tokenGroup) {
case XML_TAG_TOKEN_GROUP:
    return fileType == StdFileTypes.XML || fileType == StdFileTypes.XHTML || fileType == StdFileTypes.JSPX;
case JSP_TOKEN_GROUP:
  return true;
default :
return false;
}
}
