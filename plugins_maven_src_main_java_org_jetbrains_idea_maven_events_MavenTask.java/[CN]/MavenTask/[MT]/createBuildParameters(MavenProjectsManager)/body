{
  final VirtualFile virtualFile=LocalFileSystem.getInstance().findFileByPath(pomPath);
  if (virtualFile != null) {
    return new MavenRunnerParameters(true,virtualFile.getParent().getPath(),Arrays.asList(goal),projectsManager.getActiveProfiles());
  }
  return null;
}
