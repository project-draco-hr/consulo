{
  final Iterator it=revisions.iterator();
  Revision item;
  while (it.hasNext()) {
    item=(Revision)it.next();
    if (item.getNumber().equals(number)) {
      return item;
    }
  }
  return null;
}
