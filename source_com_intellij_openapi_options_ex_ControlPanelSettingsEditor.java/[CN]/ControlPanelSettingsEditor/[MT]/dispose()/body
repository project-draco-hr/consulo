{
  if (myPopup[0] != null) {
    myPopup[0].cancel();
  }
  myGlassPanel=null;
  final JRootPane pane=myPanel.getRootPane();
  if (pane != null) {
    pane.setGlassPane(null);
  }
  super.dispose();
}
