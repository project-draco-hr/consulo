{
synchronized (this) {
    if (myAuthenticationInfo != null) {
      Map info=(Map)myAuthenticationInfo.get(realm);
      if (info != null) {
        return decodeData(info);
      }
    }
  }
  return null;
}
