{
  pointFeedback(decorator);
  decorator.addPoint(new DirectionResizePoint(Color.orange,Color.black,Position.WEST,TYPE){
    @Override protected Point getLocation(    DecorationLayer layer,    RadComponent component){
      Point location=super.getLocation(layer,component);
      location.x-=((RadViewComponent)component).getMargins().x;
      return location;
    }
  }
);
  pointRight(decorator,Color.orange,0.25,TYPE);
  decorator.addPoint(new DirectionResizePoint(Color.orange,Color.black,Position.NORTH,TYPE){
    @Override protected Point getLocation(    DecorationLayer layer,    RadComponent component){
      Point location=super.getLocation(layer,component);
      location.y-=((RadViewComponent)component).getMargins().y;
      return location;
    }
  }
);
  pointBottom(decorator,Color.orange,0.25,TYPE);
}
