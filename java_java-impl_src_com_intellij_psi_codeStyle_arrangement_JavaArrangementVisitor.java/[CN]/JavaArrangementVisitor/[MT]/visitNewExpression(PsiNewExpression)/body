{
  PsiAnonymousClass anonymousClass=expression.getAnonymousClass();
  if (anonymousClass == null) {
    return;
  }
  JavaElementArrangementEntry entry=createNewEntry(anonymousClass,ArrangementEntryType.CLASS,anonymousClass.getName(),false);
  processEntry(entry,null,anonymousClass);
}
