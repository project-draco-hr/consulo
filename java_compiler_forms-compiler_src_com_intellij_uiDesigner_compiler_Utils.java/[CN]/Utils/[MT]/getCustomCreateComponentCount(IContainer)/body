{
  final int[] result=new int[1];
  result[0]=0;
  container.accept(new ComponentVisitor(){
    public boolean visit(    IComponent c){
      if (c.isCustomCreate()) {
        result[0]++;
      }
      return true;
    }
  }
);
  return result[0];
}
