{
  final PsiElement parent=literalExpression.getParent();
  final String originalText=parent instanceof PsiPrefixExpression ? parent.getText() : literalExpression.getText();
  return originalText + 'L';
}
