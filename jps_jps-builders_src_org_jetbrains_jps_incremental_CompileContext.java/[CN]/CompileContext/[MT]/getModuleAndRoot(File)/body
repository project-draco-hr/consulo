{
  File current=file;
  while (current != null) {
    final RootDescriptor descriptor=myRootToModuleMap.get(current);
    if (descriptor != null) {
      return descriptor;
    }
    current=FileUtil.getParentFile(current);
  }
  return null;
}
