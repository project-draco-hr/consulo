{
  final int failuresCount=failedTestsSet.size();
  if (failuresCount > 0) {
    myProgressLine.setColor(ColorProgressBar.RED);
  }
  myStatusLabel.setText(TestsPresentationUtil.getProgressStatus_Text(startTime,endTime,testsTotal,testsCount,failuresCount));
}
