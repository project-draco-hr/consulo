{
  super(project,false);
  myProject=project;
  myVCSComboBox.setModel(VcsDirectoryConfigurationPanel.buildVcsWrappersModel(project));
  myDirectoryTextField.addBrowseFolderListener("Select Directory","Select directory to map to a VCS",project,new FileChooserDescriptor(false,true,false,false,false,false));
  setTitle(title);
  init();
}
