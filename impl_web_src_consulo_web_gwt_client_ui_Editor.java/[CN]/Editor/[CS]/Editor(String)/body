{
  myBuilder=new SegmentBuilder(text);
  int lineCount=0;
  for (int i=0; i < text.length(); i++) {
    if (text.charAt(i) == '\n') {
      lineCount++;
    }
  }
  myLineCount=lineCount;
  setWidth("100%");
  setHeight("100%");
}
