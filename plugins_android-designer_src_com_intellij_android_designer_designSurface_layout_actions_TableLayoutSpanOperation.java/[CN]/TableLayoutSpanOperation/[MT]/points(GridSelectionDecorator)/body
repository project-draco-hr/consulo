{
  decorator.addPoint(new SpanPoint(COLOR,Color.black,Position.WEST,TYPE,"Change layout:column x layout:span",decorator));
  decorator.addPoint(new SpanPoint(COLOR,Color.black,Position.EAST,TYPE,"Change layout:span",decorator));
}
