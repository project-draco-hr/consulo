{
  return it instanceof JBIterator ? (JBIterator<E>)it : new JBIterator<E>(){
    @Override protected E nextImpl(){
      return it.hasNext() ? it.next() : stop();
    }
    @Override public void remove(){
      it.remove();
    }
  }
;
}
