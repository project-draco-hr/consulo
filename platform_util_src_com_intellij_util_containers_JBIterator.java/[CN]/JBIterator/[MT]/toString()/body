{
  JBIterable<Op> ops=JBIterable.generate(firstOp.nextOp,new Function<Op,Op>(){
    @Override public Op fun(    Op op){
      return op.nextOp;
    }
  }
);
  return "{cur=" + cur + "; ops["+ ops.size()+ "]="+ ops+ "}";
}
