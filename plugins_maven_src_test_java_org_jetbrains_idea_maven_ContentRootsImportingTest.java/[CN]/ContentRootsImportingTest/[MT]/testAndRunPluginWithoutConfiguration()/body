{
  importProject("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<build>"+ "  <plugins>"+ "    <plugin>"+ "      <groupId>org.apache.maven.plugins</groupId>"+ "      <artifactId>maven-antrun-plugin</artifactId>\n"+ "    </plugin>"+ "  </plugins>"+ "</build>");
  assertModules("project");
}
