{
  Map<String,String> env=commandLine.getEnvironment();
  if (isLocaleDefined(env)) {
    return true;
  }
  if (commandLine.isPassParentEnvironment()) {
    Map<String,String> parentEnv=EnvironmentUtil.getEnvironmentMap();
    if (isLocaleDefined(parentEnv)) {
      return true;
    }
  }
  return false;
}
