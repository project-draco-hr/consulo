{
  if (UTF8Length >= maxSize) {
    out.append("\";");
    nl();
    nextChunk();
  }
 else {
    if (linepos >= maxEntries) {
      out.append("\"+");
      nl();
      out.append(indent);
      out.append("\"");
      linepos=0;
    }
  }
}
