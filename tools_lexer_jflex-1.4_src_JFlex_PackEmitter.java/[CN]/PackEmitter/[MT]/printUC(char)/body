{
  if (c > 255) {
    out.append("\\u");
    if (c < 0x1000)     out.append("0");
    out.append(Integer.toHexString(c));
  }
 else {
    out.append("\\");
    out.append(Integer.toOctalString(c));
  }
}
