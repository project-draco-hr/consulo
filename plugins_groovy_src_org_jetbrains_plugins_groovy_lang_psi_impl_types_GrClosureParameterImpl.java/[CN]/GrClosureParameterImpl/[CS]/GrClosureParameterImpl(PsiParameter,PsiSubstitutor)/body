{
  this(substitutor.substitute(parameter.getType()),parameter instanceof GrParameter ? ((GrParameter)parameter).isOptional() : false,parameter instanceof GrParameter ? ((GrParameter)parameter).getDefaultInitializer() : null);
}
