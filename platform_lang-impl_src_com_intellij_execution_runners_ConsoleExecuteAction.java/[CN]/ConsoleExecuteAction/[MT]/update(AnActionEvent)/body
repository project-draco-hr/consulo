{
  EditorEx editor=myConsole.getConsoleEditor();
  Lookup lookup=LookupManager.getActiveLookup(editor);
  e.getPresentation().setEnabled(!editor.isRendererMode() && isEnabled() && (lookup == null || !lookup.isCompletion()));
}
