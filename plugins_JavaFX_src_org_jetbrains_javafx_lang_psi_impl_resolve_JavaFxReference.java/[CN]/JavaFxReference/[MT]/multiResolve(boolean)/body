{
  final ResolveResult[] results=((PsiManagerEx)myElement.getManager()).getResolveCache().resolveWithCaching(this,ourResolver,true,incompleteCode);
  if (results.length == 0) {
    return ResolveResult.EMPTY_ARRAY;
  }
  return results;
}
