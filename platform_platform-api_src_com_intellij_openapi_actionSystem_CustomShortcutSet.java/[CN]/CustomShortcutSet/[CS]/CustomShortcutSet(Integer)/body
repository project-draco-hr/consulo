{
  myShortcuts=ContainerUtil.map(keyCodes,new Function<Integer,Shortcut>(){
    @Override public Shortcut fun(    Integer integer){
      return new KeyboardShortcut(KeyStroke.getKeyStroke(integer,0),null);
    }
  }
,Shortcut.EMPTY_ARRAY);
}
