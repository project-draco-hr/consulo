{
  if (element instanceof IProperty) {
    @NonNls String info="\n\"" + ((IProperty)element).getValue() + "\"";
    PsiFile file=element.getContainingFile();
    if (file != null) {
      info+=" [" + file.getName() + "]";
    }
    return info;
  }
  return null;
}
