{
  if (oItem == null || itemWrapper == null) {
    if (oItem != itemWrapper) {
      return false;
    }
    return true;
  }
  if (oItem.getDirectory() != itemWrapper.getDirectory()) {
    return false;
  }
  return true;
}
