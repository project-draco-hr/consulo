{
  FileTypeManager fileTypeManager=FileTypeManager.getInstance();
  FileType type=fileTypeManager.getFileTypeByFileName(fileName);
  if (type == StdFileTypes.UNKNOWN)   type=associateFileType(fileName);
  return type;
}
