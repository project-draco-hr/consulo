{
  return ContainerUtil.map(getInspectionsNodes(node),new Function<InspectionConfigTreeNode,HighlightDisplayKey>(){
    @Override public HighlightDisplayKey fun(    final InspectionConfigTreeNode node){
      return node.getKey();
    }
  }
);
}
