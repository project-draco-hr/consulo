{
  int membersIdx=title.indexOf('M');
  if (membersIdx >= 0) {
    title=title.replaceFirst("M","&M");
  }
 else {
    membersIdx=title.indexOf('m');
    if (membersIdx >= 0) {
      title=title.replaceFirst("m","&m");
    }
  }
  return title;
}
