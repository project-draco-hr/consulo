{
  final RubyRunConfigurationFactory factory=new RubyRunConfigurationFactory(TestUnitRunConfigurationType.getInstance());
  final RTestsRunConfiguration runConfiguration=new RTestsRunConfiguration(getProject(),factory,"name");
  return runConfiguration;
}
