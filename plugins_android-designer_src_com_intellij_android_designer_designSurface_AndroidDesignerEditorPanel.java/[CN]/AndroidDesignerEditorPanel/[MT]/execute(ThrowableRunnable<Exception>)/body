{
  try {
    myPSIChangeListener.stop();
    operation.run();
    updateRenderer(true);
    return true;
  }
 catch (  Throwable e) {
    showError("Execute command",e);
    return false;
  }
 finally {
    myPSIChangeListener.start();
  }
}
