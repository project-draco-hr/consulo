{
  return new ComponentCreationFactory(){
    @NotNull @Override public RadComponent create() throws Exception {
      return ModelParser.createComponent(null,paletteItem.getMetaModel());
    }
  }
;
}
