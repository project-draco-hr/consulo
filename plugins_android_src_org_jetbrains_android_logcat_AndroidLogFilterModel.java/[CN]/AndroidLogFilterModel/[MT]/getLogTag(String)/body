{
  String prevTag=null;
  final Matcher matcher=ANDROID_LOG_MESSAGE_PATTERN.matcher(line);
  if (matcher.matches()) {
    final String tag=matcher.group(1).trim();
    if (tag.length() > 0) {
      prevTag=tag;
    }
  }
  return prevTag;
}
