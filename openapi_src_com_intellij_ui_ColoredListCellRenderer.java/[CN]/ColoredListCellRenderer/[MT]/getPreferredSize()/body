{
  final Font oldFont=getFont();
  if (oldFont == null) {
    setFont(UIUtil.getListFont());
  }
  final Dimension result=super.getPreferredSize();
  if (oldFont == null) {
    setFont(null);
  }
  return result;
}
