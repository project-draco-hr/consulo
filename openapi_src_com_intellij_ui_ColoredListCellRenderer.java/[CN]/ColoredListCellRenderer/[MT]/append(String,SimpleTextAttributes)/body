{
  if (mySelected) {
    super.append(fragment,new SimpleTextAttributes(attributes.getStyle(),UIManager.getColor("List.selectionForeground")));
  }
 else {
    super.append(fragment,attributes);
  }
}
