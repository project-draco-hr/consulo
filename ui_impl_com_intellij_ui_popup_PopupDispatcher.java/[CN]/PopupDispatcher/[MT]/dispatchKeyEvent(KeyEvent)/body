{
  if (ourShowingStep == null) {
    return false;
  }
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      if (ourShowingStep != null) {
        ourShowingStep.dispatch(e);
      }
    }
  }
);
  return true;
}
