{
  if (className == null)   return null;
  File file=myClass2File.get(className);
  if (file != null) {
    return file.toURL();
  }
  file=new File(className);
  if (file.exists()) {
    return file.toURL();
  }
  return null;
}
