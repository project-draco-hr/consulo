{
  return new EmptyIcon(ICON_SIZE){
    @Override public void paintIcon(    Component component,    Graphics g,    int i,    int j){
      Color color=getCurrentColor();
      if (color != null) {
        g.setColor(color);
        g.fillRect(i,j,getIconWidth(),getIconHeight());
      }
    }
  }
;
}
