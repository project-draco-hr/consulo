{
  String s=GroovyGrailsConfiguration.getInstance().getGrailsVersion();
  PsiBuilder.Marker rootMarker=builder.mark();
  CompilationUnit.parse(builder);
  rootMarker.done(root);
  return builder.getTreeBuilt();
}
