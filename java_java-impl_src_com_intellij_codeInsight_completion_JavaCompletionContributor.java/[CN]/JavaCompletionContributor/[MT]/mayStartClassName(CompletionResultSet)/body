{
  String prefix=result.getPrefixMatcher().getPrefix();
  if (StringUtil.isEmpty(prefix)) {
    return false;
  }
  return StringUtil.isCapitalized(prefix) || CodeInsightSettings.getInstance().COMPLETION_CASE_SENSITIVE == CodeInsightSettings.NONE;
}
