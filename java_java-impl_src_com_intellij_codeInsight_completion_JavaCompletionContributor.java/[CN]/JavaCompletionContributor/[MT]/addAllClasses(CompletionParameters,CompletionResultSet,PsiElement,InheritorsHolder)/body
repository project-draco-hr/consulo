{
  if (shouldRunClassNameCompletion(result,position)) {
    result.runRemainingContributors(parameters.withType(CompletionType.CLASS_NAME),new Consumer<LookupElement>(){
      @Override public void consume(      LookupElement lookupElement){
        if (!inheritors.alreadyProcessed(lookupElement)) {
          result.addElement(lookupElement);
        }
      }
    }
);
  }
}
