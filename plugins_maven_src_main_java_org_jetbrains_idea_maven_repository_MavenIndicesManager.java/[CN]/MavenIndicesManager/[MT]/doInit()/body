{
  isInitialized=true;
  try {
    initIndices();
    try {
      checkLocalIndex();
      checkProjectIndex();
    }
 catch (    MavenIndexException e) {
      throw new MavenException(e);
    }
  }
 catch (  MavenException e) {
    showError(e);
  }
  listenForArtifactChanges();
}
