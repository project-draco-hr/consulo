{
  if ((file == null) || (!ExcludedFileIndex.getInstance(myProject).isInContent(file)) || (ProjectLevelVcsManager.getInstance(myProject).getVcsRootFor(file) == null)) {
    setErrorMessage("File to patch found outside content root: " + name);
    return false;
  }
  return true;
}
