{
  if (myManager == null) {
    if (getTreeParent() != null)     return getTreeParent().getManager();
    return (PsiManagerEx)SourceTreeToPsiMap.treeElementToPsi(this).getManager();
  }
  return myManager;
}
