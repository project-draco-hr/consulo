def popen2(cmd, env=None, newlines=False):
    p = subprocess.Popen(cmd, shell=True, bufsize=(-1), close_fds=closefds, stdin=subprocess.PIPE, stdout=subprocess.PIPE, universal_newlines=newlines, env=env)
    return (p.stdin, p.stdout)
