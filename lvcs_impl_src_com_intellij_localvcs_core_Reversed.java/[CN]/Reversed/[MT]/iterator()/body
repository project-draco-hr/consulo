{
  return new Iterator<T>(){
    int i=myList.size() - 1;
    public boolean hasNext(){
      return i >= 0;
    }
    public T next(){
      return myList.get(i--);
    }
    public void remove(){
      throw new UnsupportedOperationException();
    }
  }
;
}
