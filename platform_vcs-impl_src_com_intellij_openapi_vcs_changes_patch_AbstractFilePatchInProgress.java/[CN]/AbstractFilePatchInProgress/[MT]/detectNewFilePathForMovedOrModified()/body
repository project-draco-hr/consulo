{
  return FilePatchStatus.MOVED_OR_RENAMED.equals(myStatus) ? VcsUtil.getFilePath(PathMerger.getFile(new File(myBase.getPath()),myPatch.getAfterName()),false) : (myCurrentBase != null) ? VcsUtil.getFilePath(myCurrentBase) : VcsUtil.getFilePath(myIoCurrentBase,false);
}
