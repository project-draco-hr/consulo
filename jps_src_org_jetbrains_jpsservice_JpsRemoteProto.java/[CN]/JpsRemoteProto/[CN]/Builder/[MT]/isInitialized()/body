{
  if (hasCompileRequest()) {
    if (!getCompileRequest().isInitialized()) {
      return false;
    }
  }
  if (hasStatusRequest()) {
    if (!getStatusRequest().isInitialized()) {
      return false;
    }
  }
  return true;
}
