{
  int size=content.size();
  if (start > size) {
    start=size;
  }
  int index=start;
  while (index >= 0) {
    if (!isAllWhitespace(content.get(index - 1)))     break;
    --index;
  }
  return index;
}
