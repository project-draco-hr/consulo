{
  if (start < 0) {
    start=0;
  }
  int index=start;
  int size=content.size();
  while (index < size) {
    if (!isAllWhitespace(content.get(index))) {
      return index;
    }
    index++;
  }
  return index;
}
