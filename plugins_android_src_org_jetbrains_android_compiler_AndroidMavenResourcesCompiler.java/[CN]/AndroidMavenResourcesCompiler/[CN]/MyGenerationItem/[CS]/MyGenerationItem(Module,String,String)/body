{
  myModule=module;
  myPackage=aPackage;
  myGeneratedFile=new File(sourceRootPath,aPackage.replace('.',File.separatorChar) + File.separator + AndroidCommonUtils.R_JAVA_FILENAME);
  mySourceRootPath=sourceRootPath;
  myValidityState=new MyValidityState(myModule);
}
