{
  final ArrayList<FoldingDescriptor> regions=new ArrayList<FoldingDescriptor>();
  process(node,document,regions);
  return regions.size() > 0 ? regions.toArray(new FoldingDescriptor[regions.size()]) : FoldingDescriptor.EMPTY;
}
