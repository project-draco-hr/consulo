{
  while (elem != null && elem.getNode() != null && TokenSets.WHITE_SPACES_SET.contains(elem.getNode().getElementType())) {
    if (forward) {
      elem=elem.getNextSibling();
    }
 else {
      elem=elem.getPrevSibling();
    }
  }
  return elem;
}
