{
  if (myIndex == 1)   return MergeList.BASE_SIDE;
  if (myIndex == 0)   if (change.getChangeList() != myMergeList.getChanges(FragmentSide.SIDE1))   return null;
  if (myIndex == 2)   if (change.getChangeList() != myMergeList.getChanges(FragmentSide.SIDE2))   return null;
  return MergeList.BRANCH_SIDE;
}
