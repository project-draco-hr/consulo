{
  myPanel.removeAll();
  Content content=event.getContent();
  if (content != null) {
    myPanel.add(content.getComponent(),BorderLayout.CENTER);
    myPanel.validate();
    myPanel.repaint();
  }
}
