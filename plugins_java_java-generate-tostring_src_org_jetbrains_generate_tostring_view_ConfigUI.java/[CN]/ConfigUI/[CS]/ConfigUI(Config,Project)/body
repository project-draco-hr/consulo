{
  super(new BorderLayout());
  filterFieldName=new LanguageTextField(ourRegExpLanguagePointer.get(),project,config.getFilterFieldName());
  filterFieldType=new LanguageTextField(ourRegExpLanguagePointer.get(),project,config.getFilterFieldType());
  filterMethodName=new LanguageTextField(ourRegExpLanguagePointer.get(),project,config.getFilterMethodName());
  filterMethodType=new LanguageTextField(ourRegExpLanguagePointer.get(),project,config.getFilterMethodType());
  init();
  setConfig(config);
}
