{
  LOG.info("We have tried to bind to 127.0.0.1 host but have got exception (" + SystemInfo.OS_NAME + " "+ SystemInfo.OS_VERSION+ "), "+ "so, try to check - are we really need to bind to 127.0.0.1");
  try {
    return checkPort(localAddress);
  }
 catch (  Throwable innerE) {
    LOG.error(innerE);
    return false;
  }
}
