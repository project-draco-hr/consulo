{
  List<PsiFile> result=new ArrayList<PsiFile>();
  if (!processFilesWithText(scope,searchContext,caseSensitively,text,new CommonProcessors.CollectProcessor<PsiFile>(result),progress)) {
    return Collections.emptyList();
  }
  return result;
}
