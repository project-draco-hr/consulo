{
  return new MultiMap<VirtualFile,RequestWithProcessor>(){
    @Override protected Collection<RequestWithProcessor> createCollection(){
      return new SmartList<RequestWithProcessor>();
    }
  }
;
}
