{
  return rangeEntry.getEnd() instanceof PsiField && initToAnonClassAttr.isMatch(rangeEntry.getModifiers()) && transientAttr.isMatch(rangeEntry.getModifiers())&& volatileAttr.isMatch(rangeEntry.getModifiers())&& typeAttr.isMatch(rangeEntry.getType())&& super.isMatch(rangeEntry);
}
