{
  return manager.createProblemDescriptor(cast.getCastType(),"Casting <code>" + cast.getOperand().getText() + "</code> to "+ "<code>#ref</code> #loc is redundant",myQuickFixAction,ProblemHighlightType.LIKE_UNUSED_SYMBOL);
}
