{
  if (myCategoryMap.get(DEFAULT_CATEGORY) == null)   return null;
  String name=getContentName(groupId);
  EventLogConsole console=myCategoryMap.get(name);
  return console != null ? console : createNewContent(name);
}
