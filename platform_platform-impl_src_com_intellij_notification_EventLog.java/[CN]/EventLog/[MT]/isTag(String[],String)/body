{
  tag=tag.substring(1,tag.length() - 1);
  tag=StringUtil.trimEnd(StringUtil.trimStart(tag,"/"),"/");
  int index=tag.indexOf(' ');
  if (index != -1) {
    tag=tag.substring(0,index);
  }
  return ArrayUtil.indexOf(tags,tag) != -1;
}
