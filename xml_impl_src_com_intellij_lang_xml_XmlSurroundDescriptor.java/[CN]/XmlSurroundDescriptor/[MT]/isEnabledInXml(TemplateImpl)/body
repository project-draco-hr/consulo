{
  final TemplateContext context=template.getTemplateContext();
  return new XmlContextType().isEnabled(context) || new HtmlContextType().isEnabled(context) || new JspContextType().isEnabled(context);
}
