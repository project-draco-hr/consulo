{
  if (dataNode == null) {
    throw new IllegalArgumentException(String.format("Can't build id for %s. Reason: given data holder is undefined",this));
  }
  ModuleData moduleData=dataNode.getData(ExternalSystemProjectKeys.MODULE);
  if (moduleData == null) {
    throw new IllegalArgumentException(String.format("Can't build id for %s. Reason: given data holder doesn't contain information about owner module",this));
  }
  return new ContentRootId(getOwner(),moduleData.getName(),getRootPath());
}
