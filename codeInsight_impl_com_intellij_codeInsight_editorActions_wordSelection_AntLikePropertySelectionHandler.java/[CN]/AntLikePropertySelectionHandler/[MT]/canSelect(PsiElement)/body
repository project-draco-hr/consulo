{
  Language l=e.getLanguage();
  if (!(StdLanguages.JAVA.equals(l) || StdLanguages.XML.equals(l) || StdLanguages.ANT.equals(l))) {
    return false;
  }
  return PsiTreeUtil.getParentOfType(e,PsiComment.class) == null;
}
