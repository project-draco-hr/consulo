{
  waitForZipToFinish(record);
  Callable action;
synchronized (myLock) {
    action=myPendingWriteRequests.get(record);
  }
  if (action != null) {
    try {
      action.call();
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
}
