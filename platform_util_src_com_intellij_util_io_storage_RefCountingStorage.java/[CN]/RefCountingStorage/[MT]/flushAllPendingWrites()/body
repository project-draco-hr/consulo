{
  for (  Map.Entry<Integer,Future<?>> entry : myPendingZipRequests.entrySet()) {
    try {
      entry.getValue().get();
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
  flushPendingWrites();
}
