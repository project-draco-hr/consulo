{
  if (path.equals(".")) {
    return path;
  }
 else {
    StringBuffer sb=new StringBuffer();
    sb.append("..");
    int count=normalize(path).split("/").length;
    while (--count > 0) {
      sb.append("/..");
    }
    return sb.toString();
  }
}
