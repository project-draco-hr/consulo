{
  if (myArgList.getExpressions().length == 1) {
    return "Cast parameter to '" + HighlightUtil.formatType(myToType) + "'";
  }
  return MessageFormat.format("Cast {0} parameter to ''{1}''",new Object[]{getNumerical(),HighlightUtil.formatType(myToType)});
}
