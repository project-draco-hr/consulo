{
  final Insets insets=getDelegee().getInsets();
  if (x < insets.left || x > getWidth() - insets.right || y < 0 || y > insets.top) {
    return super.getInplaceProperty(x,y);
  }
  return new MyBorderTitleProperty();
}
