{
  final PsiPackage elementPackage=element.getContainingFile().getContainingDirectory().getPackage();
  return !PsiUtil.isAccessible(myTargetClass,element,null) || (visibility.equals(PsiModifier.PACKAGE_LOCAL) && !Comparing.equal(elementPackage,myTargetPackage));
}
