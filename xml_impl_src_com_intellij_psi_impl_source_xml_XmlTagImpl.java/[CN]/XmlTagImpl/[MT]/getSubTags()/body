{
  XmlTag[] tags=myTags;
  if (tags == null) {
    final List<XmlTag> result=new ArrayList<XmlTag>();
    fillSubTags(result);
    myTags=tags=result.toArray(new XmlTag[result.size()]);
  }
  return tags;
}
