{
  super.visitVariable(variable);
  if (variable.getInitializerGroovy() != null || variable.getParent() instanceof GrTupleDeclaration && ((GrTupleDeclaration)variable.getParent()).getInitializerGroovy() != null) {
    ReadWriteVariableInstruction writeInst=new ReadWriteVariableInstruction(variable.getName(),variable,myInstructionNumber++,WRITE);
    checkPending(writeInst);
    addNode(writeInst);
  }
}
