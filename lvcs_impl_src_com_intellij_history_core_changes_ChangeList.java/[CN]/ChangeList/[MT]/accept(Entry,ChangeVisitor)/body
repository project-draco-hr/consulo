{
  v.started(root.copy());
  try {
    for (    Change change : Reversed.list(myChanges)) {
      change.accept(v);
    }
  }
 catch (  ChangeVisitor.StopVisitingException e) {
  }
  v.finished();
}
