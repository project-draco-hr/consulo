{
  PsiMethod method;
  while ((method=getContainingMethod(element)) != null)   if (ApplicationConfigurationType.isMainMethod(method))   return method;
 else   element=method.getParent();
  return null;
}
