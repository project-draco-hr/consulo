{
  List<DeploymentSource> sources=new ArrayList<DeploymentSource>();
  ArtifactPointerManager pointerManager=ArtifactPointerUtil.getPointerManager(project);
  for (  Artifact artifact : artifacts) {
    sources.add(createArtifactDeploymentSource(pointerManager.create(artifact)));
  }
  return sources;
}
