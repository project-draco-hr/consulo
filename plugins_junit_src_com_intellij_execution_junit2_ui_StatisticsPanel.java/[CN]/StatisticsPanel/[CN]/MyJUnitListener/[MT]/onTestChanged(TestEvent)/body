{
  if (event instanceof StatisticsChanged) {
    if (myCurrentTest == event.getSource())     updateStatistics();
  }
 else   if (event instanceof NewChildEvent) {
    if (event.getSource() == myCurrentTest && !mySuiteInfo.isVisible())     updateStatistics();
  }
}
