{
  try {
synchronized (attId) {
      try {
        r.lock();
        return readAttribute(fileId,attId);
      }
  finally {
        r.unlock();
      }
    }
  }
 catch (  Throwable e) {
    throw DbConnection.handleError(e);
  }
}
