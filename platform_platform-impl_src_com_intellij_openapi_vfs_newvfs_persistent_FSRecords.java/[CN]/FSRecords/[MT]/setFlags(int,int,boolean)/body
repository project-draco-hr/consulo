{
synchronized (lock) {
    try {
      if (markAsChange) {
        DbConnection.markDirty();
        incModCount(id);
      }
      putRecordInt(id,FLAGS_OFFSET,flags);
    }
 catch (    Throwable e) {
      throw DbConnection.handleError(e);
    }
  }
}
