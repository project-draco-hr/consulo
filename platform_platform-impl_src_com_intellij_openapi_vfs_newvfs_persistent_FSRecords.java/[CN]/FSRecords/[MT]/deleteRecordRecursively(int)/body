{
  lock.lock();
  try {
    DbConnection.markDirty();
    incModCount(id);
    doDeleteRecursively(id);
  }
 catch (  Throwable e) {
    throw DbConnection.handleError(e);
  }
 finally {
    lock.unlock();
  }
}
