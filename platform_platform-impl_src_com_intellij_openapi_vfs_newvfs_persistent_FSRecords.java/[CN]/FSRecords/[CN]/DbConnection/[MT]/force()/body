{
synchronized (lock) {
    try {
      if (myRecords != null) {
        markClean();
      }
    }
 catch (    IOException e) {
      LOG.info(e);
    }
    if (myNames != null) {
      myNames.force();
      myAttributes.force();
      myContents.force();
      myRecords.force();
    }
  }
}
