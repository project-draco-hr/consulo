{
  lock.lock();
  try {
    try {
      markClean();
    }
 catch (    IOException e) {
    }
    if (myNames != null) {
      myNames.force();
      myAttributes.force();
      myContents.force();
      myRecords.force();
    }
  }
  finally {
    lock.unlock();
  }
}
