{
  lock.lock();
  try {
    myNames.force();
    if (myAttributes.flushSome() && myContents.flushSome()) {
      try {
        markClean();
      }
 catch (      IOException e) {
      }
      myRecords.force();
    }
  }
  finally {
    lock.unlock();
  }
}
