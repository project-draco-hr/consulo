{
  if (!ourIsDisposed) {
    if (!myCorrupted && w.tryLock()) {
      w.unlock();
      createBrokenMarkerFile(e);
      myCorrupted=true;
      force();
    }
  }
  return new RuntimeException(e);
}
