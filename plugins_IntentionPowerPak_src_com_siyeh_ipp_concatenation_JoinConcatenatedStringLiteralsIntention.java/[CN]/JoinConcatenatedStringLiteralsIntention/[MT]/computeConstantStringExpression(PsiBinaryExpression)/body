{
  final PsiExpression lhs=expression.getLOperand();
  final Object lhsConstant=ExpressionUtils.computeConstantExpression(lhs);
  final String lhsText;
  if (lhsConstant == null) {
    lhsText="";
  }
 else {
    lhsText=lhsConstant.toString();
  }
  String result;
  if (lhsText.length() == 0) {
    result="";
  }
 else {
    result=lhsText;
  }
  final PsiExpression rhs=expression.getROperand();
  final Object rhsConstant=ExpressionUtils.computeConstantExpression(rhs);
  if (rhsConstant != null) {
    result+=rhsConstant.toString();
  }
  result=StringUtil.escapeStringCharacters(result);
  return result;
}
