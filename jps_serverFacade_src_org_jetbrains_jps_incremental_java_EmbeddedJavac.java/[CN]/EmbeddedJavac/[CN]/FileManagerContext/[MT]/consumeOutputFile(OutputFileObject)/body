{
  incTaskCount();
  myTaskRunner.submit(new Runnable(){
    public void run(){
      try {
        runProcessors(cls);
      }
  finally {
        try {
          myOutputFileSink.save(cls);
        }
  finally {
          decTaskCount();
        }
      }
    }
  }
);
}
