{
  if (who.equals(whom)) {
    return true;
  }
  final ClassRepr repr=reprByName(who);
  for (  StringCache.S s : repr.getSupers()) {
    if (isInheritorOf(s,whom)) {
      return true;
    }
  }
  return false;
}
