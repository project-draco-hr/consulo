{
  return with(new PatternCondition<Character>("javaIdentifierPart"){
    public boolean accepts(    @NotNull final Character character,    final MatchingContext matchingContext,    @NotNull final TraverseContext traverseContext){
      return Character.isJavaIdentifierPart(character.charValue());
    }
  }
);
}
