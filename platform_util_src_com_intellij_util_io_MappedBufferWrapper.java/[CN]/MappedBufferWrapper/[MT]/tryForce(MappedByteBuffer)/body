{
  for (int i=0; i < MAX_FORCE_ATTEMPTS; i++) {
    try {
      buffer.force();
      return true;
    }
 catch (    Throwable e) {
      LOG.info(e);
      try {
        Thread.sleep(10);
      }
 catch (      InterruptedException ignored) {
      }
    }
  }
  return false;
}
