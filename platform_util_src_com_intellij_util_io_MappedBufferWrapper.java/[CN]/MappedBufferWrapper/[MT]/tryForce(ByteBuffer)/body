{
  for (int i=0; i < MAX_FORCE_ATTEMPTS; i++) {
    try {
      ((MappedByteBuffer)buffer).force();
      return true;
    }
 catch (    Throwable e) {
      try {
        Thread.sleep(10);
      }
 catch (      InterruptedException e1) {
      }
    }
  }
  return false;
}
