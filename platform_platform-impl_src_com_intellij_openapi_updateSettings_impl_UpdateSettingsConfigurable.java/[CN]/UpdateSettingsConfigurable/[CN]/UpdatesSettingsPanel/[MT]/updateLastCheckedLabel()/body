{
  final long lastChecked=UpdateSettings.getInstance().getLastTimeChecked();
  myLastCheckedDate.setText(lastChecked == 0 ? IdeBundle.message("updates.last.check.never") : DateFormatUtil.formatPrettyDateTime(lastChecked));
}
