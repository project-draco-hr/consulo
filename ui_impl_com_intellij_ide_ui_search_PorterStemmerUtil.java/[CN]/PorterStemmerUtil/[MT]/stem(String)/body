{
  if (str.length() > 0) {
    char[] c=str.toCharArray();
    for (    char character : c) {
      if (!Character.isLetter(character))       return null;
    }
  }
 else {
    return null;
  }
  str=step1a(str);
  str=step1b(str);
  str=step1c(str);
  str=step2(str);
  str=step3(str);
  str=step4(str);
  str=step5a(str);
  str=step5b(str);
  return str;
}
