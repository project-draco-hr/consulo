{
  int targetApiLevel=target != null ? target.getVersion().getApiLevel() : 0;
  int renderingTargetApiLevel=renderingTarget != null ? renderingTarget.getVersion().getApiLevel() : targetApiLevel;
  return targetApiLevel >= 11 && renderingTargetApiLevel >= 11 && screenSize == ScreenSize.XLARGE ? ResourceResolver.PREFIX_ANDROID_STYLE + "Theme.Holo" : ResourceResolver.PREFIX_ANDROID_STYLE + "Theme";
}
