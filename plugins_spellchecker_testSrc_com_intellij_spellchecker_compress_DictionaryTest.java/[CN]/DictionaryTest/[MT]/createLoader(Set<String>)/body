{
  return new Loader(){
    @Override public void load(    @NotNull Consumer<String> consumer){
      for (      String word : words) {
        consumer.consume(word);
      }
    }
    @Override public String getName(){
      return "test";
    }
  }
;
}
