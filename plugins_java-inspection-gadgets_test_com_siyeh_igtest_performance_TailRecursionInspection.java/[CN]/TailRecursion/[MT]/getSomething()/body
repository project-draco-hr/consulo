{
  if (something == null) {
    if (isDuplicate()) {
      final TailRecursion recursion=getOriginal();
      return recursion.getSomething();
    }
 else {
      something=new Something();
    }
  }
  return something;
}
