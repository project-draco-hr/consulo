{
  if (myIsRealPopup) {
    myPopup.setSize(size);
  }
 else {
    if (myHintHint.isAwtTooltip()) {
      return;
    }
 else {
      myComponent.setSize(size);
      myComponent.revalidate();
      myComponent.repaint();
    }
  }
}
