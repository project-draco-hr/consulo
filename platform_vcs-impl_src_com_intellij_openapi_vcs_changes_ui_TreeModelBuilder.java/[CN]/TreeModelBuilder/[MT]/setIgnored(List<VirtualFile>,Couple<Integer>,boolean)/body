{
  if (ContainerUtil.isEmpty(ignoredFiles))   return this;
  return insertSpecificNodeToModel(ignoredFiles,new ChangesBrowserIgnoredFilesNode(myProject,sizes.getFirst(),sizes.getSecond(),ignoredFiles.size() > UNVERSIONED_MAX_SIZE,updatingMode));
}
