{
  if (!JVM_IS_GC_CAPABLE)   return;
  addElements(5);
  checkSameElements();
  myHolder.remove(0);
  myHolder.remove(0);
  gc();
  checkSameNotNulls();
  myCollection.compress(-1);
  checkSameElements();
  assertEquals(0,myCollection.getCorpseCount());
}
