{
  if (myInsideChange > 0) {
    return;
  }
  myInsideChange++;
  try {
    queueUpdate().doWhenDone(new Runnable(){
      @Override public void run(){
        syncSelection();
      }
    }
);
  }
  finally {
    myInsideChange--;
  }
}
