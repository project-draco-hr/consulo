{
  if (myReplaceButton != null) {
    myReplaceButton.setEnabled(mySearchResults != null && mySearchResults.getCursor() != null && !myLivePreviewController.isReplaceDenied() && (mySearchResults.getFindModel().isGlobal() || !mySearchResults.getEditor().getSelectionModel().hasBlockSelection()));
  }
}
