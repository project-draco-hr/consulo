{
  FeatureUsageTracker.getInstance().triggerFeatureUsed("find.recent.search");
  showCompletionPopup(new JList(ArrayUtil.reverseArray(FindSettings.getInstance().getRecentFindStrings())),"Recent Searches",byClickingToolbarButton);
}
