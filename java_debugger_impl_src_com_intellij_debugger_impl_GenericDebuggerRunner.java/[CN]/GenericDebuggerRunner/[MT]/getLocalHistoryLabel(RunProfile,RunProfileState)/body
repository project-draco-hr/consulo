{
  final String name=profile.getName();
  return state instanceof RemoteState ? DebuggerBundle.message("debugger.runner.vcs.label.remote.debug",name) : DebuggerBundle.message("debugger.runner.vcs.label.debugging",name);
}
