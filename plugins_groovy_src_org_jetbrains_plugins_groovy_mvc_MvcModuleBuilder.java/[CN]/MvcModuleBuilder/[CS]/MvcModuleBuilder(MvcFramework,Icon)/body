{
  super(framework.getFrameworkName(),framework.getDisplayName() + " Module",framework.getDisplayName() + " modules are used for creating <b>" + framework.getDisplayName()+ "</b> applications.",bigIcon);
  myFramework=framework;
}
