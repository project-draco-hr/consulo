{
synchronized (PassExecutorService.class) {
    LOG.debug(StringUtil.repeatSymbol(' ',getThreadNum() * 4) + info + "; progress="+ (progressIndicator == null ? null : progressIndicator.hashCode())+ "; canceled="+ (progressIndicator != null && progressIndicator.isCanceled()));
  }
}
