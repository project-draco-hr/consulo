{
  if (used) {
    return;
  }
  super.visitReferenceExpression(ref);
  final PsiElement referent=ref.resolve();
  if (referent == null) {
    return;
  }
  if (referent.equals(variable)) {
    used=true;
  }
}
