{
  if (!file.isDirectory()) {
    return 1;
  }
  int result=0;
  File[] subFiles=file.listFiles();
  if (subFiles == null) {
    subFiles=new File[0];
  }
  for (  File subFile : subFiles) {
    result+=calculateFilesIn(subFile);
  }
  return result;
}
