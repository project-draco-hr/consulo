{
  final StringBuffer buffer=new StringBuffer();
  buffer.append("    Root: " + fullName + ", package "+ packageName+ "\n");
  buffer.append("    Subclasses:");
  if (subClasses != null)   for (  String s : subClasses) {
    buffer.append(" " + s);
  }
  buffer.append(".\n");
  return buffer.toString();
}
