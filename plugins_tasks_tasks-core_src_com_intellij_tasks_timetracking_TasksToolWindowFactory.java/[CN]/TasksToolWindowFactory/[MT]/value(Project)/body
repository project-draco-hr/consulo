{
  final TaskManager manager=TaskManager.getManager(project);
  final LocalTask activeTask=manager.getActiveTask();
  final boolean isNotUsed=activeTask.isDefault() && manager.getLocalTasks().size() == 1 && Comparing.equal(activeTask.getCreated(),activeTask.getUpdated());
  return !isNotUsed;
}
