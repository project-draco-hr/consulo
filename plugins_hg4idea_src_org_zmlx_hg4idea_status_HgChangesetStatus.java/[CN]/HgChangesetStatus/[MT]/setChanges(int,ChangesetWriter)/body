{
  ApplicationManager.getApplication().invokeLater(new Runnable(){
    public void run(){
      if (count == 0) {
        numChanges=0;
        toolTip="";
        return;
      }
      numChanges=count;
      toolTip=formatter.asString();
    }
  }
);
}
