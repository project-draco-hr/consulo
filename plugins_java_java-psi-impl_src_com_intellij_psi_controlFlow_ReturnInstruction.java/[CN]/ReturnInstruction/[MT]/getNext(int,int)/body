{
  if (offset == 0) switch (no) {
case 0:
    return getProcBegin() - 5;
case 1:
  return getProcBegin() - 3;
case 2:
return getProcBegin() - 1;
default :
LOG.assertTrue(false);
return -1;
}
 else switch (no) {
case 0:
return offset;
default :
LOG.assertTrue(false);
return -1;
}
}
