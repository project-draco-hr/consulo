{
  final Set<String> parentsHashes=o.getParentsHashes();
  return new CommitHashPlusParents(o.getShortHash(),parentsHashes.toArray(new String[parentsHashes.size()]),o.getDate().getTime(),o.getAuthor());
}
