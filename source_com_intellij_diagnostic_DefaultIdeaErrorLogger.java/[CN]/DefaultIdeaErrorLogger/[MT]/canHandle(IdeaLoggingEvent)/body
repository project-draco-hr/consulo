{
  boolean notificationEnabled=!"disabled".equals(System.getProperty("idea.fatal.error.notification","enabled"));
  return notificationEnabled || ApplicationManagerEx.getApplicationEx().isInternal() || event.getThrowable() instanceof OutOfMemoryError;
}
