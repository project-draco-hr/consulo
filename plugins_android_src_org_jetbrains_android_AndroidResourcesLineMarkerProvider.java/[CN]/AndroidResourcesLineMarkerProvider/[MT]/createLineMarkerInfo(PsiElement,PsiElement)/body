{
  final String toolTip=targets.length == 1 ? getToolTip(targets[0]) : "Resource not found";
  return new LineMarkerInfo<PsiElement>(element,element.getTextOffset(),ICON,Pass.UPDATE_OVERRIDEN_MARKERS,new ConstantFunction<PsiElement,String>(toolTip),new MyNavigationHandler(targets));
}
