{
  final TypeDescriptor type=getType();
  if (type instanceof ComplexTypeDescriptor) {
    return ((ComplexTypeDescriptor)type).canContainTag("a",namespace) || XmlUtil.tagFromTemplateFramework(context);
  }
  return false;
}
