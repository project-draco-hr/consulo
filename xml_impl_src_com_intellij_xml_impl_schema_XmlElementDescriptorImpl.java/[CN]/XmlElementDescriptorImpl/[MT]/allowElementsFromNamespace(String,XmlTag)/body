{
  final TypeDescriptor type=getType(context);
  if (type instanceof ComplexTypeDescriptor) {
    return ((ComplexTypeDescriptor)type).canContainTag("a",namespace) || XmlUtil.nsFromTemplateFramework(namespace);
  }
  return false;
}
