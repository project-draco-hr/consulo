{
  final TypeDescriptor type=getType();
  if (type instanceof ComplexTypeDescriptor) {
    return ((ComplexTypeDescriptor)type).canContainTag("a",namespace) || XmlUtil.tagFromTemplateFramework(context) || XmlUtil.nsFromTemplateFramework(namespace);
  }
  return false;
}
