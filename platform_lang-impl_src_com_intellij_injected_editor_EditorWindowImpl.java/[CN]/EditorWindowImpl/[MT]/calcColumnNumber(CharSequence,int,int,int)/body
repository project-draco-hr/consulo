{
  int column=0;
  for (int i=start; i < offset; i++) {
    char c=text.charAt(i);
    if (c == '\t') {
      column=((column / tabSize) + 1) * tabSize;
    }
 else {
      column++;
    }
  }
  return column;
}
