{
  if (myProject.isDefault())   return;
  myCopiesRefreshManager=new SvnCopiesRefreshManager(myProject,(SvnFileUrlMappingImpl17)getSvnFileUrlMapping());
  invokeRefreshSvnRoots(true);
  myWorkingCopiesContent.activate();
}
