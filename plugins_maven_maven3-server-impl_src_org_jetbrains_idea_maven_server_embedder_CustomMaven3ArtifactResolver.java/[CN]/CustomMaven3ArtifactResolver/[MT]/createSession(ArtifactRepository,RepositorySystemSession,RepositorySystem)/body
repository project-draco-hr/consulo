{
  RepositorySystemSession res=LegacyLocalRepositoryManager.overlay(localRepository,session,repoSystem);
  return new FilterRepositorySystemSession(res){
    @Override public LocalRepository getLocalRepository(){
      return getLocalRepositoryManager().getRepository();
    }
  }
;
}
