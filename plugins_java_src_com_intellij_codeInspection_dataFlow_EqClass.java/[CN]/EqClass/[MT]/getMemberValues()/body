{
  final List<DfaValue> result=new ArrayList<DfaValue>(size());
  forEach(new TIntProcedure(){
    @Override public boolean execute(    int c1){
      DfaValue value=myFactory.getValue(c1);
      result.add(value);
      return true;
    }
  }
);
  return result;
}
