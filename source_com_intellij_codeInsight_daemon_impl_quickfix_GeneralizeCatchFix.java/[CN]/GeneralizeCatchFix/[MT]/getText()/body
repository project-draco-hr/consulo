{
  String text=MessageFormat.format("Generalize catch for ''{0}'' to ''{1}''",new Object[]{HighlightUtil.formatType(myCatchParameter.getType()),HighlightUtil.formatType(myUnhandledException)});
  return text;
}
