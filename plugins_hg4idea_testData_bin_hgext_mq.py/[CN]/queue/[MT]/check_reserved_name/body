def check_reserved_name(self, name):
    if ((name in self._reserved) or name.startswith('.hg') or name.startswith('.mq') or ('#' in name) or (':' in name)):
        raise util.Abort((_('"%s" cannot be used as the name of a patch') % name))
