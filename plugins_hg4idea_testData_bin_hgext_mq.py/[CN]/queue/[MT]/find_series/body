def find_series(self, patch):
    pre = re.compile('(\\s*)([^#]+)')
    index = 0
    for l in self.full_series:
        m = pre.match(l)
        if m:
            s = m.group(2)
            s = s.rstrip()
            if (s == patch):
                return index
        index += 1
    return None
