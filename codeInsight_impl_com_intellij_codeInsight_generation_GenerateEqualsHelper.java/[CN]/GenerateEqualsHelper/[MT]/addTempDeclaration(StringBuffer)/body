{
  for (  PsiField hashCodeField : myHashCodeFields)   if (PsiType.DOUBLE == hashCodeField.getType()) {
    final String name=getUniqueLocalVarName(TEMP_VARIABLE,myHashCodeFields);
    buffer.append("long ").append(name).append(";\n");
    return name;
  }
  return null;
}
