{
  super(markers.get(0).getElement(),getCommonTextRange(markers),markers.get(0).getCommonIcon(markers),4,markers.get(0).getCommonTooltip(markers),getCommonNavigationHandler(markers),GutterIconRenderer.Alignment.LEFT);
}
