{
  File f=new File(myDir,"counter");
  f.createNewFile();
  FileOutputStream fs=new FileOutputStream(f);
  try {
    Stream s=new Stream(fs);
    s.writeInteger(i);
  }
  finally {
    fs.close();
  }
}
