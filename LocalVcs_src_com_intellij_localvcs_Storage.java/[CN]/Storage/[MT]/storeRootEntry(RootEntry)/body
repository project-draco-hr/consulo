{
  File f=new File(myDir,"rootEntry");
  f.createNewFile();
  FileOutputStream fs=new FileOutputStream(f);
  try {
    Stream s=new Stream(fs);
    s.writeEntry(e);
  }
  finally {
    fs.close();
  }
}
