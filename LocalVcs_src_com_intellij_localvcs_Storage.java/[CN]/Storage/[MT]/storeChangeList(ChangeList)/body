{
  File f=new File(myDir,"changeList");
  f.createNewFile();
  FileOutputStream fs=new FileOutputStream(f);
  try {
    Stream s=new Stream(fs);
    s.writeChangeList(c);
  }
  finally {
    fs.close();
  }
}
