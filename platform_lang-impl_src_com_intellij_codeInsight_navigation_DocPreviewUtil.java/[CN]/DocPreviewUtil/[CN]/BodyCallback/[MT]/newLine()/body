{
  if (onLastRow()) {
    if (myQName != null) {
      myContext.buffer.append(String.format(" <a href='psi_element://%s'>...</a>",myQName));
    }
    return false;
  }
  addText("<br/>",false);
  myCurrentColumn=0;
  myScheduleNewLine=false;
  myCurrentRow++;
  return true;
}
