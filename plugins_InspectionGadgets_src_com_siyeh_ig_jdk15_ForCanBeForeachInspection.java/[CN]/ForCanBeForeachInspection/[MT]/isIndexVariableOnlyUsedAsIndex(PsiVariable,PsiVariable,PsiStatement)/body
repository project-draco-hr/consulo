{
  if (body == null) {
    return true;
  }
  final VariableOnlyUsedAsIndexVisitor visitor=new VariableOnlyUsedAsIndexVisitor(arrayVariable,indexVar);
  body.accept(visitor);
  return visitor.isIndexVariableUsedOnlyAsIndex();
}
