{
  if (!getViewProvider().isPhysical() || myExplicitlySetAsValid)   return true;
  final VirtualFile vFile=getViewProvider().getVirtualFile();
  return vFile.isValid() && myManager.findFile(vFile) == this;
}
