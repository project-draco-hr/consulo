{
  final String name=myPsiElement instanceof PsiFileSystemItem ? UsageViewUtil.getShortName(myPsiElement) : UsageViewUtil.getDescriptiveName(myPsiElement);
  return (UsageViewUtil.getType(myPsiElement) + " " + name).trim();
}
