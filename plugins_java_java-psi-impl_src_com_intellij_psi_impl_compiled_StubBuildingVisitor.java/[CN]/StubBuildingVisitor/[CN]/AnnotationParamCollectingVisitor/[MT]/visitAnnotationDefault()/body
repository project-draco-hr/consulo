{
  return new AnnotationTextCollector(null,new AnnotationResultCallback(){
    @Override public void callback(    final String text){
      ((PsiMethodStubImpl)myOwner).setDefaultValueText(text);
    }
  }
);
}
