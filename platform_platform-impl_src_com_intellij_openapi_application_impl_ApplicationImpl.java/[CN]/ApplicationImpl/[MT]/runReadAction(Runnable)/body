{
  Status status=getStatus();
  if (isReadAccessAllowed(status)) {
    action.run();
  }
 else {
    startRead(status);
    try {
      action.run();
    }
  finally {
      endRead(status);
    }
  }
}
