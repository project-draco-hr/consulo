{
  Status status=getStatus();
  if (isReadAccessAllowed(status)) {
    return computation.compute();
  }
  startRead(status);
  try {
    return computation.compute();
  }
  finally {
    endRead(status);
  }
}
