{
  while (root != null) {
    int compResult=comparator.compare(key,root.key);
    if (compResult < 0) {
      root=root.getLeft();
    }
 else     if (compResult > 0) {
      root=root.getRight();
    }
 else {
      return root;
    }
  }
  return root;
}
