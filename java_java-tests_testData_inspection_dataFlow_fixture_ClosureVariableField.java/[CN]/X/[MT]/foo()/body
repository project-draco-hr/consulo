{
  final MyReference isAlive=new MyReference();
  if (isAlive.getValue()) {
    new Runnable(){
      @Override public void run(){
        if (isAlive.getValue()) {
          System.out.println();
        }
      }
    }
;
  }
}
