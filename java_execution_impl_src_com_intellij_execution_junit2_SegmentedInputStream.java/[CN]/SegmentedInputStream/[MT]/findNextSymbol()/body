{
  int nextByte;
  while (true) {
    nextByte=readNext();
    if (nextByte != SegmentedStream.SPECIAL_SYMBOL)     break;
    final boolean packetRead=readControlSequence();
    if (!packetRead)     break;
  }
  return nextByte;
}
