{
  return ResolveCache.getInstance(myAttribute.getProject()).resolveWithCaching(this,new ResolveCache.Resolver(){
    @Nullable public PsiElement resolve(    @NotNull PsiReference psiReference,    boolean b){
      return resolveImpl();
    }
  }
,false,false);
}
