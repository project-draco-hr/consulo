{
  if (root == null) {
    return;
  }
  Collection<Change> changes=result.get(root);
  if (changes == null) {
    changes=new HashSet<Change>();
    result.put(root,changes);
  }
  changes.add(change);
}
