{
  if (myCurrentIndex == 3)   return false;
  if (myCurrent != null && myCurrent.hasMoreElements()) {
    return true;
  }
  if (myCurrentIndex == 0) {
    myCurrent=mySecond;
  }
 else   if (myCurrentIndex == 1) {
    myCurrent=myThird;
  }
  myCurrentIndex++;
  return hasMoreElements();
}
