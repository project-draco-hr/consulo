{
  if (myState != state) {
    if (state.isFinal()) {
      addLast(state);
    }
 else {
      fireOnNewPrintable(state);
    }
    if (myState != null) {
      state.merge(myState,getParent());
    }
    myState=state;
    fireStateChanged();
  }
}
