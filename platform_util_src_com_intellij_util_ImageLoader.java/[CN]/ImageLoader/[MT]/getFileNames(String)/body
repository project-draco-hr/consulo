{
  if (UIUtil.isRetina()) {
    return Arrays.asList(Pair.create(FileUtil.getNameWithoutExtension(file) + "@2x." + FileUtil.getExtension(file),2),Pair.create(file,1));
  }
  return Collections.singletonList(Pair.create(file,1));
}
