{
  Pair<Integer,Integer> fragment=getFragment(myMutableGraph.getIndexInPermanentGraph(lowerNodeIndex),upNodesFun,downNodesFun,thisNodeCantBeInMiddle);
  return fragment == null ? null : new GraphFragment(myMutableGraph.toVisibleIndex(fragment.second),myMutableGraph.toVisibleIndex(fragment.first));
}
