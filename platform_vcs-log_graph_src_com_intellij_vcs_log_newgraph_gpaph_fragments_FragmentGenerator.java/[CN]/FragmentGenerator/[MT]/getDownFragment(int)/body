{
  Pair<Integer,Integer> fragment=getFragment(myMutableGraph.getIndexInPermanentGraph(upperVisibleNodeIndex),downNodesFun,upNodesFun,thisNodeCantBeInMiddle);
  return fragment == null ? null : new GraphFragment(myMutableGraph.toVisibleIndex(fragment.first),myMutableGraph.toVisibleIndex(fragment.second));
}
