{
  if (!(obj instanceof AntTypeId))   return false;
  AntTypeId right=(AntTypeId)obj;
  return myId.getFirst().equals(right.myId.getFirst()) && (myId.getSecond() == null || right.myId.getSecond() == null || myId.getSecond().equals(right.myId.getSecond()));
}
