{
  if (delegate.getProtocol().equals(LocalFileSystem.PROTOCOL) && Registry.is("filesystem.useNative") && SystemInfo.isWindows&& Win32LocalFileSystem.isAvailable()) {
    delegate=Win32LocalFileSystem.getWin32Instance();
  }
  return delegate;
}
