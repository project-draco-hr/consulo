{
  int i=0;
  for (; i < points.size(); i++) {
    Pair<Integer,Integer> integerIntegerPair=points.get(i);
    if (integerIntegerPair.getFirst() >= width)     break;
  }
  i=i == 0 ? 0 : i - 1;
  return i;
}
