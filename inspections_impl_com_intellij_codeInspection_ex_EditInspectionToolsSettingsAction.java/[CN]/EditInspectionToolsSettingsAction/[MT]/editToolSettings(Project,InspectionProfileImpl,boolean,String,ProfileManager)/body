{
  final boolean isOK=ShowSettingsUtil.getInstance().editConfigurable(project,"#com.intellij.codeInspection.ex.EditInspectionToolsSettingsAction",new InspectionToolsConfigurable(project,canChooseDifferentProfile,manager,inspectionProfile,selectedToolShortName));
  return isOK;
}
