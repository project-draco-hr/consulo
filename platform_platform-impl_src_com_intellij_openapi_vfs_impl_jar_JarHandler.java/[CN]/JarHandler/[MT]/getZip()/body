{
  ZipFile zip=myZipFile.get();
  if (zip == null) {
    try {
      zip=new ZipFile(getMirrorFile(getOriginalFile()));
      myZipFile.set(zip);
    }
 catch (    IOException e) {
      return null;
    }
  }
  return zip;
}
