{
synchronized (this) {
    if (myListeners.isEmpty()) {
      return;
    }
  }
  ApplicationManager.getApplication().runWriteAction(new Runnable(){
    public void run(){
      fireRootSetChanged();
    }
  }
);
}
