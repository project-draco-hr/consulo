{
  if (StringUtil.isEmpty(signature))   return null;
  try {
    return JavaPsiFacade.getInstance(myProject).getElementFactory().createMethodFromText("void " + MethodParameterInjection.fixSignature(signature,true) + "{}",null);
  }
 catch (  IncorrectOperationException e) {
  }
  return null;
}
