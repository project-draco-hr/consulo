{
  final boolean og=o2.isGlobal();
  final boolean g=o1.isGlobal();
  if (og && !g) {
    return 1;
  }
 else   if (!og && g) {
    return -1;
  }
 else {
    return o1.getName().compareTo(o2.getName());
  }
}
