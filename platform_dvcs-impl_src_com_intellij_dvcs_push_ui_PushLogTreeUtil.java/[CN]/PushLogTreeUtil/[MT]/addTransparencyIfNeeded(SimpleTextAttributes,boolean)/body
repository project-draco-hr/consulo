{
  if (isActive)   return baseStyle;
  Color color=baseStyle.getFgColor();
  if (color == null) {
    color=JBColor.black;
  }
  return new SimpleTextAttributes(baseStyle.getStyle(),new Color(color.getRed(),color.getGreen(),color.getBlue(),85));
}
