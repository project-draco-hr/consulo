{
  VirtualFile documentFile=getDocumentFile(myDocument);
  String documentTitle=documentFile != null ? ElementPresentation.forVirtualFile(documentFile).getNameWithFQComment() : DiffBundle.message("diff.content.editor.content.title");
  return new String[]{getElementPresentation().getNameWithFQComment(),documentTitle};
}
