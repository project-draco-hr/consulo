{
  VirtualFile documentFile=getDocumentFile(myDocument);
  String documentTitle=documentFile != null ? getVirtualFileContentTitle(documentFile) : DiffBundle.message("diff.content.editor.content.title");
  return new String[]{getVirtualFileContentTitle(myFile),documentTitle};
}
