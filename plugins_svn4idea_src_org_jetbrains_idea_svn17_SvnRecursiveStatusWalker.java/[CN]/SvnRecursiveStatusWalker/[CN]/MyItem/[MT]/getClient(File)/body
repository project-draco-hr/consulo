{
  if (!SVNDepth.INFINITY.equals(myDepth)) {
    return mySvnClient;
  }
  if (CheckJavaHL.isPresent() && SvnConfiguration17.UseAcceleration.javaHL.equals(myConfiguration17.myUseAcceleration) && Svn17Detector.is17(myProject,file)) {
    return new JavaHLSvnStatusClient(myProject);
  }
  return mySvnClient;
}
