{
  Iterator<Keymap> it=myKeymaps.iterator();
  while (it.hasNext()) {
    Keymap keymap=it.next();
    if (keymap.canModify()) {
      it.remove();
    }
  }
  myActiveKeymap=null;
  if (myKeymaps.size() > 0) {
    myActiveKeymap=(KeymapImpl)myKeymaps.get(0);
  }
}
