{
  if (!(o instanceof FilePath)) {
    return false;
  }
 else {
    if (!isSpecialName(myName) && !isSpecialName(((FilePath)o).getName()) && (!Comparing.equal(myName,((FilePath)o).getName())))     return false;
    return myFile.equals(((FilePath)o).getIOFile());
  }
}
