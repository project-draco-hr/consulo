{
  for (  LineMarkerInfo lineMarkerInfo : infoCollection) {
    GutterIconRenderer gutterIconRenderer=lineMarkerInfo.createGutterRenderer();
    final HighlightInfo highlightInfo=HighlightInfo.createHighlightInfo(HighlightInfoType.INFO,lineMarkerInfo.startOffset,lineMarkerInfo.startOffset,gutterIconRenderer != null ? gutterIconRenderer.getTooltipText() : lineMarkerInfo.toString());
    result.add(highlightInfo);
    highlightInfo.setGutterIconRenderer(gutterIconRenderer);
  }
}
