{
  PsiElement parent=element.getParent();
  PsiElement grand;
  if (element instanceof PsiCodeBlock && parent instanceof PsiMethod && !((PsiMethod)parent).isConstructor()&& (grand=parent.getParent()) instanceof PsiClass&& !(grand instanceof PsiAnonymousClass)) {
    return parent;
  }
  return null;
}
