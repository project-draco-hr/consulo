{
  super.update(e);
  Project eventProject=getEventProject(e);
  if (eventProject == null) {
    return;
  }
  e.getPresentation().setVisible(FILE.equals(DependencyUISettings.getInstance().SCOPE_TYPE) && PsiPackageSupportProviders.isPackageSupported(eventProject));
}
