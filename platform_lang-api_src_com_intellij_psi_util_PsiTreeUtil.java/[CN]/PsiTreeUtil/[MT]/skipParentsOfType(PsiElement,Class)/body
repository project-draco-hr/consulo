{
  if (element == null)   return null;
  NextSibling:   for (PsiElement e=element.getParent(); e != null; e=e.getParent()) {
    for (    Class aClass : parentClasses) {
      if (instanceOf(aClass,e))       continue NextSibling;
    }
    return e;
  }
  return null;
}
