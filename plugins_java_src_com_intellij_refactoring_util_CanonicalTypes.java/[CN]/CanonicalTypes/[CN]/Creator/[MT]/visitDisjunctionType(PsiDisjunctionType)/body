{
  final List<Type> types=ContainerUtil.map(disjunctionType.getDisjunctions(),new Function<PsiType,Type>(){
    @Override public Type fun(    PsiType type){
      return createTypeWrapper(type);
    }
  }
);
  return new DisjunctionType(types);
}
