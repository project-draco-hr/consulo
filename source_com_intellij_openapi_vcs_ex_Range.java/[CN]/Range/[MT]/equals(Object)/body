{
  if (!(object instanceof Range))   return false;
  Range other=(Range)object;
  return (myOffset1 == other.myOffset1) && (myUpToDateOffset1 == other.myUpToDateOffset1) && (myUpToDateOffset2 == other.myUpToDateOffset2)&& (myOffset1 == other.myOffset1)&& (myOffset2 == other.myOffset2)&& (myType == other.myType);
}
