{
  if (!firstWasFound) {
    if (test.isDefect() && TestConsoleProperties.SELECT_FIRST_DEFECT.value(myConsoleProperties)) {
      myResultsViewer.selectAndNotify(test);
    }
    firstWasFound=true;
  }
}
