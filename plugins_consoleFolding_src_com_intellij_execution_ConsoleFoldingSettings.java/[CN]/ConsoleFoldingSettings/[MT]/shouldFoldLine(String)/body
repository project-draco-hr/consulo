{
  positive:   for (  String pattern : myPositivePatterns) {
    if (line.contains(pattern)) {
      for (      String negativePattern : myNegativePatterns) {
        if (line.contains(negativePattern)) {
          continue positive;
        }
      }
      return true;
    }
  }
  return false;
}
