{
switch (myDoneEvent.getType()) {
case DONE:
    return ExecutionBundle.message("junit.runing.info.status.done.count",testCount);
default :
  return ExecutionBundle.message("junit.runing.info.status.terminated.count",testCount);
}
}
