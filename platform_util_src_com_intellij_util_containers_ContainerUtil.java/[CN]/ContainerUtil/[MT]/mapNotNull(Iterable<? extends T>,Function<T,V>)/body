{
  List<V> result=null;
  for (  T t : iterable) {
    final V o=mapping.fun(t);
    if (o != null) {
      if (result == null) {
        result=new ArrayList<V>();
      }
      result.add(o);
    }
  }
  return result == null ? Collections.<V>emptyList() : result;
}
