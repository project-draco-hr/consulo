{
  assertIsDispatchThread();
  myPlainSpaceWidth=-1;
synchronized (myLock) {
    myLineHeight=-1;
    myDescent=-1;
    myCharHeight=-1;
    myTabSize=-1;
  }
  reset();
  setPrefix(myPrefixText,myPrefixAttributes);
  invalidateFoldRegionLayouts();
}
