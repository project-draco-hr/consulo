{
  MavenFacadeEmbedder w=getWrappee();
  if (w == null)   return;
  try {
    w.release();
  }
 catch (  RemoteException e) {
    handleRemoteError(e);
  }
  resetConsole(null);
  resetIndicator(null);
}
