{
  int flag=ourModifierNameToFlagMap.get(name);
  if (flag == 0) {
    return PsiModifier.PACKAGE_LOCAL.equals(name) && (myFlags & (ClsUtil.ACC_PUBLIC | ClsUtil.ACC_PROTECTED | ClsUtil.ACC_PRIVATE)) == 0;
  }
  return (myFlags & flag) != 0;
}
