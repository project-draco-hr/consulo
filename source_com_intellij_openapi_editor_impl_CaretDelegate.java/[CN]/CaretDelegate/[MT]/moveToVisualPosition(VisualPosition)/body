{
  int rangeLine=myEditorDelegate.getDocument().getLineNumber(myRange.getStartOffset());
  VisualPosition newPos=new VisualPosition(pos.line + rangeLine,pos.column + myRange.getStartOffset() - myEditorDelegate.getDocument().getLineStartOffset(rangeLine));
  myDelegate.moveToVisualPosition(newPos);
}
