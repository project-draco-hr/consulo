{
  int rangeLine=myEditorDelegate.getDocument().getLineNumber(myRange.getStartOffset());
  LogicalPosition newPos=new LogicalPosition(pos.line + rangeLine,pos.column + myRange.getStartOffset() - myEditorDelegate.getDocument().getLineStartOffset(rangeLine));
  myDelegate.moveToLogicalPosition(newPos);
}
