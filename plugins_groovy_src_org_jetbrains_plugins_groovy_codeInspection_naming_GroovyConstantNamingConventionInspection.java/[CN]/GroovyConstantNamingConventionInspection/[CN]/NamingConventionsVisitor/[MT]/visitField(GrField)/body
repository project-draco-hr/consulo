{
  super.visitField(grField);
  if (!grField.hasModifierProperty(PsiModifier.STATIC) || !grField.hasModifierProperty(PsiModifier.FINAL)) {
    return;
  }
  final String name=grField.getName();
  if (isValid(name)) {
    return;
  }
  registerVariableError(grField,name);
}
