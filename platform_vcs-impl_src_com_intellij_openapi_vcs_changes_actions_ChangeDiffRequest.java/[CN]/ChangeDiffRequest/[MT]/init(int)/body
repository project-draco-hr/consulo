{
  if (idx < 0 || idx > (mySteps.size() - 1))   return null;
  myIndex=idx - 1;
  final DiffRequest result=moveForward();
  if (result == null && idx > 0) {
    myIndex=-1;
    return moveForward();
  }
  return result;
}
