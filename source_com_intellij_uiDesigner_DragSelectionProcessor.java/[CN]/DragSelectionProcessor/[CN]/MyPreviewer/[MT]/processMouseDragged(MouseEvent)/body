{
  if (!myPreview) {
    myTimer.restart();
    return;
  }
  if ((Math.abs(e.getX() - myDropPoint.getX()) > TREMOR || Math.abs(e.getY() - myDropPoint.getY()) > TREMOR)) {
    cancelPreview();
    myEditor.refresh();
  }
}
