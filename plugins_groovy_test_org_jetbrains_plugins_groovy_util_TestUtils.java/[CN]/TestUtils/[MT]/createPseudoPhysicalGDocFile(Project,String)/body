{
  return PsiManager.getInstance(project).getElementFactory().createFileFromText(GDOC_TEMP_FILE,FileTypeManager.getInstance().getFileTypeByFileName(GDOC_TEMP_FILE),text,LocalTimeCounter.currentTime(),true);
}
