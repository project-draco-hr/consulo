{
  for (int i=0; i < myModalEntities.length; i++) {
    Object entity=myModalEntities[i].get();
    if (entity == null)     continue;
    if (!contains(((ModalityStateEx)anotherState).myModalEntities,entity))     return true;
  }
  return false;
}
