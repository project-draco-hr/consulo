{
  final Collection<ProcessorConfigProfile> allProfiles=JpsJavaExtensionService.getInstance().getOrCreateCompilerConfiguration(model.getProject()).getAnnotationProcessingConfigurations();
  ProcessorConfigProfile profile=null;
  for (  ProcessorConfigProfile p : allProfiles) {
    if (p.getModuleNames().contains(getModuleName())) {
      if (p.isEnabled()) {
        profile=p;
      }
      break;
    }
  }
  return profile;
}
