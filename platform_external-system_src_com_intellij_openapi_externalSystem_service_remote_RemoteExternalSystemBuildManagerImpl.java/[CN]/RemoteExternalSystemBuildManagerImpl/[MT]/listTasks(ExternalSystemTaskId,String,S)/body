{
  return execute(id,new Producer<Collection<ExternalSystemTaskDescriptor>>(){
    @Nullable @Override public Collection<ExternalSystemTaskDescriptor> produce(){
      return myDelegate.listTasks(id,projectPath,settings);
    }
  }
);
}
