{
  WorkingContextManager manager=getContextManager();
  manager.saveContext("foo","bar");
  File file=manager.getContextFile();
  assertTrue(file.length() > 0);
  FileUtil.writeToFile(file,"123");
  manager.saveContext("foo","bar");
}
