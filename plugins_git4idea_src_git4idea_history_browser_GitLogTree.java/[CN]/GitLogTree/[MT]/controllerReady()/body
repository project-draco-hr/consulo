{
  new AbstractCalledLater(myProject,ModalityState.NON_MODAL){
    public void run(){
      for (      Runnable initWaiter : myInitWaiters) {
        initWaiter.run();
      }
    }
  }
.callMe();
}
