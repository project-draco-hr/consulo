{
  if (referencies != null && (!referencies.isEmpty())) {
    final SimpleTextAttributes attrs=new SimpleTextAttributes(SimpleTextAttributes.STYLE_BOLD | SimpleTextAttributes.STYLE_UNDERLINE,color);
    for (    String reference : referencies) {
      append(reference,attrs);
      append(" ");
    }
  }
}
