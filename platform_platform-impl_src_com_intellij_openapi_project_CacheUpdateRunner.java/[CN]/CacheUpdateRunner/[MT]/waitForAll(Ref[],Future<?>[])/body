{
  try {
    for (    Future<?> future : futures) {
      future.get();
    }
    boolean allFinished=true;
    for (    Ref ref : finishedRefs) {
      if (!(Boolean)ref.get()) {
        allFinished=false;
        break;
      }
    }
    return allFinished;
  }
 catch (  Throwable throwable) {
    LOG.error(throwable);
  }
  return false;
}
