{
  if (aClass instanceof PsiAnonymousClass) {
    return;
  }
  if (!ClassUtils.isSubclass(aClass,CommonClassNames.JAVA_UTIL_COMPARATOR)) {
    return;
  }
  if (SerializationUtils.isSerializable(aClass)) {
    return;
  }
  registerClassError(aClass);
}
