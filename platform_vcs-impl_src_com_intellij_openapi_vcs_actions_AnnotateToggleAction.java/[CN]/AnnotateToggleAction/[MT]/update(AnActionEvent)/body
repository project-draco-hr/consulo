{
  super.update(e);
  final boolean enabled=isEnabled(VcsContextFactory.SERVICE.getInstance().createContextOn(e)) || AnnotateDiffViewerAction.isEnabled(e);
  e.getPresentation().setEnabled(enabled);
}
