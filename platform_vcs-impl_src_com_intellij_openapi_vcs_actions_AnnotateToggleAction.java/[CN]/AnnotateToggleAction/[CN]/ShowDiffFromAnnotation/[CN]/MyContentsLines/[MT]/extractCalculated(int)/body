{
  String text=myContents.substring(myLinesStartOffsets.get(number),(number > (myLinesStartOffsets.size() - 1)) ? myContents.length() : myLinesStartOffsets.get(number + 1));
  text=text.endsWith("\r\n") ? text.substring(0,text.length() - 2) : text.substring(0,text.length() - 1);
  return text;
}
