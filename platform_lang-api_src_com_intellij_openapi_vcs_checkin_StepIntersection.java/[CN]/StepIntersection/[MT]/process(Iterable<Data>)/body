{
  final List<Data> result=new ArrayList<Data>();
  process(data,new PairConsumer<Data,Area>(){
    @Override public void consume(    Data data,    Area area){
      result.add(data);
    }
  }
);
  return result;
}
