{
  if (myStatement == null || !myStatement.isValid()) {
    return false;
  }
  return !(myStatement instanceof PsiExpressionStatement) || !HighlightUtil.isSuperOrThisMethodCall(((PsiExpressionStatement)myStatement).getExpression());
}
