{
  final UIOptions result=new UIOptions();
  @NonNls Element temp=new Element("temp");
  try {
    writeExternal(temp);
    result.readExternal(temp);
  }
 catch (  Exception e) {
  }
  return result;
}
