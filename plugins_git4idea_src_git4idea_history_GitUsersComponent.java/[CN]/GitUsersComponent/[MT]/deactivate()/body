{
  if (myState != null)   try {
    myState.close();
  }
 catch (  IOException e) {
    LOG.info(e);
  }
  myState=null;
synchronized (myLock) {
    myAccessMap.clear();
  }
  myManager.removeVcsListener(myVcsListener);
  myVcs=null;
  myIsActive=false;
}
