{
  PsiTypeElement typeElement=parameter.getTypeElement();
  return typeElement.getType() instanceof PsiDisjunctionType ? PsiTreeUtil.getChildrenOfTypeAsList(typeElement,PsiTypeElement.class) : Collections.singletonList(typeElement);
}
