{
  myState=new RemoteClassValidityState(myOutputClassFile.getTimeStamp(),myStub.exists() ? myStub.lastModified() : -1L,mySkel.exists() ? mySkel.lastModified() : -1L,myTie.exists() ? myTie.lastModified() : -1L);
}
