{
  if (!d.hasDiffs()) {
    Messages.showMessageDialog(project,VcsBundle.message("message.text.nothing.was.found.to.commit"),VcsBundle.message("message.title.nothing.was.found.to.commit"),Messages.getInformationIcon());
    return false;
  }
  d.show();
  return d.isOK();
}
