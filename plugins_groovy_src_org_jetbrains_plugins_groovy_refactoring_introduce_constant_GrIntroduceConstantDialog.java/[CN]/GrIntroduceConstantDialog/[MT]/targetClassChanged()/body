{
  final String targetClassName=getTargetClassName();
  myTargetClass=JavaPsiFacade.getInstance(myContext.project).findClass(targetClassName,GlobalSearchScope.projectScope(myContext.project));
  updateVisibilityPanel();
}
