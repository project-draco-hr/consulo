{
  super.update(e);
  final Project project=e.getData(PlatformDataKeys.PROJECT);
  e.getPresentation().setVisible(project != null && ActionPlaces.MAIN_TOOLBAR.equals(e.getPlace()) && ProjectFacetManager.getInstance(project).getFacets(AndroidFacet.ID).size() > 0);
}
