{
  boolean result=element instanceof XmlElementDecl || element instanceof XmlAttributeDecl || element instanceof XmlTag;
  if (!result && styleHandler != null) {
    result=styleHandler.canFindUsagesFor(element);
  }
  return result;
}
