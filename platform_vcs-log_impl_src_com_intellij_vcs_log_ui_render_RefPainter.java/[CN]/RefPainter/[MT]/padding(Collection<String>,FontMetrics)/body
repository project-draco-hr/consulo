{
  int p=0;
  for (  String ref : refs) {
    XAndWidth xAndWidth=getXAndWidth(ref,0,fontMetrics);
    p+=xAndWidth.x + xAndWidth.width + LABEL_PADDING;
  }
  if (p > 0) {
    p+=LABEL_PADDING;
  }
  return p;
}
