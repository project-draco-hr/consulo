{
  if (!(obj instanceof ClassAttributes)) {
    return false;
  }
  final ClassAttributes ca=(ClassAttributes)obj;
  return super.equals(ca) && abAttr.equals(ca.abAttr) && enumAttr.equals(ca.enumAttr);
}
