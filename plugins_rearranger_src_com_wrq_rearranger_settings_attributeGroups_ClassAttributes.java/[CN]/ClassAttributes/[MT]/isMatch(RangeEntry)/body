{
  return entry.getEnd().getParent() instanceof PsiClass && entry.getEnd() instanceof PsiJavaToken && entry.getEnd().getText().equals("{")&& super.isMatch(entry)&& abAttr.isMatch(entry.getModifiers())&& enumAttr.isMatch(entry.getModifiers());
}
