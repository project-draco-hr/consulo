{
  final StringBuilder sb=new StringBuilder().append(PathManager.getSystemPath()).append(File.separator).append("userHistory").append(File.separator).append(myType).append(Long.toHexString(StringHash.calc(myId))).append(".hist.xml");
  return new File(sb.toString());
}
