{
  vcs.createFile("f",b("old"),null);
  vcs.apply();
  vcs.changeFileContent("f",b("new"),null);
  vcs.apply();
  initModelFor("f");
  m.selectLabels(1,1);
  assertDifferenceModelContents("old","new");
}
