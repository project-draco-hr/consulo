{
  final MutableMarker marker=builder.mark();
  final IElementType type=builder.parse(rightPriority);
  if (type == null) {
    marker.rollback();
  }
 else {
    marker.finish();
  }
  return type;
}
