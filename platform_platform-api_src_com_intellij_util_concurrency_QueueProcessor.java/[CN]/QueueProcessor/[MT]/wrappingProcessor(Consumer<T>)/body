{
  return new PairConsumer<T,Runnable>(){
    @Override public void consume(    final T item,    Runnable runnable){
      runSafely(new Runnable(){
        @Override public void run(){
          processor.consume(item);
        }
      }
);
      runnable.run();
    }
  }
;
}
