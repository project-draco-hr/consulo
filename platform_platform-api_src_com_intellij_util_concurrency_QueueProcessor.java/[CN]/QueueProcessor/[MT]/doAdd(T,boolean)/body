{
synchronized (myQueue) {
    if (!isProcessing) {
      isProcessing=true;
      startProcessing(element);
      return;
    }
    if (atHead) {
      myQueue.addFirst(element);
    }
 else {
      myQueue.add(element);
    }
  }
}
