{
  createProjectPom("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<properties>"+ "  <foo>@<caret>project.version@</foo>"+ "</properties>");
  assertResolved(myProjectPom,findTag("project.version"));
}
