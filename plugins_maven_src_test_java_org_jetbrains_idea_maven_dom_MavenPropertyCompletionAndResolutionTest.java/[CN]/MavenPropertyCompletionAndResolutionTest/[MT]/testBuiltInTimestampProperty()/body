{
  createProjectPom("<groupId>test</groupId" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<name>${<caret>maven.build.timestamp}</name>");
  assertResolved(myProjectPom,findTag("project.name"));
}
