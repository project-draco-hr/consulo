{
  createProjectPom("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<name>${<error>xxx</error>}</name>"+ "<properties>"+ "  <foo>${<error>zzz</error>}</foo>"+ "</properties>");
  checkHighlighting();
}
