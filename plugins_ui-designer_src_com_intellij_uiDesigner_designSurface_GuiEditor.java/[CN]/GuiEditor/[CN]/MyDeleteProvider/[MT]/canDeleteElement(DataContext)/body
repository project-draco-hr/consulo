{
  final UIDesignerToolWindowManager manager=UIDesignerToolWindowManager.getInstance(getProject());
  return !manager.getPropertyInspector().isEditing() && !myInplaceEditingLayer.isEditing() && FormEditingUtil.canDeleteSelection(GuiEditor.this);
}
