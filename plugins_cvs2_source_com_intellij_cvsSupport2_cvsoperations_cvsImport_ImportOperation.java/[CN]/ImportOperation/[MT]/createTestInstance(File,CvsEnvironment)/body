{
  ImportDetails details=new ImportDetails(sourceLocation,"VENDOR","RELEASE_TAG","LOG_MESSAGE",sourceLocation.getName(),env,new ArrayList(),new IIgnoreFileFilter(){
    public boolean shouldBeIgnored(    AbstractFileObject abstractFileObject,    ICvsFileSystem cvsFileSystem){
      return false;
    }
  }
);
  return new ImportOperation(details);
}
