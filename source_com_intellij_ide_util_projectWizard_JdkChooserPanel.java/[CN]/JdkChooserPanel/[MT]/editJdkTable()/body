{
  ProjectJdksEditor editor=new ProjectJdksEditor((ProjectJdk)myList.getSelectedValue(),myProject != null ? myProject : ProjectManager.getInstance().getDefaultProject(),myList);
  editor.show();
  if (editor.isOK()) {
    ProjectJdk selectedJdk=editor.getSelectedJdk();
    updateList(selectedJdk);
  }
}
