{
  if (text == null)   return 0;
  char[] chars=text.toCharArray();
  int counter=0;
  for (int i=0; i < chars.length; i++) {
    char aChar=chars[i];
    if (aChar == '\n')     counter++;
  }
  return counter;
}
