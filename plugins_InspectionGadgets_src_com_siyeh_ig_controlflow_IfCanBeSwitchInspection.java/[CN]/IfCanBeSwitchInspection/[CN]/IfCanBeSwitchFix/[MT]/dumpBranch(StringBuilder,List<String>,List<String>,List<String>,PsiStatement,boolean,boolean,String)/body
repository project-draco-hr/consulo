{
  dumpComments(switchStatementText,comments);
  dumpLabels(switchStatementText,labels);
  dumpComments(switchStatementText,statementComments);
  dumpBody(switchStatementText,body,wrap,renameBreaks,breakLabelName);
}
