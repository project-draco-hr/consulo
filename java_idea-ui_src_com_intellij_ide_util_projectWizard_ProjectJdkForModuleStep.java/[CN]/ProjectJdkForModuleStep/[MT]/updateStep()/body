{
  if (!myInitialized) {
    myJdkChooser.fillList(myType);
    Sdk defaultJdk=getDefaultJdk();
    if (defaultJdk != null) {
      myJdkChooser.selectJdk(defaultJdk);
    }
    myInitialized=true;
  }
}
