{
  new HgTagBranchCommand(project,root).listTags(new Consumer<List<HgTagBranch>>(){
    @Override public void consume(    List<HgTagBranch> tags){
      tagSelector.setModel(new DefaultComboBoxModel(tags.toArray()));
    }
  }
);
}
