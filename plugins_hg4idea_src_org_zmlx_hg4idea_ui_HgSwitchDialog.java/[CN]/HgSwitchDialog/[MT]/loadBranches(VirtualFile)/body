{
  new HgTagBranchCommand(project,root).listBranches(new Consumer<List<HgTagBranch>>(){
    @Override public void consume(    List<HgTagBranch> branches){
      branchSelector.setModel(new DefaultComboBoxModel(branches.toArray()));
    }
  }
);
}
