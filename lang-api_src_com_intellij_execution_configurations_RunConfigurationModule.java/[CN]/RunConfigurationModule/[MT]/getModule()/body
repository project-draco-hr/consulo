{
  if (myModuleName != null) {
    myModule=findModule(myModuleName);
    if (myModule != null) {
      myModuleName=null;
    }
  }
  if (myModule != null && myModule.isDisposed()) {
    myModule=null;
  }
  return myModule;
}
