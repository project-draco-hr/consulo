{
  Change c=new CreateFileChange(1,p("file"),null);
  c.applyTo(root);
  root.doCreateFile(2,p("another file"),null);
  List<Difference> d=c.getDifferences(root,root.getEntry(p("another file")));
  assertTrue(d.isEmpty());
}
