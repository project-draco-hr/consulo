{
  if (0 <= val && val < 192) {
    record.writeByte((int)val);
  }
 else {
    record.writeByte(192 + (int)(val & 0x3F));
    val>>>=6;
    while (val >= 128) {
      record.writeByte((int)(val & 0x7F) | 0x80);
      val>>>=7;
    }
    record.writeByte((int)val);
  }
}
