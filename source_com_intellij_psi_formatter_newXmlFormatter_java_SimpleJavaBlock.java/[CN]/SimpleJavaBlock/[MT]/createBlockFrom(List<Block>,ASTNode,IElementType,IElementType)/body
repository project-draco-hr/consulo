{
  return createSynteticBlock(result,child,from,to,getCodeBlockExternalIndent(),getCodeBlockInternalIndent(child),new WrappingStrategy(Formatter.getInstance().createWrap(Wrap.NORMAL,true)){
    protected boolean shouldWrap(    final IElementType type){
      return true;
    }
  }
,null);
}
