{
  final PsiElement nameElement=getNameElement();
  if (nameElement != null) {
    return ReferenceProvidersRegistry.getInstance(getProject()).getManipulator(nameElement).handleContentChange(nameElement,new TextRange(0,nameElement.getTextLength()),name);
  }
  return null;
}
