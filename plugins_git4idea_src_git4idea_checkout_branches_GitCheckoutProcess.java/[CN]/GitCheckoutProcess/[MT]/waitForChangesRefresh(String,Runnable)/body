{
  UIUtil.invokeLaterIfNeeded(new Runnable(){
    public void run(){
      myChangeManager.invokeAfterUpdate(runnable,InvokeAfterUpdateMode.BACKGROUND_NOT_CANCELLABLE,title,ModalityState.NON_MODAL);
    }
  }
);
}
