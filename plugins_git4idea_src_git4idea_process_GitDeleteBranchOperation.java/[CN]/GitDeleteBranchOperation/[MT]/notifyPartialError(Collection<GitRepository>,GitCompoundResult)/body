{
  String title="Couldn't delete branch " + myBranchName + " in some repositories";
  StringBuilder message=new StringBuilder();
  message.append("Successfully removed in ").append(joinRepositoryUrls(succeeded,"<br/>")).append(compoundResult.getErrorOutputWithReposIndication());
  showFatalError(title,message.toString(),myProject);
}
