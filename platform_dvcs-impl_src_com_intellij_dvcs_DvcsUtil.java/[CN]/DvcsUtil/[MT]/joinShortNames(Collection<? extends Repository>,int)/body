{
  return joinWithAnd(ContainerUtil.map(repositories,new Function<Repository,String>(){
    @Override public String fun(    @NotNull Repository repository){
      return getShortRepositoryName(repository);
    }
  }
),limit);
}
