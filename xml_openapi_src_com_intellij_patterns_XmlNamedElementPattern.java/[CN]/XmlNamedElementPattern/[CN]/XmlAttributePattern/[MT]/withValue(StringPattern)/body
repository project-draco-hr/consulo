{
  return with(new PatternConditionPlus<XmlAttribute,String>("withValue",pattern){
    @Override public boolean processValues(    XmlAttribute t,    ProcessingContext context,    PairProcessor<String,ProcessingContext> processor){
      return processor.process(t.getValue(),context);
    }
  }
);
}
