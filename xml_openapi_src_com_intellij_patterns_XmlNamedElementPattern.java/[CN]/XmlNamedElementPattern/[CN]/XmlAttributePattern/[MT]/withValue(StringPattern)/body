{
  return with(new PatternCondition<XmlAttribute>("withValue"){
    @Override public boolean accepts(    @NotNull XmlAttribute xmlAttribute,    ProcessingContext context){
      return pattern.accepts(xmlAttribute.getValue(),context);
    }
  }
);
}
