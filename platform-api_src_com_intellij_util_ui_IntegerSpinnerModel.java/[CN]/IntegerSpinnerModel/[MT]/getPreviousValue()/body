{
  int result;
  if (myMinValue >= 0 && myValue == myMinValue) {
    if (myMaxValue < 0)     return null;
    result=myMaxValue;
  }
 else {
    result=myValue - 1;
  }
  return new Integer(result);
}
