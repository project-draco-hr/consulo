{
  final PsiElement element=(PsiElement)context.getData(DataConstants.PSI_ELEMENT);
  if (element == null) {
    return null;
  }
  if (element instanceof PsiDirectory) {
    return ((PsiDirectory)element).getPackage();
  }
  return null;
}
