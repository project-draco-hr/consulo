{
  final XmlAttributeValue element=computeElement();
  if (element == null) {
    throw new IncorrectOperationException("Cannot find resource '" + myResourceInfo.getName() + "' in file "+ myResourceInfo.getContainingFile().getPath());
  }
  System.out.println("Lazy;set name: " + name);
  return new ValueResourceElementWrapper(element).setName(name);
}
