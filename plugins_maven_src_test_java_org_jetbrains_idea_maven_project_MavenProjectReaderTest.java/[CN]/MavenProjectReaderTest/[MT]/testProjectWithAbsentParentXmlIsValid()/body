{
  createProjectPom("<parent>" + "  <groupId>test</groupId>" + "  <artifactId>parent</artifactId>"+ "  <version>1</version>"+ "</parent>");
  assertTrue(readProject(myProjectPom,new NullProjectLocator()).isValid);
}
