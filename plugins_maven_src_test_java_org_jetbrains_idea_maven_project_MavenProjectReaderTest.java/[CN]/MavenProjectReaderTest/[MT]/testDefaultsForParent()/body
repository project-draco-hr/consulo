{
  createProjectPom("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<parent>"+ "  dummy"+ "</parent>");
  MavenModel p=readProject(myProjectPom);
  assertParent(p,"Unknown","Unknown","Unknown");
}
