{
  if (!super.processDeclarations(processor,state,lastParent,place))   return false;
  if (isWebFileType())   return ScriptSupportUtil.processDeclarations(this,processor,state,lastParent,place);
  return true;
}
