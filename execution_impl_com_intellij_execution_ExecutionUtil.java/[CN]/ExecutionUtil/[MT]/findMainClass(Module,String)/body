{
  final PsiManager psiManager=PsiManager.getInstance(module.getProject());
  return JavaPsiFacade.getInstance(psiManager.getProject()).findClass(mainClassName.replace('$','.'),GlobalSearchScope.moduleWithDependenciesAndLibrariesScope(module));
}
