{
  super(DOM.createDiv(),DOM.createDiv(),preventBoxStyles(DOM.createDiv()),preventBoxStyles(DOM.createDiv()));
  container=preventBoxStyles(DOM.createDiv());
  buildDOM();
  setStyleName("gwt-HorizontalSplitPanel2");
  impl.init(this);
  setHeight("100%");
}
