{
  VirtualFile virtualFile=getVirtualFile();
  myPhysical=isEventSystemEnabled() && !(virtualFile instanceof LightVirtualFile) && !(virtualFile.getFileSystem() instanceof DummyFileSystem)&& !(virtualFile.getFileSystem() instanceof TempFileSystem);
}
