{
  if (filter instanceof VcsLogBranchFilter) {
    Collection<String> values=((VcsLogBranchFilter)filter).getBranchNames();
    myBranchFilterComponent.apply(values,MultipleValueFilterPopupComponent.displayableText(values),MultipleValueFilterPopupComponent.tooltip(values));
  }
}
