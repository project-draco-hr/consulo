{
  if (value instanceof PsiMethod) {
    return PsiFormatUtil.formatMethod((PsiMethod)value,PsiSubstitutor.EMPTY,PsiFormatUtil.SHOW_NAME | PsiFormatUtil.SHOW_PARAMETERS,PsiFormatUtil.SHOW_TYPE,999);
  }
  return value.toString();
}
