{
  return previousRevision == null ? null : SvnRepositoryContentRevision.create(myVcs,myRepositoryRoot,previousRevision.getPath(),new FilePathImpl(previousRevision.getFile().getIOFile(),isDir),((SvnRevisionNumber)previousRevision.getRevisionNumber()).getRevision().getNumber());
}
