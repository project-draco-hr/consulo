{
  LOG.assertTrue(myChanges == null);
  myChanges=new ConflictChange[]{new ConflictChange(this,FragmentSide.SIDE1,leftMarker),new ConflictChange(this,FragmentSide.SIDE2,rightMarker)};
  return myChanges;
}
