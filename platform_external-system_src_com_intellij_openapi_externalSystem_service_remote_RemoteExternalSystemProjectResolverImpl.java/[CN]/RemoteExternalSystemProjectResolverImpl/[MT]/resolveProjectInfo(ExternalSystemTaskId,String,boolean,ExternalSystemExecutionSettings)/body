{
  return execute(id,new Producer<DataHolder<ProjectData>>(){
    @Nullable @Override public DataHolder<ProjectData> produce(){
      return myDelegate.resolveProjectInfo(id,projectPath,downloadLibraries,getSettings());
    }
  }
);
}
