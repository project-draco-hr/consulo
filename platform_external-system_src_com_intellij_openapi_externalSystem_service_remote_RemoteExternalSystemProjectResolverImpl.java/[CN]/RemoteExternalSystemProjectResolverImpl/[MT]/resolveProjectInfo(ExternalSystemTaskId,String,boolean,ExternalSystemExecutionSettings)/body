{
  return execute(id,new Producer<ExternalProject>(){
    @Nullable @Override public ExternalProject produce(){
      return myDelegate.resolveProjectInfo(id,projectPath,downloadLibraries,getSettings());
    }
  }
);
}
