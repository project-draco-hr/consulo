{
  if (myTracker != tracker) {
    deactivateTracker();
    myTracker=tracker;
    refreshCursor();
    if (myTracker != null) {
      myTracker.setToolProvider(myToolProvider);
      myTracker.setArea(myArea);
      myTracker.activate();
    }
  }
}
