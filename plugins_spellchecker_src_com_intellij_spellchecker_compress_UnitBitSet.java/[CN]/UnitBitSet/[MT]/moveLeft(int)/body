{
  if (n <= 0) {
    return;
  }
  for (int i=0; i < size() - n * bitsPerUnit; i++) {
    set(i,get(n * bitsPerUnit + i));
  }
  for (int i=size() - n * bitsPerUnit; i < size(); i++) {
    set(i,false);
  }
}
