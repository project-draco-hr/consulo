{
  if (normalized)   return 0;
  int delta=0;
  IntervalNode node=this;
  while (node != null) {
    if (node.isValid()) {
      delta+=node.delta;
    }
    node=node.getParent();
  }
  return delta;
}
