{
  if (!node.key.isValid())   return;
  int realMax=Math.max(Math.max(maxEndOf(node.getLeft(),deltaUpToRoot),maxEndOf(node.getRight(),deltaUpToRoot)),deltaUpToRoot + node.key.intervalEnd());
  node.maxEnd=realMax - deltaUpToRoot;
}
