{
  try {
    l.writeLock().lock();
    checkMax(true);
    modCount++;
    MyNode newNode=createNewNode(interval);
    insert(newNode);
    checkMax(false);
    return newNode;
  }
  finally {
    l.writeLock().unlock();
  }
}
