{
  try {
    l.writeLock().lock();
    checkMax(true);
    processReferenceQueue();
    modCount++;
    IntervalNode newNode=createNewNode(interval,start,end,data);
    insert(newNode);
    checkMax(true);
    return newNode;
  }
  finally {
    l.writeLock().unlock();
  }
}
