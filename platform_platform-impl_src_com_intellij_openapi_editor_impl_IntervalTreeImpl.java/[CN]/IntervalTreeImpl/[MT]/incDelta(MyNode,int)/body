{
  if (root == null)   return;
  if (root.key.isValid()) {
    int newDelta=root.delta+=delta;
    if (newDelta != 0) {
      normalized=false;
    }
  }
 else {
    incDelta(root.getLeft(),delta);
    incDelta(root.getRight(),delta);
  }
}
