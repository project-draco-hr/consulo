{
  try {
    l.writeLock().lock();
    checkMax(true);
    MyNode node=(MyNode)delete(interval);
    if (node == null)     return false;
    MyNode parent=node.getParent();
    correctMaxUp(parent);
    checkMax(true);
    return true;
  }
  finally {
    l.writeLock().unlock();
  }
}
