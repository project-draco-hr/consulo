{
  if (collectedAway.isEmpty())   return;
  try {
    l.writeLock().lock();
    for (    IntervalNode node : collectedAway) {
      removeNode(node);
    }
    checkMax(true);
  }
  finally {
    l.writeLock().unlock();
  }
}
