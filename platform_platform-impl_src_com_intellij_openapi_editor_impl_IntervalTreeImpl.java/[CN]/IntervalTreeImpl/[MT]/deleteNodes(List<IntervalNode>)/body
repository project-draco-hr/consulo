{
  if (collectedAway.isEmpty())   return;
  try {
    l.writeLock().lock();
    checkMax(true);
    for (    IntervalNode node : collectedAway) {
      removeNode(node);
    }
    checkMax(true);
  }
  finally {
    l.writeLock().unlock();
  }
}
