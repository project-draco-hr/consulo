{
  if (normalized)   return;
  try {
    l.writeLock().lock();
    processReferenceQueue();
    if (normalized)     return;
    normalize(getRoot());
    normalized=true;
  }
  finally {
    l.writeLock().unlock();
  }
}
