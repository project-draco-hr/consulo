{
  normalize();
  try {
    l.writeLock().lock();
    checkMax(true);
    removeNode(node);
    changeAttributes.run();
    insert(node);
    checkMax(true);
  }
  finally {
    l.writeLock().unlock();
  }
}
