{
  int pos=offset;
  final int lBufferEnd=myBufferEndOffset;
  final char[] lBuffer=myBuffer;
  while (pos < lBufferEnd && lBuffer[pos] != '\n' && lBuffer[pos] != '\r') {
    pos++;
  }
  return pos;
}
