{
  Set<String> s=((ProjectImpl)myModule.getProject()).getStateStore().getMacroTrackingSet();
  return new ModuleStateStorageManager(PathMacroManager.getInstance(getComponentManager()).createTrackingSubstitutor(s),myModule);
}
