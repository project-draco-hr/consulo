{
  final int x=data.position + (data.lastGhostVisible ? data.lastGhost.width : 0);
  return new Rectangle(x,data.insets.top + myTabs.getSelectionTabVShift(),data.moreRectAxisSize - 1,myTabs.myHeaderFitSize.height - 1);
}
