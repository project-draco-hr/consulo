{
  final JpsAndroidModuleExtension extension=AndroidJpsUtil.getExtension(module);
  result.addAll(targetRegistry.getModuleBasedTargets(module,extension != null && extension.isPackTestCode() ? BuildTargetRegistry.ModuleTargetSelector.ALL : BuildTargetRegistry.ModuleTargetSelector.PRODUCTION));
}
