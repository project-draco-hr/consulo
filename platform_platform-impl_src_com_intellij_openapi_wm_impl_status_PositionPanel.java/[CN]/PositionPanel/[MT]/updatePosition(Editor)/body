{
  if (editor == null) {
    myText="";
    myStatusBar.updateWidget(ID());
  }
 else {
    if (!isOurEditor(editor))     return;
    myText=getPositionText(editor);
    myStatusBar.updateWidget(ID());
  }
}
