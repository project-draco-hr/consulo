{
  if (element == null)   return null;
  if (strict) {
    element=element.getParent();
  }
  while (element != null && !instanceOf(aClass,element)) {
    element=element.getParent();
  }
  return (T)element;
}
