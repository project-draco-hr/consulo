{
  PsiElement run=element;
  if (strict) {
    run=run.getParent();
  }
  while (run != null) {
    for (    Class<? extends T> aClass : classes) {
      if (aClass.isInstance(run))       return (T)run;
    }
    run=run.getParent();
  }
  return null;
}
