{
  if (strict) {
    element=element.getParent();
  }
  while (element != null) {
    for (    Class aClass : classes) {
      if (aClass.isInstance(element))       return (T)element;
    }
    element=element.getParent();
  }
  return (T)element;
}
