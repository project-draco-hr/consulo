{
  if (strict) {
    element=element.getParent();
  }
  while (element != null) {
    for (    Class aClass : classes) {
      if (aClass.isInstance(element))       return element;
    }
    element=element.getParent();
  }
  return element;
}
