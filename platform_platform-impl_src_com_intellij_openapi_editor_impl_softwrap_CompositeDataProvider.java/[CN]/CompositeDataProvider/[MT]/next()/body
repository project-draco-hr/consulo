{
  if (!hasData()) {
    return false;
  }
  DataProvider<?,?> provider=myProviders[0];
  if (!provider.next()) {
    myProviders[0]=null;
  }
  if (hasData()) {
    sort();
    return true;
  }
  return false;
}
