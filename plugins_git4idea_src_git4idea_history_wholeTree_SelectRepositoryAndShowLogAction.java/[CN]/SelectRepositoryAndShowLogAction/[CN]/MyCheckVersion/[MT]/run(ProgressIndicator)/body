{
  GitVcs vcs=GitVcs.getInstance(myProject);
  if (vcs == null) {
    return;
  }
  myVersion=vcs.getVersion();
  if (myVersion.isNull()) {
    vcs.checkVersion();
    myVersion=vcs.getVersion();
  }
}
