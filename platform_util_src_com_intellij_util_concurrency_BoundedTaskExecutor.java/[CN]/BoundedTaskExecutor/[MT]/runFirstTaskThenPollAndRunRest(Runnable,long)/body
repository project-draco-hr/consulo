{
  Runnable task=first;
  do {
    try {
      task.run();
    }
 catch (    Error ignored) {
    }
catch (    RuntimeException ignored) {
    }
    task=pollOrGiveUp(status);
  }
 while (task != null);
}
