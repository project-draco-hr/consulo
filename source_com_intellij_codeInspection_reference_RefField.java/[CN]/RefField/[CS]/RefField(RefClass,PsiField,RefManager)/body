{
  super(field,manager);
  ownerClass.add(this);
  PsiClass psiClass=field.getContainingClass();
  if (psiClass.isInterface()) {
    setIsStatic(true);
    setIsFinal(true);
  }
  setCanBeStatic(isStatic());
}
