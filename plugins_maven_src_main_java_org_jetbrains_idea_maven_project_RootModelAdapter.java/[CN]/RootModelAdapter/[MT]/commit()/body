{
  if (myLibraryTable != null && myLibraryTable.isChanged()) {
    myLibraryTable.commit();
  }
  if (myRootModel.isChanged()) {
    myRootModel.commit();
  }
 else {
    myRootModel.dispose();
  }
}
