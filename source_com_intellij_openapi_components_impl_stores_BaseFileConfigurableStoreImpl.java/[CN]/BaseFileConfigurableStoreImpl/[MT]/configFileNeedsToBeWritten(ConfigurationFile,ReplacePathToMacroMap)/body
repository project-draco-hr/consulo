{
  final VirtualFile vFile=file.getVirtualFile();
  return !ProjectManagerEx.getInstanceEx().isFileSavedToBeReloaded(vFile) && file.needsSave(saveToXml(null,vFile),replacements,getLineSeparator(vFile));
}
