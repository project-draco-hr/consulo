{
  final VirtualFile vFile=file.getVirtualFile();
  return !ProjectManagerEx.getInstanceEx().isFileSavedToBeReloaded(vFile) && file.needsSave(saveToXml(vFile),replacements,getLineSeparator(vFile));
}
