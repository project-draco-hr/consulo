{
  int size=memoizedSerializedSize;
  if (size != -1)   return size;
  size=0;
  if (((bitField0_ & 0x00000001) == 0x00000001)) {
    size+=com.google.protobuf.CodedOutputStream.computeBytesSize(1,getClassNameBytes());
  }
{
    int dataSize=0;
    for (int i=0; i < importStatement_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeBytesSizeNoTag(importStatement_.getByteString(i));
    }
    size+=dataSize;
    size+=1 * getImportStatementList().size();
  }
{
    int dataSize=0;
    for (int i=0; i < staticImport_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeBytesSizeNoTag(staticImport_.getByteString(i));
    }
    size+=dataSize;
    size+=1 * getStaticImportList().size();
  }
{
    int dataSize=0;
    for (int i=0; i < identifier_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeBytesSizeNoTag(identifier_.getByteString(i));
    }
    size+=dataSize;
    size+=1 * getIdentifierList().size();
  }
  memoizedSerializedSize=size;
  return size;
}
