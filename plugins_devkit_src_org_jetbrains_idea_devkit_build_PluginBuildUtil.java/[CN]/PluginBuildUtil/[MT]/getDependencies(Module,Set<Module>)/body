{
  productionRuntimeDependencies(module).forEachModule(new Processor<Module>(){
    @Override public boolean process(    Module dep){
      if (!modules.contains(dep)) {
        modules.add(dep);
        getDependencies(dep,modules);
      }
      return true;
    }
  }
);
}
