{
  runInContext(context,CompilerBundle.message("progress.saving.caches"),new ThrowableRunnable<RuntimeException>(){
    public void run(){
      context.getDependencyCache().dispose();
    }
  }
);
}
