{
  final EnumSet<Scope> scopeSet=myIssue.getScope();
  if (scopeSet.size() != 1) {
    return true;
  }
  final Scope scope=scopeSet.iterator().next();
  return scope != Scope.JAVA_FILE && scope != Scope.MANIFEST && scope != Scope.RESOURCE_FILE && scope != Scope.PROGUARD_FILE;
}
