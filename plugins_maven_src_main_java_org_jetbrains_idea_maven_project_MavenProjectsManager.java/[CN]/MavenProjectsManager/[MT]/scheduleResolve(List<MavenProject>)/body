{
  runWhenFullyOpen(new Runnable(){
    public void run(){
      for (      MavenProject each : projects) {
        myResolvingProcessor.scheduleTask(new MavenProjectsProcessorResolvingTask(each,myProjectsTree,getGeneralSettings()));
      }
    }
  }
);
}
