{
  checkScheduled();
  if (canceled)   return;
  canceled=true;
  PrioritizedFutureTask[] tasks=getTasks();
  for (  PrioritizedFutureTask future : tasks) {
    future.cancel(false);
  }
  runningTasks.set(0);
}
