{
  final int magnitude=testProxy.getState().getMagnitude();
  if (magnitude == PoolOfTestStates.RUNNING_INDEX)   return session == null || !session.isPaused() ? Animator.getCurrentFrame() : Animator.PAUSED_ICON;
 else   return ourIcons.get(new Integer(magnitude));
}
