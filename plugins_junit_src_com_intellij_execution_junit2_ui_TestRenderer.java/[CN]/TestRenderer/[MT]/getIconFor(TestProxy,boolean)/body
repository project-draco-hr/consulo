{
  final int magnitude=testProxy.getState().getMagnitude();
  if (magnitude == PoolOfTestStates.RUNNING_INDEX)   return isPaused ? Animator.PAUSED_ICON : Animator.getCurrentFrame();
 else   return ourIcons.get(new Integer(magnitude));
}
