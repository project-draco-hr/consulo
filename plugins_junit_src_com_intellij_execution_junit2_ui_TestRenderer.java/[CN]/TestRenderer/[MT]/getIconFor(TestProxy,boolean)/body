{
  final int magnitude=testProxy.getState().getMagnitude();
  if (magnitude == PoolOfTestStates.RUNNING_INDEX)   return isPaused ? AllIcons.RunConfigurations.TestPaused : Animator.getCurrentFrame();
 else   return ourIcons.get(new Integer(magnitude));
}
