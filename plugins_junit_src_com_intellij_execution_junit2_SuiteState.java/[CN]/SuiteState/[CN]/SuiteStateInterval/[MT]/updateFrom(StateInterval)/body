{
  final TestState otherMax=interval.getMax();
  if (myMax.getMagnitude() < otherMax.getMagnitude())   myMax=otherMax;
  final TestState otherMin=interval.getMin();
  if (myMin.getMagnitude() > otherMin.getMagnitude())   myMin=otherMin;
}
