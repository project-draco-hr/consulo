{
  final VirtualFilePointerManager instance=VirtualFilePointerManager.getInstance();
  return instance.create(myProject.getBaseDir().getUrl() + "/" + myDefaultCompilerPathsManager.getCompilerOutputUrl()+ "/"+ contentFolderType.name().toLowerCase()+ "/"+ module.getName()+ "/",module,null);
}
