{
  if (RefUtil.isEntryPoint(owner))   return true;
  final PsiElement element=owner.getElement();
  if (element instanceof PsiModifierListOwner && SpecialAnnotationsUtil.isSpecialAnnotationPresent((PsiModifierListOwner)element,ADDITIONAL_ANNOTATIONS)) {
    return true;
  }
  for (  DeadCodeExtension extension : myExtensions) {
    if (extension.isEntryPoint(owner)) {
      return true;
    }
  }
  return false;
}
