{
  myIndices.updateOrRepair(myIndices.add(myRepositoryFixture.getTestDataPath("local1"),MavenIndex.Kind.LOCAL),true,new EmptyProgressIndicator());
  shutdownIndices();
  initIndices();
  assertUnorderedElementsAreEqual(myIndices.getIndices().get(0).getGroupIds(),"junit");
}
