{
  myIndices.update(myIndices.add(myDataTestFixture.getTestDataPath("local1"),MavenIndex.Kind.LOCAL),new EmptyProgressIndicator());
  shutdownIndices();
  initIndices();
  assertUnorderedElementsAreEqual(myIndices.getIndices().get(0).getGroupIds(),"junit");
}
