{
  myIndices.updateOrRepair(myIndices.add(myRepositoryHelper.getTestDataPath("local1"),MavenIndex.Kind.LOCAL),true,new EmptyProgressIndicator());
  shutdownIndices();
  initIndices();
  assertUnorderedElementsAreEqual(myIndices.getIndices().get(0).getGroupIds(),"junit");
}
