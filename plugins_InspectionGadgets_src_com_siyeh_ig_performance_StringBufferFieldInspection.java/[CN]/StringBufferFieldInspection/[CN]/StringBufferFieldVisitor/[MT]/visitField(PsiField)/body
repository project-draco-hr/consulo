{
  super.visitField(field);
  final PsiType type=field.getType();
  if (type == null) {
    return;
  }
  final String text=type.getCanonicalText();
  if (!"java.lang.StringBuffer".equals(text) && !"java.lang.StringBuilder".equals(text)) {
    return;
  }
  registerFieldError(field);
}
