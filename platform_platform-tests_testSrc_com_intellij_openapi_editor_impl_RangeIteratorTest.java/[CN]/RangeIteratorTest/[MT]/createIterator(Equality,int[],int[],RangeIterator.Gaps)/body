{
  RangeIterator iterator=new RangeIterator(gaps,equality,new MyHighlighterIterator(starts,ends),ANY);
  iterator.init(new TextRange(0,Integer.MAX_VALUE));
  iterator.advance();
  return iterator;
}
