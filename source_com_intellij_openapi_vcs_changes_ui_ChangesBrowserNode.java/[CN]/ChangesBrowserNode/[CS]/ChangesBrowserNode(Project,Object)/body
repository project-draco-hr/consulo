{
  super(userObject);
  myProject=project;
  if ((userObject instanceof Change && !ChangesUtil.getFilePath((Change)userObject).isDirectory()) || (userObject instanceof VirtualFile && !((VirtualFile)userObject).isDirectory()) || userObject instanceof FilePath && !((FilePath)userObject).isDirectory()) {
    count=1;
  }
}
