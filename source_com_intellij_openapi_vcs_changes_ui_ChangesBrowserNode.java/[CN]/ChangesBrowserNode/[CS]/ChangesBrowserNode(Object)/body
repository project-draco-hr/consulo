{
  super(userObject);
  if ((userObject instanceof Change && !ChangesUtil.getFilePath((Change)userObject).isDirectory()) || userObject instanceof VirtualFile || userObject instanceof FilePath && !((FilePath)userObject).isDirectory()) {
    count=1;
  }
}
