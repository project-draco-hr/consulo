{
  Library[] libraries=GroovyConfigUtils.getGroovyLibrariesByModule(myEditorContext.getModule());
  if (libraries.length != 1) {
    myComboBox.setSelectedIndex(0);
    isSdkChanged=false;
  }
 else {
    Library library=libraries[0];
    if (library != null && LibraryTablesRegistrar.getInstance().getLibraryTable().getLibraryByName(library.getName()) != null) {
      myComboBox.selectLibrary(library);
      isSdkChanged=false;
    }
  }
}
