{
  try {
    final int size=in.readInt();
    for (int i=0; i < size; i++) {
      acc.add(in.readInt());
    }
    return acc;
  }
 catch (  IOException x) {
    throw new RuntimeException(x);
  }
}
