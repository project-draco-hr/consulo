{
  final String p=quotePath(path);
  final String m="$" + macroName + "$";
  put(p,m);
  put("file:" + p,"file:" + m);
  put("file:/" + p,"file:/" + m);
  put("file://" + p,"file://" + m);
  put("jar:" + p,"jar:" + m);
  put("jar:/" + p,"jar:/" + m);
  put("jar://" + p,"jar://" + m);
}
