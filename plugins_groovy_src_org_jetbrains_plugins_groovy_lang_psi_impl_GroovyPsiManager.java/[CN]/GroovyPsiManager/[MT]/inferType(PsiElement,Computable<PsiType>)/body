{
  final List<PsiElement> curr=myElementsWithTypesBeingInferred.get();
  if (curr.size() > 7) {
    return null;
  }
  try {
    curr.add(element);
    return computable.compute();
  }
  finally {
    curr.remove(element);
  }
}
