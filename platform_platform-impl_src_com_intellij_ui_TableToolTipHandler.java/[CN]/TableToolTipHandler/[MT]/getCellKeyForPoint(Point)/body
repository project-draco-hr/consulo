{
  int rowIndex=myComponent.rowAtPoint(point);
  if (rowIndex == -1) {
    return null;
  }
  int columnIndex=myComponent.columnAtPoint(point);
  if (columnIndex == -1) {
    return null;
  }
  return new TableCellKey(rowIndex,columnIndex);
}
