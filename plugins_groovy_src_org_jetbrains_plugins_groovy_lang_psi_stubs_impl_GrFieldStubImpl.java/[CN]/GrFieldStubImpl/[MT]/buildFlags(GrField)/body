{
  byte f=0;
  if (field instanceof GrEnumConstant) {
    f|=IS_ENUM_CONSTANT;
  }
  if (field.isProperty()) {
    f|=IS_PROPERTY;
  }
  if (field.isDeprecated()) {
    f|=IS_DEPRECATED;
  }
  return f;
}
