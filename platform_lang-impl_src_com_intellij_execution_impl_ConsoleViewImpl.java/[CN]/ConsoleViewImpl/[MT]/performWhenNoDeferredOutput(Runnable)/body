{
  if (!hasDeferredOutput()) {
    runnable.run();
  }
 else {
    if (mySpareTimeAlarm.isDisposed())     return;
    mySpareTimeAlarm.addRequest(new Runnable(){
      public void run(){
        performWhenNoDeferredOutput(runnable);
      }
    }
,100,ModalityState.stateForComponent(myJLayeredPane));
  }
}
