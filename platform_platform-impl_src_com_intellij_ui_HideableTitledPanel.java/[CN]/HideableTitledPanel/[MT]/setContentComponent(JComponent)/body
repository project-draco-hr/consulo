{
  if (content == null && myContent != null) {
    remove(myContent);
  }
  myContent=content;
  if (myContent != null) {
    myContent.setVisible(myOn);
    add(myContent,BorderLayout.CENTER);
  }
}
