{
  if (file == null) {
    throw new IllegalArgumentException("file cannot be null");
  }
  return FileTypeManager.getInstance().getFileTypeByFile(file) == StdFileTypes.GUI_DESIGNER_FORM && !StdFileTypes.GUI_DESIGNER_FORM.isBinary() && ModuleUtil.getModuleForFile(project,file) != null;
}
