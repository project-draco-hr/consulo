{
  for (  GraphEdge downEdge : myDownEdges.get(upNodeIndex)) {
    if (downEdge.getDownNodeIndex() == downNodeIndex)     return downEdge.getType();
  }
  throw new IllegalStateException("Not found edge: " + upNodeIndex + ", "+ downNodeIndex);
}
