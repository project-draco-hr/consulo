{
  GotItMessage.createMessage("Test","Test Message").setDisposable(e.getProject()).show(new RelativePoint(WindowManager.getInstance().getFrame(e.getProject()),new Point(0,0)),Balloon.Position.above);
}
