{
  if (myConflictTracker.hasConflict(file)) {
    return new HandleType("using conflict tracker",false){
      @Override public void processFiles(      Collection<VirtualFile> virtualFiles){
      }
    }
;
  }
  return null;
}
