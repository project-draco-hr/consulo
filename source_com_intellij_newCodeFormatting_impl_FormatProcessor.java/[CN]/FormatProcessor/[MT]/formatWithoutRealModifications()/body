{
  while (true) {
    myAlignAgain=false;
    myCurrentBlock=myFirstTokenBlock;
    while (myCurrentBlock != null) {
      processToken();
    }
    if (!myAlignAgain)     return;
    reset();
  }
}
