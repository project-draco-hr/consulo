{
  super(null);
  myProject=project;
  myPlatformFacade=context.getPlatformFacade();
  myProjectStructureHelper=context.getProjectStructureHelper();
  myChangesModel=context.getChangesModel();
  myNodeComparator=new GradleProjectStructureNodeComparator(context);
  context.getChangesModel().addListener(new ExternalProjectStructureChangeListener(){
    @Override public void onChanges(    @NotNull final Collection<ExternalProjectStructureChange> oldChanges,    @NotNull final Collection<ExternalProjectStructureChange> currentChanges){
      processChanges(oldChanges,currentChanges);
    }
  }
);
  if (rebuild) {
    rebuild();
  }
}
