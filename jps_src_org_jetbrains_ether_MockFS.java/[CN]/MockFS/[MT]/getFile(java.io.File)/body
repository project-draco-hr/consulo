{
  if (f.getAbsolutePath().startsWith(myRoot.getAbsolutePath())) {
    File g=myFiles.get(f);
    if (g != null) {
      return g;
    }
    g=new File(f,false);
    myFiles.put(f,g);
    return g;
  }
  return null;
}
