{
  if (!myTryAgain) {
    for (    String line : lines) {
      line=decodeIso8859_1(line);
      processNewLine(line);
      if (line.indexOf(BAD_ACCESS_ERROR) >= 0) {
        myTryAgain=true;
        break;
      }
    }
  }
}
