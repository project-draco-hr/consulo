{
  final SingleRootFileViewProvider clone=new SingleRootFileViewProvider(getManager(),new MockVirtualFile(getVirtualFile().getName(),getRealFileType(),getContents(),getModificationStamp()),false);
  return clone;
}
