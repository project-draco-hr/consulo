{
  Set<String> originalUrls=new HashSet<String>(Arrays.asList(LibraryImpl.this.getUrls(rootType)));
  for (  VirtualFile file : getFiles(rootType)) {
    originalUrls.add(file.getUrl());
  }
  return ArrayUtil.toStringArray(originalUrls);
}
