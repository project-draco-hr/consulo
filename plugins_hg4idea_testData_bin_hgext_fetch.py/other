'pull, update and merge in one command'
from mercurial.i18n import _
from mercurial.node import nullid, short
from mercurial import commands, cmdutil, hg, util, url, error
from mercurial.lock import release
cmdtable = {'fetch': (fetch, ((([('r', 'rev', [], _('a specific revision you would like to pull')), ('e', 'edit', None, _('edit commit message')), ('', 'force-editor', None, _('edit commit message (DEPRECATED)')), ('', 'switch-parent', None, _('switch parents when merging'))] + commands.commitopts) + commands.commitopts2) + commands.remoteopts), _('hg fetch [SOURCE]')), }
