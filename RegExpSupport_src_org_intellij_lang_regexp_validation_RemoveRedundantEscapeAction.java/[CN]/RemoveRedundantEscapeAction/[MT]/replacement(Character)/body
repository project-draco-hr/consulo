{
  final PsiElement context=myChar.getContainingFile().getContext();
  return context instanceof PsiLiteralExpression ? StringUtil.escapeStringCharacters(v.toString()) : (context instanceof XmlElement ? XmlStringUtil.escapeString(v.toString()) : v.toString());
}
