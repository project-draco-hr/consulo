{
  if (!value.booleanValue()) {
    myResult.set(false);
  }
 else {
    if (!condition()) {
      myResult.set(true);
    }
 else {
      body().addConsumer(SameThreadExecutor.INSTANCE,this);
    }
  }
}
