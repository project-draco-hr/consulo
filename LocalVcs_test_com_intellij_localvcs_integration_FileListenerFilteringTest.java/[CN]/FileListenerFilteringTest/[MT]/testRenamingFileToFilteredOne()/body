{
  vcs.createFile("allowed",null,null);
  vcs.apply();
  VirtualFile renamed=new TestVirtualFile("filtered",null,null);
  fireRenamed("allowed",renamed);
  assertFalse(vcs.hasEntry("allowed"));
  assertFalse(vcs.hasEntry("filtered"));
}
