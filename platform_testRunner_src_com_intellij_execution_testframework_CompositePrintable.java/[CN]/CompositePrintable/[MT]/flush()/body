{
  if (myWrapper == null) {
    myWrapper=new PrintablesWrapper();
  }
  if (myWrapper != null) {
synchronized (myNestedPrintables) {
      myWrapper.flash(myNestedPrintables);
      clear();
    }
  }
}
