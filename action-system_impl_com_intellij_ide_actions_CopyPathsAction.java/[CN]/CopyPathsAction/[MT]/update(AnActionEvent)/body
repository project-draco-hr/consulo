{
  final VirtualFile[] files=getFiles(event);
  final Presentation presentation=event.getPresentation();
  presentation.setEnabled(files != null && files.length > 0);
  presentation.setText((files != null && files.length == 1) ? IdeBundle.message("action.copy.path") : IdeBundle.message("action.copy.paths"));
}
