{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  FirefoxSettings settings=(FirefoxSettings)o;
  return Comparing.equal(myProfilesIniPath,settings.myProfilesIniPath) && Comparing.equal(myProfile,settings.myProfile);
}
