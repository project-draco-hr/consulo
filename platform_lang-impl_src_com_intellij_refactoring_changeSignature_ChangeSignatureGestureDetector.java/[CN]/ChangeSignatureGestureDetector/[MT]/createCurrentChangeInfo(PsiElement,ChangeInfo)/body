{
  for (  ChangeSignatureGestureDetectorExtension detectorExtension : Extensions.getExtensions(ChangeSignatureGestureDetectorExtension.EP_NAME)) {
    final ChangeInfo newChangeInfo=detectorExtension.createCurrentChangeSignature(element,changeInfo);
    if (newChangeInfo != null)     return newChangeInfo;
  }
  return null;
}
