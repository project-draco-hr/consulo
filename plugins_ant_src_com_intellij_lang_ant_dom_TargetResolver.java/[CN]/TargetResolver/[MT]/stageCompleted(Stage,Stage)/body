{
  if (completedStage == Stage.RESOLVE_MAP_BUILDING_STAGE) {
    if (myResult == null) {
      final AntDomTarget target=getTargetByName(myDeclaredTargetRef);
      if (target != null) {
        myResult=new Pair<AntDomTarget,String>(target,myDeclaredTargetRef);
      }
    }
    stop();
  }
}
