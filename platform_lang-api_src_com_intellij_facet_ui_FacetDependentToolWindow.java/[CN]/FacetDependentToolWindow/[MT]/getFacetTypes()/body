{
  return ContainerUtil.map(getFacetIds(),new Function<String,FacetType>(){
    @Override public FacetType fun(    String facetId){
      return FacetTypeRegistry.getInstance().findFacetType(facetId);
    }
  }
);
}
