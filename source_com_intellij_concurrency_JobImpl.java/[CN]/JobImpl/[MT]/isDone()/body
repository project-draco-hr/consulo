{
  myLock.lock();
  try {
    if (myFutures == null)     return false;
    for (    Future<T> future : myFutures) {
      if (!future.isDone())       return false;
    }
    return true;
  }
  finally {
    myLock.unlock();
  }
}
