{
  final THashSet<String> forbiddenApi=ourForbiddenAPI.get(api);
  if (forbiddenApi == null)   return false;
  return forbiddenApi.contains(getSignature(member)) || isForbiddenSignature(member,api + 1);
}
