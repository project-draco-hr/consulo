{
  final Lexer lexer=lang.getParserDefinition().createLexer();
  lexer.start(((LeafElement)tree).textToCharArray());
  return new PsiBuilderImpl(lexer,SharedImplUtil.findCharTableByTree(tree),false,lang.getParserDefinition().getWhitespaceTokens(),lang.getParserDefinition().getCommentTokens());
}
