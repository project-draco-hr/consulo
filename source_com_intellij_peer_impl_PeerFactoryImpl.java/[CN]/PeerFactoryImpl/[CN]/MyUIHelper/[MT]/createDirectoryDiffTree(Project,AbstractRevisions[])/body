{
  final CheckinPanelTreeTable result=CheckinPanelTreeTable.createOn(project,CheckinPanelRootNode.create(project,roots),new ColumnInfo[0],new ColumnInfo[0],new AnAction[0],new AnAction[0]);
  TreeUtil.expandAll(result.getTree());
  return result;
}
