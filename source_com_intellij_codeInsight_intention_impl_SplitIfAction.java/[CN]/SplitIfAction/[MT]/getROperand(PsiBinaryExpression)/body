{
  PsiElement e=expression;
  while (!(e.getParent() instanceof PsiIfStatement))   e=e.getParent();
  return expression.getManager().getElementFactory().createExpressionFromText(e.getText().substring(expression.getROperand().getTextRange().getStartOffset() - e.getTextRange().getStartOffset()),e.getParent());
}
