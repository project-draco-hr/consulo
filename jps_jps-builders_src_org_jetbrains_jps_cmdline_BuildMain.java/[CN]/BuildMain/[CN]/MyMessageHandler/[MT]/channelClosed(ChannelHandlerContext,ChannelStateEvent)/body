{
  try {
    super.channelClosed(ctx,e);
  }
  finally {
    new Thread("Shutdown thread"){
      public void run(){
        try {
          ourChannelFactory.releaseExternalResources();
        }
  finally {
          System.exit(0);
        }
      }
    }
.start();
  }
}
