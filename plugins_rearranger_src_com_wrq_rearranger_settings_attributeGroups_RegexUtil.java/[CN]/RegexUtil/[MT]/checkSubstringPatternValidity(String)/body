{
  char c=s.charAt(0);
  if (c == '+' || c == '*' || c == '?')   return false;
  if (s.length() >= 2) {
    if (s.charAt(s.length() - 1) == '\\' && s.charAt(s.length() - 2) != '\\') {
      return false;
    }
  }
  return (count(s,'[') == count(s,']') && count(s,'{') == count(s,'}'));
}
