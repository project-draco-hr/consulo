{
  char[] chars=s.toCharArray();
  int result=0;
  for (int i=0; i < chars.length; i++) {
    if (chars[i] == c) {
      if (i == 0 || chars[i - 1] != '\\') {
        result++;
      }
    }
  }
  return result;
}
