{
  if (parentClass.isInterface()) {
    String message="Cannot introduce field in interface";
    RefactoringMessageUtil.showErrorMessage(REFACTORING_NAME,message,getHelpID(),parentClass.getProject());
    return false;
  }
 else {
    return true;
  }
}
