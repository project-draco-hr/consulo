{
  if (element instanceof CandidateInfo)   return myFilter.isAcceptable(((CandidateInfo)element).getElement(),context);
 else   if (myFilter instanceof PsiElement)   return myFilter.isAcceptable(element,context);
  return false;
}
