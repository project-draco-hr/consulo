{
  try {
    AdbManager.run(new Runnable(){
      public void run(){
        AndroidDebugBridge.terminate();
      }
    }
,false);
  }
 catch (  AdbNotRespondingException e) {
    LOG.info(e);
  }
}
