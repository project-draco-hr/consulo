{
  if (!SystemInfo.isMac && e instanceof MouseWheelEvent) {
    final MouseWheelEvent mwe=(MouseWheelEvent)e;
    return mwe.isShiftDown() && mwe.getScrollType() == MouseWheelEvent.WHEEL_UNIT_SCROLL && findScrollPane(c) != null;
  }
  return false;
}
