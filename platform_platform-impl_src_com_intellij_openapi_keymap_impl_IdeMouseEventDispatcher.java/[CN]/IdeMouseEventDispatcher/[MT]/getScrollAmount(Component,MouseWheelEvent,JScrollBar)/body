{
  final int ratio=Registry.is("ide.smart.horizontal.scrolling") ? Math.max((int)Math.pow(c.getWidth() / scrollBar.getWidth(),2),10) : 10;
  return me.getUnitsToScroll() * scrollBar.getUnitIncrement() * ratio;
}
