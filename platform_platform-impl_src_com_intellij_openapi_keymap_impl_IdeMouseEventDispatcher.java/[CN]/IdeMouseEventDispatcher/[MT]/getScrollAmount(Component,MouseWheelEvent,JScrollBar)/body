{
  final int scrollBarWidth=scrollBar.getWidth();
  final int ratio=Registry.is("ide.smart.horizontal.scrolling") && scrollBarWidth > 0 ? Math.max((int)Math.pow(c.getWidth() / scrollBarWidth,2),10) : 10;
  return me.getUnitsToScroll() * scrollBar.getUnitIncrement() * ratio;
}
