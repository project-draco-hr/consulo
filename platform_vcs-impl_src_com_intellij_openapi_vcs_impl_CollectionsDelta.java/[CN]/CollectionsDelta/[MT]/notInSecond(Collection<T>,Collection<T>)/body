{
  Set<T> notInSecond=null;
  for (  T topItem : first) {
    if (!second.contains(topItem)) {
      if (notInSecond == null) {
        notInSecond=new HashSet<T>();
      }
      notInSecond.add(topItem);
    }
  }
  return notInSecond;
}
