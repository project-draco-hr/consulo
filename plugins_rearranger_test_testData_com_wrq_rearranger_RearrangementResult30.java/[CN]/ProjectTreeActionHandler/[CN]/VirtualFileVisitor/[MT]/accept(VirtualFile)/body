{
  if (f != null) {
    visitVirtualFile(f);
  }
  final VirtualFile[] vfa=f.getChildren();
  if (vfa != null) {
    for (int i=0; i < vfa.length; i++) {
      if (!cancel) {
        accept(vfa[i]);
      }
    }
  }
}
