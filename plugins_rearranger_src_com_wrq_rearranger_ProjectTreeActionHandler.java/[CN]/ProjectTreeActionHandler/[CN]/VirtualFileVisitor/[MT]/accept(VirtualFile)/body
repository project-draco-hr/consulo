{
  if (f != null) {
    visitVirtualFile(f);
  }
  final VirtualFile[] vfa=f.getChildren();
  if (vfa != null) {
    for (    VirtualFile aVfa : vfa) {
      if (!cancel) {
        accept(aVfa);
      }
    }
  }
}
