{
  boolean foo=fooBaz();
  boolean bar=fooBaz();
  boolean barangus=fooBaz();
  if (foo == bar == barangus) {
    System.out.println("");
  }
  if (foo != bar == barangus) {
    System.out.println("");
  }
}
