{
  RunConfiguration config=settings.getConfiguration();
  return config.getType().getDisplayName() + "." + settings.getName()+ (settings instanceof UnknownRunConfiguration ? config.getUniqueID() : "");
}
