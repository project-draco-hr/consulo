{
  RunConfiguration config=settings.getConfiguration();
  return config.getType().getDisplayName() + "." + settings.getName()+ (config instanceof UnknownRunConfiguration ? config.getUniqueID() : "");
}
