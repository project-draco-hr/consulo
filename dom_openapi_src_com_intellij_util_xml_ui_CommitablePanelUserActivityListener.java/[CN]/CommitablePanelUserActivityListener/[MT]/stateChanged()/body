{
  if (myApplying)   return;
  cancelAllRequests();
  myAlarm.addRequest(new Runnable(){
    public void run(){
      myApplying=true;
      try {
        applyChanges();
      }
  finally {
        myApplying=false;
      }
    }
  }
,717);
}
