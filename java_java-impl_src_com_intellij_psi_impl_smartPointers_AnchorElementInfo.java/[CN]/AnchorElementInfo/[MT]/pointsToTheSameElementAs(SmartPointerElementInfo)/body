{
  if (other instanceof AnchorElementInfo) {
    return super.pointsToTheSameElementAs(other) && stubId == ((AnchorElementInfo)other).stubId && myStubElementType == ((AnchorElementInfo)other).myStubElementType;
  }
  return Comparing.equal(restoreElement(),other.restoreElement());
}
