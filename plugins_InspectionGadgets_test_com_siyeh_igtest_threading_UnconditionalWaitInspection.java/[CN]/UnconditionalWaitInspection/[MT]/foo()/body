{
  final Object bar=new Object();
synchronized (this) {
    bar.toString();
    bar.wait();
  }
synchronized (this) {
    if (foobar()) {
    }
    bar.wait();
  }
synchronized (this) {
    wait();
  }
}
