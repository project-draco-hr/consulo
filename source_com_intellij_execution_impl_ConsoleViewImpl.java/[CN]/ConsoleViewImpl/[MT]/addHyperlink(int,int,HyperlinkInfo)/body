{
  final RangeHighlighter highlighter=myEditor.getMarkupModel().addRangeHighlighter(highlightStartOffset,highlightEndOffset,HighlighterLayer.SELECTION - 1,HYPERLINK_ATTRIBUTES,HighlighterTargetArea.EXACT_RANGE);
  myHyperlinks.add(highlighter,hyperlinkInfo);
}
