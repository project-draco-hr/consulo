{
  if (!hasDeferredOutput()) {
    runnable.run();
  }
 else {
    new Alarm().addRequest(new Runnable(){
      public void run(){
        performWhenNoDeferredOutput(runnable);
      }
    }
,100);
  }
}
