{
  expect(subject.load(1)).andReturn(b("content")).times(2);
  subject.remove(1);
  replay(subject);
  assertEquals(b("content"),s.load(1));
  s.remove(1);
  assertEquals(b("content"),s.load(1));
  verify(subject);
}
