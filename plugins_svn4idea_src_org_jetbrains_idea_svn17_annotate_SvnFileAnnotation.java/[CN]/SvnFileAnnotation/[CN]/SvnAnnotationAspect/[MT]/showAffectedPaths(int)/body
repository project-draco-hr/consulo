{
  if (lineNum >= 0 && lineNum < myInfos.size()) {
    final long revision=getRevision(lineNum);
    if (revision >= 0) {
      ShowAllAffectedGenericAction.showSubmittedFiles(myVcs.getProject(),new SvnRevisionNumber(SVNRevision.create(revision)),myFile,myVcs.getKeyInstanceMethod());
    }
  }
}
