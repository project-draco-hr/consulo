{
  vcs.beginChangeSet();
  vcs.createDirectory("dir");
  vcs.endChangeSet(null);
  assertEquals(1,vcs.getChangeList().getChangeSets().size());
  vcs.beginChangeSet();
  vcs.endChangeSet(null);
  assertEquals(1,vcs.getChangeList().getChangeSets().size());
}
