{
  final PsiMethod[] methods=aClass.findMethodsBySignature(method,true);
  for (  final PsiMethod methodToMatch : methods) {
    if (!methodToMatch.hasModifierProperty(PsiModifier.ABSTRACT) && !methodToMatch.getContainingClass().isInterface()) {
      return true;
    }
  }
  return false;
}
