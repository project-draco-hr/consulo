{
  if (element instanceof PsiImportList) {
    return "...";
  }
 else   if (element instanceof PsiMethod && CodeGenerator.SETUP_METHOD_NAME.equals(((PsiMethod)element).getName()) || (element instanceof PsiClassInitializer && isGeneratedUIInitializer((PsiClassInitializer)element))) {
    return "Automatically generated code";
  }
 else   if (element instanceof PsiMethod || element instanceof PsiClassInitializer || element instanceof PsiClass) {
    return "{...}";
  }
 else   if (element instanceof PsiDocComment) {
    return "/**...*/";
  }
 else   if (element instanceof XmlTag) {
    return "...";
  }
 else   if (element instanceof PsiFile) {
    return "/.../";
  }
 else {
    LOG.error("Unknown element:" + element);
    return null;
  }
}
