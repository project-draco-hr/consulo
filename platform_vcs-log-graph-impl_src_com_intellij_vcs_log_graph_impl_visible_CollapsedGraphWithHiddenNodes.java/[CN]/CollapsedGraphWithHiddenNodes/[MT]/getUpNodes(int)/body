{
  if (downToEdge.containsKey(nodeIndex))   return Collections.singletonList(downToEdge.get(nodeIndex));
  List<Integer> upNodes=new SmartList<Integer>();
  for (  int upNode : myDelegateGraph.getUpNodes(nodeIndex)) {
    if (nodeIsVisible(upNode))     upNodes.add(upNode);
  }
  return upNodes;
}
