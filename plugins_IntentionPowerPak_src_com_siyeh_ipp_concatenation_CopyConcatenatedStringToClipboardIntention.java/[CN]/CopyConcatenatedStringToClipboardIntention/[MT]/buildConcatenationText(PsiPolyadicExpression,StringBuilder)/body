{
  for (  PsiExpression operand : expression.getOperands()) {
    final Object value=ExpressionUtils.computeConstantExpression(operand);
    if (value == null) {
      out.append('?');
    }
 else {
      out.append(value.toString());
    }
  }
}
