{
  return ResolveCache.getInstance(myElement.getProject()).resolveWithCaching(this,new ResolveCache.PolyVariantResolver<AndroidResourceReference>(){
    @Override public ResolveResult[] resolve(    AndroidResourceReference reference,    boolean incompleteCode){
      return resolveInner();
    }
  }
,false,incompleteCode);
}
