{
  if (myModule != null) {
    renderer.setIcon(myModule.getModuleType().getNodeIcon(false));
  }
  renderer.append(CompilerBundle.message("node.text.0.compile.output",myName),myModule != null ? SimpleTextAttributes.REGULAR_ATTRIBUTES : SimpleTextAttributes.ERROR_ATTRIBUTES);
}
