{
  if (myModule != null) {
    renderer.setIcon(myModule.getModuleType().getNodeIcon(false));
  }
  renderer.append("'" + myName + "' compile output",myModule != null ? SimpleTextAttributes.REGULAR_ATTRIBUTES : SimpleTextAttributes.ERROR_ATTRIBUTES);
}
