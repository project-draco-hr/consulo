{
  if (o instanceof Pair) {
    o=((Pair)o).first;
  }
  if (o instanceof PsiClassType) {
    o=((PsiClassType)o).resolve();
  }
  if (o != null) {
    return myPattern.accepts(o);
  }
  return false;
}
