{
  int result=0;
  boolean sawNonTab=false;
  for (  char c : s.toCharArray()) {
    if (c == '\t' && !sawNonTab) {
      result+=tabSize;
    }
 else {
      sawNonTab=true;
      result++;
    }
  }
  return result;
}
