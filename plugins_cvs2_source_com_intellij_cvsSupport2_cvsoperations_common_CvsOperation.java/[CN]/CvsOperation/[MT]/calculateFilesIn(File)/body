{
  if (!file.isDirectory()) {
    return 1;
  }
  int result=0;
  File[] subFiles=file.listFiles();
  if (subFiles == null) {
    subFiles=new File[0];
  }
  for (int i=0; i < subFiles.length; i++) {
    result+=calculateFilesIn(subFiles[i]);
  }
  return result;
}
