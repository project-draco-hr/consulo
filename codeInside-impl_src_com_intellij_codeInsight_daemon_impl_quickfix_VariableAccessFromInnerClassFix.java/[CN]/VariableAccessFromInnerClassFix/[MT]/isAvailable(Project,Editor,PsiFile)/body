{
  return myClass != null && myClass.isValid() && myClass.getManager().isInProject(myClass) && myVariable != null && myVariable.isValid() && myVariable.getType() != null && myFixType != -1 && !inOwnInitializer(myVariable,myClass);
}
