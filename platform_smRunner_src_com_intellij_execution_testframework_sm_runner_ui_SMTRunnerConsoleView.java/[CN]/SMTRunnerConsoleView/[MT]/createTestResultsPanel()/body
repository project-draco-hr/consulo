{
  myResultsViewer=new SMTestRunnerResultsForm(myProperties.getConfiguration(),getConsole().getComponent(),getConsole().createConsoleActions(),myProperties,myEnvironment,mySplitterProperty);
  return myResultsViewer;
}
