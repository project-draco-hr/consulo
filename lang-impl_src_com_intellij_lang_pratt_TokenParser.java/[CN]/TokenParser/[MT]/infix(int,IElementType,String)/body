{
  return new ReducingParser(){
    @Nullable public IElementType parseFurther(    final PrattBuilder builder){
      builder.parse(rightPriority,errorMessage);
      return compositeType;
    }
  }
;
}
