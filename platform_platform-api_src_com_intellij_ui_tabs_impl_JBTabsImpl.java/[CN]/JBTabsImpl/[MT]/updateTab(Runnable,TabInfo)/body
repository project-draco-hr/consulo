{
  final TabLabel label=myInfo2Label.get(info);
  update.run();
  if (label.getRootPane() != null) {
    if (label.isValid()) {
      label.repaint();
    }
 else {
      revalidateAndRepaint(false);
    }
  }
}
