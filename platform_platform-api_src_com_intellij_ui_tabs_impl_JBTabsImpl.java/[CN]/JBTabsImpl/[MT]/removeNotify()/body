{
  super.removeNotify();
  setFocused(false);
  removeTimerUpdate();
  if (myGlassPane != null) {
    myGlassPane.removeMouseMotionPreprocessor(myTabActionsAutoHideListener);
    myGlassPane=null;
  }
}
