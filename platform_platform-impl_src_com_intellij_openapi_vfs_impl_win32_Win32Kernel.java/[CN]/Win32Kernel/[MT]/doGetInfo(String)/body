{
  FileInfo info=myCache.get(path);
  if (info == null) {
    info=myKernel.getInfo(path.replace('/','\\'));
    if (info == null) {
      return null;
    }
    myCache.put(path,info);
  }
  return info;
}
