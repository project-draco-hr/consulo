{
  final VirtualFile baseDir;
  return project != null && ("IDEA".equals(project.getName()) || "community".equals(project.getName())) && (baseDir=project.getBaseDir()) != null && baseDir.findFileByRelativePath("plugins") != null;
}
