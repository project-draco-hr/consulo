{
  if (lastParent instanceof PsiExpressionList)   return true;
  if (lastParent != null && lastParent == getBaseClassReference()) {
    return true;
  }
  return super.processDeclarations(processor,substitutor,lastParent,place);
}
