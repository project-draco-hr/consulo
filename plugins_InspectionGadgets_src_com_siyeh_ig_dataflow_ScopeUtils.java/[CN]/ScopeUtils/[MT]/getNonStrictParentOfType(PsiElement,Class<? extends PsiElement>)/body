{
  if (element == null) {
    return null;
  }
  while (element != null) {
    for (    Class<? extends PsiElement> clazz : classes) {
      if (clazz.isInstance(element)) {
        return element;
      }
    }
    element=element.getParent();
  }
  return null;
}
