{
  if (s.length() > 1) {
    return Character.toLowerCase(s.charAt(0)) + s.substring(1);
  }
 else   if (s.length() == 1) {
    return "" + Character.toLowerCase(s.charAt(0));
  }
 else {
    return s;
  }
}
