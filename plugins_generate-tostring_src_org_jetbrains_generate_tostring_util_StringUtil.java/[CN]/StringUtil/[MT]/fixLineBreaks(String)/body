{
  StringBuffer sb=new StringBuffer();
  for (int i=0; i < s.length(); i++) {
    char c=s.charAt(i);
    if (Character.LINE_SEPARATOR != c) {
      sb.append(c);
    }
  }
  return sb.toString();
}
