{
  myEditor=editor;
  myProject=project;
  myModule=module;
  myFile=file;
  setLayout(new FillLayout());
  createDesignerCard();
  createErrorCard();
  createProgressPanel();
  myToolProvider.loadDefaultTool();
}
