{
  return "[in update: " + myIsInUpdate + ", document changed: "+ isDocumentChanged+ ", perform caret merging: "+ myPerformCaretMergingAfterCurrentOperation+ ", current caret: "+ myCurrentCaret+ ", all carets: "+ ContainerUtil.map(myCarets,new Function<CaretImpl,String>(){
    @Override public String fun(    CaretImpl caret){
      return caret.dumpState();
    }
  }
)+ "]";
}
