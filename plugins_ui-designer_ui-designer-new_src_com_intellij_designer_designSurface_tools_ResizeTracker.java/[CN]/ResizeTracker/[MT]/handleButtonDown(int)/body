{
  if (button == MouseEvent.BUTTON1) {
    if (myState == STATE_INIT) {
      myState=STATE_DRAG;
    }
  }
 else {
    myState=STATE_INVALID;
    eraseFeedback();
    setExecuteEnabled(false);
  }
}
