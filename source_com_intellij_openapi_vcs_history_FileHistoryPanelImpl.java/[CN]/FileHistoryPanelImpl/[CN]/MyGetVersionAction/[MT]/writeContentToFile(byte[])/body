{
  try {
    OutputStream output=getVirtualFile().getOutputStream(this);
    try {
      output.write(revision);
    }
  finally {
      output.close();
    }
  }
 catch (  Exception e) {
    LOG.error(e);
  }
}
