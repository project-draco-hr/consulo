{
  if (!super.isEnabled())   return false;
  if (!myHistorySession.isContentAvailable((VcsFileRevision)getSelection().get(0)) || !myHistorySession.isContentAvailable((VcsFileRevision)getSelection().get(1))) {
    return false;
  }
  return true;
}
