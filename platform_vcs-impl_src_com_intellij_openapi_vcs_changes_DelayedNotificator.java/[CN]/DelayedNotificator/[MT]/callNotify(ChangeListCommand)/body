{
  myFuture.set(myService.submit(new Runnable(){
    public void run(){
      try {
        command.doNotify(myDispatcher);
      }
 catch (      Throwable e) {
        LOG.error(e);
      }
    }
  }
));
}
