{
  VariableWidthIntArray arr=(VariableWidthIntArray)super.clone();
  if (intArray != null) {
    arr.intArray=intArray.clone();
  }
  if (shortArray != null) {
    arr.shortArray=shortArray.clone();
  }
  if (byteArray != null) {
    arr.byteArray=byteArray.clone();
  }
  return arr;
}
