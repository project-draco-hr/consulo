{
  doParserTest(text,new TestParser(){
    public void parse(    final PsiBuilder builder){
      if (diamonds)       ReferenceParser.parseType(builder,false,false,true);
 else       ReferenceParser.parseTypeWithEllipsis(builder,false,false);
    }
  }
);
}
