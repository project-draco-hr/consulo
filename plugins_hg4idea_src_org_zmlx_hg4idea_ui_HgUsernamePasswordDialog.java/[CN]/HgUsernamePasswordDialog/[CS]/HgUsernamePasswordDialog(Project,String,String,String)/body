{
  super(project,false);
  setTitle(HgVcsMessages.message("hg4idea.dialog.login.password.required"));
  authPanel=new AuthenticationPanel(HgVcsMessages.message("hg4idea.dialog.login.description",url),login,password,!StringUtils.isBlank(password));
  init();
}
