{
  StringBuilder builder=new StringBuilder();
  for (  String l : errorLines) {
    if (builder.length() > 0) {
      builder.append('\n');
    }
    builder.append(l);
  }
  return builder.toString().trim();
}
