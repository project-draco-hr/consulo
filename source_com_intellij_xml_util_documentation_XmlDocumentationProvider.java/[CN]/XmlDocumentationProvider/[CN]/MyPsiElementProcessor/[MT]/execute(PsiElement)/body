{
  if (element instanceof XmlTag && ((XmlTag)element).getLocalName().equals("documentation")) {
    final XmlTag tag=((XmlTag)element);
    result=tag.getValue().getText().trim();
    url=tag.getAttributeValue("source");
    return false;
  }
  return true;
}
