{
  if (myWrapper != wrapper) {
    myWrapper=wrapper;
    if (wrapper != null) {
      wrapper.updatePreviewPanel(this);
    }
 else {
      cleanup();
      repaint();
    }
    revalidate();
  }
}
