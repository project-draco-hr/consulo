{
  List<Consumer<T>> list;
synchronized (this) {
    list=consumers;
    consumers=null;
  }
  if (list != null) {
    for (    Consumer<T> consumer : list) {
      if (!isObsolete(consumer)) {
        consumer.consume(t);
      }
    }
  }
}
