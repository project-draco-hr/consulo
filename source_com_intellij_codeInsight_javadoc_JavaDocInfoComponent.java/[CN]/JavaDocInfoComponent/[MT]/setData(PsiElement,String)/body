{
  if (myElement != null) {
    myBackStack.push(saveContext());
    myForwardStack.clear();
  }
  if (element != null) {
    myElement=element;
  }
  myIsEmpty=false;
  updateControlState();
  setDataInternal(element,text,new Rectangle(0,0));
}
