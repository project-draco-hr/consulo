{
  final String name=myLibrary.getName();
  if (name != null) {
    return name;
  }
 else {
    final String[] urls=myLibrary.getUrls(OrderRootType.CLASSES);
    if (urls.length > 0) {
      return urls[0];
    }
 else {
      return null;
    }
  }
}
