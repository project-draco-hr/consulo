{
  final LookupElementBuilder builder=LookupElementBuilder.create(psiClass,lookupString).setIcon(psiClass.getIcon(Iconable.ICON_FLAG_VISIBILITY));
  if (withLocation) {
    return builder.setTailText(" (" + PsiFormatUtil.getPackageDisplayName(psiClass) + ")",true);
  }
  return builder;
}
