{
  this(domElement,childDescription,DomUIFactory.findMethod(DomUtil.getRawType(childDescription.getType()),"setValue"),DomUIFactory.findMethod(DomUtil.getRawType(childDescription.getType()),"getValue"));
}
