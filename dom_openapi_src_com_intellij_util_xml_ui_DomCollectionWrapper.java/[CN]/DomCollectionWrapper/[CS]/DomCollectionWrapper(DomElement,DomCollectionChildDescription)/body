{
  this(domElement,childDescription,DomUIFactory.findMethod(DomReflectionUtil.getRawType(childDescription.getType()),"setValue"),DomUIFactory.findMethod(DomReflectionUtil.getRawType(childDescription.getType()),"getValue"));
}
