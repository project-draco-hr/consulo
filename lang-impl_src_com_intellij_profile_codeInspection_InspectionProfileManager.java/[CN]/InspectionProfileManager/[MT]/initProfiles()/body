{
  if (!myProfilesAreInitialized.getAndSet(true)) {
    if (ApplicationManager.getApplication().isUnitTestMode())     return;
    final Collection<Profile> profiles=mySchemesManager.loadSchemes(FILE_SPEC,myReaderWriter,RoamingType.PER_USER);
    if (profiles.isEmpty()) {
      createDefaultProfile();
    }
 else {
      for (      Profile profile : profiles) {
        addProfile(profile);
      }
    }
  }
}
