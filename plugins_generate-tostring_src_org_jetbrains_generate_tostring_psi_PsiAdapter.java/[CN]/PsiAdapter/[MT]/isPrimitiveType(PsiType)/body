{
  if (type.getCanonicalText().startsWith("java")) {
    return false;
  }
  if (type.isAssignableFrom(PsiType.BOOLEAN) || type.isAssignableFrom(PsiType.BYTE) || type.isAssignableFrom(PsiType.CHAR)|| type.isAssignableFrom(PsiType.DOUBLE)|| type.isAssignableFrom(PsiType.FLOAT)|| type.isAssignableFrom(PsiType.INT)|| type.isAssignableFrom(PsiType.LONG)|| type.isAssignableFrom(PsiType.SHORT)) {
    return true;
  }
  PsiType subType=type.getDeepComponentType();
  if (subType.isAssignableFrom(PsiType.BOOLEAN) || subType.isAssignableFrom(PsiType.BYTE) || subType.isAssignableFrom(PsiType.CHAR)|| subType.isAssignableFrom(PsiType.DOUBLE)|| subType.isAssignableFrom(PsiType.FLOAT)|| subType.isAssignableFrom(PsiType.INT)|| subType.isAssignableFrom(PsiType.LONG)|| subType.isAssignableFrom(PsiType.SHORT)) {
    return true;
  }
  return false;
}
