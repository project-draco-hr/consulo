{
  if (myDebugMode && AndroidRunConfigurationBase.isDdmsCorrupted(myFacet)) {
    getProcessHandler().notifyTextAvailable("Debug info is not available. Please close other application using ADB: DDMS, Eclipse\n",STDERR);
    return false;
  }
  return true;
}
