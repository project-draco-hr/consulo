{
  final VirtualFile vFile=getViewProvider().getVirtualFile();
  if (!vFile.isValid())   return false;
  if (!getViewProvider().isPhysical())   return true;
  if (myManager.getProject().isDisposed())   return false;
  return isPsiUpToDate(vFile);
}
