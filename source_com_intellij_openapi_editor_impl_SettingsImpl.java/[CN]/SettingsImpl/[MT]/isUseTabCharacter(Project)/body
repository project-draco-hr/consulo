{
  VirtualFile file=myEditor.getVirtualFile();
  FileType fileType=file == null ? null : file.getFileType();
  return myUseTabCharacter != null ? myUseTabCharacter.booleanValue() : CodeStyleSettingsManager.getSettings(project).useTabCharacter(fileType);
}
