{
  return new UncheckedWarningsVisitor(isOnTheFly){
    @Override protected void registerProblem(    String message,    PsiElement psiElement,    LocalQuickFix... quickFix){
      holder.registerProblem(psiElement,message,quickFix);
    }
  }
;
}
