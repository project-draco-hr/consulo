{
  return new UncheckedWarningsVisitor(isOnTheFly){
    @Override protected void registerProblem(    @NotNull String message,    @NotNull PsiElement psiElement,    @NotNull LocalQuickFix[] quickFixes){
      holder.registerProblem(psiElement,message,quickFixes);
    }
  }
;
}
