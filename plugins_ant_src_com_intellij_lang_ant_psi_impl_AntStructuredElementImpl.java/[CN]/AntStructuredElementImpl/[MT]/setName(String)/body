{
  try {
    if (hasNameElement()) {
      getNameElement().setIdentifierName(name);
    }
 else     if (hasIdElement()) {
      getIdElement().setIdentifierName(name);
    }
    return this;
  }
  finally {
    clearCaches();
  }
}
