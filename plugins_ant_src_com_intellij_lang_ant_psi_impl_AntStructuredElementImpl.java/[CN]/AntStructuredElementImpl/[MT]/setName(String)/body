{
  if (hasNameElement()) {
    getNameElement().setName(name);
    subtreeChanged();
  }
 else   if (hasIdElement()) {
    getIdElement().setName(name);
    subtreeChanged();
  }
 else {
    super.setName(name);
  }
  return this;
}
