{
  if (myNameElement != null) {
    myNameElement.setName(name);
    subtreeChanged();
  }
 else   if (myIdElement != null) {
    myIdElement.setName(name);
    subtreeChanged();
  }
 else {
    super.setName(name);
  }
  return this;
}
