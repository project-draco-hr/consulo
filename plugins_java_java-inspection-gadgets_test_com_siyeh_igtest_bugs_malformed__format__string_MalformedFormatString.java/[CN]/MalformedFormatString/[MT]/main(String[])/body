{
  String local="hmm";
  String good=String.format("%s %s",1,2);
  String warn=String.format("%s %s",1);
  String invalid=String.format("%s %s" + local,1);
  String interesting=String.format("%s %s" + "hmm",1);
  String intAsChar=String.format("symbol '%1$c' (numeric value %1$d)",60);
}
