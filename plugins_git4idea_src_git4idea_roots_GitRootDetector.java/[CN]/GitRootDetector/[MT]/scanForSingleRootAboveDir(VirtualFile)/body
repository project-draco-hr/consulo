{
  if (myProject.isDisposed()) {
    return null;
  }
  VirtualFile par=dir.getParent();
  while (par != null) {
    if (hasGitDir(par)) {
      return par;
    }
    par=par.getParent();
  }
  return null;
}
