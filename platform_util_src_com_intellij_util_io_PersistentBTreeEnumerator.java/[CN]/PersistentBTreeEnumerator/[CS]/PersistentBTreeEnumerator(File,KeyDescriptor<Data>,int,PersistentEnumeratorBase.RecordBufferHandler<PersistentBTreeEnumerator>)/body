{
  super(file,new MappedFileSimpleStorage(file,initialSize,1024 * 1024,false),dataDescriptor,initialSize,ourVersion,recordBufferHandler);
  myInlineKeysNoMapping=myDataDescriptor instanceof InlineKeyDescriptor && !wantInlineKeyMapping();
  if (btree == null) {
synchronized (ourLock) {
      storeVars(false);
      initBtree(false);
      storeBTreeVars(false);
    }
  }
}
