{
  List<Sdk> result=new ArrayList<Sdk>();
synchronized (mySdks) {
    for (    Sdk sdk : mySdks) {
      if (sdk.getSdkType() == type) {
        result.add(sdk);
      }
    }
  }
  return result;
}
