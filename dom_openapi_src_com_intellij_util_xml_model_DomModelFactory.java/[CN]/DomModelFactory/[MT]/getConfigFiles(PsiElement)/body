{
  if (psiElement == null) {
    return Collections.emptySet();
  }
  final M model=getModel(psiElement);
  if (model == null) {
    return Collections.emptySet();
  }
 else {
    return model.getConfigFiles();
  }
}
