{
  if (myPsiClass != null) {
    myModel.clear();
    myModel.setSelectedItem(null);
    PsiMethod[] allMethods=myPsiClass.getAllMethods();
    for (    PsiMethod allMethod : allMethods) {
      if (qualifies(allMethod))       myModel.add(new MethodWrapper(allMethod));
    }
  }
}
