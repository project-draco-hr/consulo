{
  set("xxx");
  set(g);
  fs("oo",this);
  String o=(String)((null));
  set(o);
  set(new String());
  set(nn());
  set(CON);
  String nn=g == null ? CON : g;
  set(nn);
  String other=g == "" ? CON : g;
  set(other);
  g.hashCode();
  set(g);
  set(nu());
  set(hz());
}
