{
  try {
    final XmlTag tag=getXmlTag();
    if (tag != null) {
      deleteTag(tag);
      undefineChildren();
      fireUndefinedEvent();
    }
  }
 catch (  Exception e) {
    LOG.error(e);
  }
}
