{
  MavenIndicesManager m=MavenIndicesManager.getInstance();
  myProjectIndices.set(m.ensureIndicesExist(MavenProjectsManager.getInstance(myProject).getLocalRepository(),collectRemoteRepositories()));
}
