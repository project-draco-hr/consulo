{
  try {
    MavenIndicesManager m=MavenIndicesManager.getInstance();
    myProjectIndices.set(m.ensureIndicesExist(myProject,MavenProjectsManager.getInstance(myProject).getLocalRepository(),collectRemoteRepositories()));
  }
 catch (  MavenIndexException e) {
    MavenLog.info(e);
  }
}
