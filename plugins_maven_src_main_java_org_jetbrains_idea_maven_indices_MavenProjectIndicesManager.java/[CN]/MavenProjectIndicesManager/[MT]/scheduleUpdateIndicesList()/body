{
  myUpdateQueue.queue(new Update(MavenProjectIndicesManager.this){
    public void run(){
      List<MavenIndex> newIndices=MavenIndicesManager.getInstance().ensureIndicesExist(getLocalRepository(),collectRemoteRepositories());
      myProjectIndices=newIndices;
    }
  }
);
}
