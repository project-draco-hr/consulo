{
  myUpdateQueue.queue(new Update(MavenProjectIndicesManager.this){
    public void run(){
      List<MavenIndex> newIndices=MavenIndicesManager.getInstance().ensureIndicesExist(myProject,getLocalRepository(),collectRemoteRepositoriesIdsAndUrls());
      myProjectIndices=newIndices;
    }
  }
);
}
