{
  if (myDirty) {
    if (!dirty) {
      myStorage.seek(0);
      myStorage.writeInt(CORRECTLY_CLOSED_MAGIC);
      myDirty=false;
    }
  }
 else {
    if (dirty) {
      myStorage.seek(0);
      myStorage.writeInt(DIRTY_MAGIC);
      myDirty=true;
    }
  }
}
