{
  if (r instanceof JBRectangle) {
    x=r.x;
    y=r.y;
    width=r.width;
    height=r.height;
  }
 else {
    x=JBUI.scale(r.x);
    y=JBUI.scale(r.y);
    width=JBUI.scale(r.width);
    height=JBUI.scale(r.height);
  }
}
