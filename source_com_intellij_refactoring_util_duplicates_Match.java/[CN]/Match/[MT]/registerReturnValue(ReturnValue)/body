{
  if (myReturnValue == null) {
    myReturnValue=returnValue;
    return true;
  }
 else {
    if (!myReturnValue.isEquivalent(returnValue)) {
      return false;
    }
 else {
      return true;
    }
  }
}
