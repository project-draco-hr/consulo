{
  CheckUtil.checkWritable(element);
  if (!SourceTreeToPsiMap.hasTreeElement(element))   return element;
  return SourceTreeToPsiMap.treeElementToPsi(new ReferenceAdjuster(myProject).process((TreeElement)element.getNode(),(flags & DO_NOT_ADD_IMPORTS) == 0,(flags & UNCOMPLETE_CODE) != 0));
}
