{
  int idx=str.indexOf('"');
  if (idx < 0)   return str;
  StringBuffer buf=new StringBuffer(str);
  while (idx < buf.length()) {
    if (buf.charAt(idx) == '"') {
      buf.replace(idx,idx + 1,"\\\"");
      idx+=2;
    }
 else {
      idx+=1;
    }
  }
  return buf.toString();
}
