{
  return new TextProvider(){
    @Override public String getText(){
      return (isGroupEnabled(group) ? "Disable" : "Enable") + (PluginGroups.getInstance().getSets(group).size() > 1 ? " All" : "");
    }
  }
;
}
