def testhunk(a, b, bstart):
    alen = len(a)
    blen = len(b)
    if (alen > (blen - bstart)):
        return (-1)
    for i in xrange(alen):
        if (a[i][1:] != b[(i + bstart)]):
            return (-1)
    return 0
