{
  if (startTagEndOffset < 0) {
    return false;
  }
  for (int i=startTagEndOffset + 1; i < myOffset; i++) {
    char c=myText.charAt(i);
    if (c != ' ' && c != '\t') {
      return false;
    }
  }
  return true;
}
