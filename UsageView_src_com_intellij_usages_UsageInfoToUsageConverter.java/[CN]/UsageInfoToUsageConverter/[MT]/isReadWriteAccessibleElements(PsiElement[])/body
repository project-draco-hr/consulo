{
  if (elements.length == 0) {
    return false;
  }
  for (int idx=0; idx < elements.length; idx++) {
    if (!(elements[idx] instanceof PsiVariable)) {
      return false;
    }
  }
  return true;
}
