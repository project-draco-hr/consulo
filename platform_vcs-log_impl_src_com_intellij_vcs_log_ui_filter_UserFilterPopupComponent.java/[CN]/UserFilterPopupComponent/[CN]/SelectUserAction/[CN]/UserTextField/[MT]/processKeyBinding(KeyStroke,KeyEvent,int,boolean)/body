{
  if (e.getKeyCode() == KeyEvent.VK_ENTER) {
    myPopup.closeOk(e);
    return true;
  }
 else   if (e.getKeyCode() == KeyEvent.VK_ESCAPE) {
    myPopup.cancel(e);
    return true;
  }
  return false;
}
