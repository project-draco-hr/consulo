{
  final CvsRootConfiguration newConfiguration=CvsApplicationLevelConfiguration.createNewConfiguration(CvsApplicationLevelConfiguration.getInstance());
  try {
    saveTo(newConfiguration);
  }
 catch (  InputException e) {
    e.show();
    return null;
  }
  return newConfiguration;
}
