{
  UIUtil.invokeLaterIfNeeded(new Runnable(){
    @Override public void run(){
      Messages.showMessageDialog(component,CvsBundle.message("operation.status.connection.successful"),CvsBundle.message("operation.name.test.connection"),Messages.getInformationIcon());
    }
  }
);
}
