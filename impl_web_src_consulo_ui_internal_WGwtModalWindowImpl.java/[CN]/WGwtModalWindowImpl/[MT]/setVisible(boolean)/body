{
  if (myVisible == value) {
    return;
  }
  myVisible=value;
  if (myVisible) {
    mySemaphore.down();
    GwtUIAccess gwtUIAccess=GwtUIAccess.get();
    gwtUIAccess.showModal(this);
    mySemaphore.waitFor();
  }
 else {
    disposeImpl();
  }
  markAsChanged();
}
