{
  for (  String rootUrl : module.getContentRootsList().getUrls()) {
    File root=JpsPathUtil.urlToFile(rootUrl);
    if (new File(root,"griffon-app").isDirectory() && new File(root,"application.properties").isFile()) {
      return true;
    }
  }
  return false;
}
