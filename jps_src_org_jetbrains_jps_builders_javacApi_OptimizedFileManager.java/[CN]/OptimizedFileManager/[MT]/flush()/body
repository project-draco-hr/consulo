{
  super.flush();
  for (  DelayedClassFileWriter f : myWriters) {
    try {
      f.commit();
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  myWriters.clear();
  if (classFinder != null) {
    classFinder.releaseResources();
  }
}
