{
  return getDocument() instanceof DocumentImpl && !isInplaceRenamerActive() && StringUtil.indexOf(e.getOldFragment(),'\n') == -1 && StringUtil.indexOf(e.getNewFragment(),'\n') == -1 && !(e.getNewLength() == 1 && DOCUMENT_CHARS_TO_SKIP.contains(e.getNewFragment().charAt(0)));
}
