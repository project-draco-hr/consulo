{
  if (count > 0) {
    if (buffer.length() > 0) {
      buffer.append(" and ");
    }
    buffer.append(count);
    buffer.append(' ');
    if (count == 1) {
      buffer.append(singleName);
    }
 else {
      buffer.append(multipleName);
    }
  }
}
