{
  if (count > 0) {
    if (buffer.length() > 0) {
      buffer.append(" " + IdeBundle.message("prompt.delete.and") + " ");
    }
    buffer.append(count);
    buffer.append(' ');
    buffer.append(IdeBundle.message(propertyKey,count));
  }
}
