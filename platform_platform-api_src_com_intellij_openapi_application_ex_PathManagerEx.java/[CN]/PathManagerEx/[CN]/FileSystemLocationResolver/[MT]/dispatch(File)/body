{
  if (myResult != null) {
    return;
  }
  for (  File file : dir.listFiles()) {
    if (myResult != null) {
      return;
    }
    if (file.isDirectory()) {
      dispatch(file);
    }
 else {
      check(file);
    }
  }
}
