{
  final PsiExpression arrayExp=arrayAccessExpression.getArrayExpression();
  final PsiExpression indexExp=arrayAccessExpression.getIndexExpression();
  return removeParentheses(arrayExp) + '[' + removeParentheses(indexExp)+ ']';
}
