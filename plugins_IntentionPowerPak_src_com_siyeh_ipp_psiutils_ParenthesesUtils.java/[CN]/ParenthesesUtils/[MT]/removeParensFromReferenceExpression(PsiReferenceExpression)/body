{
  final PsiExpression qualifier=expression.getQualifierExpression();
  if (qualifier != null) {
    return removeParentheses(qualifier) + '.' + expression.getReferenceName();
  }
 else {
    return expression.getText();
  }
}
