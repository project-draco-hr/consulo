{
  report(level,module,context,e);
  if (failFast) {
    if (e instanceof IOException) {
      throw (IOException)e;
    }
 else     if (e instanceof ConversionException) {
      throw (ConversionException)e;
    }
 else     throw new ConversionException(e.getMessage());
  }
}
