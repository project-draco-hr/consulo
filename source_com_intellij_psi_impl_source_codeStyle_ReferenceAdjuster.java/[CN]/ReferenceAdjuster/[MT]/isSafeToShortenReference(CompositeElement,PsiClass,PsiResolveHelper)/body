{
  PsiClass newRefClass=helper.resolveReferencedClass(refClass.getName(),SourceTreeToPsiMap.treeElementToPsi(reference));
  return refClass.getManager().areElementsEquivalent(refClass,newRefClass);
}
