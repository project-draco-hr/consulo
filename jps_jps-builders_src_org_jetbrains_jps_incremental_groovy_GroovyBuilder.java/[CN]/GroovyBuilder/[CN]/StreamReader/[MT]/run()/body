{
  while (true) {
    String line=null;
    try {
      line=myStream.readLine();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    if (line == null) {
      break;
    }
    myWriter.println(line);
  }
}
