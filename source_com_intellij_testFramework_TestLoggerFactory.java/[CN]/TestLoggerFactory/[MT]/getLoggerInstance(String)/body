{
synchronized (this) {
    try {
      if (!isInitialized()) {
        init();
      }
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
    return new TestLogger(org.apache.log4j.Logger.getLogger(name));
  }
}
