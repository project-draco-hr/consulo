{
  ElementFilter filter=new OrFilter(new ClassFilter[]{new ClassFilter(PsiModifierListOwner.class),new ClassFilter(PsiPackage.class)});
  FilterScopeProcessor proc=new FilterScopeProcessor(filter,processor);
  PsiScopesUtil.resolveAndWalk(proc,this,null,true);
}
