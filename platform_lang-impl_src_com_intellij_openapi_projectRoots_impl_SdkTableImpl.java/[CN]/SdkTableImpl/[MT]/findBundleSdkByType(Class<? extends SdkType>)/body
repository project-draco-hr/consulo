{
  SdkType sdkType=SdkType.EP_NAME.findExtension(sdkTypeClass);
  if (sdkType == null) {
    return null;
  }
  for (  Sdk sdk : mySdks) {
    if (sdk.isBundled() && sdk.getSdkType() == sdkType) {
      return sdk;
    }
  }
  return null;
}
