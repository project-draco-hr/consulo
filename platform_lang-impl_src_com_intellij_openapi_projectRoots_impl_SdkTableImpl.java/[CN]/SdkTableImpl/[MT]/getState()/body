{
  Element element=new Element("SdkTable");
  for (  Sdk sdk : mySdks) {
    if (sdk.isBundled()) {
      continue;
    }
    element.addContent(((SdkImpl)sdk).getState().setName(ELEMENT_SDK));
  }
  return element;
}
