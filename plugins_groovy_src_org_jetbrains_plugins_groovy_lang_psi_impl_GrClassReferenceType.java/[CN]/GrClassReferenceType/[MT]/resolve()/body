{
  PsiElement resolved=myReferenceElement.resolve();
  return resolved instanceof PsiClass ? (PsiClass)resolved : resolved instanceof PsiMethod ? ((PsiMethod)resolved).getContainingClass() : null;
}
