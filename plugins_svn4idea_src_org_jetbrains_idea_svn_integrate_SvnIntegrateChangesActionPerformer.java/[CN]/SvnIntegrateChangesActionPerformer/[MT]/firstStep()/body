{
  final CommittedChangeList firstList=myChangeLists.get(0);
  myCurrentBranch=((SvnChangeList)firstList).getBranchUrl();
  SelectBranchPopup.showForVCSRoot(myProject,((SvnChangeList)firstList).getVcsRoot(),this,SvnBundle.message("action.Subversion.integrate.changes.select.branch.text"));
}
