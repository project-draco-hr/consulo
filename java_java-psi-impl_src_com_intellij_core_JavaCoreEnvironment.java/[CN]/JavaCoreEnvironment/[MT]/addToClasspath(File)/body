{
  myFileManager.addToClasspath(path);
  final VirtualFile root=path.isFile() ? myJarFileSystem.findFileByPath(path + "!/") : getLocalFileSystem().findFileByPath(path.getPath());
  if (root != null) {
    myFileIndexFacade.addLibraryRoot(root);
  }
}
