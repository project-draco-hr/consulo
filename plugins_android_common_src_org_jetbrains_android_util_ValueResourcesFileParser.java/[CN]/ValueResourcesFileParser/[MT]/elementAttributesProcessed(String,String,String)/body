{
  if (myLastNameAttr != null && name != null) {
    final String resTypeStr="item".equals(name) ? myLastTypeAttr : AndroidCommonUtils.getResourceTypeByTagName(name);
    final ResourceType resType=resTypeStr != null ? ResourceType.getEnum(resTypeStr) : null;
    if (resType != null && !(resType == ResourceType.ATTR && myLastNameAttr.startsWith("android:"))) {
      process(new ResourceEntry(resTypeStr,myLastNameAttr));
    }
  }
}
