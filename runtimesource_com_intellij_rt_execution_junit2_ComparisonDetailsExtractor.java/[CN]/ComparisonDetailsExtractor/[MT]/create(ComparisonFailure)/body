{
  try {
    return new ComparisonDetailsExtractor(assertion,(String)EXPECTED_FIELD.get(assertion),(String)ACTUAL_FIELD.get(assertion));
  }
 catch (  Throwable e) {
    return new ExceptionPacketFactory(PoolOfTestStates.FAILED_INDEX,assertion);
  }
}
