{
  if (!binDir.exists()) {
    return null;
  }
  for (  String exec : new String[]{GIT_CMD,GIT_EXE}) {
    File fe=new File(binDir,exec);
    if (fe.exists()) {
      return fe.getPath();
    }
  }
  return null;
}
