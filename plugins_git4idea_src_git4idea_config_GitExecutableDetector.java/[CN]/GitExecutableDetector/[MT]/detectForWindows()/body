{
  String exec=checkInPath();
  if (exec != null) {
    return exec;
  }
  exec=checkProgramFiles();
  if (exec != null) {
    return exec;
  }
  exec=checkCygwin();
  if (exec != null) {
    return exec;
  }
  return checkSoleExecutable();
}
