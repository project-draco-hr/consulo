{
  LOG.assertTrue(files.size() > 0);
  if ((files.size() > 1) || firstElementContainsChildren(files))   return new AddMultiplyFilesOptionsDialog(project,files,options);
 else   return new AddOneFileOptionsDialog(project,options,files.iterator().next());
}
