{
  final TreeElement lastChild=(TreeElement)element.getLastChildNode();
  if (lastChild == null)   return null;
  final ASTNode last=lastChild.getTransformedLastOrSelf();
  return last == null ? null : last.getPsi();
}
