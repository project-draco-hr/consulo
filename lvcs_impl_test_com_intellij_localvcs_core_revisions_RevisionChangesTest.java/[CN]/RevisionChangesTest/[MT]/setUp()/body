{
  c1=new CreateFileChange(1,"f",null,-1);
  c2=new ChangeFileContentChange("f",null,-1);
  c3=new RenameChange("f","ff");
  list=new ChangeList();
  applyAndAdd(c1);
  applyAndAdd(c2);
  applyAndAdd(c3);
}
