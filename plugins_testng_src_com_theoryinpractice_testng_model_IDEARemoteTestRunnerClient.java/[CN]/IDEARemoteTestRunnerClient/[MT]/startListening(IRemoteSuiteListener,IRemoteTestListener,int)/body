{
  ServerConnection srvConnection=new ServerConnection(port){
    @Override protected void handleThrowable(    Throwable cause){
      cause.printStackTrace();
    }
  }
;
  startListening(new IRemoteSuiteListener[]{suiteListener},new IRemoteTestListener[]{testListener},srvConnection);
}
