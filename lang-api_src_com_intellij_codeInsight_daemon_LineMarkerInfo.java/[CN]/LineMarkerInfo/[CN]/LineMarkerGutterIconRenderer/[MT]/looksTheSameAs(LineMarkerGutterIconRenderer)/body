{
  return myInfo.getElement() != null && renderer.myInfo.getElement() != null && myInfo.getElement() == renderer.myInfo.getElement() && Comparing.equal(getTooltipText(),renderer.getTooltipText()) && Comparing.equal(myInfo.myIcon,renderer.myInfo.myIcon);
}
