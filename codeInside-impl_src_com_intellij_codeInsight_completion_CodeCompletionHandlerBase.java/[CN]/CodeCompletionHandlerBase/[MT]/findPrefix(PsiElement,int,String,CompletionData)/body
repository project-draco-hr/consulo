{
  final String result=(completionData != null) ? completionData.findPrefix(insertedElement,offset) : CompletionData.findPrefixStatic(insertedElement,offset);
  if (result.endsWith(dummyIdentifier))   return result.substring(0,result.length() - dummyIdentifier.length());
  return result;
}
