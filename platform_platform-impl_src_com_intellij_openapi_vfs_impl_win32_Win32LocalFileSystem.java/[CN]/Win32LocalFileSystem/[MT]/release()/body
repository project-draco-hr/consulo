{
  Win32LocalFileSystem system=ourSystem;
  ourSystem=null;
  if (system != null) {
    try {
      system.myKernel.release();
    }
 catch (    Throwable throwable) {
      LOG.error(throwable);
    }
  }
}
