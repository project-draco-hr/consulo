{
  String result="<html>";
  if (style != null) {
    result+="<div style=\"" + style + "\">";
  }
  result+="<b>" + notification.getTitle() + "</b><p>"+ notification.getContent()+ "</p>";
  if (style != null) {
    result+="</div>";
  }
  result+="</html>";
  return result;
}
