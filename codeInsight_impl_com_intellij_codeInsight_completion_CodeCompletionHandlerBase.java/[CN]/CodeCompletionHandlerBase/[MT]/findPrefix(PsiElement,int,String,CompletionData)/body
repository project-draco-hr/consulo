{
  final String result=completionData == null ? CompletionData.findPrefixStatic(insertedElement,offset) : completionData.findPrefix(insertedElement,offset);
  return result.endsWith(dummyIdentifier) ? result.substring(0,result.length() - dummyIdentifier.length()) : result;
}
