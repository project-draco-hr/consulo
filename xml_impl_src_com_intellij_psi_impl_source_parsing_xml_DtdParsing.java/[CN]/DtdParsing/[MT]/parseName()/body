{
  if (myBuilder.getTokenType() == XML_NAME) {
    addToken();
    return true;
  }
  if (myBuilder.getTokenType() == XML_ENTITY_REF_TOKEN) {
    parseEntityRef();
    return true;
  }
  return false;
}
