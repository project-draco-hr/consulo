{
synchronized (SCAN_LOCK) {
    if (myScanning) {
      return;
    }
    myScanning=true;
    myRootProblemNotifier.rescanAndNotifyIfNeeded();
    myExecuteAfterScan.run();
    myScanning=false;
  }
}
