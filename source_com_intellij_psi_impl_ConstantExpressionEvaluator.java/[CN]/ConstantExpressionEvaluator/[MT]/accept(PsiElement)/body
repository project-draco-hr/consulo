{
  if (element == null)   return null;
  Project project=element.getProject();
  final Key<CachedValue<Map<PsiElement,Object>>> key=myThrowExceptionOnOverflow ? CONSTANT_VALUE_WITH_OVERFLOW_MAP_KEY : CONSTANT_VALUE_WO_OVERFLOW_MAP_KEY;
  return calculateWithCaching(project,key,element);
}
