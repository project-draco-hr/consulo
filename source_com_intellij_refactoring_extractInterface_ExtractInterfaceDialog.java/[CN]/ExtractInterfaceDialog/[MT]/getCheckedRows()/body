{
  int count=0;
  for (int idx=0; idx < myMemberInfos.length; idx++) {
    if (myMemberInfos[idx].isChecked()) {
      count++;
    }
  }
  int[] rows=new int[count];
  int currentRow=0;
  for (int idx=0; idx < myMemberInfos.length; idx++) {
    if (myMemberInfos[idx].isChecked()) {
      rows[currentRow++]=idx;
    }
  }
  return rows;
}
