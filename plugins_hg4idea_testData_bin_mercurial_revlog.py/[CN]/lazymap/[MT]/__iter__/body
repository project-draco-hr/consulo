def __iter__(self):
    yield nullid
    for (i, ret) in enumerate(self.p.index):
        if (not ret):
            self.p.loadindex(i)
            ret = self.p.index[i]
        if isinstance(ret, str):
            ret = _unpack(indexformatng, ret)
        yield ret[7]
