{
  ConcurrentMap<Key,Object> map=myUserMap;
  if (map == null) {
synchronized (MAP_LOCK) {
      map=myUserMap;
      if (map == null) {
        myUserMap=map=createDataMap(2);
      }
    }
  }
  return map;
}
