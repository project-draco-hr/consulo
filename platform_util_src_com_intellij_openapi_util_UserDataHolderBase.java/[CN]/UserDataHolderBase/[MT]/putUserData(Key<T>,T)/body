{
  Map<Key,Object> map=getOrCreateMap();
  if (value == null) {
    map.remove(key);
    if (map.isEmpty()) {
synchronized (MAP_LOCK) {
        if (myUserMap != null && myUserMap.isEmpty()) {
          myUserMap=null;
        }
      }
    }
  }
 else {
    map.put(key,value);
  }
}
