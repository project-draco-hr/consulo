{
  super(project,true);
  myProject=project;
  myHelper=helper;
  setUpNameField();
  setModal(true);
  getRootPane().setDefaultButton(buttonOK);
  setTitle(REFACTORING_NAME);
  init();
  setUpDialog();
  update();
}
