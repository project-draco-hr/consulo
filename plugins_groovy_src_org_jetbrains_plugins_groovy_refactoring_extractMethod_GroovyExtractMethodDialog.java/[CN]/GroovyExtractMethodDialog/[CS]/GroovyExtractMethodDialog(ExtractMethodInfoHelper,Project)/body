{
  super(project,true);
  myProject=project;
  myHelper=helper;
  setUpNameField();
  myParameterTablePanel.init(this,myHelper);
  setModal(true);
  setTitle(GroovyExtractMethodHandler.REFACTORING_NAME);
  init();
  setUpDialog();
  update();
}
