{
  if (object == this) {
    return true;
  }
  if (!(object instanceof HgFileRevision)) {
    return false;
  }
  HgFileRevision that=(HgFileRevision)object;
  return new EqualsBuilder().append(hgFile,that.hgFile).append(vcsRevisionNumber,that.vcsRevisionNumber).isEquals();
}
