{
  if (isNumericType(lType) && isNumericType(rType))   return true;
  if (lType.equalsToText("java.lang.String") && isNumericType(rType) || rType.equalsToText("java.lang.String") && isNumericType(lType)) {
    return true;
  }
  return TypeConversionUtil.isAssignable(lType,rType);
}
