{
  if (PsiUtil.isLocalClass(aClass)) {
    TextRange textRange=ClassUtil.getClassDeclarationTextRange(aClass);
    return HighlightInfo.createHighlightInfo(HighlightInfoType.ERROR,textRange,"Modifier 'interface' not allowed here");
  }
  return null;
}
