{
  char[] strchars=str.toCharArray();
  for (  char strchar : strchars) {
    if (isVowel(strchar))     return true;
  }
  if (str.indexOf('y') > -1) {
    return true;
  }
 else {
    return false;
  }
}
