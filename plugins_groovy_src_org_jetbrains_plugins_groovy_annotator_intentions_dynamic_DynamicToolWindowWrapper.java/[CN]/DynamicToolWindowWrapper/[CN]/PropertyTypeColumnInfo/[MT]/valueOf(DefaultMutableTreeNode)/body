{
  Object userObject=treeNode.getUserObject();
  if (userObject instanceof DPropertyElement)   return QuickfixUtil.shortenType(((DPropertyElement)userObject).getType());
  if (userObject instanceof DMethodElement)   return QuickfixUtil.shortenType(((DMethodElement)userObject).getType());
  return null;
}
