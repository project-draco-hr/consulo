{
  Object userObject=treeNode.getUserObject();
  if (userObject instanceof DPropertyNode)   return ((DPropertyNode)userObject).getElement().getDynamicVirtualElement().getType();
  if (userObject instanceof DMethodNode)   return ((DMethodNode)userObject).getElement().getDynamicVirtualElement().getType();
  return null;
}
