{
synchronized (myLock) {
    final Key key=getKey(obj);
    final Boolean value=myWasCancelled.get(key);
    if (value == null) {
      myWasCancelled.put(key,false);
      return true;
    }
    if (value) {
      myWasCancelled.put(key,false);
    }
    return value;
  }
}
