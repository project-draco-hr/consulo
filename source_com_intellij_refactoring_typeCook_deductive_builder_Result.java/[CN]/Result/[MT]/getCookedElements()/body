{
  final HashSet<PsiElement> set=new HashSet<PsiElement>();
  for (final Iterator<PsiElement> e=myVictims.iterator(); e.hasNext(); ) {
    final PsiElement element=e.next();
    final PsiType originalType=element instanceof PsiMethod ? ((PsiMethod)element).getReturnType() : element instanceof PsiVariable ? ((PsiVariable)element).getType() : ((PsiExpression)element).getType();
    if (!originalType.equals(getCookedType(element))) {
      set.add(element);
    }
  }
  return set;
}
