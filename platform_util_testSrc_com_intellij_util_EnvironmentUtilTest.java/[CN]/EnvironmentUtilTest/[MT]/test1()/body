{
  Map enviromentProperties=EnvironmentUtil.getEnvironmentProperties();
  assertNotNull(enviromentProperties);
  if (SystemInfo.isWindows)   assertNotNull(enviromentProperties.get("Path"));
 else   assertNotNull(enviromentProperties.get("PATH"));
}
