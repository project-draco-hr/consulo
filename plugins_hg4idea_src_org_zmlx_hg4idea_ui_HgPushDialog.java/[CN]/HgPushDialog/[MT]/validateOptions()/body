{
  return !StringUtil.isEmptyOrSpaces(repositoryTxt.getText()) && !(revisionCbx.isSelected() && StringUtil.isEmptyOrSpaces(revisionTxt.getText())) && !(branchCheckBox.isSelected() && (branchComboBox.getSelectedItem() == null));
}
