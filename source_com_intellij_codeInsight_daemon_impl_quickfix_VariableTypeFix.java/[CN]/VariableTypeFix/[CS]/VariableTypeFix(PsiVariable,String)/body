{
  myVariable=variable;
  PsiType type;
  try {
    type=variable.getManager().getElementFactory().createTypeFromText(toReturn,variable);
  }
 catch (  Exception e) {
    type=null;
  }
  myReturnType=type;
}
