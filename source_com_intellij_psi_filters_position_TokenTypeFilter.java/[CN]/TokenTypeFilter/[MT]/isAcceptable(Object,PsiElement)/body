{
  if (element instanceof PsiDocToken)   return ((PsiDocToken)element).getTokenType() == myType;
 else   if (element instanceof XmlToken)   return ((XmlToken)element).getTokenType() == myType;
 else   if (element instanceof JspToken)   return ((JspToken)element).getTokenType() == myType;
  return false;
}
