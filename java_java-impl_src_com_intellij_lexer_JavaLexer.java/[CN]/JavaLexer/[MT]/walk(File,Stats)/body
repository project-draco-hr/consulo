{
  if (root.isDirectory()) {
    System.out.println("Lexing in " + root.getPath());
    for (    File file : root.listFiles()) {
      walk(file,stats);
    }
  }
 else {
    if (root.getName().endsWith(".java")) {
      lex(root,stats);
    }
  }
}
