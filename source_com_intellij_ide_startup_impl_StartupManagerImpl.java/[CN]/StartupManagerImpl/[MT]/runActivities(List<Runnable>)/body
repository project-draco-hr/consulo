{
  try {
    while (!activities.isEmpty()) {
      final Runnable runnable=activities.remove(0);
      try {
        runnable.run();
      }
 catch (      Throwable ex) {
        LOG.error(ex);
      }
    }
  }
  finally {
    activities.clear();
  }
}
