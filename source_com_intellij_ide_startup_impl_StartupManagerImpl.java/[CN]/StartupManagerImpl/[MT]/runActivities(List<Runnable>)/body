{
  try {
    for (Iterator<Runnable> iterator=activities.iterator(); iterator.hasNext(); ) {
      Runnable runnable=iterator.next();
      try {
        runnable.run();
      }
 catch (      Exception ex) {
        LOG.error(ex);
      }
    }
  }
  finally {
    activities.clear();
  }
}
