{
  final List<T> result=new ArrayList<T>();
  for (  Map.Entry<?,T> entry : map.entrySet()) {
    final T value=entry.getValue();
    if (value != null) {
      result.add(value);
    }
  }
  return result;
}
