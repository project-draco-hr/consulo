{
  final int r=Messages.showYesNoDialog(project,"You have to rebuild project to see fixed preview. Would you like to do it?","Rebuild project",Messages.getQuestionIcon());
  if (r == Messages.YES) {
    CompilerManager.getInstance(project).rebuild(null);
  }
}
