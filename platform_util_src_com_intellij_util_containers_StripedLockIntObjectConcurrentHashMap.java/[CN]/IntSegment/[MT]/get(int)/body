{
  if (count != 0) {
    IntHashEntry<V> e=getFirst(key);
    while (e != null) {
      if (key == e.key) {
        V v=e.value;
        if (v != null) {
          return v;
        }
        return readValueUnderLock(e);
      }
      e=e.next;
    }
  }
  return null;
}
