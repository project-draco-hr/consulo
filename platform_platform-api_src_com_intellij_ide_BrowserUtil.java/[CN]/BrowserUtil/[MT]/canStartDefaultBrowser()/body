{
  if (SystemInfo.isMac) {
    return true;
  }
  if (SystemInfo.isWindows) {
    return true;
  }
  if (SystemInfo.isLinux && Desktop.getDesktop().isSupported(Desktop.Action.BROWSE)) {
    return true;
  }
  return false;
}
