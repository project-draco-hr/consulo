{
  checkConnected();
  final JpsRemoteProto.Message.Request request=isMake ? ProtoUtil.createMakeRequest(projectId,modules) : ProtoUtil.createForceCompileRequest(projectId,modules,paths);
  return sendRequest(request,handler);
}
