{
  if (isDone()) {
    return false;
  }
  if (!myCanceledState.getAndSet(true)) {
    try {
      sendCancelBuildRequest(getRequestID());
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
  return true;
}
