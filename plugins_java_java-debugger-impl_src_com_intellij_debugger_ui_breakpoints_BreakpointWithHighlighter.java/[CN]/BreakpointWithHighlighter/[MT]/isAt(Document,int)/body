{
  RangeHighlighter highlighter=getHighlighter();
  return highlighter != null && highlighter.isValid() && document.equals(highlighter.getDocument()) && getSourcePosition().getLine() == document.getLineNumber(offset);
}
