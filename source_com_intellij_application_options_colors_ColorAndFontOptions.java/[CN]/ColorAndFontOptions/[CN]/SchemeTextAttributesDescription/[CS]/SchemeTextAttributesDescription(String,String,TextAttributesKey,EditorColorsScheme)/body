{
  super(name,group,scheme.getAttributes(key) == null ? new TextAttributes() : (TextAttributes)scheme.getAttributes(key).clone(),key,scheme);
  this.key=key;
  myAttributesToApply=scheme.getAttributes(key);
  initCheckedStatus();
}
