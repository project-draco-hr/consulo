{
  return getDelegate() == documentRange.getDelegate() && myHostRange.getStartOffset() == documentRange.myHostRange.getStartOffset() && myHostRange.getEndOffset() == documentRange.myHostRange.getEndOffset() && myPrefix.equals(documentRange.myPrefix) && mySuffix.equals(documentRange.mySuffix);
}
