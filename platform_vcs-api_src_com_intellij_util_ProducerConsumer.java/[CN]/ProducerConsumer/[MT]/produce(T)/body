{
synchronized (myLock) {
    while (myQueue.size() >= myMaxSize) {
      try {
        myLock.notifyAll();
        myLock.wait(10);
      }
 catch (      InterruptedException e) {
      }
    }
    myQueue.addLast(t);
    myLock.notifyAll();
  }
}
