{
  super(project,true);
  myInitialFile=initialFile;
  myFilter=filter;
  myFileType=fileType;
  setTitle(title);
  myProject=project;
  init();
  if (initialFile != null) {
    selectFile(initialFile);
  }
  handleSelectionChanged();
}
