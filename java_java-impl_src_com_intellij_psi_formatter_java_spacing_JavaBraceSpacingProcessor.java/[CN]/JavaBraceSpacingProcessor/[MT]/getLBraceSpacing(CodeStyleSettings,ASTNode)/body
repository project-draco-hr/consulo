{
  if (shouldUseFlyingGeeseStyleForLeftBrace(settings,rightNode)) {
    return getFlyingGeesSpacing(settings);
  }
  return Spacing.createSpacing(0,0,settings.BLANK_LINES_AFTER_CLASS_HEADER + 1,settings.KEEP_LINE_BREAKS,settings.KEEP_BLANK_LINES_IN_DECLARATIONS);
}
