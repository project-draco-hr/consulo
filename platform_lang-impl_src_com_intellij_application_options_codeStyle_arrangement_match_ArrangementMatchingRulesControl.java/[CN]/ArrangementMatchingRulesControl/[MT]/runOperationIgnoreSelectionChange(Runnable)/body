{
  mySkipSelectionChange=true;
  try {
    if (isEditing()) {
      getCellEditor().stopCellEditing();
    }
    task.run();
  }
  finally {
    mySkipSelectionChange=false;
    refreshEditor();
  }
}
