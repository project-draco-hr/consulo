{
  final TextRange range=super.getTextRange();
  if (myOffset == 0)   return range;
  return new TextRange(myOffset + range.getStartOffset(),myOffset + (myRange != null ? myRange.getLength() : range.getLength()));
}
