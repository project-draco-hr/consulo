{
  FileFilter f=new FileFilter(myRootManager.getFileIndex(),myTypeManager);
  myService=new LocalVcsService(myVcs,new IdeaGateway(myProject),myStartupManager,myRootManager,myFileManager,myDocumentManager,f,myCommandProcessor);
}
