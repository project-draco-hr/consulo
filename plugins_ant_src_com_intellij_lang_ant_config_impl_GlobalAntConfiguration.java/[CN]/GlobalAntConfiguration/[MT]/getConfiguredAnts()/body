{
  HashMap<AntReference,AntInstallation> map=ContainerUtil.assignKeys(ANTS.getIterator(getProperties()),AntInstallation.REFERENCE_TO_ANT);
  map.put(AntReference.BUNDLED_ANT,myBundledAnt);
  return map;
}
