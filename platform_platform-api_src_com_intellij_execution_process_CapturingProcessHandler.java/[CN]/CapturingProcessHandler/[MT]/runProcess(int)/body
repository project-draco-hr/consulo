{
  if (timeoutInMilliseconds <= 0) {
    return runProcess();
  }
  startNotify();
  if (waitFor(timeoutInMilliseconds)) {
    myOutput.setExitCode(getProcess().exitValue());
  }
 else {
    destroyProcess();
    myOutput.setTimeout();
  }
  return myOutput;
}
