{
  if (!(validityState instanceof TopLevelDependencyValidityState))   return false;
  return ((TopLevelDependencyValidityState)validityState).myTimestamp == this.myTimestamp && members.equals(((TopLevelDependencyValidityState)validityState).members);
}
