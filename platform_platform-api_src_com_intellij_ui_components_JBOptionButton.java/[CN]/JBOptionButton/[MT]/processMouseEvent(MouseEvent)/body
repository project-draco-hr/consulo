{
  if (myMoreRecMouse.contains(e.getPoint())) {
    if (e.getID() == MouseEvent.MOUSE_PRESSED) {
      if (myPopup == null) {
        togglePopup();
      }
    }
  }
 else {
    super.processMouseEvent(e);
  }
}
