{
  if (myIsDefault) {
    myChanges.add(change);
    return true;
  }
  for (  Change oldChange : myOutdatedChanges) {
    if (changesEqual(oldChange,change)) {
      myChanges.add(change);
      return true;
    }
  }
  return false;
}
