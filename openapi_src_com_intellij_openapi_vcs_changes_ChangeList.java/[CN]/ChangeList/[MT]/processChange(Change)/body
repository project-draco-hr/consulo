{
  if (myIsDefault) {
    addChange(change);
    return true;
  }
  for (  Change oldChange : myOutdatedChanges) {
    if (changesEqual(oldChange,change)) {
      addChange(change);
      return true;
    }
  }
  return false;
}
