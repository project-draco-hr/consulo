{
  myTime+=statistics.getTime();
  myMemoryUsage+=statistics.getMemoryUsage();
  if (myIsEmpty)   myBeforeMemory=statistics.getBeforeMemory();
  myAfterMemory=statistics.getAfterMemory();
  myIsEmpty=false;
}
