{
  if (!file.exists()) {
    return;
  }
  if (!file.canWrite()) {
    fileReadOnlyHandler.setFileReadOnly(file,false);
  }
  if (!file.delete()) {
    throw new IOException("Could not delete file " + file);
  }
}
