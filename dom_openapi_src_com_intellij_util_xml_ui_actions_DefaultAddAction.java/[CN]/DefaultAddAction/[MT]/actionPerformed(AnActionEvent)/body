{
  final Runnable runnable=new Runnable(){
    public void run(){
      if (beforeAddition()) {
        final T result=performElementAddition();
        if (result != null) {
          afterAddition(result);
        }
      }
    }
  }
;
  runnable.run();
}
