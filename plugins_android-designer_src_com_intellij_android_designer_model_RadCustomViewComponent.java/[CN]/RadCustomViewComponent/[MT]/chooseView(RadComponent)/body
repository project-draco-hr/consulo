{
  ModuleProvider moduleProvider=rootComponent.getClientProperty(ModelParser.MODULE_KEY);
  ChooseClassDialog dialog=new ChooseClassDialog(moduleProvider.getModule(),"Views",false,"android.view.View");
  dialog.show();
  if (dialog.isOK()) {
    return dialog.getClassName();
  }
  return null;
}
