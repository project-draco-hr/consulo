{
  if (!lastHasEqualWords && !hasEqualWords)   return true;
  if (isEqualsIgnoreWhitespace(newBlock) && isEqualsIgnoreWhitespace(lastBlock))   return true;
  if (noWordsInside(lastBlock) || noWordsInside(newBlock))   return true;
  return false;
}
