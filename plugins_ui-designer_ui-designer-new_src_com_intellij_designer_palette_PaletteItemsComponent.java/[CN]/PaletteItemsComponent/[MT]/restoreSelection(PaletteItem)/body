{
  if (paletteItem == null) {
    clearSelection();
  }
 else {
    int index=myGroup.getItems().indexOf(paletteItem);
    if (index == -1) {
      clearSelection();
    }
 else {
      takeFocusFrom(index);
    }
  }
}
