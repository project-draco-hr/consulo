{
  w.lock();
  try {
    V prev=get(key);
    if (!Comparing.equal(oldValue,prev)) {
      return false;
    }
    if (newValue == null) {
      remove(key);
    }
 else {
      put(key,newValue);
    }
    return true;
  }
  finally {
    w.unlock();
  }
}
