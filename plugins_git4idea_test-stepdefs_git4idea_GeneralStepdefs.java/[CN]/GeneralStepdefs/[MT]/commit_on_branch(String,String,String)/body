{
  CommitDetails commit=CommitDetails.parse(hash,commitDetails);
  if (!branch.equals("master")) {
    checkout(branch);
  }
  commit.apply();
  if (!branch.equals("master")) {
    checkout("master");
  }
}
