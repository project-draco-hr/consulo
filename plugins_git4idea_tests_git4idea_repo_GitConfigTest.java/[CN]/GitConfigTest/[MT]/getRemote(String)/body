{
  String[] remoteInfo=remoteString.split(" ");
  return new GitRemote(getOrEmpty(remoteInfo,0),Collections.singletonList(getOrEmpty(remoteInfo,1)),Collections.singletonList(getOrEmpty(remoteInfo,2)),getOrEmpty(remoteInfo,3),getOrEmpty(remoteInfo,4));
}
