{
  String[] remoteInfo=remoteString.split(" ");
  return new GitRemote(getOrEmpty(remoteInfo,0),Collections.singletonList(getOrEmpty(remoteInfo,1)),Collections.singletonList(getOrEmpty(remoteInfo,2)),Collections.singletonList(getOrEmpty(remoteInfo,3)),Collections.singletonList(getOrEmpty(remoteInfo,4)));
}
