{
  GitConfig config=GitConfig.read(new GitTestPlatformFacade(),configFile);
  GitTestUtil.assertEqualCollections(config.parseTrackInfos(config.parseRemotes(),Collections.<GitBranch>emptyList(),Collections.<GitBranch>emptyList()),readBranchResults(resultFile));
}
