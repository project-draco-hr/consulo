{
  GitConfig config=GitConfig.read(new GitTestPlatformFacade(),configFile);
  GitTestUtil.assertEqualCollections(config.parseTrackInfos(Collections.<GitLocalBranch>emptyList(),Collections.<GitRemoteBranch>emptyList()),readBranchResults(resultFile));
}
