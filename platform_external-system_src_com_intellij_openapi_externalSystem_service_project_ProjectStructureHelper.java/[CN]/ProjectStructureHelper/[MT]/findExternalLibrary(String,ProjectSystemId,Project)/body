{
  final ExternalProject project=myChangesModel.getExternalProject(externalSystemId,ideProject);
  if (project == null) {
    return null;
  }
  for (  ExternalLibrary library : project.getLibraries()) {
    if (libraryName.equals(library.getName())) {
      return library;
    }
  }
  return null;
}
