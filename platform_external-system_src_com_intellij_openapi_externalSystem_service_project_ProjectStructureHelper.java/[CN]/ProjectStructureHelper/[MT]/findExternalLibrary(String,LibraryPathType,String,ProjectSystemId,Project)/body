{
  ExternalLibrary library=findExternalLibrary(libraryName,externalSystemId,ideProject);
  if (library == null) {
    return null;
  }
  return library.getPaths(jarType).contains(jarPath) ? library : null;
}
