{
  final ExternalModule module=findExternalModule(id.getModuleName(),externalSystemId,ideProject);
  if (module == null) {
    return null;
  }
  for (  ExternalContentRoot root : module.getContentRoots()) {
    if (id.getRootPath().equals(root.getRootPath())) {
      return root;
    }
  }
  return null;
}
