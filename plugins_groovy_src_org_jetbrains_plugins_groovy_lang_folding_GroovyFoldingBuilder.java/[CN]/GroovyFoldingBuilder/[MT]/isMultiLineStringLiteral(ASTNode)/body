{
  return (TokenSets.STRING_LITERAL_SET.contains(node.getElementType()) || node.getElementType().equals(GSTRING) || node.getElementType().equals(REGEX)) && isMultiline(node.getPsi()) && isWellEndedString(node.getPsi());
}
