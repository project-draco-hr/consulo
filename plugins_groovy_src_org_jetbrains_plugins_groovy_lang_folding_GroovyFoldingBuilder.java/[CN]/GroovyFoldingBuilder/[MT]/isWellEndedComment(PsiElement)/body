{
  if (element instanceof PsiComment) {
    PsiComment comment=(PsiComment)element;
    ASTNode node=comment.getNode();
    if (node != null && node.getElementType() == GroovyTokenTypes.mML_COMMENT && node.getText().endsWith("*/"))     return true;
  }
  return true;
}
