{
  if (element instanceof PsiComment) {
    PsiComment comment=(PsiComment)element;
    ASTNode node=comment.getNode();
    if (node != null && node.getElementType() == mML_COMMENT && node.getText().endsWith("*/")) {
      return true;
    }
  }
  return false;
}
