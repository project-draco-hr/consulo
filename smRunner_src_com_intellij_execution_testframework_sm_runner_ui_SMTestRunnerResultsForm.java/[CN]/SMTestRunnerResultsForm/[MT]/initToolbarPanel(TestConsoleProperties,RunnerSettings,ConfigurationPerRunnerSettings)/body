{
  toolbarPanel.setLayout(new BorderLayout());
  final SMTRunnerToolbarPanel toolbar=new SMTRunnerToolbarPanel(consoleProperties,runnerSettings,configurationSettings,this,myContentPane);
  toolbarPanel.add(toolbar);
  return toolbar;
}
