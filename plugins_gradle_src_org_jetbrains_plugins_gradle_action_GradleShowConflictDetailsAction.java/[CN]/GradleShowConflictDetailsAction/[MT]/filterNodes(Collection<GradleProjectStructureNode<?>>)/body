{
  if (nodes.size() != 1) {
    nodes.clear();
    return;
  }
  final GradleProjectStructureNode<?> node=nodes.iterator().next();
  if (node.getConflictChanges().isEmpty()) {
    nodes.clear();
  }
}
