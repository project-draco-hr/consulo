{
  PsiElement parent=place;
  while (true) {
    if (parent instanceof PsiMember && !(parent instanceof PsiTypeParameter))     return (PsiMember)parent;
    if (parent instanceof PsiFile)     return null;
    parent=parent.getParent();
  }
}
