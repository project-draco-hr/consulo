{
  if (!firstBranch) {
    ifStatementString.append("else ");
  }
  dumpCaseValues(expressionText,caseValues,useEquals,ifStatementString);
  dumpBody(bodyStatements,breakLabel,variables,renameBreaks,ifStatementString);
}
