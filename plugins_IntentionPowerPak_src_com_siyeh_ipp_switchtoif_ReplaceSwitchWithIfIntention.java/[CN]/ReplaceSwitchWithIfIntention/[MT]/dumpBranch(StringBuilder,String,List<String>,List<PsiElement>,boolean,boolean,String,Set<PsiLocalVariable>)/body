{
  if (!firstBranch) {
    ifStatementString.append("else ");
  }
  dumpCaseValues(ifStatementString,expressionText,caseValues);
  dumpBody(ifStatementString,bodyStatements,renameBreaks,breakLabel,variables);
}
