{
  myProject=project;
  myManager=manager instanceof PsiAwareFileEditorManagerImpl ? (PsiAwareFileEditorManagerImpl)manager : null;
  myAutoTestAlarm=new Alarm(Alarm.ThreadToUse.SWING_THREAD,project);
  myRunTestsRunnable=new Runnable(){
    public void run(){
      runAutoTests();
    }
  }
;
  myListener=new MyDocumentAdapter();
}
