{
  myStorage=new Storage(getStorageDir());
  myVcs=new ThreadSafeLocalVcs(new LocalVcs(myStorage){
    @Override protected long getPurgingPeriod(){
      return LocalHistoryComponent.this.getPurgingPeriod();
    }
  }
);
}
