{
switch (fontType) {
case Font.PLAIN:
    return myPlainCache.charWidth(c);
case Font.BOLD:
  return myBoldCache.charWidth(c);
case Font.ITALIC:
return myItalicCache.charWidth(c);
case Font.BOLD | Font.ITALIC:
return myBoldItalicCache.charWidth(c);
}
return 0;
}
