{
  if (e.getID() != KeyEvent.KEY_TYPED)   return false;
  char c=e.getKeyChar();
  if (isReallyTypedEvent(e) && c >= 0x20 && c != 0x7F) {
    processKeyTyped(c);
    return true;
  }
 else {
    return false;
  }
}
