{
  this.data[myCount]=data;
  this.x[myCount]=x;
  this.y[myCount]=y;
  this.color[myCount]=color;
  myCount++;
  if (myCount >= CACHED_CHARS_BUFFER_SIZE || SystemInfo.isMac) {
    flushContent(g);
  }
}
