{
  for (  Object eachKey : myPools.keySet()) {
    LinkedList<MavenEmbedderWrapper> eachPool=myPools.get(eachKey);
    if (eachPool == null)     continue;
    for (    MavenEmbedderWrapper each : eachPool) {
      each.release();
    }
    eachPool.clear();
  }
  myPools.clear();
}
