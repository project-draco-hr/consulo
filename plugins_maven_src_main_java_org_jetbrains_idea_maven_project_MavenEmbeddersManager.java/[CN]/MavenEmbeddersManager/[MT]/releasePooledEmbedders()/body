{
  for (  LinkedList<MavenEmbedderWrapper> eachPool : myPools.values()) {
    for (    MavenEmbedderWrapper each : eachPool) {
      each.release();
    }
    eachPool.clear();
  }
  myPools.clear();
}
