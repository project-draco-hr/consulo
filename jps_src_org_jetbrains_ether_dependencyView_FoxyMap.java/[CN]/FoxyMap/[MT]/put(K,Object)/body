{
  final Object c=get(key);
  if (c == null) {
    map.put(key,value);
  }
 else {
    if (c instanceof Collection) {
      ((Collection)c).add(value);
    }
 else {
      final List d=new LinkedList();
      d.add(c);
      d.add(value);
      map.put(key,d);
    }
  }
  return c;
}
