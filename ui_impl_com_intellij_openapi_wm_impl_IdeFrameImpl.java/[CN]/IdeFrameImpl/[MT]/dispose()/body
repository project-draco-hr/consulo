{
  if (myRootPane != null) {
    final StatusBarImpl statusBar=(StatusBarImpl)myRootPane.getStatusBar();
    if (statusBar != null) {
      statusBar.disposeListeners();
    }
    myRootPane=null;
  }
  super.dispose();
}
