{
  if (location == null) {
    return null;
  }
  final String name=JavaCompletionUtil.getLookupObjectName(element.getObject());
  return JavaCompletionUtil.getNameEndMatchingDegree(name,JavaCompletionUtil.EXPECTED_TYPES.getValue(location),element.getPrefixMatcher().getPrefix());
}
