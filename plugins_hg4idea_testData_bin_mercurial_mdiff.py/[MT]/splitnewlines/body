def splitnewlines(text):
    'like str.splitlines, but only split on newlines.'
    lines = [(l + '\n') for l in text.split('\n')]
    if lines:
        if (lines[(-1)] == '\n'):
            lines.pop()
        else:
            lines[(-1)] = lines[(-1)][:(-1)]
    return lines
