def patchtext(bin):
    pos = 0
    t = []
    while (pos < len(bin)):
        (p1, p2, l) = struct.unpack('>lll', bin[pos:(pos + 12)])
        pos += 12
        t.append(bin[pos:(pos + l)])
        pos += l
    return ''.join(t)
