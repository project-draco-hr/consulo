{
  final Revision cvsRevision=revision.getRevision();
  final CvsChangeList version=findOrCreateVersionFor(cvsRevision.getMessage(),revision.getTime(),cvsRevision.getAuthor(),revision.getBranch(),revision.getFile());
  version.addFileRevision(revision);
  return version;
}
