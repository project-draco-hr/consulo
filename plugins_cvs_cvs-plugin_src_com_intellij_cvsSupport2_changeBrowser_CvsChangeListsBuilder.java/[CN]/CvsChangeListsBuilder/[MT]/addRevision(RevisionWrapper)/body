{
  final Revision cvsRevision=revision.getRevision();
  CvsChangeList version=findOrCreateVersionFor(cvsRevision.getMessage(),revision.getTime(),cvsRevision.getAuthor(),revision.getBranch(),revision.getFile());
  version.addFileRevision(revision);
  return version;
}
