{
  final SMTestProxy testProxy=myRunningTestsFullNameToProxy.get(fullTestName);
  if (testProxy == null) {
    LOG.error("Cant find running test for [" + fullTestName + "]. Current running tests: {"+ dumpRunningTestsNames()+ "}");
  }
  return testProxy;
}
