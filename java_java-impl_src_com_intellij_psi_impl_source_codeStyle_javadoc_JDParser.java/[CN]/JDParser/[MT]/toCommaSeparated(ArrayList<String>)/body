{
  if (l == null || l.size() == 0)   return null;
  StringBuffer sb=new StringBuffer();
  for (int i=0; i < l.size(); i++) {
    String s=l.get(i);
    if (i != 0) {
      sb.append(", ");
    }
    sb.append(s);
  }
  return sb.toString();
}
