{
  if (l == null || l.size() == 0)   return null;
  StringBuffer sb=new StringBuffer();
  for (  Object aL : l) {
    String s=(String)aL;
    if (sb.length() > 0) {
      sb.append(lineSeparator);
    }
    sb.append(s);
  }
  return sb.toString();
}
