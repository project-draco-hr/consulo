{
  try {
    return isValidType(context.getManager().getElementFactory().createTypeFromText(type.getCanonicalText(),context),context);
  }
 catch (  IncorrectOperationException e) {
    LOG.error("Incorrect operation during factory.createTypeFromText");
  }
  return false;
}
