{
  if (myInferenceVariables.containsKey(param))   return;
  final InferenceVariable variable=new InferenceVariable(param);
  variable.setCaptured(true);
  myInferenceVariables.put(param,variable);
}
