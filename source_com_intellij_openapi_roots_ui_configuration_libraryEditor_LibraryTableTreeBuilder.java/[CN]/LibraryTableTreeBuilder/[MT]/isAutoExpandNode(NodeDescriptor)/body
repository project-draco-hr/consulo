{
  final Object element=nodeDescriptor.getElement();
  final Object rootElement=getTreeStructure().getRootElement();
  return rootElement.equals(element) || element instanceof ClassesElement || element instanceof SourcesElement|| element instanceof JavadocElement|| element instanceof AnnotationElement;
}
