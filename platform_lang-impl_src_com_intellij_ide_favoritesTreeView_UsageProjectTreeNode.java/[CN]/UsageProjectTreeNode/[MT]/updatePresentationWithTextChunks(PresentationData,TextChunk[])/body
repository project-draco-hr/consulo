{
  for (  TextChunk chunk : text) {
    final TextAttributes attributes=chunk.getAttributes();
    presentation.addText(chunk.getText(),new SimpleTextAttributes(attributes.getBackgroundColor(),attributes.getForegroundColor(),attributes.getEffectColor(),attributes.getFontType()));
  }
}
