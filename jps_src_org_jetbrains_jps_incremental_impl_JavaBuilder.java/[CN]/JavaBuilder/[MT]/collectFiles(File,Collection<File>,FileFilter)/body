{
  final File[] files=root.listFiles();
  if (files != null) {
    for (    File file : files) {
      if (file.isDirectory()) {
        collectFiles(file,container,filter);
      }
 else {
        if (filter.accept(file)) {
          container.add(file);
        }
      }
    }
  }
}
