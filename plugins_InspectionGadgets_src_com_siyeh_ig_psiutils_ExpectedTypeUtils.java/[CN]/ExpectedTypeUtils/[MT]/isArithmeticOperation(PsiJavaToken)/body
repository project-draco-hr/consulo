{
  IElementType sign=javaToken.getTokenType();
  return JavaTokenType.PLUS.equals(sign) || JavaTokenType.MINUS.equals(sign) || JavaTokenType.ASTERISK.equals(sign)|| JavaTokenType.DIV.equals(sign)|| JavaTokenType.PERC.equals(sign);
}
