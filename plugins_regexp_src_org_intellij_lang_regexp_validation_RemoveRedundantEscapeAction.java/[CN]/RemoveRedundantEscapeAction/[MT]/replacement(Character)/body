{
  final PsiElement context=myChar.getContainingFile().getContext();
  return RegExpElementImpl.isLiteralExpression(context) ? StringUtil.escapeStringCharacters(v.toString()) : (context instanceof XmlElement ? XmlStringUtil.escapeString(v.toString()) : v.toString());
}
