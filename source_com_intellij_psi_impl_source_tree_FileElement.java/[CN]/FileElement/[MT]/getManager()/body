{
  final PsiManagerEx manager=getUserData(MANAGER_KEY);
  if (manager == null) {
    if (parent != null)     return parent.getManager();
 else     return (PsiManagerEx)SourceTreeToPsiMap.treeElementToPsi(this).getManager();
  }
 else {
    return manager;
  }
}
