{
  final Sdk sdk=SdkTable.getInstance().createSdk(name,sdkType);
  ApplicationManager.getApplication().runWriteAction(new Runnable(){
    public void run(){
      SdkTable.getInstance().addSdk(sdk);
    }
  }
);
  mySdks.add(sdk);
  return sdk;
}
