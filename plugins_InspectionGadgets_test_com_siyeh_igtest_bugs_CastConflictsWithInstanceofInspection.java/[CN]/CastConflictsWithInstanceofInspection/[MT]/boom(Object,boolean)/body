{
  if (feed instanceof JComponent) {
    if (!(feed instanceof JButton) || ((JButton)feed).isDefaultButton()) {
      return feed;
    }
  }
 else {
    String string=(String)feed;
  }
  return null;
}
