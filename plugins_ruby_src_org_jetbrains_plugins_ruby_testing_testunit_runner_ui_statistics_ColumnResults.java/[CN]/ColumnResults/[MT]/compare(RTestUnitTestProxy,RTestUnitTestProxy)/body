{
  if (proxy1.isSuite()) {
    if (proxy2.isSuite()) {
      return compareSuites(proxy1,proxy2);
    }
 else {
      return +1;
    }
  }
 else {
    if (proxy2.isSuite()) {
      return -1;
    }
 else {
      return compareTests(proxy1,proxy2);
    }
  }
}
