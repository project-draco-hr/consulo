{
  expect(subject.load(2)).andReturn("content".getBytes()).times(1);
  replay(subject);
  assertArrayEquals("content".getBytes(),s.load(2));
  assertArrayEquals("content".getBytes(),s.load(2));
  verify(subject);
}
