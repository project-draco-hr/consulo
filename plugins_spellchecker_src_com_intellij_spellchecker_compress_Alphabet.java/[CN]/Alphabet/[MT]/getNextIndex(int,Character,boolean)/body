{
  for (int i=startFrom; i <= lastIndexUsed; i++) {
    if (letters[i] != null && letters[i].equals(letter)) {
      return i;
    }
  }
  if (!forceAdd) {
    return -1;
  }
  return add(letter);
}
