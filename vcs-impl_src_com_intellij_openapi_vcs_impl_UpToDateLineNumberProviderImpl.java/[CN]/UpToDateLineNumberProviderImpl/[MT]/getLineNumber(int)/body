{
  LineStatusTracker tracker=LineStatusTrackerManager.getInstance(myProject).getLineStatusTracker(myDocument);
  if (tracker == null) {
    tracker=LineStatusTrackerManager.getInstance(myProject).setUpToDateContent(myDocument,fixLatestLineSeparator(myDocument,myUpToDateContent));
  }
  return calcLineNumber(tracker,currentNumber);
}
