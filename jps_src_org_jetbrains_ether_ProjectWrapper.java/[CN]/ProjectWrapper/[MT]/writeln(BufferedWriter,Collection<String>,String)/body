{
  writeln(w,Integer.toString(c.size()));
  if (c instanceof List) {
    for (    String e : c) {
      writeln(w,e);
    }
  }
 else {
    final List<String> sorted=sort(c);
    for (    String e : sorted) {
      writeln(w,e);
    }
  }
}
