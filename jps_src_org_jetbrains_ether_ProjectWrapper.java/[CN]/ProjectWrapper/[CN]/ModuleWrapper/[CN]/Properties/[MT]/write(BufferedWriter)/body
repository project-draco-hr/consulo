{
  writeln(w,"Roots:");
  writeln(w,myRoots,null);
  writeln(w,"Sources:");
  writeln(w,mySources);
  writeln(w,"Output:");
  writeln(w,myOutput == null ? "" : myOutput);
  writeln(w,"Outputs:");
  writeln(w,myOutputs);
}
