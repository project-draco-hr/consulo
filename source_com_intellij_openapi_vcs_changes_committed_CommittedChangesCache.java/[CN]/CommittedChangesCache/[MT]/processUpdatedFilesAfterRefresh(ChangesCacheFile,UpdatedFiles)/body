{
  refreshCacheAsync(cache,false,new Runnable(){
    public void run(){
      try {
        cache.processUpdatedFiles(updatedFiles);
      }
 catch (      IOException e) {
        LOG.error(e);
      }
    }
  }
);
}
