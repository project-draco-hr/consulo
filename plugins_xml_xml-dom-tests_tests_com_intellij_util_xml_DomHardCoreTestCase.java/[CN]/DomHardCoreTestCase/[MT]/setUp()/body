{
  super.setUp();
  myCallRegistry=new CallRegistry<DomEvent>();
  getDomManager().addDomEventListener(new DomEventListener(){
    @Override public void eventOccured(    DomEvent event){
      myCallRegistry.putActual(event);
    }
  }
,myProject);
}
