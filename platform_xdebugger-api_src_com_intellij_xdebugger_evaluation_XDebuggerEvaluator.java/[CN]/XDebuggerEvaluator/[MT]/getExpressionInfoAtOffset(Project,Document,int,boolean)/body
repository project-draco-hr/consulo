{
  @SuppressWarnings("deprecation") Pair<TextRange,String> result=getExpressionAtOffset(project,document,offset,sideEffectsAllowed);
  return result == null ? null : new ExpressionInfo(result.first,result.second);
}
