{
  if (shortcut instanceof KeyboardShortcut) {
    KeyStroke keyStroke=((KeyboardShortcut)shortcut).getFirstKeyStroke();
    return keyStroke != null && keyStroke.getKeyCode() == KeyEvent.VK_ENTER && (keyStroke.getModifiers() & InputEvent.CTRL_MASK) != 0;
  }
  return false;
}
