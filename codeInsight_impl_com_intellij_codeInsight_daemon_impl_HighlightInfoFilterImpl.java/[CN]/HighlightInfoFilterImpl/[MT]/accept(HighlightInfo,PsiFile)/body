{
  TextAttributes attributes=info.getTextAttributes();
  return attributes != null && !(attributes.isEmpty() && info.getSeverity() == HighlightSeverity.INFORMATION && info.getGutterIconRenderer() == null);
}
