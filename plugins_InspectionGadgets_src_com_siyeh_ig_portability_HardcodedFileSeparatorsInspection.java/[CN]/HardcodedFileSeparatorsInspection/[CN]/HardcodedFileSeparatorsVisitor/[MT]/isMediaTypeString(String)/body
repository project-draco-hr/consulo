{
  if (string.indexOf((int)'\\') != -1 || string.indexOf((int)'/') == -1) {
    return false;
  }
  if (m_recognizeExampleMediaType && string.matches(EXAMPLE_MIME_MEDIA_TYPE_PATTERN)) {
    return true;
  }
  return mimeTypes.contains(string);
}
