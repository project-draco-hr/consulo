{
  doAddLogConsole(new LogConsoleBase(myProject,reader,name,false,new DefaultLogFilterModel(myProject),mySearchScope){
    @Override public boolean isActive(){
      return isConsoleActive(id);
    }
  }
,id,icon);
}
