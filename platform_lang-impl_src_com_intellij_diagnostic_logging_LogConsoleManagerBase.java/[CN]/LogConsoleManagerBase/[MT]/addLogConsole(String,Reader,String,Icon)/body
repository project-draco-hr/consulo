{
  doAddLogConsole(new LogConsoleBase(myProject,reader,name,false,new DefaultLogFilterModel(myProject)){
    @Override public boolean isActive(){
      return isConsoleActive(id);
    }
  }
,id,icon);
}
