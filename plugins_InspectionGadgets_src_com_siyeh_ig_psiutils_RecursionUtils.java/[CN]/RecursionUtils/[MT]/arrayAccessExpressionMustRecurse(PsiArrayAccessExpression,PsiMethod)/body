{
  final PsiExpression arrayExp=expression.getArrayExpression();
  final PsiExpression indexExp=expression.getIndexExpression();
  return expressionMustRecurse(arrayExp,method) || expressionMustRecurse(indexExp,method);
}
