{
  try {
    if (myDirectoryFlag) {
      return myNameTrie.getStringByIndex(myNameIndex);
    }
 else {
      return myNameTrie.getReversedStringByIndex(myNameIndex);
    }
  }
 catch (  IOException e) {
    LOG.error(e);
  }
  return "";
}
