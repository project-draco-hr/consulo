{
  if (!isCharsetSet()) {
    try {
      LoadTextUtil.detectCharsetAndSkipBOM(this,contentsToByteArray());
    }
 catch (    IOException e) {
      LOG.error(e);
    }
  }
  return super.getCharset();
}
