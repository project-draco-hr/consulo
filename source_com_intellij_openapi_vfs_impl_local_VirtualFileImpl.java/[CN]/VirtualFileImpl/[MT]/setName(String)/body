{
  try {
    if (myDirectoryFlag) {
      myNameIndex=myNameTrie.addString(name);
    }
 else {
      myNameIndex=myNameTrie.addReversedString(name);
    }
  }
 catch (  IOException e) {
    LOG.error(e);
  }
}
