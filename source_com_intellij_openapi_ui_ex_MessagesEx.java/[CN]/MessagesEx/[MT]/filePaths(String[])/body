{
  StringBuffer result=new StringBuffer();
  for (int i=0; i < files.length; i++) {
    String file=files[i];
    if (i > 0) {
      result.append(",");
      result.append("\n");
    }
    result.append(file);
  }
  return result.toString();
}
