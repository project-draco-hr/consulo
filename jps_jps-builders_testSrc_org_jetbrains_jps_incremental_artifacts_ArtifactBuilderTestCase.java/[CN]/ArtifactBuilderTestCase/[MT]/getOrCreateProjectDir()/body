{
  if (myProjectDir == null) {
    try {
      myProjectDir=FileUtil.createTempDirectory("prj",null);
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  return myProjectDir;
}
