{
  if (!(otherState instanceof MyValidityState)) {
    return false;
  }
  final MyValidityState st=(MyValidityState)otherState;
  return myTimestamp == st.myTimestamp && myRawDirTimestamp == st.myRawDirTimestamp && myFileExists == st.myFileExists;
}
