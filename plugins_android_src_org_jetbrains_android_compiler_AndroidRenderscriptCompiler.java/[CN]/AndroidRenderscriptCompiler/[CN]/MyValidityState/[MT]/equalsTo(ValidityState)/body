{
  if (!(otherState instanceof MyValidityState)) {
    return false;
  }
  final MyValidityState st=(MyValidityState)otherState;
  return myTimestamp == st.myTimestamp && myFileExists == st.myFileExists;
}
