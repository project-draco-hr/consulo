{
  if (word == null || dictionaries == null) {
    return false;
  }
  for (  Dictionary dictionary : dictionaries) {
    if (dictionary.contains(word)) {
      return true;
    }
  }
  return false;
}
