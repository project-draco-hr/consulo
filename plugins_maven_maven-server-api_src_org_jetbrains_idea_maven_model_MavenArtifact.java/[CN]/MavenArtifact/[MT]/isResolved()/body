{
  if (myResolved && !myStubbed) {
    long currentTime=System.currentTimeMillis();
    if (myLastFileCheckTimeStamp + 2000 < currentTime) {
      if (!myFile.exists()) {
        return false;
      }
      myLastFileCheckTimeStamp=currentTime;
    }
    return true;
  }
  return false;
}
