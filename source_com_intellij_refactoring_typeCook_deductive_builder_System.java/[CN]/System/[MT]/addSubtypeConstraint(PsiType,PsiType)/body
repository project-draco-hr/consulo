{
  if ((Util.bindsTypeVariables(left) || Util.bindsTypeVariables(right)) && !(left instanceof PsiPrimitiveType || right instanceof PsiPrimitiveType)) {
    final Subtype c=new Subtype(left,right);
    if (!myConstraints.contains(c)) {
      myConstraints.add(c);
    }
  }
}
