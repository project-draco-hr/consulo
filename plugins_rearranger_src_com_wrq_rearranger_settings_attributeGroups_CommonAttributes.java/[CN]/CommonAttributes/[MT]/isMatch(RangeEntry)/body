{
  return myProtectionLevelAttributes.isMatch(rangeEntry.getModifiers()) && myStaticAttribute.isMatch(rangeEntry.getModifiers()) && myFinalAttribute.isMatch(rangeEntry.getModifiers())&& myNameAttribute.isMatch(rangeEntry.getName());
}
