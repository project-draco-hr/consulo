{
  final boolean result=plAttr.isMatch(rangeEntry.getModifiers()) && stAttr.isMatch(rangeEntry.getModifiers()) && fAttr.isMatch(rangeEntry.getModifiers())&& nameAttr.isMatch(rangeEntry.getName());
  return result;
}
