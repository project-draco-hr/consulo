{
  int l1=firstPrefix.length();
  int l2=secondPrefix.length();
  int l3=thirdPrefix.length();
  if (testee.length() < l1 + l2 + l3)   return false;
  return testee.startsWith(firstPrefix) && testee.regionMatches(l1,secondPrefix,0,l2) && testee.regionMatches(l1 + l2,thirdPrefix,0,l3);
}
