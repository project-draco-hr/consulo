{
  final StringBuilder buf=StringBuilderSpinAllocator.alloc();
  try {
    buf.append(str);
    escapeSlashes(buf);
    return buf.toString();
  }
  finally {
    StringBuilderSpinAllocator.dispose(buf);
  }
}
