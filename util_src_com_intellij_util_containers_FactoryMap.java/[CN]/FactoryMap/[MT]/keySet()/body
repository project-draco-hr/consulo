{
  final Set<T> ts=myMap.keySet();
  if (ts.contains(NULL)) {
    final HashSet<T> hashSet=new HashSet<T>(ts);
    hashSet.remove(NULL);
    hashSet.add(null);
    return hashSet;
  }
  return ts;
}
