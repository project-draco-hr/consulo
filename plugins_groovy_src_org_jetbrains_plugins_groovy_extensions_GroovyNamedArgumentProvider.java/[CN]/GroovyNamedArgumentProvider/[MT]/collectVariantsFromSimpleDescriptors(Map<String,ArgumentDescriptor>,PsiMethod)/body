{
  PsiClass containingClass=method.getContainingClass();
  if (containingClass != null) {
    Map<String,ArgumentDescriptor> map=SimpleGroovyNamedArgumentProvider.getMethodMap(containingClass.getQualifiedName(),method.getName());
    if (map != null) {
      res.putAll(map);
    }
  }
}
