{
  for (  String word : filterWords) {
    if (changeList.getComment().toLowerCase().indexOf(word) >= 0 || changeList.getCommitterName().toLowerCase().indexOf(word) >= 0 || Long.toString(changeList.getNumber()).indexOf(word) >= 0) {
      return true;
    }
  }
  return false;
}
