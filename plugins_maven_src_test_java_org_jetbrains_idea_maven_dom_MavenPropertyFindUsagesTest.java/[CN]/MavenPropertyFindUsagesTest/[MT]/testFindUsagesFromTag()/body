{
  createModulePom("module1","<groupId>test</groupId>" + "<artifactId>module1</artifactId>" + "<<caret>version>1</version>"+ "<name>${project.version}</name>"+ "<description>${version}</description>");
  assertSearchResult(myModule1Pom,findTag(myModule1Pom,"project.name"),findTag(myModule1Pom,"project.description"));
}
