{
  if (!myVeryFirstProjectOpening || ApplicationManagerEx.getApplicationEx().isInternal() || !GeneralSettings.getInstance().isReopenLastProject()) {
    return;
  }
  myVeryFirstProjectOpening=false;
  if (checkNeeded()) {
    CheckForUpdateAction.actionPerformed(true);
  }
}
