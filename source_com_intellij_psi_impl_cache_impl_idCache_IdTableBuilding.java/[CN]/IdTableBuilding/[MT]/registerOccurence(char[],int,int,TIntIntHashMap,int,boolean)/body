{
  final int hashCode=(caseInsensitive) ? StringUtil.stringHashCodeInsensitive(chars,start,end - start) : StringUtil.stringHashCode(chars,start,end - start);
  IdCacheUtil.addOccurrence(table,hashCode,occurrenceMask);
}
