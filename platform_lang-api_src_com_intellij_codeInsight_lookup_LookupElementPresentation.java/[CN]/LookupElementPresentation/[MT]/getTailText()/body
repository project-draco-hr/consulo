{
  if (myTail == null)   return null;
  return StringUtil.join(myTail,new Function<TextFragment,String>(){
    @Override public String fun(    TextFragment fragment){
      return fragment.text;
    }
  }
,"");
}
