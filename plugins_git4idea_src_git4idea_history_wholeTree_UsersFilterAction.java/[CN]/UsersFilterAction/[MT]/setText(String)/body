{
  final String[] pieces=text.split(",");
  if (pieces.length == 0) {
    myLabel.setText(ALL);
  }
 else   if (pieces.length == 1) {
    myLabel.setText(pieces[0].trim());
  }
 else {
    myLabel.setText(pieces[0].trim() + "+");
  }
}
