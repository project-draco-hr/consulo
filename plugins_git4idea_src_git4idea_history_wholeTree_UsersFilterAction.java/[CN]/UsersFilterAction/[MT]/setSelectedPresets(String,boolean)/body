{
  myCurrentText=selected;
  if (selected == null) {
    myLabel.setText(ALL);
  }
 else   if (meSelected) {
    myLabel.setText(getMeText(selected));
    myPanel.setToolTipText(USER + " " + selected);
  }
 else {
    setText(selected);
    myPanel.setToolTipText(USER + " " + selected);
  }
}
