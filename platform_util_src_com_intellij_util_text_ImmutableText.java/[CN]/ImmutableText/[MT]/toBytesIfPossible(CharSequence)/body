{
  byte[] bytes=new byte[seq.length()];
  for (int i=0; i < bytes.length; i++) {
    char c=seq.charAt(i);
    if ((c & 0xff00) != 0) {
      return null;
    }
    bytes[i]=(byte)c;
  }
  return bytes;
}
