{
  ImmutableText P=this._head;
  if (P._data != null)   return this;
  ImmutableText A=P._head;
  ImmutableText B=P._tail;
  ImmutableText C=this._tail;
  return new ImmutableText(A,new ImmutableText(B,C));
}
