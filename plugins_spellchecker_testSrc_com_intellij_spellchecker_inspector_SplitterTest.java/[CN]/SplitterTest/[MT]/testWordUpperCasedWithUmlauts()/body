{
  String text="rechtsb??ndig".toUpperCase();
  List<CheckArea> checkAreas=TextSplitter.splitText(text);
  correctListToCheck(checkAreas,text,new String[]{"rechtsb??ndig".toUpperCase()});
  correctIgnored(checkAreas,text,new String[]{});
}
