{
  String text="#AABBFF";
  List<CheckArea> checkAreas=SplitterFactory.getAttributeValueSplitter().split(text);
  correctListToCheck(checkAreas,text,new String[]{});
  correctIgnored(checkAreas,text,new String[]{});
}
