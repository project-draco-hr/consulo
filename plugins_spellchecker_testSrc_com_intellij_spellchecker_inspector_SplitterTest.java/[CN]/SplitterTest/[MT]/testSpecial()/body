{
  String text="test &nbsp; test";
  List<CheckArea> checkAreas=SplitterFactory.getPlainTextSplitter().split(text);
  correctListToCheck(checkAreas,text,new String[]{"test","test"});
  correctIgnored(checkAreas,text,new String[]{});
}
