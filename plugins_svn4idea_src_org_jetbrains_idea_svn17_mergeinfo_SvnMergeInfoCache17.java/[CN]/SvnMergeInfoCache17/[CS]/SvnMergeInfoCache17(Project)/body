{
  myProject=project;
  myState=new MyState();
  final SvnVcs17 vcs=SvnVcs17.getInstance(myProject);
  myClient=vcs.createWCClient();
  myClient.setOptions(new DefaultSVNOptions(){
    @Override public byte[] getNativeEOL(){
      return new byte[]{'\n'};
    }
  }
);
}
