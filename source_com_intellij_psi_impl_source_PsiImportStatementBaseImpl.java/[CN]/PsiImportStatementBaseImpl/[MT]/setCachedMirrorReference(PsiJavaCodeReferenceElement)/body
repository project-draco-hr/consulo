{
  if (refElement == null) {
    myCachedMirrorReference=null;
  }
 else {
    myCachedMirrorReference=myManager.isBatchFilesProcessingMode() ? new PatchedWeakReference(refElement) : (Reference)new PatchedSoftReference(refElement);
  }
}
