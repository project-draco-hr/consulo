{
  for (  String[] stringArray : RegExpPropertyImpl.PROPERTY_NAMES) {
    result.addElement(TailTypeDecorator.createDecorator(createLookupElement(stringArray[0],null,emptyIcon),TailType.createSimpleTailType('}')));
  }
}
