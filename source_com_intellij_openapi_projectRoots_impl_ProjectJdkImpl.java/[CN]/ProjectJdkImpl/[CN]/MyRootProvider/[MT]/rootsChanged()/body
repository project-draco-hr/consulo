{
synchronized (this) {
    if (myListeners.size() == 0) {
      return;
    }
  }
  ApplicationManager.getApplication().runWriteAction(new Runnable(){
    public void run(){
      fireRootSetChanged();
    }
  }
);
}
