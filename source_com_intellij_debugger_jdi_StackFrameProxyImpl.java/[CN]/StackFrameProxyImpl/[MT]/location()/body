{
  checkValid();
  if (myLocation == null) {
    try {
      myLocation=getStackFrame().location();
    }
 catch (    InvalidStackFrameException e) {
      clearCaches();
      return location();
    }
  }
  return myLocation;
}
