{
  VirtualFile f;
  if (e.getParent() == null) {
    f=e.getFile();
  }
 else {
    f=new ReparentedVirtualFile(e.getParent(),e.getFile());
  }
  if (wasDeletedDuringRootsUpdate(f))   return;
  myFacade.delete(f);
}
