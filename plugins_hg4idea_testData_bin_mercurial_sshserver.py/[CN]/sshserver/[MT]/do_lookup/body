def do_lookup(self):
    (arg, key) = self.getarg()
    assert (arg == 'key')
    try:
        r = hex(self.repo.lookup(key))
        success = 1
    except Exception as inst:
        r = str(inst)
        success = 0
    self.respond(('%s %s\n' % (success, r)))
