def serve_one(self):
    cmd = self.fin.readline()[:(-1)]
    if cmd:
        impl = getattr(self, ('do_' + cmd), None)
        if impl:
            impl()
        else:
            self.respond('')
    return (cmd != '')
