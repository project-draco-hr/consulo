{
  try {
    Toolkit.getDefaultToolkit().getSystemClipboard().setContents(new TextTransferrable(myAllocation.getText(),myAllocation.getText()),null);
  }
 catch (  HeadlessException e1) {
    LOG.error(e1);
  }
}
