{
  if (myPopup == null) {
    myForcedSize=size;
  }
 else {
    if (adjustByContent) {
      if (myAdCmp != null) {
        size.height+=myAdCmp.getPreferredSize().height;
      }
    }
    updateMaskAndAlpha(setSize(myContent,size));
  }
}
