{
  super.paint(g);
  if (myResizable && myDrawMacCorner) {
    ourMacCorner.paintIcon(this,g,getX() + getWidth() - ourMacCorner.getIconWidth(),getY() + getHeight() - ourMacCorner.getIconHeight());
  }
}
