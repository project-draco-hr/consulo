{
  super.paint(g);
  if (myResizable && myDrawMacCorner) {
    AllIcons.General.MacCorner.paintIcon(this,g,getX() + getWidth() - AllIcons.General.MacCorner.getIconWidth(),getY() + getHeight() - AllIcons.General.MacCorner.getIconHeight());
  }
}
