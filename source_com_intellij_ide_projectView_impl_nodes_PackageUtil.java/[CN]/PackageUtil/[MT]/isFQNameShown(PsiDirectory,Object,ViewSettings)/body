{
  PsiPackage aPackage;
  return value != null && !(parentValue instanceof Project) && settings.isFlattenPackages() && !isSourceRoot(value) && (aPackage=JavaDirectoryService.getInstance().getPackage(value)) != null && aPackage.getQualifiedName().length() > 0;
}
