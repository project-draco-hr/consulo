{
  PsiPackage aPackage;
  return value != null && !(parentValue instanceof Project) && settings.isFlattenPackages() && !isSourceRoot(value) && (aPackage=value.getPackage()) != null && aPackage.getQualifiedName().length() > 0;
}
