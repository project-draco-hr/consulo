{
  PsiPackage aPackage;
  return value != null && !(parentValue instanceof Project) && settings.isFlattenPackages() && !ProjectRootsUtil.isSourceRoot(value) && (aPackage=JavaDirectoryService.getInstance().getPackage(value)) != null && aPackage.getQualifiedName().length() > 0;
}
