{
  if (testNGResults != null) {
synchronized (currentTestOutput) {
      if (!currentTestOutput.isEmpty()) {
        nonTestOutput.addAll(currentTestOutput);
        currentTestOutput.clear();
      }
    }
    testNGResults.testStarted(result);
  }
}
