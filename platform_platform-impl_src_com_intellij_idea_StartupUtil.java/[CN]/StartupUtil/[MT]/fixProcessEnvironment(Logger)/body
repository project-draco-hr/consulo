{
  if (!Main.isCommandLine()) {
    System.setProperty("__idea.mac.env.lock","unlocked");
  }
  boolean envReady=EnvironmentUtil.isEnvironmentReady();
  if (!envReady) {
    log.info("initializing environment");
  }
}
