{
  return with(new PatternCondition<String>(){
    public boolean accepts(    @NotNull final String s,    final MatchingContext matchingContext,    @NotNull final TraverseContext traverseContext){
      return s.length() > minLength;
    }
  }
);
}
