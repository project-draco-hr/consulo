{
  super.messageSent(message,byteMessage,error,tagged);
  final EditedFileInfo editedFileInfo=EditedFileInfo.createOn(message);
  if (editedFileInfo != null)   myEditFileInfos.add(editedFileInfo);
}
