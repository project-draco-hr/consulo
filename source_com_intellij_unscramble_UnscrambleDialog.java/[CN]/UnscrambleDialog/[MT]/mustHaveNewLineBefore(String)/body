{
  final int nonws=CharArrayUtil.shiftForward(line,0," \t");
  if (nonws < line.length()) {
    line=line.substring(nonws);
  }
  if (line.startsWith("at"))   return true;
  if (line.startsWith("Caused"))   return true;
  if (line.startsWith("- locked"))   return true;
  if (line.startsWith("- waiting"))   return true;
  if (line.startsWith("\""))   return true;
  return false;
}
