{
  final long commit=new SvnCommitRunner(myExecutablePath,myHandler,myAuthenticationCallback).commit(path,message,depth,noUnlock,keepChangelist,changelists,revpropTable);
  if (commit < 0) {
    throw new BindClientException("Wrong committed revision number: " + commit,null,-1);
  }
  return commit;
}
