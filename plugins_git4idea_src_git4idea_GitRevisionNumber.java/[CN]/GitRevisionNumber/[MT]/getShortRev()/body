{
  if (myRevisionStr.length() == 0)   return "";
  if (myRevisionStr.length() == 40)   return myRevisionStr.substring(0,8);
  if (myRevisionStr.length() > 40) {
    return myRevisionStr.substring(myRevisionStr.indexOf("[") + 1,8);
  }
  return myRevisionStr;
}
