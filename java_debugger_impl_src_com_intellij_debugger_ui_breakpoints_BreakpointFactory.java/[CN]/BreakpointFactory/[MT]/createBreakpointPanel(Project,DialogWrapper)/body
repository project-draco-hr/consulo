{
  BreakpointPanel panel=new BreakpointPanel(project,createBreakpointPropertiesPanel(project),createBreakpointPanelActions(project,parentDialog),getBreakpointCategory(),getDisplayName(),getHelpID());
  configureBreakpointPanel(panel);
  return panel;
}
