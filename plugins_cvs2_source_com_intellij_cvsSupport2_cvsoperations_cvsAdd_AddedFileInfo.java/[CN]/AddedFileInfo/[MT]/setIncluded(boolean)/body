{
  if (myIncluded == aBoolean)   return;
  myIncluded=aBoolean;
  if (!myIncluded) {
    excludeAllChildren();
  }
 else {
    encludeAllParents();
  }
  myExcludedObservable.setChanged();
  myExcludedObservable.notifyObservers();
}
