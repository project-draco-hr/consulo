{
  String rc=Messages.showInputDialog(myProject,VcsBundle.message("changes.dialog.newchangelist.text"),VcsBundle.message("changes.dialog.newchangelist.title"),Messages.getQuestionIcon());
  if (rc != null) {
    if (rc.length() == 0) {
      rc=getUniqueName();
    }
    addChangeList(rc);
  }
}
