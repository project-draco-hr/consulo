{
  if (ControlFlowUtil.isVariableAssignedInLoop(expression,resolved)) {
    String description=MessageFormat.format("Variable ''{0}'' might be assigned in loop",new Object[]{((PsiVariable)resolved).getName()});
    HighlightInfo highlightInfo=HighlightInfo.createHighlightInfo(HighlightInfoType.ERROR,expression,description);
    QuickFixAction.registerQuickFixAction(highlightInfo,new ModifierFix((PsiVariable)resolved,PsiModifier.FINAL,false));
    return highlightInfo;
  }
  return null;
}
