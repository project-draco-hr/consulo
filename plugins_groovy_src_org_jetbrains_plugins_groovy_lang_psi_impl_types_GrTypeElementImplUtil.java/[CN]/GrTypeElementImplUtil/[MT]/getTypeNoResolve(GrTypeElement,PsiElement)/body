{
  String text=typeElement.getText().trim();
  try {
    return JavaPsiFacade.getInstance(typeElement.getProject()).getElementFactory().createTypeFromText(text,context);
  }
 catch (  IncorrectOperationException e) {
    String s="Parent: " + DebugUtil.psiToString(typeElement.getParent(),false);
    s+="Context: " + DebugUtil.psiToString(context,false);
    LOG.error(s,e);
    return null;
  }
}
