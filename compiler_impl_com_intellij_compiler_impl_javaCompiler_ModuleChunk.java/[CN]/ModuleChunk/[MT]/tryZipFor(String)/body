{
  final File zip=CompilerPathsEx.getZippedOutputPath(myContext.getProject(),outputDir);
  if (zip.exists()) {
    try {
      myContext.commitZip(outputDir);
    }
 catch (    IOException e) {
      LOG.info(e);
    }
    return zip.getPath();
  }
  return outputDir;
}
