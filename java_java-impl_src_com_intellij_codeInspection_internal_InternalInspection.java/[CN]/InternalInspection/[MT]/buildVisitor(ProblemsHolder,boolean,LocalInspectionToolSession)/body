{
  if (JavaPsiFacade.getInstance(holder.getProject()).findClass(JBList.class.getName(),GlobalSearchScope.allScope(holder.getProject())) == null) {
    return new PsiElementVisitor(){
    }
;
  }
  return super.buildVisitor(holder,isOnTheFly,session);
}
