{
  final Semaphore semaphore=new Semaphore();
  semaphore.down();
  ourUpdateAlarm.execute(new Runnable(){
    @Override public void run(){
      semaphore.up();
    }
  }
);
  semaphore.waitFor();
}
