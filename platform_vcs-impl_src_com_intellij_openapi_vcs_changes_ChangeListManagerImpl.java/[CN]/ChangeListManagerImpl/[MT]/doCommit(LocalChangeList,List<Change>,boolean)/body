{
  return new CommitHelper(myProject,changeList,changes,changeList.getName(),changeList.getComment(),new ArrayList<CheckinHandler>(),false,synchronously,new NullableFunction<Object,Object>(){
    @Override public Object fun(    Object o){
      return null;
    }
  }
).doCommit();
}
