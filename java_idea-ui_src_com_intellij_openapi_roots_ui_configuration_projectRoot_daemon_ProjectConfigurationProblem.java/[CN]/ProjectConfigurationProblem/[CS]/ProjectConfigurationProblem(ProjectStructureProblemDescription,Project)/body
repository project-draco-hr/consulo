{
  super(computeMessage(description),computeDescription(description),ProjectStructureProblemsSettings.getInstance(project).isIgnored(description));
  myDescription=description;
  myProject=project;
}
