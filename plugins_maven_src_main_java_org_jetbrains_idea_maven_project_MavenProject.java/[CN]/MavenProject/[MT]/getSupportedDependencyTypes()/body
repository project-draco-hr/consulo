{
  Set<String> result=new THashSet<String>(Arrays.asList(MavenConstants.TYPE_POM,MavenConstants.TYPE_JAR,MavenConstants.TYPE_TEST_JAR,"ejb","ejb-client","war","ear"));
  for (  MavenImporter each : getSuitableImporters()) {
    each.getSupportedDependencyTypes(result);
  }
  return result;
}
