{
  for (  MavenArtifact each : getUnresolvedDependencies(state)) {
    result.add(new MavenProjectProblem(ProjectBundle.message("maven.project.problem.unresolvedDependency",each.displayStringWithType()),false));
  }
}
