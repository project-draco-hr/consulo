{
  String t=artifact.getType();
  if (t.equalsIgnoreCase(MavenConstants.TYPE_JAR) || t.equalsIgnoreCase("test-jar") || t.equalsIgnoreCase("ejb")|| t.equalsIgnoreCase("ejb-client")) {
    return true;
  }
  for (  MavenImporter each : getSuitableImporters()) {
    if (each.isSupportedDependency(artifact))     return true;
  }
  return false;
}
