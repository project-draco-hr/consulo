{
  if (isAggregator())   return;
  MavenProjectReaderResult result=MavenProjectReader.readProject(project,embedder,getFile(),getActiveProfilesIds(),locator,process);
  if (result.isValid)   setDependencies(result,true);
}
