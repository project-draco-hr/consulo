{
  if (isAggregator())   return;
  MavenProjectReaderResult result=new MavenProjectReader().generateSources(embedder,importingSettings,getFile(),getActiveProfilesIds(),console,p);
  if (result != null)   setFolders(result);
}
