{
  MavenProjectReaderResult result=new MavenProjectReader().resolveProject(generalSettings,embedder,getFile(),getActiveProfilesIds(),locator,process);
  set(result,result.isValid);
  return result.nativeMavenProject;
}
