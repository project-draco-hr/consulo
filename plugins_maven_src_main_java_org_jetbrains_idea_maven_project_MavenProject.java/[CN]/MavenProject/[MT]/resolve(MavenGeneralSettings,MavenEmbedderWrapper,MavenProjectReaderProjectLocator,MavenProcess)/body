{
  if (isAggregator())   return;
  MavenProjectReaderResult result=new MavenProjectReader().readProject(generalSettings,embedder,getFile(),getActiveProfilesIds(),locator,process);
  if (result.isValid)   setDependencies(result,true);
}
