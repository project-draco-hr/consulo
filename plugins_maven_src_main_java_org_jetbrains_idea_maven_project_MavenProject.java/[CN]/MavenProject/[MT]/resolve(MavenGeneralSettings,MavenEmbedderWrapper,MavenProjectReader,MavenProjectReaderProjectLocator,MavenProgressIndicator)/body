{
  MavenProjectReaderResult result=reader.resolveProject(generalSettings,embedder,getFile(),getActiveProfilesIds(),locator,process);
  set(result,false,result.isValid);
  return result.isValid ? result.nativeMavenProject : null;
}
