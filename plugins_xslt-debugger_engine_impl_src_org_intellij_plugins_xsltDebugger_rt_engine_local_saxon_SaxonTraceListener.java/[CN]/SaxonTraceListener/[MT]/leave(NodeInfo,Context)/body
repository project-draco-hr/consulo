{
  if (element.getNodeType() == NodeInfo.ELEMENT) {
    myDebugger.leave();
    if (TRACE) {
      indent=indent.substring(0,indent.length() - 1);
      trace(indent + "</Instruction> <!-- " + element.getDisplayName()+ " -->");
    }
  }
}
