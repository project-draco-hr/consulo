{
  final Context context=new Context(document);
  context.init();
  myStrategy.calculateWrapPosition(context.document,0,context.document.length(),context.preferredIndex,allowToBeyondMaxPreferredOffset);
}
