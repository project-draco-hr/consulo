{
  final Context context=new Context(document);
  context.init();
  int actual=myStrategy.calculateWrapPosition(createMockDocument(context.document),0,context.document.length(),context.edgeIndex,allowToBeyondMaxPreferredOffset);
  assertSame(context.wrapIndex,actual);
}
