{
  final Context context=new Context(document);
  context.init();
  int actual=myStrategy.calculateWrapPosition(context.document,0,context.document.length(),context.edgeIndex,allowToBeyondMaxPreferredOffset);
  assertSame(context.wrapIndex,actual);
}
