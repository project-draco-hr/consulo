{
  if (framework instanceof JavaTestFramework && super.isValidFor(targetClass,framework)) {
    return ((JavaTestFramework)framework).isParameterized(targetClass) && ((JavaTestFramework)framework).findParametersMethod(targetClass) == null;
  }
  return false;
}
