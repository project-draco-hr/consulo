{
  if (!(element instanceof GrStatement) && !(element instanceof PsiComment) && !element.getText().equals(";")&& !StringUtil.isEmptyOrSpaces(element.getText())) {
    return false;
  }
  return element.getParent() instanceof GrStatementOwner;
}
