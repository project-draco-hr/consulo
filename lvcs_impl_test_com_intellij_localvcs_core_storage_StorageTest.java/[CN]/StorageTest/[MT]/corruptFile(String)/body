{
  try {
    File f=new File(tempDir,name);
    assertTrue(f.exists());
    FileWriter w=new FileWriter(f);
    w.write("bla-bla-bla");
    w.close();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
