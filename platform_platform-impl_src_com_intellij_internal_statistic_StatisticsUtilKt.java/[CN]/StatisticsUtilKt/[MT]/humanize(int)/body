{
  if (number == 0)   return "0";
  int m=number / mega;
  int k=(number % mega) / kilo;
  int r=(number % kilo);
  String ms=m > 0 ? (m + "M") : "";
  String ks=k > 0 ? (k + "K") : "";
  String rs=r > 0 ? String.valueOf(r) : "";
  return ms + ks + rs;
}
