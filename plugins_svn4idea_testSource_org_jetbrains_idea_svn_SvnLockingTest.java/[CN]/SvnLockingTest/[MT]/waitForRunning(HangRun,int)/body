{
  int cnt=10 * multiply;
  while (cnt > 0) {
    try {
      Thread.sleep(10 * multiply);
    }
 catch (    InterruptedException e) {
    }
    if (operation1.isRunning())     break;
    --cnt;
  }
}
