{
  CacheEntry oldFirst=firstId;
  entry.nextId=oldFirst;
  entry.prevId=null;
  firstId=entry;
  if (oldFirst != null) {
    oldFirst.prevId=entry;
  }
 else {
    lastId=entry;
  }
  queueSize++;
  if (queueSize <= queueLimit) {
    return null;
  }
  Entry eldest=lastId;
  removeEntry(eldest);
  return eldest;
}
