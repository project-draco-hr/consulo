{
  String fileName=file.getName();
  return fileName.startsWith(".") || "out".equals(fileName) || "testData".equalsIgnoreCase(fileName)|| ("src".equals(fileName) && !"testFramework".equals(parent.getName()));
}
