{
  File homeFile=new File(STORE_PATH);
  if (!homeFile.exists()) {
    if (!homeFile.mkdirs()) {
      Messages.showMessageDialog("Cannot create folder " + STORE_PATH + " to save system information.","Error",Messages.getErrorIcon());
      return false;
    }
  }
  return true;
}
