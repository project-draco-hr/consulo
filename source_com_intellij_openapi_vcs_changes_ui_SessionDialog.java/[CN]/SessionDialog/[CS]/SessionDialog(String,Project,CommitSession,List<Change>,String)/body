{
  super(project,true);
  mySession=session;
  myChanges=changes;
  myCommitMessage=commitMessage;
  myConfigurationComponent=mySession.getAdditionalConfigurationUI();
  setTitle(title);
  init();
  updateButtons();
}
