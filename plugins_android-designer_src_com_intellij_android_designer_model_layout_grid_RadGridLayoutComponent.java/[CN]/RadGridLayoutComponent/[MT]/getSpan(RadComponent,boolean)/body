{
  try {
    String span=((RadViewComponent)component).getTag().getAttributeValue(row ? "layout_rowSpan" : "layout_columnSpan",SdkConstants.NS_RESOURCES);
    return Integer.parseInt(span);
  }
 catch (  Throwable e) {
    return 1;
  }
}
