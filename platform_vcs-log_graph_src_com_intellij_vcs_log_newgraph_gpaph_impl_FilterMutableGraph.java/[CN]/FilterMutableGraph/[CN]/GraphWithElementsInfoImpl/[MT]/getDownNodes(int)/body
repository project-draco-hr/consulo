{
  List<Integer> result=new SmartList<Integer>();
  for (  int downNode : myGraph.getDownNodes(nodeIndex)) {
    if (downNode != SomeGraph.NOT_LOAD_COMMIT && myVisibleNodes.get(downNode))     result.add(downNode);
  }
  return result;
}
