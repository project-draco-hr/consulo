{
  if (!isSoftWrappingEnabled()) {
    return;
  }
  initIfNecessary();
  dropDataIfNecessary();
  myActive++;
  try {
    myApplianceManager.registerSoftWrapIfNecessary(chars,start,end,x,fontType);
  }
  finally {
    myActive--;
  }
}
