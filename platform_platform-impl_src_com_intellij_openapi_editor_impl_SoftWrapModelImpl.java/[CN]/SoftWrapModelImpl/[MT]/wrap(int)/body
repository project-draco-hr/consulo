{
  TextChange result=myWraps.get(offset);
  if (result != null) {
    return result;
  }
  dropDataIfNecessary();
  result=new TextChange("\n    ",offset);
  myWraps.put(offset,result);
  return result;
}
