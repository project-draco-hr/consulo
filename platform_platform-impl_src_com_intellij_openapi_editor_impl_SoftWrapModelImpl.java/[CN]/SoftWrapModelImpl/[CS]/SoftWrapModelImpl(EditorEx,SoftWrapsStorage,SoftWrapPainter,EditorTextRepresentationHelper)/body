{
  this(editor,storage,painter,new DefaultSoftWrapApplianceManager(storage,editor,painter,representationHelper),new SoftWrapDataMapper(editor,storage,representationHelper),new SoftWrapDocumentChangeManager(editor,storage));
}
