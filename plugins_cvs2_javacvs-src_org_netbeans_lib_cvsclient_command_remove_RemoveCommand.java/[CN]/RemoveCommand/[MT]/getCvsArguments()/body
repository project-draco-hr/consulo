{
  final StringBuffer toReturn=new StringBuffer("");
  if (!isRecursive()) {
    toReturn.append("-l ");
  }
  if (isDeleteBeforeRemove()) {
    toReturn.append("-f ");
  }
  return toReturn.toString();
}
