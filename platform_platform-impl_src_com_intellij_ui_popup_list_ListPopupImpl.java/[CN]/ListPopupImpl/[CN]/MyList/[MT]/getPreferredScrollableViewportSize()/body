{
  Dimension result=super.getPreferredScrollableViewportSize();
  result.width+=JBUI.scale(14);
  int rowCount=getVisibleRowCount();
  int size=getModel().getSize();
  if (rowCount < size) {
    return result;
  }
  result.height=getPreferredSize().height;
  return result;
}
