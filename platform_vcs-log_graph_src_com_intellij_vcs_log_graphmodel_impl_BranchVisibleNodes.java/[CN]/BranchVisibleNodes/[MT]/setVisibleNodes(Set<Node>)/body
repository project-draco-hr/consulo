{
  myVisibleNodes=visibleNodes;
  myVisibleHashes=ContainerUtil.map2Set(myVisibleNodes,new Function<Node,Hash>(){
    @Override public Hash fun(    Node node){
      return node.getCommitHash();
    }
  }
);
}
