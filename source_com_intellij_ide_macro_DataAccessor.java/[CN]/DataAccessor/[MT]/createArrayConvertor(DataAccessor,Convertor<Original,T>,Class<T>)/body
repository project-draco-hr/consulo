{
  return new DataAccessor(){
    public Object getImpl(    DataContext dataContext) throws NoDataException {
      ArrayList<T> converted=CollectUtil.SKIP_NULLS.toList((Object[])original.getNotNull(dataContext),convertor);
      return converted.toArray((Object[])Array.newInstance(aClass,converted.size()));
    }
  }
;
}
