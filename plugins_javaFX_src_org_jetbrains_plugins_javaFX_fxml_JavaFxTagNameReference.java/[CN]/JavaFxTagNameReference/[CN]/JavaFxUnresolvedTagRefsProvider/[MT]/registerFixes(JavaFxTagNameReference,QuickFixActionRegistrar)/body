{
  registrar.register(new JavaFxImportClassFix(ref,ref.getTagElement()){
    @Override protected XmlTag getTagElement(    JavaFxTagNameReference ref){
      return ref.getTagElement();
    }
  }
);
}
