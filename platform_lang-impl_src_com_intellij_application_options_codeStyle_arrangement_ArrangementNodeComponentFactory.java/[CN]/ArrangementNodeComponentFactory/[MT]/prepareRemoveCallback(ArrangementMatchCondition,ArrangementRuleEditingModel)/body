{
  if (model == null) {
    return null;
  }
  return new Runnable(){
    @Override public void run(){
      model.removeAndCondition(condition);
      myRemoveConditionCallback.run();
    }
  }
;
}
