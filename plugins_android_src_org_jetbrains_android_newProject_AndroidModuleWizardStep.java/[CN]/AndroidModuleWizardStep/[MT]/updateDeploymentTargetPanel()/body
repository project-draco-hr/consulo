{
  final boolean enabled=myCreateDefaultStructure.isSelected() && (myApplicationProjectButton.isSelected() || myTestProjectButton.isSelected());
  UIUtil.setEnabled(myDeploymentTargetPanel,enabled,true);
  if (enabled) {
    myAvdComboComponent.setEnabled(myEmulatorRadioButton.isSelected());
  }
}
