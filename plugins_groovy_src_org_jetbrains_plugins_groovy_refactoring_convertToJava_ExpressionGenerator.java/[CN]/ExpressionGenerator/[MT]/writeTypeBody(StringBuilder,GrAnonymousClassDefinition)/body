{
  builder.append("{\n");
  new ClassGenerator(context.project,new GeneratorClassNameProvider(),new ClassItemGeneratorImpl(context.project)).writeMembers(builder,anonymous,true);
  builder.append('}');
}
