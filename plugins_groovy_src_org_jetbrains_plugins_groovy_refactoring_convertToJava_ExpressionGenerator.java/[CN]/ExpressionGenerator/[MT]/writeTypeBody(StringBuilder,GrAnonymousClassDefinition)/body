{
  builder.append("{\n");
  new ClassGenerator(context.project,new GeneratorClassNameProvider(),new ClassItemGeneratorImpl(context.extend())).writeMembers(builder,anonymous,true);
  builder.append('}');
}
