{
  if (getLanguage(dataContext) == GroovyFileType.GROOVY_FILE_TYPE.getLanguage() || getLanguage(dataContext) == GspFileType.GSP_FILE_TYPE.getLanguage()) {
    return true;
  }
  return originalHandler == null || originalHandler.isEnabled(editor,dataContext);
}
