{
  if (myEnableRemoteDebugCheckBox.isSelected() != mySettings.isEnableRemoteDebug() || !myPortField.getText().equals(String.valueOf(mySettings.getRemoteShellPort())) || myUseCustomProfileCheckBox.isSelected() != mySettings.isUseCustomProfile()) {
    return true;
  }
  String configuredPath=getConfiguredUserDataDirPath();
  String storedPath=mySettings.getUserDataDirectoryPath();
  if (myDefaultUserDirPath.equals(configuredPath) && storedPath == null)   return false;
  return !configuredPath.equals(storedPath);
}
