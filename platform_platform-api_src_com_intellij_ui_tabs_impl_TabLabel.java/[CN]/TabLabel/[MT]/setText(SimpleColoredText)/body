{
  myLabel.change(new Runnable(){
    public void run(){
      myLabel.clear();
      myLabel.setIcon(myIcon);
      if (text != null) {
        text.appendToComponent(myLabel);
      }
    }
  }
,false);
  invalidateIfNeeded();
}
