{
  myLabel.change(new Runnable(){
    public void run(){
      myLabel.clear();
      myLabel.setIcon(hasIcons() ? myIcon : null);
      if (text != null) {
        text.appendToComponent(myLabel);
      }
    }
  }
,false);
  invalidateIfNeeded();
}
