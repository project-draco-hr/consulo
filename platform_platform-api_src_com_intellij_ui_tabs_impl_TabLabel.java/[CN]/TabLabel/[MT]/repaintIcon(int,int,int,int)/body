{
  final Rectangle r=new Rectangle(x,y,width,height);
  doTranslate(new PairConsumer<Integer,Integer>(){
    @Override public void consume(    Integer x,    Integer y){
      r.translate(x,y);
    }
  }
);
  repaint(r.x,r.y,r.width,r.height);
}
