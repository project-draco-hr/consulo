{
  final Dimension size=super.getPreferredSize();
  final JBTabsPosition pos=myTabs.getTabsPosition();
switch (pos) {
case top:
case bottom:
    size.height+=getSelectedOffset();
  break;
case left:
case right:
size.width+=getSelectedOffset();
break;
}
return size;
}
