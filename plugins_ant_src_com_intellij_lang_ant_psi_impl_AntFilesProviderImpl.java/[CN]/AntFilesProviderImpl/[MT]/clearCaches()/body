{
synchronized (PsiLock.LOCK) {
    super.clearCaches();
    myPattern=null;
    myCachedFiles=null;
    final AntElement parent=getAntParent();
    if (parent != null) {
      parent.clearCaches();
    }
  }
}
