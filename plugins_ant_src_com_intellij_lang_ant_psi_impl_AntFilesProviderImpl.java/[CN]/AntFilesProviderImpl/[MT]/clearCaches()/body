{
synchronized (PsiLock.LOCK) {
    super.clearCaches();
    myPattern=null;
    clearCachedFiles();
    final AntElement parent=getAntParent();
    if (parent != null) {
      parent.clearCaches();
    }
  }
}
