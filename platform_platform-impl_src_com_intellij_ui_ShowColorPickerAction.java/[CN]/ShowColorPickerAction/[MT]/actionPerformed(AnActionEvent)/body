{
  final Project project=e.getProject();
  final IdeFrame frame=project == null ? WindowManager.getInstance().getAllFrames()[0] : WindowManager.getInstance().getIdeFrame(project);
  if (frame != null) {
    final ColorPicker.ColorPickerDialog picker=new ColorPicker.ColorPickerDialog(frame.getComponent(),"Color Picker",null,true,e.getData(LangDataKeys.PSI_ELEMENT));
    picker.setModal(false);
    picker.show();
  }
}
