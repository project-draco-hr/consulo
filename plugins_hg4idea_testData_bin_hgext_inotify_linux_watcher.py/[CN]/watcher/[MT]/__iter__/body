def __iter__(self):
    'Yield a (path, watch descriptor, event mask) tuple for each\n        entry being watched.'
    for (path, (wd, mask)) in self._paths.iteritems():
        yield (path, wd, mask)
