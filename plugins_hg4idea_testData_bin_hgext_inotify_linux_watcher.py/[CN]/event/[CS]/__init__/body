def __init__(self, raw, path):
    self.path = path
    self.raw = raw
    if raw.name:
        self.fullpath = ((path + '/') + raw.name)
    else:
        self.fullpath = path
    self.wd = raw.wd
    self.mask = raw.mask
    self.cookie = raw.cookie
    self.name = raw.name
