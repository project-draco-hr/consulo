{
  return preferencesForm.isModified(myImporterPreferences) || !IdeaAPIHelper.equalAsSets(myOriginalProjects,projectChooser.getMarkedElements()) || !IdeaAPIHelper.equalAsSets(myOriginalProfiles,profileChooser.getMarkedElements());
}
