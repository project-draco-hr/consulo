{
  if (configuration != null) {
    return new Pair<String,Integer>(type.getDisplayName(),RunManagerImpl.getUniqueID(configuration));
  }
 else {
    return new Pair<String,Integer>(type.getDisplayName(),null);
  }
}
