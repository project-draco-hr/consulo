{
  if (found != null && found.getElementType() == ElementType.XML_COMMENT_START && found.getTreeParent() != null && found.getTreeParent().getStartOffset() == found.getStartOffset()) {
    return found.getTreeParent();
  }
 else {
    return found;
  }
}
