{
  checkConnected();
  final JpsRemoteProto.Message.Request request=isMake ? ProtoUtil.createMakeRequest(projectId,modules,artifacts,userData) : ProtoUtil.createForceCompileRequest(projectId,modules,artifacts,paths,userData);
  return sendRequest(request,handler);
}
