{
  checkConnected();
  final JpsRemoteProto.Message.Request request=isMake ? ProtoUtil.createMakeRequest(projectId,modules,artifacts) : ProtoUtil.createForceCompileRequest(projectId,modules,artifacts,paths);
  return sendRequest(request,handler);
}
