{
  VirtualFile existingFile=getVirtualFile().findChild(name);
  if (existingFile != null) {
    throw new IncorrectOperationException("Cannot create package - file \"" + existingFile.getPresentableUrl() + "\" already exists.");
  }
  CheckUtil.checkWritable(this);
}
