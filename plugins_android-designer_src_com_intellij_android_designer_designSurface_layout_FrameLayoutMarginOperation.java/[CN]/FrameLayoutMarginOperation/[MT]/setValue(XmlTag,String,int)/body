{
  if (value == 0) {
    XmlAttribute attribute=tag.getAttribute(name);
    if (attribute != null) {
      attribute.delete();
    }
  }
 else {
    tag.setAttribute(name,Integer.toString(value) + "dp");
  }
}
