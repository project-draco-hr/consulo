{
  int nextByte;
  while (true) {
    nextByte=readNext();
    if (nextByte != SPECIAL_SYMBOL)     break;
    final boolean packetRead=readControlSequence();
    if (!packetRead)     break;
  }
  return nextByte;
}
