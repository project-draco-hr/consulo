{
  QuoteHandler quoteHandler=getQuoteHandlerForType(file.getFileType());
  if (quoteHandler == null && file.getViewProvider().getBaseLanguage() instanceof XMLLanguage) {
    quoteHandler=getQuoteHandlerForType(StdFileTypes.XML);
  }
  return quoteHandler;
}
