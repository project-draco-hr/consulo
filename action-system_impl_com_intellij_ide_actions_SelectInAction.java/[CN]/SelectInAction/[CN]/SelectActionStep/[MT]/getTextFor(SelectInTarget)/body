{
  String text=value.toString();
  int n=myTargets.indexOf(value);
  if (n < 9) {
    text="&" + (n + 1) + ". "+ text;
  }
 else   if (n == 9) {
    text="&" + 0 + ". "+ text;
  }
 else {
    text="&" + (char)('A' + n - 10) + ". "+ text;
  }
  return text;
}
