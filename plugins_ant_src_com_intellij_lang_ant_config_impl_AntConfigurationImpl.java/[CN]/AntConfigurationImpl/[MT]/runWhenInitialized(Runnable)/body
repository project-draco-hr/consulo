{
  if (getProject().isInitialized()) {
    runnable.run();
  }
 else {
    myStartupManager.registerPostStartupActivity(new Runnable(){
      public void run(){
        runnable.run();
      }
    }
);
  }
}
