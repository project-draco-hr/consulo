{
  if (steps < 0) {
    throw new IllegalStateException("bad steps: " + steps);
  }
  if (steps == 0) {
    return prev;
  }
  M row=this.createMutable(prev);
  for (int i=0; i < steps; i++) {
    row=oneStep(row);
  }
  return row;
}
