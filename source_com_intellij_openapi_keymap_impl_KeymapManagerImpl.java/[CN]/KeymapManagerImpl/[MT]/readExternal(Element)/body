{
  Element child=element.getChild("active_keymap");
  if (child != null) {
    myActiveKeymapName=child.getAttributeValue("name");
  }
  if (myActiveKeymapName != null) {
    Keymap keymap=getKeymap(myActiveKeymapName);
    if (keymap != null) {
      setActiveKeymap(keymap);
    }
  }
}
