{
  SVNStatus srcStatus=getFileStatus(vcs,src);
  if (srcStatus == null || SvnVcs17.svnStatusIsUnversioned(srcStatus) || SvnVcs17.svnStatusIs(srcStatus,SVNStatusType.STATUS_OBSTRUCTED) || SvnVcs17.svnStatusIs(srcStatus,SVNStatusType.STATUS_MISSING) || SvnVcs17.svnStatusIs(srcStatus,SVNStatusType.STATUS_EXTERNAL)) {
    return true;
  }
  return false;
}
