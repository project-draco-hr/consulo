{
  final AttributeChildInvocationHandler handler=new AttributeChildInvocationHandler(method.getGenericReturnType(),getXmlTag(),this,attributeName,myManager);
  myManager.createDomElement(handler);
  myAttributeChildren.put(handler.getXmlElementName(),handler);
}
