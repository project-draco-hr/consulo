{
  return ContainerUtil.map(conflicts,new Function<MergeRange,MergeLineFragment>(){
    @Override public MergeLineFragment fun(    MergeRange ch){
      return new MergeLineFragmentImpl(ch);
    }
  }
);
}
