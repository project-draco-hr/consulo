{
  final Color color;
  if (myHex.hasFocus()) {
    color=ColorUtil.fromHex(myHex.getText(),null);
  }
 else {
    color=gatherRGB();
  }
  if (color != null) {
    updatePreview(color,src == myHex);
  }
}
