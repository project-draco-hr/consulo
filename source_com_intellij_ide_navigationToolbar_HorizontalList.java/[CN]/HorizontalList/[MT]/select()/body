{
  if (!myList.isEmpty()) {
    clearBorder();
    mySelectedIndex=myModel.size() - 1;
    while (getWidth() < getChainWidth()) {
      scrollToVisible(1);
    }
    paintBorder();
    requestFocusInWindow();
  }
}
