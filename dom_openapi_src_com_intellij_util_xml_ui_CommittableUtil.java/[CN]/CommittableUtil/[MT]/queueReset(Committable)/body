{
  ourApplication.assertIsDispatchThread();
  ourResetQueue.add(committable);
  if (!ourResetQueue.contains(committable) && CommandProcessor.getInstance().getCurrentCommand() == null && !CommandProcessor.getInstance().isUndoTransparentActionInProgress()) {
    committable.reset();
    ourResetQueue.remove(committable);
  }
}
