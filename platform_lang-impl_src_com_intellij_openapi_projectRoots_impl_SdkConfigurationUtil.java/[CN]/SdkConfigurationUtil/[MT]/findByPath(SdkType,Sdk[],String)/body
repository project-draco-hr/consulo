{
  for (  Sdk sdk : sdks) {
    if (sdk.getSdkType() == sdkType && FileUtil.pathsEqual(FileUtil.toSystemIndependentName(sdk.getHomePath()),FileUtil.toSystemIndependentName(sdkHome))) {
      return sdk;
    }
  }
  return null;
}
