{
  GitUIUtil.notifyMessage(project,"File doesn't exist in branch",String.format("File <code>%s</code> doesn't exist in branch <code>%s</code>",file.getPresentableUrl(),branchToCompare),NotificationType.WARNING,true,null);
}
