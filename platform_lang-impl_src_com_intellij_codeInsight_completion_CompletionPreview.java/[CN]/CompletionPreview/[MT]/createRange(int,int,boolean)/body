{
  return myLookup.getEditor().getMarkupModel().addRangeHighlighter(myPreviewStart + start,myPreviewStart + end,HighlighterLayer.LAST,new TextAttributes(generated ? Color.LIGHT_GRAY : Color.BLACK,null,null,null,Font.PLAIN),HighlighterTargetArea.EXACT_RANGE);
}
