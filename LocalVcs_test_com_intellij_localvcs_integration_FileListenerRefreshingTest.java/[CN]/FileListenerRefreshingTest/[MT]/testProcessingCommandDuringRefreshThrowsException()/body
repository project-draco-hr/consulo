{
  l.beforeRefreshStart(false);
  try {
    l.commandStarted(null);
    fail();
  }
 catch (  IllegalStateException e) {
  }
  try {
    l.commandFinished(null);
    fail();
  }
 catch (  IllegalStateException e) {
  }
}
