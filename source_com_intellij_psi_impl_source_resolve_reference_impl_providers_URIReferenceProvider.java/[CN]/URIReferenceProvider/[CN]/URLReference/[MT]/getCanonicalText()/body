{
  final String text=myElement.getText();
  if (text.length() > 1) {
    return myRange == null ? text.substring(1,text.length() - 1) : text.substring(myRange.getStartOffset(),myRange.getEndOffset());
  }
  return "";
}
