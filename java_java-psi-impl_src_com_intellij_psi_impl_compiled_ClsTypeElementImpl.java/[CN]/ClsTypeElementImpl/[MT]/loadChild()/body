{
  if (isPrimitive()) {
synchronized (LAZY_BUILT_LOCK) {
      myChildSet=true;
    }
    return;
  }
  if (isArray() || isVarArgs()) {
    createComponentTypeChild();
  }
 else {
    createClassReferenceChild();
  }
}
