{
  String result=name;
  if (!isOKTest(name,true).equals("ok") && !increaseNumber || name.length() == 0) {
    return "";
  }
  int i=1;
  while (!isOKTest(result,true).equals("ok")) {
    result=name + i;
    i++;
  }
  return result;
}
