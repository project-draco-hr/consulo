{
  invokeLaterIfNeeded(new MyRunnable(){
    @Override public void run(    Throwable allocation){
      getBusyContainer(null).addActivity(activity,allocation,effectiveModalityState);
    }
  }
);
}
