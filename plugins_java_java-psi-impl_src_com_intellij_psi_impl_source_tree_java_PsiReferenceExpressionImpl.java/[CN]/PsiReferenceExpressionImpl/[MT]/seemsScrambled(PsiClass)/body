{
  if (!(element instanceof PsiCompiledElement)) {
    return false;
  }
  final String qualifiedName=element.getQualifiedName();
  return qualifiedName != null && qualifiedName.length() <= 2 && !qualifiedName.isEmpty() && Character.isLowerCase(qualifiedName.charAt(0));
}
