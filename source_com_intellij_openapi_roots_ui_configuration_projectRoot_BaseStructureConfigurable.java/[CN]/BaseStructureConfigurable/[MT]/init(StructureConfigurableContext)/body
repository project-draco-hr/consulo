{
  myContext=context;
  myContext.addCacheUpdateListener(new Runnable(){
    public void run(){
      if (!myTree.isShowing())       return;
      myTree.revalidate();
      myTree.repaint();
    }
  }
);
}
