{
  myUiDisposed=false;
  if (!myWasTreeInitialized) {
    initTree();
    myTree.setShowsRootHandles(false);
    loadTree();
  }
 else {
    myRoot.removeAllChildren();
    myTree.setShowsRootHandles(false);
    loadTree();
  }
  super.reset();
}
