{
  return new Comparator<AppletConfiguration.AppletParameter>(){
    public int compare(    final AppletConfiguration.AppletParameter parameter1,    final AppletConfiguration.AppletParameter parameter2){
      return COMPARATOR.compare(valueOf(parameter1),valueOf(parameter2));
    }
  }
;
}
