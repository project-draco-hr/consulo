{
  super(parent,false);
  myProvider=LanguageCodeStyleSettingsProvider.forLanguage(language);
  MyListModel model=new MyListModel();
  myPredefinedCodeStyleList.setModel(model);
  setTitle("Select Predefined Style");
  init();
}
