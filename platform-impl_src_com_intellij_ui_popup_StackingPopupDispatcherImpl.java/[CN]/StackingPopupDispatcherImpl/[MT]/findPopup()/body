{
  while (true) {
    if (myStack.isEmpty())     break;
    final JBPopupImpl each=myStack.peek();
    if (each == null || each.isDisposed()) {
      myStack.pop();
    }
 else {
      return each;
    }
  }
  return null;
}
