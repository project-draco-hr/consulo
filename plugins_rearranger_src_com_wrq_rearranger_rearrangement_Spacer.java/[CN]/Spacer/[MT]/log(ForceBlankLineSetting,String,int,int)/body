{
  if (oldbias != bias) {
    LOG.debug(fbls.getObjectName() + " " + name+ (fbls.isBefore() ? " before" : " after")+ (fbls.isOpenBrace() ? " left brace:" : " right brace:")+ (oldbias < bias ? " inserted " + (bias - oldbias) : " removed " + (oldbias - bias))+ " newlines");
  }
}
