{
  if (source instanceof ArtifactDeploymentSource) {
    String artifactName=((ArtifactDeploymentSource)source).getArtifactPointer().getName();
    element.addContent(new Element("artifact").setAttribute("name",artifactName));
  }
}
