{
  ArtifactPointerManager artifactPointerManager=ArtifactPointerUtil.getPointerManager(project);
  Element artifact=element.getChild("artifact");
  return createArtifactDeploymentSource(artifactPointerManager.create(artifact.getAttributeValue("name")));
}
