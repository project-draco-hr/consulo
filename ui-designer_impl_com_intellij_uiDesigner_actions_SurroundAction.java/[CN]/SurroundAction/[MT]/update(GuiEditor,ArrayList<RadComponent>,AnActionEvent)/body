{
  RadContainer selectionParent=getSelectionParent(selection);
  e.getPresentation().setEnabled(selectionParent != null && (selectionParent instanceof RadRootContainer || selectionParent.isGrid()) && isSelectionContiguous(selectionParent,selection) && canWrapSelection(selection));
}
