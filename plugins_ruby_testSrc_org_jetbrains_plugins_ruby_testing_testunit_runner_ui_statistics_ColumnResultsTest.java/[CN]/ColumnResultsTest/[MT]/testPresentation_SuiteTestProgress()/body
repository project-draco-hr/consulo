{
  mySuite.setStarted();
  final RTestUnitTestProxy test1=createTestProxy(mySuite);
  assertEmpty(myFragmentsContainer.getFragments());
  test1.setStarted();
  assertEmpty(myFragmentsContainer.getFragments());
}
