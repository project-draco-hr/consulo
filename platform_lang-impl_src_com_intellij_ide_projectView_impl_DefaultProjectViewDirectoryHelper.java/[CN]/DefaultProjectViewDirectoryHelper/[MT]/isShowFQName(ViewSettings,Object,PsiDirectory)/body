{
  PsiPackage aPackage;
  return value != null && !(parentValue instanceof Project) && settings.isFlattenPackages() && (aPackage=PsiPackageManager.getInstance(getProject()).findAnyPackage(value)) != null && !aPackage.getQualifiedName().isEmpty();
}
