{
  myInputStream=in;
  ArrayList<byte[]> result=new ArrayList<byte[]>();
  try {
    byte[] line;
    while ((line=readLineInternal()) != null)     result.add(line);
  }
  finally {
    myInputStream.close();
  }
  return result;
}
