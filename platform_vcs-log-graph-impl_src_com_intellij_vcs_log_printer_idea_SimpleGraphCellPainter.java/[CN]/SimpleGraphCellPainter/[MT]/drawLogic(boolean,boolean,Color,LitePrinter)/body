{
  if (isSelected) {
    setStroke(isUsual,true);
    printer.print(MARK_COLOR);
    setStroke(isUsual,false);
    printer.print(usualColor);
  }
 else {
    setStroke(isUsual,false);
    printer.print(usualColor);
  }
}
