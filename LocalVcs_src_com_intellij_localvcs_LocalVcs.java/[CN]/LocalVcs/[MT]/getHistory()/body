{
  List<RootEntry> result=new ArrayList<RootEntry>();
  RootEntry r=myRoot;
  while (r != null) {
    result.add(r);
    r=myChangeList.revertOn(r);
  }
  result.remove(result.size() - 1);
  return result;
}
