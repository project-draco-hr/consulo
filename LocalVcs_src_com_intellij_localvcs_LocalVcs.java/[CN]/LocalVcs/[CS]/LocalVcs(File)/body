{
  File f=new File(dir,"content");
  FileInputStream fs=new FileInputStream(f);
  try {
    Stream s=new Stream(fs);
    mySnapshot=new Snapshot(s.readChangeList(),(RootEntry)s.readEntry());
  }
  finally {
    fs.close();
  }
}
