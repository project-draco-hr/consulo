{
  File f=new File(dir,"content");
  FileInputStream fs=new FileInputStream(f);
  try {
    Stream s=new Stream(fs);
    mySnapshot=new Snapshot(s.readIdGenerator(),s.readChangeList(),s.readEntry(),s.readInteger());
  }
  finally {
    fs.close();
  }
}
