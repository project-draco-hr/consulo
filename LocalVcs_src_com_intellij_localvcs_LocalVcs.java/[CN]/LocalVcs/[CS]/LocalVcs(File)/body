{
  File f=new File(dir,"content");
  FileInputStream fs=new FileInputStream(f);
  try {
    Stream s=new Stream(fs);
    myChangeList=s.readChangeList();
    myRoot=(RootEntry)s.readEntry();
    myEntryCounter=s.readInteger();
  }
  finally {
    fs.close();
  }
}
