{
  final JBList list=new JBList(calls);
  list.setFixedCellHeight(20);
  list.installCellRenderer(createCellRenderer());
  JBPopupFactory.getInstance().createListPopupBuilder(list).setItemChoosenCallback(createItemChosenCallback(e,list)).createPopup().show(new RelativePoint(e));
}
