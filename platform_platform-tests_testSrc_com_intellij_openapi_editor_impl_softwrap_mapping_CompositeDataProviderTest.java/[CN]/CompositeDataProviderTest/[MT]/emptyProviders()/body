{
  myProvider=new CompositeDataProvider(new TestDataProvider<DataType>(DataType.ONE),new TestDataProvider<DataType>(DataType.TWO));
  assertFalse(myProvider.hasData());
  assertFalse(myProvider.next());
}
