{
  PsiType type=cast.getOperand().getType();
  if (type == null) {
    return GroovyResolveResult.EMPTY_ARRAY;
  }
  final GrTypeElement typeElement=cast.getCastTypeElement();
  final PsiType toCast=typeElement == null ? null : typeElement.getType();
  return TypesUtil.getOverloadedOperatorCandidates(type,GroovyTokenTypes.kAS,cast,new PsiType[]{TypesUtil.createJavaLangClassType(toCast,cast.getProject(),cast.getResolveScope())});
}
