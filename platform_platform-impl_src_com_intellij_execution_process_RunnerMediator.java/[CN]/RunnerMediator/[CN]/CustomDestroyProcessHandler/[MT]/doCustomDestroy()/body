{
  try {
    if (isWindows()) {
      sendCtrlBreakThroughStream();
      return true;
    }
 else     if (canSendSignals()) {
      sendSigKill(myProcessUid);
      return true;
    }
 else {
      return false;
    }
  }
 catch (  Exception e) {
    return false;
  }
}
