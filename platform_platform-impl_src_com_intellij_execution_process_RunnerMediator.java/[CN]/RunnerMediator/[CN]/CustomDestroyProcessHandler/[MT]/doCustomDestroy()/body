{
  try {
    if (isWindows()) {
      sendCtrlBreakThroughStream();
      return true;
    }
 else     if (canSendSignals()) {
      sendSigInt(myProcessUid);
      return true;
    }
 else {
      return false;
    }
  }
 catch (  Exception e) {
    return false;
  }
}
