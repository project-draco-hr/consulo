{
  Runnable r=null;
  if (runnable != null) {
    r=new Runnable(){
      public void run(){
        if (myProject.isDisposed())         return;
        runnable.run();
      }
    }
;
  }
  myToolWindow.activate(r,focus);
}
