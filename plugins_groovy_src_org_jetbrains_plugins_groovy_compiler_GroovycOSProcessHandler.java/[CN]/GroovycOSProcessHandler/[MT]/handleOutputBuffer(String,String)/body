{
  String text;
  text=outputBuffer.substring(outputBuffer.indexOf(START_MARKER) + START_MARKER.length(),outputBuffer.indexOf(END_MARKER));
  outputBuffer.delete(outputBuffer.indexOf(START_MARKER),outputBuffer.indexOf(END_MARKER) + END_MARKER.length());
  return text.trim();
}
