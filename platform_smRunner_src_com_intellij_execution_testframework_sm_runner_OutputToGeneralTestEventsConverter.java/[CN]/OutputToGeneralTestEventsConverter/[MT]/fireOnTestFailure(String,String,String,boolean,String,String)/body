{
  assertNotNull(testName);
  assertNotNull(localizedMessage);
  final GeneralTestEventsProcessor processor=myProcessor;
  if (processor != null) {
    processor.onTestFailure(testName,localizedMessage,stackTrace,isTestError,comparisionFailureActualText,comparisionFailureExpectedText);
  }
}
