{
  String text=xmlText.getText();
  if (text.indexOf('\n') >= 0) {
    return GroovyFileType.GROOVY_LANGUAGE;
  }
  if (text.indexOf('(') >= 0) {
    return GroovyFileType.GROOVY_LANGUAGE;
  }
  return null;
}
