{
  String name=getEntryName();
  String parentPath=getEntryParentPath();
  Entry e=new FileEntry(getNonAppliedState().myId,name,getNonAppliedState().myContent,getNonAppliedState().myTimestamp,getNonAppliedState().isReadOnly);
  return addEntry(r,parentPath,e);
}
