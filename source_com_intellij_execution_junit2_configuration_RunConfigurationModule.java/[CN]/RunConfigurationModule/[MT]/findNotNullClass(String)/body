{
  final PsiClass psiClass=findClass(className);
  if (psiClass == null) {
    throw new RuntimeConfigurationWarning("Class \"" + className + "\" not found in module \""+ getModuleName()+ "\"");
  }
  return psiClass;
}
