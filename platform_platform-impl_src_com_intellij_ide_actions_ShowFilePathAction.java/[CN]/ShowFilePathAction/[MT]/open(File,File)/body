{
  try {
    final String path=(SystemInfo.isWindows || SystemInfo.isMac) && toSelect != null && toSelect.exists() ? toSelect.getCanonicalPath() : ioFile.getCanonicalPath();
    doOpen(path);
  }
 catch (  Exception e) {
    LOG.warn(e);
  }
}
