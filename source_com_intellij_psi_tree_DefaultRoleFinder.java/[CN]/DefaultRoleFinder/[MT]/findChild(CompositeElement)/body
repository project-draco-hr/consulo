{
  if (myParentType != null)   LOG.assertTrue(parent.getElementType() == myParentType);
  TreeElement current=parent.firstChild;
  while (current != null && current.getElementType() != myElementType)   current=current.getTreeNext();
  return current;
}
