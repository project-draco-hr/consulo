{
  for (  VirtualFile scopeDir : myDirs) {
    boolean inDir=myIncludeSubpackages ? VfsUtil.isAncestor(scopeDir,file,false) : file.getParent().equals(scopeDir);
    if (inDir)     return true;
  }
  return false;
}
