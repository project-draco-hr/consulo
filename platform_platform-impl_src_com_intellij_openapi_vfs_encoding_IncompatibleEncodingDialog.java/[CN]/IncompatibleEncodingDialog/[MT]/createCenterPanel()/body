{
  JLabel label=new JLabel("<html><body>" + "The encoding you've chosen ('" + charset.displayName() + "') may change the contents of '"+ virtualFile.getName()+ "'.<br>"+ "Do you want to reload the file from disk or<br>"+ "convert the text and save in the new encoding?"+ "</body></html>");
  label.setIcon(Messages.getQuestionIcon());
  label.setIconTextGap(10);
  return label;
}
