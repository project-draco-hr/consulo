{
  try {
    while (true) {
      try {
        final Socket socket=mySocket.accept();
        writeLockedPaths(socket);
      }
 catch (      IOException e) {
        LOG.debug(e);
      }
    }
  }
 catch (  Throwable e) {
  }
}
