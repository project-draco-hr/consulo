{
  @NonNls final StringBuilder arguments=new StringBuilder();
  if (isSetLock()) {
    arguments.append("-l ");
  }
  if (isResetLock()) {
    arguments.append("-u ");
  }
  return arguments.toString();
}
