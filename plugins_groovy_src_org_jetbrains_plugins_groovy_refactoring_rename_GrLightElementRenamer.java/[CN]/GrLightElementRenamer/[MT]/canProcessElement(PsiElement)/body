{
  if (!(element instanceof LightElement))   return false;
  if (element instanceof GrRenamedLightElement)   return false;
  final Language language=element.getLanguage();
  return GroovyFileType.GROOVY_LANGUAGE.equals(language);
}
