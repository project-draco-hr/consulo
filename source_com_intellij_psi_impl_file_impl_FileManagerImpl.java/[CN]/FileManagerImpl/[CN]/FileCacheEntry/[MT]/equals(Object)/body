{
  return o instanceof FileCacheEntry && Comparing.equal(first,((FileCacheEntry)o).first) && (Comparing.equal(second,((FileCacheEntry)o).second) || second == Language.ANY || ((FileCacheEntry)o).second == Language.ANY);
}
