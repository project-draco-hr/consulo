{
  return new MergerFactory(){
    public IMerger createMerger(    SvnVcs vcs,    File target,    UpdateEventHandler handler,    SVNURL currentBranchUrl,    String branchName){
      return new BranchMerger(vcs,currentBranchUrl,myWcInfo.getUrl(),myWcInfo.getPath(),handler,reintegrate,myBranchName,reintegrate ? myData.getWrapped().getTargetRevision() : myData.getWrapped().getSourceRevision());
    }
    @Nullable public List<CommittedChangeList> getListsToMerge(){
      return null;
    }
    public boolean isMergeAll(){
      return true;
    }
  }
;
}
