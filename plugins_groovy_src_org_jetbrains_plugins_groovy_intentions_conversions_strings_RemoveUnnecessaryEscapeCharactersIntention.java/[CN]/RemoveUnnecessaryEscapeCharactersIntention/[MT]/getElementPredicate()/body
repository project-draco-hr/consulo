{
  return new PsiElementPredicate(){
    @Override public boolean satisfiedBy(    PsiElement element){
      String text;
      return element instanceof GrLiteral && getStartQuote(text=element.getText()) != null && !removeUnnecessaryEscapeSymbols((GrLiteral)element).equals(text);
    }
  }
;
}
