{
  return new PsiElementPredicate(){
    @Override public boolean satisfiedBy(    PsiElement element){
      final String text=element.getText();
      return element instanceof GrLiteral && getStartQuote(text) != null && !removeUnnecessaryEscapeSymbols((GrLiteral)element).equals(text);
    }
  }
;
}
