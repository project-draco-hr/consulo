{
  InspectionProfileImpl inspectionProfile=(InspectionProfileImpl)InspectionProjectProfileManager.getInstance(file.getProject()).getProfile(file);
  editToolSettings(project,inspectionProfile,true);
}
