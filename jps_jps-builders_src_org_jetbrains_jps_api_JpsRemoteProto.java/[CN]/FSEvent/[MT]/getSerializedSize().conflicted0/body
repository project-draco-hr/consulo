{
  int size=memoizedSerializedSize;
  if (size != -1)   return size;
  size=0;
  if (((bitField0_ & 0x00000001) == 0x00000001)) {
    size+=com.google.protobuf.CodedOutputStream.computeBytesSize(1,getProjectIdBytes());
  }
{
    int dataSize=0;
    for (int i=0; i < changedPaths_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeBytesSizeNoTag(changedPaths_.getByteString(i));
    }
    size+=dataSize;
    size+=1 * getChangedPathsList().size();
  }
{
    int dataSize=0;
    for (int i=0; i < deletedPaths_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeBytesSizeNoTag(deletedPaths_.getByteString(i));
    }
    size+=dataSize;
    size+=1 * getDeletedPathsList().size();
  }
  memoizedSerializedSize=size;
  return size;
}
