{
  int size=memoizedSerializedSize;
  if (size != -1)   return size;
  size=0;
{
    int dataSize=0;
    for (int i=0; i < projectId_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeBytesSizeNoTag(projectId_.getByteString(i));
    }
    size+=dataSize;
    size+=1 * getProjectIdList().size();
  }
  memoizedSerializedSize=size;
  return size;
}
