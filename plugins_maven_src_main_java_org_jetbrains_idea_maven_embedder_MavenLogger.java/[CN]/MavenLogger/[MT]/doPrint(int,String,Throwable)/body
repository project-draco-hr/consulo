{
  if (level < myThreshold)   return;
  if (!message.endsWith(MavenConsole.LINE_SEPARATOR)) {
    message+=MavenConsole.LINE_SEPARATOR;
  }
  if (myConsole != null) {
    myConsole.printMessage(level,message,throwable);
  }
}
