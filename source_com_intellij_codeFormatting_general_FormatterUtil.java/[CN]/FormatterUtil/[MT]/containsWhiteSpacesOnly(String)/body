{
  for (int i=0; i < text.length(); i++) {
    final char c=text.charAt(i);
    if (c == ' ')     continue;
    if (c == '\t')     continue;
    if (c == '\n')     continue;
    return false;
  }
  return true;
}
