{
  final GrDynamicImplicitPropertyImpl property=DynamicManager.getInstance(myProject).getProperty(myManager,getName(),getType().getCanonicalText(),getContainingClassName(),getContainingFile());
  return property == this;
}
