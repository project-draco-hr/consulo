{
  return myQualifier != null && myQualifier.isValid() && myQualifier.getManager().isInProject(myQualifier) && myExpression != null && myExpression.isValid() && myResolved != null && myResolved.isValid();
}
