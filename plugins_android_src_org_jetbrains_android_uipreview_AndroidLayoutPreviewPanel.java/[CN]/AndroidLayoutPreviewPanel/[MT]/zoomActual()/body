{
  if (myImage == null) {
    return;
  }
  if (myZoomToFit && myImagePanel.getWidth() >= myImage.getWidth() && myImagePanel.getHeight() >= myImage.getHeight()) {
    return;
  }
  myZoomToFit=false;
  setZoomFactor(1.0);
}
