{
  if (myCallQualifier != null && myPositionQualifier != null && myCallQualifier != myPositionQualifier && CodeInsightUtil.areExpressionsEquivalent(myCallQualifier,myPositionQualifier)) {
    return false;
  }
  if (myCallQualifier == null && myPositionQualifier == null) {
    return false;
  }
  return true;
}
