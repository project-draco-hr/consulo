{
  if (isArchiveFileSystem(file) && file.getParent() == null)   return true;
  return !file.isDirectory() && FileTypeManager.getInstance().getFileTypeByFile(file) == StdFileTypes.ARCHIVE && !isArchiveFileSystem(file.getParent());
}
