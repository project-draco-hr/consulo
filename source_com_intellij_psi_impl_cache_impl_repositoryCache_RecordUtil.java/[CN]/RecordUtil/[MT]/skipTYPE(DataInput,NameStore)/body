{
  final byte b=record.readByte();
  final int tag=b & 0x3;
  final int flags=0x3 & (b >> 6);
  if (tag == 0x02)   return;
  if ((flags & 1) != 0) {
    record.readByte();
  }
  if (tag == 0x00) {
    skipNAME(record,nameStore);
  }
}
