{
  if (from != null) {
    for (    VirtualFile vf : from) {
      final AbstractVcs vcs=myGuess.getVcsForDirty(vf);
      if (vcs != null) {
        to.add(new VcsRoot(vcs,vf));
      }
    }
  }
}
