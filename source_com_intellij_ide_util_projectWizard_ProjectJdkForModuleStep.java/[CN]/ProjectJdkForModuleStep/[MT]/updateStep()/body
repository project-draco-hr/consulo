{
  if (!myInitialized) {
    myJdkChooser.fillList(myType);
    ProjectJdk defaultJdk=getDefaultJdk();
    if (defaultJdk != null) {
      myJdkChooser.selectJdk(defaultJdk);
    }
    myInitialized=true;
  }
}
