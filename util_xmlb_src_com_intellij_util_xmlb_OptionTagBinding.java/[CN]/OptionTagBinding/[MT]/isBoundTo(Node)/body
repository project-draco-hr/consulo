{
  if (!(node instanceof Element))   return false;
  Element e=(Element)node;
  if (!e.getNodeName().equals("option"))   return false;
  String name=e.getAttribute(Constants.NAME);
  return name != null && name.equals(myName);
}
