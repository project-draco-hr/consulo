{
  TemplateImpl template=new TemplateImpl("","",TemplateSettings.USER_GROUP_NAME);
  myTemplateOptions.put(getKey(template),template.createOptions());
  myTemplateContext.put(getKey(template),template.createContext());
  EditTemplateDialog dialog=new EditTemplateDialog(this,CodeInsightBundle.message("dialog.add.live.template.title"),template,getTemplateGroups(),(String)myExpandByCombo.getSelectedItem(),getOptions(template),getContext(template));
  dialog.show();
  if (!dialog.isOK())   return;
  dialog.apply();
  addTemplate(template);
}
