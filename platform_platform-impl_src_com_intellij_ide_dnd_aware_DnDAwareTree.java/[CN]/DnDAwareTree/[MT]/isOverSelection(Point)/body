{
  final TreeUI ui=getUI();
  final TreePath path=ui instanceof MacTreeUI && ((MacTreeUI)ui).isWideSelection() ? getClosestPathForLocation(point.x,point.y) : getPathForLocation(point.x,point.y);
  if (path == null)   return false;
  return isPathSelected(path);
}
