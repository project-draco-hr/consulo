{
  importProject("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<distributionManagement>"+ "  <repository>"+ "   <id/>"+ "   <url/>"+ "   <layout/>"+ "  </repository>"+ "</distributionManagement>");
  MavenProjectModel root=getRootProjects().get(0);
  assertProblems(root,true);
}
