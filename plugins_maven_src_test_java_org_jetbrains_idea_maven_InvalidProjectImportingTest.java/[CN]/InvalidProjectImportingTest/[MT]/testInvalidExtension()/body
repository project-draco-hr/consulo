{
  try {
    importProjectUnsafe("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<build>"+ " <extensions>"+ "   <extension>"+ "     <groupId>group</groupId>"+ "     <artifactId>bla-bla-bla</artifactId>"+ "     <version>1</version>"+ "    </extension>"+ "  </extensions>"+ "</build>");
    fail();
  }
 catch (  MavenException e) {
    assertMessageContains(e,"group:bla-bla-bla");
  }
}
