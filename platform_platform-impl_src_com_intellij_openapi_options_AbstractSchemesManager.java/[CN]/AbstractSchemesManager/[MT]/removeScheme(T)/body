{
  for (int i=0, n=mySchemes.size(); i < n; i++) {
    T s=mySchemes.get(i);
    if (scheme.getName().equals(s.getName())) {
      onSchemeDeleted(s);
      mySchemes.remove(i);
      break;
    }
  }
}
