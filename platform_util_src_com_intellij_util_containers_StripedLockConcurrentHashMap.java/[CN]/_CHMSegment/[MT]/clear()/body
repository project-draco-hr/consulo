{
  if (count != 0) {
    try {
      lock();
      HashEntry[] tab=table;
      for (int i=0; i < tab.length; i++) {
        tab[i]=null;
      }
      count=0;
    }
  finally {
      unlock();
    }
  }
}
