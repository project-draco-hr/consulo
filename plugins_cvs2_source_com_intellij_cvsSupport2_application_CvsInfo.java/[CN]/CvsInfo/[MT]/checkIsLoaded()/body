{
  if (myIsLoaded)   return;
  try {
    loadCvsRoot();
    loadEntries();
    loadRepository();
    loadStikyTag();
    myCvsEntriesManager.watchForCvsAdminFiles(myParent);
  }
  finally {
    myIsLoaded=true;
  }
}
