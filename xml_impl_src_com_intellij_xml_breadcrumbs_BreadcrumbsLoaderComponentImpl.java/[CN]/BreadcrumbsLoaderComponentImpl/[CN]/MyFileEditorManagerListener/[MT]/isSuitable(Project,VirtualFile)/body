{
  final PsiFile psiFile=PsiManager.getInstance(project).findFile(file);
  return psiFile != null && (BreadcrumbsXmlWrapper.findInfoProvider(psiFile,myLoaderComponent) != null || file.getUserData(BREADCRUMBS_SUITABLE_FILE) != null);
}
