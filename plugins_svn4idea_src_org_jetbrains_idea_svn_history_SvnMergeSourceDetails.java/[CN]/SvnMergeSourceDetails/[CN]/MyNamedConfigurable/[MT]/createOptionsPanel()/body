{
  if (myPanel == null) {
    final SvnChangeList list=getList();
    if (list == null) {
      myPanel=new JPanel();
    }
 else {
      myPanel=new ChangeListViewerDialog(myProject,list).createCenterPanel();
    }
  }
  return myPanel;
}
