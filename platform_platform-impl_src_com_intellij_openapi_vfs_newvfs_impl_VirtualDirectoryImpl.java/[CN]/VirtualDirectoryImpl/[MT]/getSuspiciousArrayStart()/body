{
  int index=binSearch(myChildren,0,myChildren.length,new Comparer(){
    @Override public int compareMyKeyTo(    @NotNull VirtualFileSystemEntry v){
      return isSuspiciousName(v) ? -1 : 1;
    }
  }
);
  return -index - 1;
}
