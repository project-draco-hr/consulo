{
  return variable != null && variable.isValid() && !(variable instanceof PsiParameter) && !(variable instanceof ImplicitVariable) && expression != null && expression.isValid() && variable.getManager().isInProject(variable);
}
