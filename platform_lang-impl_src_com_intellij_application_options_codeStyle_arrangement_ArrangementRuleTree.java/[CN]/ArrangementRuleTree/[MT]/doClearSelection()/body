{
  mySelectionModel.clearSelection();
  myRenderers.forEachValue(new TObjectProcedure<ArrangementMatchNodeComponent>(){
    @Override public boolean execute(    ArrangementMatchNodeComponent node){
      node.setSelected(false);
      return true;
    }
  }
);
  myTree.repaint();
}
