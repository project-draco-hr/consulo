{
synchronized (myLock) {
    keepActiveVcs(new Runnable(){
      public void run(){
        final List<VcsDirectoryMapping> removed=myVcsToPaths.remove(name);
        sortedMappingsByMap();
      }
    }
);
  }
  mappingsChanged();
}
