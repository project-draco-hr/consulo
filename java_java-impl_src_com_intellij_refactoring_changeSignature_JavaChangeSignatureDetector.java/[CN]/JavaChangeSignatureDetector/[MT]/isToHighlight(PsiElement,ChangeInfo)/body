{
  element=element.getParent();
  LOG.assertTrue(changeInfo instanceof JavaChangeInfo);
  if (element instanceof PsiMethod) {
    final PsiMethod method=(PsiMethod)element;
    SearchScope useScope=method.getUseScope();
    if (useScope instanceof GlobalSearchScope) {
      final PsiSearchHelper.SearchCostResult cheapEnough=element.getManager().getSearchHelper().isCheapEnoughToSearch(((JavaChangeInfo)changeInfo).getOldName(),(GlobalSearchScope)useScope,method.getContainingFile(),null);
      if (cheapEnough == PsiSearchHelper.SearchCostResult.ZERO_OCCURRENCES)       return false;
    }
  }
  return true;
}
