{
  if (!sourceFile.isValid()) {
    LOG.error("Source file must be valid " + sourceFile.getPresentableUrl() + ", state.getClass() = "+ (extState != null ? extState.getClass().getName() : "null"));
  }
  myCache.update(sourceFile.getUrl(),new MyState(sourceFile.getTimeStamp(),extState));
}
