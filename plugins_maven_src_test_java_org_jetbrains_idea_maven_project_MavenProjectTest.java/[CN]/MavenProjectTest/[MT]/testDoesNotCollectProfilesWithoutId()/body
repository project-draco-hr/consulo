{
  importProject("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ "<profiles>"+ "  <profile>"+ "    <id>one</id>"+ "  </profile>"+ "  <profile>"+ "  </profile>"+ "</profiles>");
  assertOrderedElementsAreEqual(getMavenProject().getProfilesIds(),"one");
}
