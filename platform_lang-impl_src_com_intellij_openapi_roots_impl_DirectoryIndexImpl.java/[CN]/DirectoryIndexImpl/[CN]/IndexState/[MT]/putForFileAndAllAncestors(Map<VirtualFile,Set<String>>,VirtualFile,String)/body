{
  while (true) {
    Set<String> set=map.get(file);
    if (set == null) {
      set=new HashSet<String>();
      map.put(file,set);
    }
    set.add(value);
    file=file.getParent();
    if (file == null)     break;
  }
}
