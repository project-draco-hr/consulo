{
  int offset=currentPosition.offset;
  return offset < text.length() && text.charAt(offset) != '\n' || offset > tokenEndOffset || offset == tokenEndOffset && nextIsFoldRegion ? 0 : getInlaysPrefixWidthForOffset(offset + 1);
}
