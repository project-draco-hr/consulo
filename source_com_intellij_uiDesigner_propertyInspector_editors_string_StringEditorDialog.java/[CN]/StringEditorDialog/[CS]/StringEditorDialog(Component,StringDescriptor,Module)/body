{
  super(parent,true);
  if (module == null) {
    throw new IllegalArgumentException("module cannot be null");
  }
  myModule=module;
  myForm=new MyForm();
  setTitle("Edit Text");
  setValue(descriptor);
  init();
}
