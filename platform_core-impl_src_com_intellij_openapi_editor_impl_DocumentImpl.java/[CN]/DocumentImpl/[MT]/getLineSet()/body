{
  LineSet lineSet=myLineSet;
  if (lineSet == null) {
synchronized (myLineSetLock) {
      lineSet=myLineSet;
      if (lineSet == null) {
        lineSet=new LineSet();
        lineSet.documentCreated(this);
        myLineSet=lineSet;
      }
    }
  }
  return lineSet;
}
