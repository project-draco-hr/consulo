{
  final PrePrintCellModel prePrintCellModel=new PrePrintCellModel(hideLongEdges,layoutModel,rowIndex,selectController,commitSelectController);
  return new GraphPrintCell(){
    @Override public int countCell(){
      return prePrintCellModel.getCountCells();
    }
    @NotNull @Override public List<ShortEdge> getUpEdges(){
      return GraphPrintCellModelImpl.this.getUpEdges(rowIndex);
    }
    @NotNull @Override public List<ShortEdge> getDownEdges(){
      return prePrintCellModel.downShortEdges();
    }
    @NotNull @Override public List<SpecialPrintElement> getSpecialPrintElements(){
      return prePrintCellModel.getSpecialPrintElements();
    }
  }
;
}
