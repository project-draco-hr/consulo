{
  boolean declaration=DECLARATION_START.isAcceptable(position,position) || psiElement().withParents(PsiJavaCodeReferenceElement.class,PsiTypeElement.class,PsiMember.class).accepts(position);
  if (START_FOR.accepts(position) || INSIDE_PARAMETER_LIST.accepts(position) && !AFTER_DOT.accepts(position) || VARIABLE_AFTER_FINAL.accepts(position) || declaration) {
    for (    String primitiveType : PRIMITIVE_TYPES) {
      result.addElement(new OverrideableSpace(createKeyword(position,primitiveType)));
    }
  }
  if (declaration) {
    result.addElement(new OverrideableSpace(createKeyword(position,PsiKeyword.VOID)));
  }
}
