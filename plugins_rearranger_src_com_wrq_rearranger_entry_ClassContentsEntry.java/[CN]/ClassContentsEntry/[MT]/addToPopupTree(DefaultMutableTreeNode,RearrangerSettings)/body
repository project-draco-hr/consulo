{
  DefaultMutableTreeNode node=null;
  if ((myEnd instanceof PsiField && settings.isShowFields()) || (this instanceof ClassEntry)) {
    node=new RearrangerTreeNode(this,myName);
    parent.add(node);
  }
  return node;
}
