{
  DefaultMutableTreeNode node=null;
  if ((end instanceof PsiField && settings.isShowFields()) || (this instanceof ClassEntry)) {
    node=new RearrangerTreeNode(this,name);
    parent.add(node);
  }
  return node;
}
