{
  final GrDynamicImplicitMethodImpl method=DynamicManager.getInstance(myManager.getProject()).getCashedOrCreateMethod(myManager,getName(),getType().getCanonicalText(),getContainingClassName(),getPairs(),getContainingFile());
  return method == this;
}
