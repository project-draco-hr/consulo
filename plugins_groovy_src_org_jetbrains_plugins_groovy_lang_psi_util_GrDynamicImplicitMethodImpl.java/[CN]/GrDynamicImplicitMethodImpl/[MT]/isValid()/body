{
  final GrDynamicImplicitMethodImpl method=DynamicManager.getInstance(myManager.getProject()).getMethod(myManager,getName(),getType().getCanonicalText(),getContainingClassName(),getPairs(),getContainingFile());
  return method == this;
}
