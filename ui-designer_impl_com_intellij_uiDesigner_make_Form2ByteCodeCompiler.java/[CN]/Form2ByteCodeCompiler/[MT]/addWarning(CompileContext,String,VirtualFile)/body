{
  if (formFile != null) {
    context.addMessage(CompilerMessageCategory.WARNING,formFile.getPresentableUrl() + ": " + message,formFile.getUrl(),-1,-1);
  }
 else {
    context.addMessage(CompilerMessageCategory.WARNING,message,null,-1,-1);
  }
}
