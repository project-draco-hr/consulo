{
  String iconUrl=myJiraIssue.getTypeIconUrl();
  final Icon icon=iconUrl == null ? JiraConnectorIcons.Jira_blue_16 : isClosed() ? CachedIconLoader.getDisabledIcon(iconUrl) : CachedIconLoader.getIcon(iconUrl);
  return icon != null ? icon : TasksIcons.Other;
}
