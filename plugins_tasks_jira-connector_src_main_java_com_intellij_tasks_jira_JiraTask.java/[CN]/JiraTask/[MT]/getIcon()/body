{
  String iconUrl=myJiraIssue.getTypeIconUrl();
  return iconUrl == null ? JiraConnectorIcons.Jira_blue_16 : isClosed() ? CachedIconLoader.getDisabledIcon(iconUrl) : CachedIconLoader.getIcon(iconUrl);
}
