{
  while (element != null) {
    final PsiElement psiElement=element.getProperty(name);
    if (psiElement != null) {
      return psiElement;
    }
    element=element.getAntParent();
  }
  return null;
}
