{
  if (!myStoppedOnError) {
    if (myAccomulatedFiles.isEmpty()) {
      return;
    }
    if (myInfo.isUnderProjectRoot()) {
      showLocalCommit();
    }
 else {
      showAlienCommit();
    }
  }
  if (!myInfo.isUnderProjectRoot()) {
    finishAfterCommit();
  }
}
