{
  return subTag.getAttributeValue(IdReferenceProvider.ID_ATTR_NAME) != null || (subTag.getAttributeValue(IdReferenceProvider.NAME_ATTR_NAME) != null && !(subTag instanceof JspDirective) && subTag.getName().indexOf(".directive") == -1);
}
