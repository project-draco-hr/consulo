{
  ++myAttempts;
  int index=searchForCacheEntry(key);
  if (index == 0) {
    return null;
  }
  ++myHits;
  if (index != myTop) {
    removeEntry(index);
    add2Top(index);
  }
  return myCache[index].value;
}
