{
  if (!(file instanceof XmlFile))   return false;
  String name=file.getName();
  return name.equals(MavenConstants.POM_XML) || name.endsWith("." + MavenConstants.POM_EXTENSION) || name.equals(MavenConstants.PROFILES_XML)|| name.equals(MavenConstants.SETTINGS_XML);
}
