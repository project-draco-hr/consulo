{
  JPanel panel=new JPanel(new GridBagLayout());
  myButton=new ComboBoxButton(presentation);
  panel.add(myButton,new GridBagConstraints(0,0,1,1,1,1,GridBagConstraints.CENTER,GridBagConstraints.BOTH,new Insets(0,3,0,3),0,0));
  return panel;
}
