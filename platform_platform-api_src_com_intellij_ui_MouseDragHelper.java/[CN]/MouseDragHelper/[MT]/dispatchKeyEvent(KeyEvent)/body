{
  if (e.getKeyCode() == KeyEvent.VK_ESCAPE && e.getID() == KeyEvent.KEY_PRESSED) {
    myCancelled=true;
    if (myDetachingMode) {
      processDragOutCancel();
    }
 else {
      processDragCancel();
    }
    resetDragState();
    return true;
  }
  return false;
}
