{
  final VirtualFile virtualFile=LocalFileSystem.getInstance().findFileByPath(myMethod.replace(File.separatorChar,'/'));
  if (virtualFile != null)   return PsiLocation.fromPsiElement(project,PsiManager.getInstance(project).findFile(virtualFile));
  return null;
}
