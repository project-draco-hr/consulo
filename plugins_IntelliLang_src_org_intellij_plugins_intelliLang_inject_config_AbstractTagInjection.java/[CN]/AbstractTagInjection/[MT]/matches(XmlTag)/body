{
  if (tag == null) {
    return false;
  }
  if (!myTagName.matches(tag.getLocalName())) {
    return false;
  }
  if (!myTagNamespace.equals(tag.getNamespace())) {
    return false;
  }
  return true;
}
