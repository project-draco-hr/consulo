{
  Runnable runnable=new Runnable(){
    public void run(){
      final Object o=saveState();
      try {
        execute();
      }
  finally {
        restoreState(o);
      }
    }
  }
;
  SwingUtilities.invokeLater(runnable);
}
