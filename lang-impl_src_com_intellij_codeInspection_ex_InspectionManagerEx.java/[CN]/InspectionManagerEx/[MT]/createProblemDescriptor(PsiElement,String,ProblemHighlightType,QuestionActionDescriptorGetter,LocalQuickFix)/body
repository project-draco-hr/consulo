{
  final ProblemDescriptorImpl descriptor=new ProblemDescriptorImpl(psiElement,psiElement,descriptionTemplate,fixes,highlightType,false,null);
  descriptor.setQuestionActionDescriptorGetter(questionActionDescriptorGetter);
  return descriptor;
}
