{
  myVariants=(variants == null) ? Collections.<LookupElement>emptyList() : ContainerUtil.map(variants,new Function<String,LookupElement>(){
    public LookupElement fun(    final String s){
      return LookupElementBuilder.create(s);
    }
  }
);
}
