{
  if (isGetter()) {
    if (settings.isKeepGettersSettersTogether()) {
      determineSetter(contents,settings);
    }
  }
  if (!isNoExtractedMethods() && settings.getExtractedMethodsSettings().isMoveExtractedMethods()) {
    determineMethodCalls(contents,settings);
  }
}
