{
  insertElementAt(descriptor,0);
  for (int i=1; i < size(); i++) {
    if (descriptor.equals(getElementAt(i))) {
      remove(i);
      return;
    }
  }
  if (size() > 1) {
    remove(size() - 1);
  }
}
