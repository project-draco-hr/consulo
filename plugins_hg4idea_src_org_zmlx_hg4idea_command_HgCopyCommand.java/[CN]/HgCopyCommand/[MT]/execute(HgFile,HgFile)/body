{
  HgCommandService service=HgCommandService.getInstance(project);
  if (source.getRepo().equals(target.getRepo())) {
    service.execute(source.getRepo(),"copy",Arrays.asList("--after",source.getRelativePath(),target.getRelativePath()));
  }
}
