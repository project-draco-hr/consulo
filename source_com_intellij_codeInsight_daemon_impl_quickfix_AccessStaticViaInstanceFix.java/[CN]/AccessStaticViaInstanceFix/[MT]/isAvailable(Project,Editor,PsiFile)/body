{
  return myExpression != null && myExpression.isValid() && myExpression.getManager().isInProject(myExpression) && myMember != null && myMember.isValid() && myMember.getContainingClass() != null && PsiUtil.isAccessible(myMember.getContainingClass(),myExpression,myMember.getContainingClass());
}
