{
synchronized (myLock) {
    final List<String> wasData=myState.get("");
    if (wasData == null || (!wasData.equals(data))) {
      if (wasData != null) {
        data.addAll(wasData);
      }
      Collections.sort(data);
      myState.put("",data);
      myState.force();
    }
  }
}
