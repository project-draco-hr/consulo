{
  final DropLocation location=(point != null) ? GridInsertProcessor.getDropLocation(myEditor.getRootContainer(),point,item) : new GridDropLocation(targetContainer,null,true);
  processComponentInsert(item,location);
}
