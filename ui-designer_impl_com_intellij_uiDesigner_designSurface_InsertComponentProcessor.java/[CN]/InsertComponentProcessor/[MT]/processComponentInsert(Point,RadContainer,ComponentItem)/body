{
  final GridLocation location=(point != null) ? GridInsertProcessor.getGridInsertLocation(myEditor,point,item) : new GridLocation(targetContainer,null,GridInsertMode.InCell);
  processComponentInsert(item,location);
}
