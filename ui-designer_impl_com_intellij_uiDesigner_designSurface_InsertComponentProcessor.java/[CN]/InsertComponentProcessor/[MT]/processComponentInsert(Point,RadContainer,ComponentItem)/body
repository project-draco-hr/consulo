{
  final DropLocation location=(point != null) ? GridInsertProcessor.getDropLocation(myEditor.getRootContainer(),point) : new GridDropLocation(targetContainer,0,0);
  processComponentInsert(item,location);
}
