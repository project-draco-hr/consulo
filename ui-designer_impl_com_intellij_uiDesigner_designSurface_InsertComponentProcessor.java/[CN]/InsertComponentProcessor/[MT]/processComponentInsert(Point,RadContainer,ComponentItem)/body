{
  final DropLocation location=(point != null) ? GridInsertProcessor.getDropLocation(myEditor.getRootContainer(),point,item) : new DropLocation(targetContainer,null,true);
  processComponentInsert(item,location);
}
