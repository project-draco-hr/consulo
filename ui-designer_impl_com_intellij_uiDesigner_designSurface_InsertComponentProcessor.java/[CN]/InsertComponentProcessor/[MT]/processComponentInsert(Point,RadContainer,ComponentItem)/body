{
  final DropLocation location=(point != null) ? GridInsertProcessor.getGridInsertLocation(myEditor,point,item) : new DropLocation(targetContainer,null,true);
  processComponentInsert(item,location);
}
