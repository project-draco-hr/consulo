{
  if (offset >= 3) {
    String quote=iterator.getDocument().getText(new TextRange(offset - 3,offset));
    if ("'''".equals(quote))     return quote;
    if ("\"\"\"".equals(quote))     return quote;
  }
  return null;
}
