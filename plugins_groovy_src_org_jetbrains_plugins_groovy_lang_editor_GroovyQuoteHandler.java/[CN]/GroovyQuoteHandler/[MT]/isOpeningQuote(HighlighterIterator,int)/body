{
  final IElementType tokenType=iterator.getTokenType();
  if (tokenType == GroovyTokenTypes.mWRONG_GSTRING_LITERAL || tokenType == GroovyTokenTypes.mWRONG_STRING_LITERAL) {
    int start=iterator.getStart();
    return offset == start;
  }
  return false;
}
