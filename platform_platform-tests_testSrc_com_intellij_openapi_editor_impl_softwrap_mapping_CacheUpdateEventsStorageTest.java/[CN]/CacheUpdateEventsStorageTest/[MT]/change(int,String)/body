{
  DocumentEventImpl event=new DocumentEventImpl(myDocument,offset,myText.substring(offset,offset + newText.length()),newText,1,false);
  myStorage.add(myDocument,new IncrementalCacheUpdateEvent(event));
}
