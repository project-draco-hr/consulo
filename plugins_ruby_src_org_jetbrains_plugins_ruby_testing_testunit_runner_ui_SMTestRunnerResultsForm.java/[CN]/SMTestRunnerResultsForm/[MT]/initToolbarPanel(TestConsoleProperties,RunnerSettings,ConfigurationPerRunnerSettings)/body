{
  toolbarPanel.setLayout(new BorderLayout());
  final RTestUnitToolbarPanel toolbar=new RTestUnitToolbarPanel(consoleProperties,runnerSettings,configurationSettings,this);
  toolbarPanel.add(toolbar);
  return toolbar;
}
