{
  return Comparing.hashcode(o.getGroupId()) + 31 * Comparing.hashcode(o.getArtifactId()) + 31 * Comparing.hashcode(o.getVersion()) + 31 * Comparing.hashcode(o.getPackaging()) + 31 * Comparing.hashcode(o.getClassifier());
}
