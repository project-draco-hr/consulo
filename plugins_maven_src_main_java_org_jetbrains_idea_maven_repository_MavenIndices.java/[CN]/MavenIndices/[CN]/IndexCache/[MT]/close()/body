{
  try {
    if (groupIds != null)     groupIds.close();
  }
  finally {
    try {
      if (artifactIds != null)       artifactIds.close();
    }
  finally {
      if (versions != null)       versions.close();
    }
  }
}
