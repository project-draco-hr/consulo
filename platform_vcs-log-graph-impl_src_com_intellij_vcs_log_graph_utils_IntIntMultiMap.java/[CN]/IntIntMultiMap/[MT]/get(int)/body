{
  final int[] asArray=getAsArray(key);
  return new AbstractList<Integer>(){
    @NotNull @Override public Integer get(    int index){
      return asArray[index];
    }
    @Override public int size(){
      return asArray.length;
    }
  }
;
}
