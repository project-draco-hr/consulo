{
  final Pair<AbstractHash,AbstractHash> pair=myStashTops.get(commitI.selectRepository(myRootsHolder.getRoots()));
  return pair != null && (pair.getFirst() != null && pair.getFirst().equals(commitI.getHash()) || pair.getSecond() != null && pair.getSecond().equals(commitI.getHash()));
}
