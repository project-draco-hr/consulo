{
  return new ReadonlyList<CommitI>(){
    @Override public CommitI get(    int idx){
      return myLines.get(myIdxMap.get(new InnerIdx(root,idx)));
    }
    @Override public int getSize(){
      return myRunningRepoIdxs.get(root);
    }
  }
;
}
