{
  super(select ? "Select All" : "Unselect All",select ? "Select all items" : "Unselect all items",IconLoader.getIcon(select ? "/actions/selectall.png" : "/actions/unselectall.png"));
  mySelect=select;
}
