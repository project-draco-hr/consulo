{
  if (UpdateChecker.checkNeeded()) {
  }
  queueNextUpdateCheck(UpdateChecker.checkNeeded() ? CHECK_INTERVAL : UpdateSettings.getInstance().LAST_TIME_CHECKED - System.currentTimeMillis() + CHECK_INTERVAL);
}
