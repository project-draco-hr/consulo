{
synchronized (intervals) {
    if (myIntervalTree.modCount != treeModCount)     return false;
    this.deltaUpToRoot=deltaUpToRoot;
    this.allDeltasUpAreNull=allDeltasUpAreNull;
    modCount=treeModCount;
    return true;
  }
}
