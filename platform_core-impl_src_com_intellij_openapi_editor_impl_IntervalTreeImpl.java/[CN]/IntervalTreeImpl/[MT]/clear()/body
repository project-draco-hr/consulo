{
  process(new Processor<T>(){
    @Override public boolean process(    T t){
      reportInvalidation(t,"Clear all");
      return true;
    }
  }
);
  l.writeLock().lock();
  try {
    super.clear();
    keySize=0;
  }
  finally {
    l.writeLock().unlock();
  }
}
