{
  l.writeLock().lock();
  process(new Processor<T>(){
    @Override public boolean process(    T t){
      beforeRemove(t,"Clear all");
      return true;
    }
  }
);
  try {
    super.clear();
    keySize=0;
  }
  finally {
    l.writeLock().unlock();
  }
}
