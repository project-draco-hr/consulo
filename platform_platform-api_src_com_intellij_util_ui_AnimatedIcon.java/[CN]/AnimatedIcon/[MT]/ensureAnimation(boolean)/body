{
  boolean changes=myAnimator.isRunning() != running;
  if (running) {
    setOpaque(true);
    myAnimator.resume();
  }
 else {
    setOpaque(myPaintPassive);
    myAnimator.suspend();
  }
  return changes;
}
