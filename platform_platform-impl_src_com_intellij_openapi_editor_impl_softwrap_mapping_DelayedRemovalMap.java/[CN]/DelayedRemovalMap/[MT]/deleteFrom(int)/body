{
  myMap.retainEntries(new TIntObjectProcedure<T>(){
    @Override public boolean execute(    int a,    T b){
      if (a >= key) {
        myKeysToRemove.remove(a);
        return false;
      }
      return true;
    }
  }
);
}
