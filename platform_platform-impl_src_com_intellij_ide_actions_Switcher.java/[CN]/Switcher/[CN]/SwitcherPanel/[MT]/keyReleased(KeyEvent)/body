{
  if ((e.getKeyCode() == CTRL_KEY && UISettings.getInstance().HIDE_SWITCHER_ON_CONTROL_RELEASE) || e.getKeyCode() == VK_ENTER) {
    navigate();
  }
 else   if (e.getKeyCode() == VK_LEFT) {
    goLeft();
  }
 else   if (e.getKeyCode() == VK_RIGHT) {
    goRight();
  }
}
