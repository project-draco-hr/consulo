{
  if (mySpeedSearch != null && mySpeedSearch.isPopupActive() || lastEvent == e)   return;
  lastEvent=e;
switch (e.getKeyCode()) {
case VK_DELETE:
case VK_BACK_SPACE:
case VK_Q:
    closeTabOrToolWindow();
  break;
case VK_ESCAPE:
cancel();
break;
}
}
