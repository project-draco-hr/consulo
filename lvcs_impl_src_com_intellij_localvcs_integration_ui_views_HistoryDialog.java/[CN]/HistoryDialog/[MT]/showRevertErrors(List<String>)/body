{
  String formatted="";
  if (errors.size() == 1) {
    formatted+=errors.get(0);
  }
 else {
    for (    String e : errors) {
      formatted+="\n    -" + e;
    }
  }
  myGateway.showError("Can not revert because " + formatted);
}
