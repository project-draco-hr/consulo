{
  return new CompositeExtensionPointNameWithArea<E>(epName,clazz){
    @Override protected boolean validateArea(    @Nullable AreaInstance areaInstance){
      return areaInstance instanceof Module;
    }
  }
;
}
