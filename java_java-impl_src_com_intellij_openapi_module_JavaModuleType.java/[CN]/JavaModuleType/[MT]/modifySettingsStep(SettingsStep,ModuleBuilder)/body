{
  return ProjectWizardStepFactory.getInstance().createJavaSettingsStep(settingsStep,moduleBuilder,new Condition<SdkType>(){
    @Override public boolean value(    SdkType sdk){
      return sdk instanceof JavaSdkType;
    }
  }
);
}
