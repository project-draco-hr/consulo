{
  type.updateClassUsages(res,s);
  for (int i=0; i < argumentTypes.length; i++) {
    argumentTypes[i].updateClassUsages(res,s);
  }
  if (exceptions != null)   for (  TypeRepr.AbstractType typ : exceptions) {
    typ.updateClassUsages(res,s);
  }
}
