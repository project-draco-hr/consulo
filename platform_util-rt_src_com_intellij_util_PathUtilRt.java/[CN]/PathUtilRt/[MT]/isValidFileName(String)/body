{
  if (fileName.length() == 0) {
    return false;
  }
  for (int i=0; i < fileName.length(); i++) {
    if (!isValidFileNameChar(fileName.charAt(i))) {
      return false;
    }
  }
  return true;
}
