{
  if (c == '/' || c == '\\')   return false;
  if ((strict || os == Platform.WINDOWS) && (c < 32 || WINDOWS_CHARS.indexOf(c) >= 0))   return false;
  if (strict && c == ';')   return false;
  return true;
}
