{
  dekllist.checkcontext();
  explist.checkcontext(functions);
  arguments.checkcontext(new SymTab());
  if (arguments.length() != inputs.size())   Main.error("Argument list and input variables list differ!");
}
