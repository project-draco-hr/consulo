{
  if (Math.abs(f) < FloatConsts.MIN_NORMAL && f != 0.0f) {
    String s=doubleToHexString(FpUtils.scalb((double)f,DoubleConsts.MIN_EXPONENT - FloatConsts.MIN_EXPONENT));
    return s.replaceFirst("p-1022$","p-126");
  }
 else   return doubleToHexString(f);
}
