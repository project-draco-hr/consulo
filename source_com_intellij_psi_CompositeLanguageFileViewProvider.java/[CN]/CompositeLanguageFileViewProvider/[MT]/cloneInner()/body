{
  final CompositeLanguageFileViewProvider viewProvider=new CompositeLanguageFileViewProvider(getManager(),new LightVirtualFile(getVirtualFile().getName(),getVirtualFile().getFileType(),getContents(),getModificationStamp()),false);
  return viewProvider;
}
