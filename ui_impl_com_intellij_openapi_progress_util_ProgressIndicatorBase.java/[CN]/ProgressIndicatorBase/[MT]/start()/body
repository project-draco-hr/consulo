{
synchronized (this) {
    LOG.assertTrue(!isRunning());
    myText="";
    myFraction=0;
    myText2="";
    myCanceled=false;
    myRunning=true;
    if (myStateDelegate != null) {
      myStateDelegate.start();
    }
    onStateChange();
  }
  enterModality();
}
