{
  LOG.assertTrue(!isRunning());
synchronized (this) {
    myText="";
    myFraction=0;
    myText2="";
    myCanceled=false;
    myRunning=true;
    if (myStateDelegate != null) {
      myStateDelegate.start();
    }
    onStateChange();
  }
  enterModality();
}
