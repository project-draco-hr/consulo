{
  final Project project=psiMethod.getProject();
  ((JUnitConfiguration)context.getConfiguration().getConfiguration()).beMethodConfiguration(new MethodLocation(project,psiMethod,new PsiLocation<PsiClass>(project,aClass)));
  performRunnable.run();
}
