{
  LOG.assertTrue(range1 != null);
  LOG.assertTrue(range2 != null);
  mySides=new Side[]{createSide(changeList,range1,FragmentSide.SIDE1),createSide(changeList,range2,FragmentSide.SIDE2)};
  myType=type;
  myChangeList=changeList;
}
