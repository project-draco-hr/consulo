{
  for (Iterator<PsiElement> iterator=myElements.iterator(); iterator.hasNext(); ) {
    PsiElement element=iterator.next();
    if (!(element instanceof PsiAntElement) && !(element instanceof XmlElement))     return false;
  }
  return true;
}
