def converted(self, rev, destrev):
    if (self.convertfp is None):
        self.convertfp = open(os.path.join(self.path, '.hg', 'shamap'), 'a')
    self.convertfp.write(('%s %s\n' % (destrev, rev)))
    self.convertfp.flush()
