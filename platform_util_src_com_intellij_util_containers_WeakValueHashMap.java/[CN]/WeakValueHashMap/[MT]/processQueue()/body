{
  while (true) {
    MyReference ref=(MyReference)myQueue.poll();
    if (ref == null) {
      return;
    }
    @SuppressWarnings("unchecked") K key=(K)ref.key;
    if (myMap.get(key) == ref) {
      myMap.remove(key);
    }
  }
}
