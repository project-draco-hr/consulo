{
  Set<String> namedParameters=psi.getNamedParameters().keySet();
  return new GrMethodStub(parentStub,StringRef.fromString(psi.getName()),GrStubUtils.getAnnotationNames(psi),namedParameters.toArray(new String[namedParameters.size()]),this);
}
