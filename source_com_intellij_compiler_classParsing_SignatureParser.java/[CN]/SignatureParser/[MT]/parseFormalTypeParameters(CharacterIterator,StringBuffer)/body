{
  if (it.current() != '<') {
    throw new SignatureParsingException("'<' expected");
  }
  buf.append(it.current());
  it.next();
  while (it.current() != '>') {
    parseFormalTypeParameter(it,buf);
  }
  buf.append(it.current());
  it.next();
}
