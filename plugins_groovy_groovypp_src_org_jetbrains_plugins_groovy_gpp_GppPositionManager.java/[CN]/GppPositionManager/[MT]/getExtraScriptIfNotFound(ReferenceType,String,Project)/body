{
  final PsiClass trait=JavaPsiFacade.getInstance(project).findClass(StringUtil.trimEnd(runtimeName,TRAIT_IMPL),GlobalSearchScope.allScope(project));
  if (trait != null) {
    return trait.getContainingFile();
  }
  return null;
}
