{
  try {
synchronized (myLock) {
      if (myLocalFile == null) {
        myLocalFile=myManager.getStorage().createLocalFile(myUrl);
      }
      FileUtil.writeToFile(myLocalFile,content.getBytes());
    }
    updateState(StdFileTypes.HTML);
  }
 catch (  IOException e) {
    LOG.info(e);
  }
}
