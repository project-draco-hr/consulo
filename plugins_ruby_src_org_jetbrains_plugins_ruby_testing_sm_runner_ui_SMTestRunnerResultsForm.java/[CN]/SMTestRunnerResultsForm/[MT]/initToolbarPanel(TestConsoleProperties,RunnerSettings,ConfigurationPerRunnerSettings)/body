{
  toolbarPanel.setLayout(new BorderLayout());
  final SMTRunnerToolbarPanel toolbar=new SMTRunnerToolbarPanel(consoleProperties,runnerSettings,configurationSettings,this);
  toolbarPanel.add(toolbar);
  return toolbar;
}
