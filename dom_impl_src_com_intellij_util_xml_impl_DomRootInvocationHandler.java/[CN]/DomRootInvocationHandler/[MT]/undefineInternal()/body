{
  try {
    final XmlTag tag=getXmlTag();
    if (tag != null) {
      deleteTag(tag);
      detachChildren();
      fireUndefinedEvent();
    }
  }
 catch (  Exception e) {
    LOG.error(e);
  }
}
