{
  Configuration cfg=null;
  try {
    cfg=load(getClass().getResourceAsStream("/" + COMPONENT_NAME + ".xml"));
  }
 catch (  Exception e) {
    LOG.warn(e);
  }
  if (cfg == null)   return;
  importFrom(cfg);
}
