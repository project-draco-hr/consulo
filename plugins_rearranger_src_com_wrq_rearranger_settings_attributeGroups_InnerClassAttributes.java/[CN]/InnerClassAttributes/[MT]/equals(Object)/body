{
  if (!(obj instanceof InnerClassAttributes)) {
    return false;
  }
  final InnerClassAttributes ica=(InnerClassAttributes)obj;
  return super.equals(ica) && abAttr.equals(ica.abAttr) && enumAttr.equals(ica.enumAttr);
}
