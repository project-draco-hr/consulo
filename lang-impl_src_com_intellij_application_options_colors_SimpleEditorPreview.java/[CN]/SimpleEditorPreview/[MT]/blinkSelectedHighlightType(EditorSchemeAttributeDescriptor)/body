{
  if (description == null)   return;
  String type=description.getType();
  java.util.List<HighlightData> highlights=startBlinkingHighlights(myEditor,myHighlightData,type,myPage.getHighlighter(),true,myBlinkingAlarm,BLINK_COUNT,myPage);
  scrollHighlightInView(highlights,type,myEditor);
}
