{
  return new PredefinedValueAction(Collections.singleton(name)){
    @Override public void actionPerformed(    @NotNull AnActionEvent e){
      myFilterModel.setFilter(BranchFilterPopupComponent.this.createFilter(myValues));
    }
  }
;
}
