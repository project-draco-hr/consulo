def merge(self, f):
    'Mark a file merged.'
    self._dirty = True
    s = os.lstat(self._join(f))
    self._addpath(f)
    self._map[f] = ('m', s.st_mode, s.st_size, int(s.st_mtime))
    if (f in self._copymap):
        del self._copymap[f]
