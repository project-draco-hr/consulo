def getcwd(self):
    cwd = os.getcwd()
    if (cwd == self._root):
        return ''
    rootsep = self._root
    if (not util.endswithsep(rootsep)):
        rootsep += os.sep
    if cwd.startswith(rootsep):
        return cwd[len(rootsep):]
    else:
        return cwd
