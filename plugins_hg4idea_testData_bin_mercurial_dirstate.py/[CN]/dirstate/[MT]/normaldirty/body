def normaldirty(self, f):
    'Mark a file normal, but dirty.'
    self._dirty = True
    self._addpath(f)
    self._map[f] = ('n', 0, (-2), (-1))
    if (f in self._copymap):
        del self._copymap[f]
