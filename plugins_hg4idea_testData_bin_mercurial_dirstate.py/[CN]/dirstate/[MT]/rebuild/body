def rebuild(self, parent, files):
    self.clear()
    for f in files:
        if ('x' in files.flags(f)):
            self._map[f] = ('n', 511, (-1), 0)
        else:
            self._map[f] = ('n', 438, (-1), 0)
    self._pl = (parent, nullid)
    self._dirty = True
