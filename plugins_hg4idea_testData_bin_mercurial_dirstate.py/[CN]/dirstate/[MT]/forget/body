def forget(self, f):
    'Forget a file.'
    self._dirty = True
    try:
        self._droppath(f)
        del self._map[f]
    except KeyError:
        self._ui.warn((_('not in dirstate: %s\n') % f))
