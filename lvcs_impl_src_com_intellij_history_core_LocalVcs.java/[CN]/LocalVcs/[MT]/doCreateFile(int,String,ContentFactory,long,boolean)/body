{
  String parent=Paths.getParentOf(path);
  if (parent != null && !checkEntryExists(parent))   return;
  Content c=createContentFrom(f);
  applyChange(new CreateFileChange(id,path,c,timestamp,isReadOnly));
}
