{
  readLock();
  try {
    List<MavenProjectModel> modules=myModuleMapping.get(aggregator);
    return modules == null ? Collections.<MavenProjectModel>emptyList() : new ArrayList<MavenProjectModel>(modules);
  }
  finally {
    readUnlock();
  }
}
