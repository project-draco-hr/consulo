{
  if (updatedProjects.isEmpty() && deletedProjects.isEmpty())   return;
  fireProjectsUpdated(updatedProjects.isEmpty() ? Collections.EMPTY_LIST : new ArrayList<MavenProject>(updatedProjects),deletedProjects.isEmpty() ? Collections.EMPTY_LIST : new ArrayList<MavenProject>(deletedProjects));
}
