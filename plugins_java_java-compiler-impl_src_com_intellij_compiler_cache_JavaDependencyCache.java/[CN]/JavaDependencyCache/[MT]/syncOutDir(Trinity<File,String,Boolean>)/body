{
  final String className=trinity.getSecond();
  if (className != null) {
    final int id=getSymbolTable().getId(className);
    addTraverseRoot(id);
    final boolean sourcePresent=trinity.getThird().booleanValue();
    if (!sourcePresent) {
      markSourceRemoved(id);
    }
  }
}
