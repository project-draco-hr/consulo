{
  if (!WindowManager.isFullScreenSupportedInCurrentOS() || project == null)   return false;
  return project.getUserData(SHOULD_OPEN_IN_FULLSCREEN) == Boolean.TRUE || PropertiesComponent.getInstance(project).getBoolean(FULL_SCREEN,false);
}
