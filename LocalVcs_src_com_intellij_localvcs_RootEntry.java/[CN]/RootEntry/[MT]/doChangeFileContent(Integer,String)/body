{
  Entry oldEntry=getEntry(id);
  Entry newEntry=new FileEntry(oldEntry.getId(),oldEntry.getName(),content);
  Path path=oldEntry.getPath();
  removeEntry(id);
  addEntry(path.getParent(),newEntry);
}
