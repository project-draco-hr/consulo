{
  String name=Path.getNameOf(path);
  String parentPath=Path.getParentOf(path);
  if (parentPath == null || !hasEntry(parentPath)) {
    parentPath=null;
    name=path;
  }
  DirectoryEntry e=new DirectoryEntry(id,name,timestamp);
  addEntry(parentPath,e);
}
