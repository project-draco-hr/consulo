{
synchronized (myLock) {
    myHistoryCache.put(new BaseKey(filePath,vcsKey),new CachedHistory(filePath,session.getRevisionList(),session.getCurrentRevisionNumber(),factory.getAddinionallyCachedData(session)));
  }
}
