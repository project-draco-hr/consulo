{
  final String text=getText();
  if (text.startsWith("'")) {
    if (!text.endsWith("'")) {
      return false;
    }
  }
 else   if (text.startsWith("\"")) {
    if (!text.endsWith("\"")) {
      return false;
    }
  }
  return text.indexOf('\n') == -1 && text.indexOf("\r") == -1;
}
