{
  super(file,initialSize,lockContext,pageSize,valuesAreBufferAligned);
  myReadStream=new MyDataIS(this);
  myFileLength=(int)length();
  myCompressedAppendableFile=false ? new MyCompressedAppendableFile(file) : null;
}
