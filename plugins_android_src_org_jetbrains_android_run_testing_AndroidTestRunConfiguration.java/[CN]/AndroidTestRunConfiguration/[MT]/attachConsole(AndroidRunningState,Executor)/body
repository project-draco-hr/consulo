{
  TestConsoleProperties properties=new AndroidTestConsoleProperties(this,executor);
  BaseTestsOutputConsoleView consoleView=SMTestRunnerConnectionUtil.attachRunner("Android",state.getProcessHandler(),properties,state.getRunnerSettings(),state.getConfigurationSettings());
  Disposer.register(state.getAndroidFacet().getModule().getProject(),consoleView);
  return consoleView;
}
