{
  myFile=null;
  myEditor=null;
  int offset=-1;
  for (  String filePath : filePaths) {
    int fileOffset=configureByFileInner(filePath);
    if (fileOffset > 0) {
      offset=fileOffset;
    }
  }
  return offset;
}
