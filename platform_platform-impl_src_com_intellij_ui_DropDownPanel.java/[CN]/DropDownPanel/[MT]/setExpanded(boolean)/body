{
  if (myExpanded == expanded) {
    return;
  }
  myLabel.setIcon(expanded ? UP : DOWN);
  if (myContent != null) {
    if (expanded) {
      add(myContent,BorderLayout.CENTER);
    }
 else {
      remove(myContent);
    }
  }
  myExpanded=expanded;
  revalidate();
  repaint();
}
