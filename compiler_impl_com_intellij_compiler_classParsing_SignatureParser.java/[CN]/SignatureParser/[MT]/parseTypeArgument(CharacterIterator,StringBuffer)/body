{
  if (it.current() == '*') {
    parseWildcardIndicator(it,buf);
  }
 else {
    if (it.current() == '+' || it.current() == '-') {
      parseWildcardIndicator(it,buf);
    }
    parseFieldTypeSignature(it,buf);
  }
}
