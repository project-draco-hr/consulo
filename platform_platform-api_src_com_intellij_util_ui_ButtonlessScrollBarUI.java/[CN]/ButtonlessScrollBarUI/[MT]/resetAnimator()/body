{
  myAnimator.reset();
  if (scrollbar != null && scrollbar.getValueIsAdjusting() || myMouseIsOverThumb) {
    myAnimator.suspend();
    myAnimationColorShift=40;
  }
 else {
    myAnimator.resume();
  }
}
