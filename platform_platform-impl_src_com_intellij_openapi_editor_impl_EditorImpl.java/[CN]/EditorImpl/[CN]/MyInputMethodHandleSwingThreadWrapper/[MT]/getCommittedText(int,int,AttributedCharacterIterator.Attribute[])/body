{
  return execute(new Computable<AttributedCharacterIterator>(){
    @Override public AttributedCharacterIterator compute(){
      return myDelegate.getCommittedText(beginIndex,endIndex,attributes);
    }
  }
);
}
