{
  return "use soft wraps: " + (mySoftWrapModel.isSoftWrappingEnabled() ? "on" : "off") + ", soft wraps data: "+ getSoftWrapModel()+ ", folding data: "+ getFoldingModel()+ ", document info: "+ myDocument.dumpState();
}
