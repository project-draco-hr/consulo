{
  return myDocument instanceof DocumentImpl && !isInplaceRenamerActive() && !contains(e.getOldFragment(),'\n')&& !contains(e.getNewFragment(),'\n')&& !(e.getNewLength() == 1 && DOCUMENT_CHARS_TO_SKIP.contains(e.getNewFragment().charAt(0)));
}
