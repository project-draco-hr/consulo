{
  if (isWhitespaceAt(chars,x,y)) {
    UIUtil.drawDottedLine(g,x,y,x,y + getLineHeight(),getBackroundColor(),selected ? getForegroundColor() : myScheme.getColor(EditorColors.WHITESPACES_COLOR));
  }
}
