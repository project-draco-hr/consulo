{
  Dimension size=mySizeContainer.getContentSize();
  return new Dimension(size.width,size.height + (mySettings.getAdditionalLinesCount() + mySoftWrapModel.getSoftWrapsIntroducedLinesNumber()) * getLineHeight());
}
