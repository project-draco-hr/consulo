{
  PsiElement parent=position.getParent();
  return parent instanceof GrReferenceElement && ((GrReferenceElement)parent).getQualifier() == null && !PsiJavaPatterns.psiElement().inside(GrImportStatement.class).accepts(position);
}
