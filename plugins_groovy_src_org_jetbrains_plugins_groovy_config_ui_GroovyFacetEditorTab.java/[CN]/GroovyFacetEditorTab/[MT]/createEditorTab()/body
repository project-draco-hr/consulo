{
  GroovyApplicationSettings settings=GroovyApplicationSettings.getInstance();
  return new GroovyFacetEditorTab(settings.GROOVY_VERSIONS.toArray(new String[settings.GROOVY_VERSIONS.size()]),settings.DEFAULT_GROOVY_VERSION);
}
