{
  if (rangeMarker instanceof SelectionModelImpl.MyRangeMarker)   return;
  ApplicationManagerEx.getApplicationEx().assertReadAccessToDocumentsAllowed();
  myRangeMarkers.add((RangeMarkerImpl)rangeMarker);
}
