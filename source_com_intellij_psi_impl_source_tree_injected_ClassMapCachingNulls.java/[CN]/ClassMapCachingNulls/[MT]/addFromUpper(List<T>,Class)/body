{
  List<T> fromUpper=get(superclass);
  if (fromUpper != null) {
    if (value == null) {
      value=new ArrayList<T>(fromUpper);
    }
 else {
      for (      T t : fromUpper) {
        if (!value.contains(t)) {
          value.add(t);
        }
      }
    }
  }
  return value;
}
