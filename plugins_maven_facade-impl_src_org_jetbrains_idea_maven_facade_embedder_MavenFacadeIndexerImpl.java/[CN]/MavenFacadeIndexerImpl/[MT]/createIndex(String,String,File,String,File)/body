{
  try {
    IndexingContext context=myIndexer.addIndexingContext(indexId,repositoryId,file,indexDir,url,null,NexusIndexer.FULL_INDEX);
    int id=System.identityHashCode(context);
    myIndices.put(id,context);
    return id;
  }
 catch (  Exception e) {
    throw new MavenFacadeIndexerException(wrapException(e));
  }
}
