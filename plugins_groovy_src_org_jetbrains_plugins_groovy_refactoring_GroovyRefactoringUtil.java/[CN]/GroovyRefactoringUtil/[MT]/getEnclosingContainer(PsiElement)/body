{
  PsiElement parent=place.getParent();
  while (parent != null && !(parent instanceof GrCodeBlock) && !(parent instanceof GrCaseSection) && !(parent instanceof GroovyFile) && !isLoopOrForkStatement(parent)) {
    parent=parent.getParent();
  }
  return parent;
}
