{
  PsiElement parent=place;
  while (parent != null && !(parent instanceof GrCodeBlock) && !(parent instanceof GrCaseBlock) && !(parent instanceof GroovyFile)) {
    parent=parent.getParent();
  }
  return parent;
}
