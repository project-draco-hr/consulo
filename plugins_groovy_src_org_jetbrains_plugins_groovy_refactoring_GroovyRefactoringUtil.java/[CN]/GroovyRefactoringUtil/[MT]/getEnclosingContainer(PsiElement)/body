{
  PsiElement parent=place.getParent();
  while (parent != null && !(parent instanceof GrCodeBlock) && !(parent instanceof GrCaseBlock) && !(parent instanceof GroovyFile) && !isLoopOrForkStatement(parent)) {
    parent=parent.getParent();
  }
  return parent;
}
