{
  ApplicationManager.getApplication().assertIsDispatchThread();
  return !(myQuestionAction instanceof PriorityQuestionAction) || ((PriorityQuestionAction)myQuestionAction).getPriority() <= action.getPriority();
}
