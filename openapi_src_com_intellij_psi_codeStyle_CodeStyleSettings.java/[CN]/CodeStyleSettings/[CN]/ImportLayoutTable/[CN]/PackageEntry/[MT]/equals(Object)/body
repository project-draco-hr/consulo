{
  if (!(obj instanceof PackageEntry)) {
    return false;
  }
  PackageEntry entry=(PackageEntry)obj;
  return entry.myWithSubpackages == myWithSubpackages && Comparing.equal(entry.myPackageName,myPackageName);
}
