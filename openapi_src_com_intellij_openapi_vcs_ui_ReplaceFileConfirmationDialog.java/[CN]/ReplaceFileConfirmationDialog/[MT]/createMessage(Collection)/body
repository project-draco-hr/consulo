{
  if (modifiedFiles.size() == 1) {
    VirtualFile virtualFile=((VirtualFile)modifiedFiles.iterator().next());
    return "File " + FilePathSplittingPolicy.SPLIT_BY_LETTER.getPresentableName(new File(virtualFile.getPath()),40) + " has been locally modified.";
  }
 else {
    return "Some files were locally modified.";
  }
}
