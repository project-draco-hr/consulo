{
  TypeArguments.parse(builder);
  return (ParserUtils.getToken(builder,kTHIS) || ParserUtils.getToken(builder,kSUPER)) && ParserUtils.getToken(builder,mLPAREN) && !WRONGWAY.equals(ArgumentList.parse(builder,mRPAREN))&& ParserUtils.getToken(builder,mRPAREN);
}
