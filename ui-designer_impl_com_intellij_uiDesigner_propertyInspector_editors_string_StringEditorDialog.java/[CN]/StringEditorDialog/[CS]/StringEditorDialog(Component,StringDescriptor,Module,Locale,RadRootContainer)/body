{
  super(parent,true);
  myLocale=locale;
  myModule=module;
  myRootContainer=rootContainer;
  myForm=new MyForm();
  setTitle(UIDesignerBundle.message("title.edit.text"));
  setValue(descriptor);
  init();
}
