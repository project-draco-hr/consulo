{
  final ElementManipulator<PsiElement> manipulator=ReferenceProvidersRegistry.getInstance(psiElement.getProject()).getManipulator(psiElement);
  if (manipulator == null) {
    return false;
  }
  final TextRange range=manipulator.getRangeInElement(psiElement);
  final String s=range.substring(psiElement.getText());
  if (s.contains("://")) {
    final PsiReference reference=PsiReferenceBase.createSelfReference(psiElement,psiElement);
    references.add(reference);
    return true;
  }
  return false;
}
