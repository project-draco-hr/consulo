{
  super(project,MavenProjectsManager.getInstance(project),project.getComponent(MavenEventsManager.class));
  myTreeViewSettings=project.getComponent(MavenProjectNavigator.class).getTreeViewSettings();
}
