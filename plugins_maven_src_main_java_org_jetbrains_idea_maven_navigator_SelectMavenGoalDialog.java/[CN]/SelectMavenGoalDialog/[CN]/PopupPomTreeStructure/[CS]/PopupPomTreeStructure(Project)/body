{
  super(project,MavenProjectsState.getInstance(project),project.getComponent(MavenRepository.class),project.getComponent(MavenEventsHandler.class));
  myTreeViewSettings=project.getComponent(MavenProjectNavigator.class).getTreeViewSettings();
}
