{
  if (file == null) {
    return null;
  }
  for (  VirtualFile root : myRoots) {
    if (isChangeBranchFile(file,root) || isUndoChangeBranchFile(file,root)) {
      return root;
    }
  }
  return null;
}
