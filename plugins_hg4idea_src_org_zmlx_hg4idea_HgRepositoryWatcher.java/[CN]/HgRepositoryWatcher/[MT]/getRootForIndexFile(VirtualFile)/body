{
  if (file == null) {
    return null;
  }
  for (  VirtualFile root : myRoots) {
    if (isDirstateFile(file,root) || isUndoDirstateFile(file,root)) {
      return root;
    }
  }
  return null;
}
