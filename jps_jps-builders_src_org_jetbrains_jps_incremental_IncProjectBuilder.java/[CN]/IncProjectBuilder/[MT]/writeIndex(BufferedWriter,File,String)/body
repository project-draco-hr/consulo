{
  writer.write(path);
  writer.write('\n');
  final File[] files=file.listFiles();
  if (files != null) {
    for (    File child : files) {
      writeIndex(writer,child,path + "/" + child.getName());
    }
  }
}
