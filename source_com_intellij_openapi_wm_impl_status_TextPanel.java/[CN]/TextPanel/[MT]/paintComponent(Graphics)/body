{
  super.paintComponent(g);
  g.setColor(myEnabled ? UIUtil.getLabelForeground() : getBackground().darker());
  g.setFont(STATUS_FONT);
  g.drawString(myText,10,getLineHeight() + (getSize().height - getLineHeight()) / 2);
}
