{
  if (myRefilterQueue == null) {
    refilterNow(preferredSelection);
  }
 else {
    myRefilterQueue.queue(new Update(this){
      public void run(){
        refilterNow(preferredSelection);
      }
    }
);
  }
}
