{
  if (item == null) {
    return false;
  }
  if (myAllFiles.contains(item)) {
    return false;
  }
  if (isUrlInserted()) {
    myModel.insertElementAt(item,myModel.size() - 1);
  }
 else {
    myModel.addElement(item);
  }
  myAllFiles.add(item);
  return true;
}
