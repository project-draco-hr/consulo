{
  keepSelectionState();
  clearList();
  myEnabled=files != null;
  if (myEnabled) {
    for (int i=0; i < files.length; i++) {
      addElement(files[i]);
    }
  }
  setModified(false);
  updateButtons();
}
