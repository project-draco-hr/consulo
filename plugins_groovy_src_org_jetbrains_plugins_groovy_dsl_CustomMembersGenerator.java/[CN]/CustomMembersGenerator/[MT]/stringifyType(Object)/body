{
  return type instanceof Closure ? "groovy.lang.Closure" : type instanceof Map ? "java.util.Map" : type instanceof Class ? ((Class)type).getName() : type != null ? type.toString() : "java.lang.Object";
}
