{
synchronized (LOCK) {
    for (    Class<?> aClass : ourRegisteredClasses) {
      try {
        cleanupClass(aClass);
      }
 catch (      Exception e) {
        LOG.error(e);
      }
    }
  }
}
