{
  return "However merge has succeeded for the following " + repositories() + ":<br/>"+ successfulRepositoriesJoined()+ "<br/>You may rollback (reset to the commit before merging) not to let branches diverge.";
}
