{
  if (!myLocalBranch) {
    super.notifySuccess(message);
  }
 else {
    String description=message + "<br/><a href='delete'>Delete " + myBranchToMerge+ "</a>";
    NotificationManager.getInstance(myProject).notify(GitVcs.NOTIFICATION_GROUP_ID,"",description,NotificationType.INFORMATION,new DeleteMergedLocalBranchNotificationListener());
  }
}
