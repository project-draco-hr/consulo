{
  if (event.getEventType() == HyperlinkEvent.EventType.ACTIVATED && event.getDescription().equalsIgnoreCase("delete")) {
    new GitBrancherImpl(myProject,new ArrayList<GitRepository>(getRepositories()),myCurrentRepository).deleteBranch(myBranchToMerge);
  }
}
