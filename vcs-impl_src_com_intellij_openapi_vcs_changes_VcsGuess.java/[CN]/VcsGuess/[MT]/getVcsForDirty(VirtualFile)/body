{
  if (!file.isInLocalFileSystem()) {
    return null;
  }
  if (myExcludedFileIndex.isInContent(file) || isFileInBaseDir(file) || myVcsManager.hasExplicitMapping(file)) {
    if (myExcludedFileIndex.isExcludedFile(file)) {
      return null;
    }
    return myVcsManager.getVcsFor(file);
  }
  return null;
}
