{
  PsiElement parent=element.getParent();
  if (parent instanceof PsiAssignmentExpression && element.equals(((PsiAssignmentExpression)parent).getLExpression())) {
    return true;
  }
 else {
    return isPlusPlusOrMinusMinus(parent);
  }
}
