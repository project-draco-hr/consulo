{
  final XmlDocument doc=xmlDocumentChanged.getDocument();
  final AntFile antFile=(AntFile)doc.getContainingFile().getViewProvider().getPsi(AntSupport.getLanguage());
  if (antFile != null) {
    antFile.clearCaches();
    updateBuildFile(antFile);
  }
}
