{
  LOG.assertTrue(dataContext != null);
  final Module module=(Module)dataContext.getData(DataConstantsEx.MODULE_CONTEXT);
  LOG.assertTrue(module != null);
  Messages.showInputDialog(project,"Enter new module name","Rename Module",Messages.getQuestionIcon(),module.getName(),new MyInputValidator(project,module));
}
