{
  if (start < 0 || end > chars.length || start > end)   throw new IndexOutOfBoundsException("chars.length:" + chars.length + ", start:"+ start+ ", end:"+ end);
  myChars=chars;
  myStart=start;
  myEnd=end;
}
