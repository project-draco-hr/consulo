{
  init(tree,treeModel,new AbstractTreeStructure.Delegate(treeStructure){
    @Override public AsyncResult revalidateElement(    Object element){
      return _revalidateElement(element);
    }
  }
,comparator,DEFAULT_UPDATE_INACTIVE);
  myProject=project;
}
