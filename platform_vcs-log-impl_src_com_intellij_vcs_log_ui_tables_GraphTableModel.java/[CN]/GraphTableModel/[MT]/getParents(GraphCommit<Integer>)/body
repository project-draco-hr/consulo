{
  return StringUtil.join(commit.getParents(),new Function<Integer,String>(){
    @Override public String fun(    Integer integer){
      return myDataHolder.getHash(integer).toShortString();
    }
  }
,", ");
}
