{
  return new DupOutputStream(new BufferedOutputStream(FILE_CONTENT.writeAttribute(file)),myDelegate.getOutputStream(file,requestor,modStamp,timeStamp)){
    public void close() throws IOException {
      super.close();
      executeTouch(file,false);
    }
  }
;
}
