{
  final PsiElement element=getManipulatorElement();
  final AntStructuredElement antElement=getElement();
  getManipulator(element).handleContentChange(element,getRangeInElement().shiftRight(antElement.getTextRange().getStartOffset() - element.getTextRange().getStartOffset()),newElementName);
  return antElement;
}
