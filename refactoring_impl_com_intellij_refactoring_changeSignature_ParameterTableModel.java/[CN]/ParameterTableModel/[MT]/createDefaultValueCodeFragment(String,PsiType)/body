{
  PsiExpressionCodeFragment codeFragment=JavaPsiFacade.getInstance(myParameterList.getProject()).getElementFactory().createExpressionCodeFragment(expressionText,myReferenceExpression,expectedType,true);
  codeFragment.setVisibilityChecker(PsiCodeFragment.VisibilityChecker.EVERYTHING_VISIBLE);
  return codeFragment;
}
