{
synchronized (this) {
    myAccessCounter++;
    ByteBuffer buffer=myBuffer;
    if (buffer == null) {
synchronized (LOCK) {
        buffer=map();
        ourMappedWrappers.add(this);
        myBuffer=buffer;
      }
    }
    return buffer;
  }
}
