{
  if (myProperties == null) {
    final ReflectedProject reflected=ReflectedProject.getProject(getClassLoader());
synchronized (this) {
      if (myProperties == null) {
        myProperties=loadPredefinedProperties(reflected.getProperties(),Collections.<String,String>emptyMap());
      }
    }
  }
  return myProperties;
}
