{
  if (tooltipObject != null) {
    HintManager.getInstance().getTooltipController().showTooltipByMouseMove(myEditor,e,tooltipObject,myEditor.getVerticalScrollbarOrientation() == EditorEx.VERTICAL_SCROLLBAR_RIGHT,ERROR_STRIPE_TOOLTIP_GROUP);
  }
}
