{
  myFlushingLock.lock();
  try {
    final ValueContainer<Value> container=myCache.get(key);
    if (container != null) {
      return container;
    }
    return readAndCache(key);
  }
  finally {
    myFlushingLock.unlock();
  }
}
