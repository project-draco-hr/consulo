{
  System.out.println("Cache hit rate = " + myCache.hitRate());
  myFlushingLock.lock();
  myCache.removeAll();
  try {
    myMap.flush();
  }
 catch (  IOException e) {
    LOG.error(e);
  }
 finally {
    myFlushingLock.unlock();
  }
}
