{
  myKeyBeingRemoved=key;
  try {
    do {
      myCache.remove(key);
    }
 while (myCache.isCached(key));
    myMap.remove(key);
  }
 catch (  IOException e) {
    throw new StorageException(e);
  }
 finally {
    myKeyBeingRemoved=null;
  }
}
