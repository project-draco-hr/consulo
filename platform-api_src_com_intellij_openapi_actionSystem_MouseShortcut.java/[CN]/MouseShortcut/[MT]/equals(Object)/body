{
  if (!(obj instanceof MouseShortcut)) {
    return false;
  }
  MouseShortcut shortcut=(MouseShortcut)obj;
  return myButton == shortcut.myButton && myModifiers == shortcut.myModifiers && myClickCount == shortcut.myClickCount;
}
