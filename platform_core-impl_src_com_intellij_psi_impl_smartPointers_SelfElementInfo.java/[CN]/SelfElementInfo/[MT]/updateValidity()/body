{
  if (myPsiRange != null) {
    LOG.error("Non-fastened smart pointer " + this + " "+ myRangeMarker);
    myPsiRange=null;
    myRangeMarker=null;
  }
  if (myRangeMarker != null && !myRangeMarker.isValid()) {
    myRangeMarker=null;
  }
}
