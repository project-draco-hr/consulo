{
  BuildNumber currentBuild=ApplicationInfo.getInstance().getBuild();
  for (  PatchInfo each : myPatches) {
    if (each.isAvailable() && each.getFromBuild().asString().equals(currentBuild.asString()))     return each;
  }
  return null;
}
