{
synchronized (LOCK) {
    for (int i=myRequests.size() - 1; i >= 0; i--) {
      Request r=myRequests.get(i);
      if (r.getTask() == request) {
        r.cancel();
        myRequests.remove(i);
      }
    }
    return true;
  }
}
