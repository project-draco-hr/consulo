{
  Set<ServerNode> serverNodes=content.getSelectedServerNodes();
  return content.getBuilder().getSelectedElements().size() == serverNodes.size() && serverNodes.size() == 1 && isEnabledForServer(serverNodes.iterator().next());
}
