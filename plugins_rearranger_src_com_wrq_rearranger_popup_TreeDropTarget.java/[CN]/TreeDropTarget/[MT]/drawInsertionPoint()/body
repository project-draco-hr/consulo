{
  if (canInsert) {
    Graphics g=this.targetTree.getGraphics();
    g.setXORMode(xorBackgroundColor);
    g.drawRect(this.lastRowRect.x,this.lastRowRect.y,this.lastRowRect.width,this.lastRowRect.height);
    rectangleDrawn=!rectangleDrawn;
  }
}
