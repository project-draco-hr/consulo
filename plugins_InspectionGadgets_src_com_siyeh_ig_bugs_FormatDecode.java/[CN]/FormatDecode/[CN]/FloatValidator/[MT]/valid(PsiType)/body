{
  final String text=type.getCanonicalText();
  return type == PsiType.DOUBLE || "java.lang.Double".equals(text) || type == PsiType.FLOAT || "java.lang.Float".equals(text) || "java.math.BigDecimal".equals(text);
}
