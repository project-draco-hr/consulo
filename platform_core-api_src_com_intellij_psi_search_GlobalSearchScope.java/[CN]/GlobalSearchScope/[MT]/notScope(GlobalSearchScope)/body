{
  return new DelegatingGlobalSearchScope(scope){
    @Override public boolean contains(    final VirtualFile file){
      return !myBaseScope.contains(file);
    }
    @Override public boolean isSearchOutsideRootModel(){
      return true;
    }
  }
;
}
