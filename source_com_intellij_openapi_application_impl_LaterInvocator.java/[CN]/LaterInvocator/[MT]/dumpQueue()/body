{
synchronized (LOCK) {
    if (!ourQueue.isEmpty()) {
      ArrayList<Object> r=new ArrayList<Object>();
      r.addAll(ourQueue);
      Collections.reverse(r);
      return r;
    }
  }
  return null;
}
