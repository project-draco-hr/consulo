{
  if (myStates.isEmpty()) {
    return true;
  }
  State state=myStates.peek();
  doIteration(state);
  if (myStates.isEmpty()) {
    state.wrappedBlock.setIndent((IndentImpl)Indent.getNoneIndent());
    return true;
  }
  return false;
}
