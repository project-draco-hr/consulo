{
  if (myTabs.getSelectedInfo() != myInfo) {
    g.translate(0,2);
  }
 else {
    g.translate(0,1);
  }
  super.paint(g);
  if (myTabs.getSelectedInfo() != myInfo) {
    g.translate(0,-2);
  }
 else {
    g.translate(0,1);
  }
}
