{
  final PsiClass containingClass=ClassUtils.getContainingClass(classRef);
  final String containingClassName=containingClass.getName();
  return "Class " + containingClassName + " references subclass #ref #loc";
}
