{
  if (cc >= 'a' && cc <= 'z' || cc == '.' || cc == '/')   return cc;
  if (cc >= 'A' && cc <= 'Z')   return (char)(cc - 'A' + 'a');
  char c=SystemInfo.isFileSystemCaseSensitive ? cc : Character.toLowerCase(cc);
  return c == File.separatorChar ? '/' : c;
}
