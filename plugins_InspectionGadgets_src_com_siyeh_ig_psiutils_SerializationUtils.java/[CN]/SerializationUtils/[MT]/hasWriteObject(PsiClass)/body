{
  final PsiMethod[] methods=aClass.findMethodsByName(WRITE_OBJECT,false);
  for (  final PsiMethod method : methods) {
    if (isWriteObject(method)) {
      return true;
    }
  }
  return false;
}
