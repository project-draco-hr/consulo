{
  if (isConstructor()) {
    return null;
  }
  final PsiType type=getNominalType();
  if (type != null) {
    return type;
  }
  return PsiType.getJavaLangObject(getManager(),getResolveScope());
}
