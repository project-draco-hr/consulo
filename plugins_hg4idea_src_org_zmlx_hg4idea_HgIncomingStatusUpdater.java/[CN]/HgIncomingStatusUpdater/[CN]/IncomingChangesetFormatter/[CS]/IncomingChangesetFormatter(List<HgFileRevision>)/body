{
  builder.append("<html>");
  builder.append("<b>Incoming changesets</b>:<br>");
  for (  HgFileRevision changeset : changesets) {
    builder.append(changeset.getRevisionNumber().asString()).append(" ").append(changeset.getCommitMessage()).append(" ").append("(").append(changeset.getAuthor()).append(")<br>");
  }
  builder.append("</html>");
}
