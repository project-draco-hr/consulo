{
  if (myIndices != null) {
    try {
      myIndices.close();
    }
 catch (    Exception e) {
      MavenLog.error(e);
    }
    myIndices=null;
  }
  if (myEmbedder != null) {
    try {
      myEmbedder.stop();
    }
 catch (    Exception e) {
      MavenLog.error(e);
    }
    myEmbedder=null;
  }
}
