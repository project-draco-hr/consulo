{
  try {
    if (myIndices != null) {
      myIndices.close();
    }
    if (myEmbedder != null)     myEmbedder.stop();
    myIndices=null;
    myEmbedder=null;
  }
 catch (  Exception e) {
    MavenLog.error(e);
  }
}
