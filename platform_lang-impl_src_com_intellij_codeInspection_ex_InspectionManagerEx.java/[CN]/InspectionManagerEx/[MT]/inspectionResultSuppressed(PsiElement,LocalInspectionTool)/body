{
  if (tool instanceof CustomSuppressableInspectionTool) {
    return ((CustomSuppressableInspectionTool)tool).isSuppressedFor(place);
  }
  String alternativeId;
  String id;
  return isSuppressed(place,id=tool.getID()) || (alternativeId=tool.getAlternativeID()) != null && !alternativeId.equals(id) && isSuppressed(place,alternativeId);
}
