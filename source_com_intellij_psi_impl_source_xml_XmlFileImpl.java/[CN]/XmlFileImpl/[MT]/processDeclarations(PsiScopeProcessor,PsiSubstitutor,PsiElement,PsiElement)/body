{
  if (!super.processDeclarations(processor,substitutor,lastParent,place))   return false;
  if (isWebFileType())   return ScriptSupportUtil.processDeclarations(this,processor,substitutor,lastParent,place);
  return true;
}
