{
  if (path == null) {
    return null;
  }
  final List<String> tail=new ArrayList<String>();
  final T file=getFile(merger,path,tail);
  if (tail.isEmpty()) {
    return file;
  }
  return null;
}
