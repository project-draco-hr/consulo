{
  if (myContent == null) {
    try {
      myContent=new String(getUpToDateBinaryContent(),myFile.getCharset().name());
    }
 catch (    Exception ex) {
      throw new VcsException(ex);
    }
  }
  return myContent;
}
