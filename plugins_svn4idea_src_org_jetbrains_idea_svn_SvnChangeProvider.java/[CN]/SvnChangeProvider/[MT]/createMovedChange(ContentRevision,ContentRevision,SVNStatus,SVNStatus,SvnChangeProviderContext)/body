{
  return new ConflictedSvnChange(before,after,ConflictState.mergeState(getState(copiedStatus,context),getState(deletedStatus,context)),copiedStatus.getTreeConflict() != null ? after.getFile() : before.getFile());
}
