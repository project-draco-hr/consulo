{
  if (isInterface())   return Icons.INTERFACE;
  if (isAnnotationType())   return Icons.ANNOTAION;
  if (isEnum())   return Icons.ENUM;
  if (hasModifierProperty(PsiModifier.ABSTRACT))   return Icons.ABSTRACT;
  return Icons.CLAZZ;
}
