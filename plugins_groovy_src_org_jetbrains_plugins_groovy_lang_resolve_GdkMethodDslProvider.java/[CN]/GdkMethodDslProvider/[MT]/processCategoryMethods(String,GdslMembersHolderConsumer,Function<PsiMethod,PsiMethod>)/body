{
  if (consumer.getPsiType() == null)   return;
  List<PsiMethod> methods=CategoryMethodProvider.provideMethods(consumer.getPsiType(),consumer.getProject(),className,consumer.getResolveScope(),converter);
  final DelegatedMembersHolder holder=new DelegatedMembersHolder();
  for (  PsiMethod m : methods) {
    holder.addMember(m);
  }
  consumer.addMemberHolder(holder);
}
