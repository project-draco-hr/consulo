{
  if (qName.indexOf('[') != -1) {
    return false;
  }
  for (int i=0; i < classFilters.length; i++) {
    ClassFilter filter=classFilters[i];
    if (isFiltered(filter,qName)) {
      return true;
    }
  }
  return false;
}
