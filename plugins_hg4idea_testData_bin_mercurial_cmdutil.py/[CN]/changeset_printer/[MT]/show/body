def show(self, ctx, copies=None, **props):
    if self.buffered:
        self.ui.pushbuffer()
        self._show(ctx, copies, props)
        self.hunk[ctx.rev()] = self.ui.popbuffer()
    else:
        self._show(ctx, copies, props)
