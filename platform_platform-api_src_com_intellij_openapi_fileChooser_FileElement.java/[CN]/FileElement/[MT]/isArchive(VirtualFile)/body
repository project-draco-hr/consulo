{
  if (isArchiveFileSystem(file) && file.getParent() == null)   return true;
  return !file.isDirectory() && FileTypeManager.getInstance().getFileTypeByFile(file) == FileTypes.ARCHIVE && !isArchiveFileSystem(file.getParent());
}
