{
  if (file == null || !file.isValid())   return false;
  if (!file.isInLocalFileSystem())   return false;
  final File ioFile=new File(file.getPath().replace('/',File.separatorChar));
  return ioFile.isHidden() && ioFile.getParent() != null;
}
