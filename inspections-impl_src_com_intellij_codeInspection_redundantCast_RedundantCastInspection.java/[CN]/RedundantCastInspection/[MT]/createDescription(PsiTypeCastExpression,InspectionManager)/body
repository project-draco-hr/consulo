{
  return manager.createProblemDescriptor(cast.getCastType(),InspectionsBundle.message("inspection.redundant.cast.problem.descriptor","<code>" + cast.getOperand().getText() + "</code>","<code>#ref</code> #loc"),myQuickFixAction,ProblemHighlightType.LIKE_UNUSED_SYMBOL);
}
