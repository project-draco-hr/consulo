{
  int left=-1;
  int right=length;
  while (right - left > 1) {
    int middle=(left + right) / 2;
    if (sequence[middle] > val) {
      right=middle;
    }
 else {
      left=middle;
    }
  }
  return left + 1;
}
