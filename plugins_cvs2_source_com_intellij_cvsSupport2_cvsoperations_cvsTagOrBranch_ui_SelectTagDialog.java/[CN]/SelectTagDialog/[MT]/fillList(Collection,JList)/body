{
  DefaultListModel model=new DefaultListModel();
  list.setModel(model);
  for (Iterator each=tags.iterator(); each.hasNext(); ) {
    model.addElement(each.next());
  }
  if (!tags.isEmpty())   list.getSelectionModel().addSelectionInterval(0,0);
}
