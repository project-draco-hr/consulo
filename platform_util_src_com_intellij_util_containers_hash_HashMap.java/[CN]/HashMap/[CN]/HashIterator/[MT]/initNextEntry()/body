{
  Entry<K,V> result=e;
  if (result != null) {
    result=result.hashNext;
  }
  final Entry<K,V>[] table=this.table;
  while (result == null && index < table.length) {
    result=table[index++];
  }
  e=result;
}
