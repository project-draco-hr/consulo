{
  this(descriptor,new Comparator<ProjectStructureNode<?>>(){
    @Override public int compare(    ProjectStructureNode<?> o1,    ProjectStructureNode<?> o2){
      return o1.getDescriptor().getName().compareTo(o2.getDescriptor().getName());
    }
  }
);
}
