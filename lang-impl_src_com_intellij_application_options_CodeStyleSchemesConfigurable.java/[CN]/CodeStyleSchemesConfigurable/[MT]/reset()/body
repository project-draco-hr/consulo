{
  if (!myInitResetInvoked) {
    if (!myResetCompleted) {
      try {
        resetImpl();
      }
  finally {
        myResetCompleted=true;
      }
    }
    myInitResetInvoked=true;
  }
 else {
    revert();
  }
}
