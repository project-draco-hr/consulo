{
  this(modifierList,manager,null,JavaPsiFacade.getInstance(manager.getProject()).getElementFactory().createTypeByFQClassName(type,ProjectScope.getAllScope(manager.getProject())),false,referenceExpression);
  myNameIdentifier=new GrLightIdentifier(myManager,name);
}
