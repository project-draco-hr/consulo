def revsplit(self, rev):
    (url, revnum) = rev.rsplit('@', 1)
    revnum = int(revnum)
    parts = url.split('/', 1)
    uuid = parts.pop(0)[4:]
    mod = ''
    if parts:
        mod = ('/' + parts[0])
    return (uuid, mod, revnum)
