def exists(self, path, optrev):
    try:
        svn.client.ls(((self.url.rstrip('/') + '/') + urllib.quote(path)), optrev, False, self.ctx)
        return True
    except SubversionException:
        return False
