def tidy_dirs(self, names):
    deleted = []
    for d in sorted(self.dirs_of(names), reverse=True):
        wd = self.wjoin(d)
        if (os.listdir(wd) == '.svn'):
            self.run0('delete', d)
            deleted.append(d)
    return deleted
