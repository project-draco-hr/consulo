def add_dirs(self, files):
    add_dirs = [d for d in sorted(self.dirs_of(files)) if (not os.path.exists(self.wjoin(d, '.svn', 'entries')))]
    if add_dirs:
        self.xargs(add_dirs, 'add', non_recursive=True, quiet=True)
    return add_dirs
