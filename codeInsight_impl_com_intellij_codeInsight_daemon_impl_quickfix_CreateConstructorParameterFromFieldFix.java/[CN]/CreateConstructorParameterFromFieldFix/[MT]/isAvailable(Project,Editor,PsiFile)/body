{
  final PsiField field=getField();
  return field != null && field.getManager().isInProject(field) && field.getContainingClass() != null && !(field.getContainingClass() instanceof JspClass) && !field.hasModifierProperty(PsiModifier.STATIC);
}
