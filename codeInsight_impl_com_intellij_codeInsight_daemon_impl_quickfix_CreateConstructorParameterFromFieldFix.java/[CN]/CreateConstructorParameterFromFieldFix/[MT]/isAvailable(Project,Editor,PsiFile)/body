{
  final PsiField fieldElement=(PsiField)myField.getElement();
  return myField.getElement() != null && fieldElement != null && fieldElement.getManager().isInProject(fieldElement) && fieldElement.getContainingClass() != null;
}
