{
  return new ConfigurableFilter(){
    @Override public boolean isIncluded(    final Configurable configurable){
      return !isDefault() || !ConfigurableWrapper.isNonDefaultProject(configurable);
    }
  }
;
}
