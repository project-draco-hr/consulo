{
  return myCurrentSelectionIndex < mySelectionStarts.length && (myReverseIteration ? lessThan(myStartOffset,mySelectionEnds[myCurrentSelectionIndex],!atBreak) : lessThan(mySelectionStarts[myCurrentSelectionIndex],myStartOffset,!atBreak));
}
