{
  return myCurrentSelectionIndex < mySelectionStarts.length && (myReverseIteration ? myStartOffset <= mySelectionEnds[myCurrentSelectionIndex] : myStartOffset >= mySelectionStarts[myCurrentSelectionIndex]);
}
