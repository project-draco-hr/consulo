{
synchronized (ourLock) {
    if (myDirty) {
      if (!dirty) {
        markClean();
      }
    }
 else {
      if (dirty) {
        myStorage.putInt(0,DIRTY_MAGIC);
        myDirty=true;
      }
    }
  }
}
