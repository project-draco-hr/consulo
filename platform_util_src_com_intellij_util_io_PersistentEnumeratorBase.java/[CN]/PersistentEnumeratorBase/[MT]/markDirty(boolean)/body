{
  if (dirty && myDirty)   return;
synchronized (ourLock) {
    if (myDirty) {
      if (!dirty) {
        markClean();
      }
    }
 else {
      if (dirty) {
        myStorage.putInt(0,myVersion.dirtyMagic);
        myDirty=true;
      }
    }
  }
}
