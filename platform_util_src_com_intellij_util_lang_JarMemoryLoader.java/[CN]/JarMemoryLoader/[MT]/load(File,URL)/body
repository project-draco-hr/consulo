{
  FileInputStream inputStream=new FileInputStream(file);
  try {
    JarMemoryLoader loader=load(inputStream,baseUrl);
    if (loader != null) {
    }
    return loader;
  }
  finally {
    inputStream.close();
  }
}
