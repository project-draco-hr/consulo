{
  Collection<File> children=getChildren(oldRoot);
  if (children == null)   return oldRoot;
  if (children.size() == 1)   return splitRoot(children.iterator().next());
 else {
    return oldRoot;
  }
}
