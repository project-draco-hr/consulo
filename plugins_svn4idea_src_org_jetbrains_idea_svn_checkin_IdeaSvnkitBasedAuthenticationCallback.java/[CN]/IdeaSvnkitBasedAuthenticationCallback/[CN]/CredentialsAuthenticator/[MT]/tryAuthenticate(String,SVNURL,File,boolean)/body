{
  myRealm=realm;
  myUrl=url;
  realm=realm == null ? url.getHost() : realm;
  final List<String> kinds=getKinds(url);
  for (  String kind : kinds) {
    myKind=kind;
    if (!tryAuthenticate()) {
      return false;
    }
  }
  return true;
}
