{
  final SVNAuthentication impl=getWithPassiveImpl(passive);
  if (impl != null && !checkAuthOk(impl)) {
    clearPassiveCredentials(myRealm,myFile,impl instanceof SVNPasswordAuthentication);
    return null;
  }
  return impl;
}
