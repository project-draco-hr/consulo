{
  if (context.getParent() != null && !(context.getParent() instanceof GrTypeDefinition) && !(context.getParent() instanceof PsiErrorElement) && context.getParent().getParent() instanceof GroovyFile) {
    return true;
  }
  return false;
}
