{
  @NonNls String filename="dummy." + StdFileTypes.PROPERTIES.getDefaultExtension();
  return (PropertiesFile)PsiFileFactory.getInstance(PsiManager.getInstance(project).getProject()).createFileFromText(filename,StdFileTypes.PROPERTIES,text);
}
