{
  Project project=myEnvironment.getProject();
  if (!ExecutorRegistry.getInstance().isStarting(project,myExecutor.getId(),myRunner.getRunnerId()))   ExecutionManager.getInstance(project).restartRunProfile(myRunner,myEnvironment,myDescriptor);
}
