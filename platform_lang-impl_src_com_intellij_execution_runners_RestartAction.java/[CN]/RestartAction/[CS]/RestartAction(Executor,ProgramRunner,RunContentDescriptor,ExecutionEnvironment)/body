{
  Disposer.register(descriptor,this);
  registry.add(this);
  myEnvironment=env;
  getTemplatePresentation().setEnabled(false);
  myRunner=runner;
  myDescriptor=descriptor;
  myExecutor=executor;
}
