{
  return processProfile(profileDom,processor,new Function<MavenDomProfile,MavenDomProperties>(){
    @Override public MavenDomProperties fun(    MavenDomProfile mavenDomProfile){
      return mavenDomProfile.getProperties();
    }
  }
);
}
