{
synchronized (WRITE_LOCK) {
    Map<Key,Object> map=myUserMap;
    if (map == null) {
      if (value == null)       return;
      myUserMap=map=createMap();
    }
    if (value == null) {
      map.remove(key);
    }
 else {
      map.put(key,value);
    }
  }
}
