{
  if (who.equals(whom)) {
    return true;
  }
  final ClassRepr repr=reprByName(who);
  if (repr != null) {
    for (    DependencyContext.S s : repr.getSupers()) {
      if (isInheritorOf(s,whom)) {
        return true;
      }
    }
  }
  return false;
}
