{
  myFile=file;
  if (file != null) {
    myName=null;
    if (myContents != null) {
      OutputStream out=myFile.getOutputStream(myManager);
      out.write(myContents);
      out.close();
      myContents=null;
    }
  }
}
