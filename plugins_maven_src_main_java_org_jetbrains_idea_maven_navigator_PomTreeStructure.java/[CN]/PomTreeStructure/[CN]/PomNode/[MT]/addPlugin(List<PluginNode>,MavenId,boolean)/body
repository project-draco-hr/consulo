{
  if (!hasPlugin(mavenId)) {
    PluginDocument pluginDocument=getRepository().loadPlugin(mavenId);
    insertSorted(pluginNodes,pluginDocument == null ? new InvalidPluginNode(this,mavenId,detachable) : new ValidPluginNode(this,pluginDocument.getPlugin(),detachable));
  }
}
