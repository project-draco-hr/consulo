{
  if (!hasPlugin(mavenId)) {
    PluginPluginInfo plugin=getRepository().loadPluginInfo(mavenId);
    insertSorted(pluginNodes,plugin == null ? new InvalidPluginNode(this,mavenId,detachable) : new ValidPluginNode(this,plugin,detachable));
  }
}
