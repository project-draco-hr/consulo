{
  for (  PluginNode node : pomPluginNodes) {
    if (node.getId().equals(id)) {
      return true;
    }
  }
  for (  PluginNode node : extraPluginNodes) {
    if (node.getId().equals(id)) {
      return true;
    }
  }
  return false;
}
