{
  PluginDocument pluginDocument=getRepository().loadPlugin(mavenId);
  insertSorted(pomPluginNodes,pluginDocument != null ? new ValidPluginNode(this,pluginDocument.getPlugin()) : new InvalidPluginNode(this,mavenId));
}
