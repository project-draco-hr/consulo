{
  if (!hasPlugin(mavenId)) {
    MavenPluginInfo plugin=getRepository().loadPluginInfo(mavenId);
    pluginsNode.add(plugin == null ? new InvalidPluginNode(this,mavenId,detachable) : new ValidPluginNode(this,plugin,detachable));
  }
}
