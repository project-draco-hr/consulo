{
  this.active=active;
  clearColoredText();
  addPlainText(name);
  addColoredFragment(active ? NavigatorBundle.message("node.profile.active") : NavigatorBundle.message("node.profile.inactive"),SimpleTextAttributes.GRAY_ATTRIBUTES);
  setUniformIcon(active ? iconProfileActive : iconProfileInactive);
}
