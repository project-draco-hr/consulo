{
  if (exp == null) {
    return false;
  }
  final PsiType type=exp.getType();
  if (type == null) {
    return false;
  }
  return !ClassUtils.isPrimitive(type) && !type.equals(PsiType.NULL) && !TypeUtils.isJavaLangString(type);
}
