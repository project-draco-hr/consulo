{
  LookupItem lookup=new LookupItem(element,name){
    @NotNull @Override protected LookupElementRenderer getRenderer(){
      return DefaultLookupItemRenderer.INSTANCE;
    }
  }
;
  lookup.setIcon(icon);
  lookup.setPresentableText(name);
  return lookup;
}
