{
  return processSchema(schema,new SchemaProcessor<Boolean>(){
    @Nullable public Boolean process(    @NotNull String eachProperty){
      if (eachProperty.equals(property))       return true;
      return null;
    }
  }
) != null;
}
