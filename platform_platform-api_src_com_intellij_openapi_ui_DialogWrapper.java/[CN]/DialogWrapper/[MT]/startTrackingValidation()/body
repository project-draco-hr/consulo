{
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      if (!myValidationStarted && !myDisposed) {
        myValidationStarted=true;
        initValidation();
      }
    }
  }
);
}
