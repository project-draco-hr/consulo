{
  if (myType1 == mCOMMA) {
    createSpaceInCode(mySettings.SPACE_AFTER_COMMA_IN_TYPE_ARGUMENTS);
  }
 else   if (myType1 == mLT && myType2 == TYPE_PARAMETER || myType1 == TYPE_PARAMETER && myType2 == mGT) {
    createSpaceInCode(false);
  }
}
