{
  final Editor editor=getEditor();
  if (editor != null) {
    return editor.isColumnMode() ? UIBundle.message("status.bar.column.status.text") : editor.isInsertMode() ? UIBundle.message("status.bar.insert.status.text") : UIBundle.message("status.bar.overwrite.status.text");
  }
  return "";
}
