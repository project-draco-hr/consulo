{
  myProgress=1;
switch (myState) {
case COLLAPSING:
    setState(State.COLLAPSED);
  break;
case EXPANDING:
setState(State.TEMPORARY_EXPANDED);
break;
default :
}
revalidate();
if (myState == State.COLLAPSED) {
getParent().repaint();
}
 else {
repaint();
}
}
