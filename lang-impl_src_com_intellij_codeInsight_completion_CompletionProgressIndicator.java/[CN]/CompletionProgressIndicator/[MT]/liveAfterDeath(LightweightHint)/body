{
  registerItself();
  myHint=hint;
  hint.addHintListener(new HintListener(){
    public void hintHidden(    final EventObject event){
      if (myHint != null) {
        cleanup();
      }
    }
  }
);
}
