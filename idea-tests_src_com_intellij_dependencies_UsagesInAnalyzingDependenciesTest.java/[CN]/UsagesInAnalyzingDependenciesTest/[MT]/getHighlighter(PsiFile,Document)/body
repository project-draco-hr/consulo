{
  final LexerEditorHighlighter highlighter=HighlighterFactory.createHighlighter(UsageTreeColorsScheme.getInstance().getScheme(),psiFile.getName(),myProject);
  highlighter.setText(document.getCharsSequence());
  return highlighter;
}
