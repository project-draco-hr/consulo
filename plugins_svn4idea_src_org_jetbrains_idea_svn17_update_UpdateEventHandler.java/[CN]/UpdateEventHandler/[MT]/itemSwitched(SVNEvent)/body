{
  final File file=event.getFile();
  final SvnFileUrlMapping17 urlMapping=myVCS.getSvnFileUrlMapping();
  final SVNURL currentUrl=urlMapping.getUrlForFile(file);
  return (currentUrl != null) && (!currentUrl.equals(event.getURL()));
}
