{
  byte[] buffer=myBuffer;
  boolean read=false;
  while (myStream.available() > 0) {
    int n=myStream.read(buffer);
    if (n <= 0)     break;
    read=true;
    onBinaryAvailable(buffer,n);
  }
  return read;
}
