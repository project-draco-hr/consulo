{
  if (!ClassUtils.isSubclass(aClass,CommonClassNames.JAVA_LANG_THROWABLE)) {
    return;
  }
  if (ClassUtils.isSubclass(aClass,CommonClassNames.JAVA_LANG_EXCEPTION) && !ClassUtils.isSubclass(aClass,CommonClassNames.JAVA_LANG_RUNTIME_EXCEPTION)) {
    return;
  }
  registerClassError(aClass);
}
