{
  if (myState.get() != State.CONNECTED) {
    return null;
  }
  return sendRequest(rebuild ? ProtoUtil.createRebuildRequest(projectId,modules) : ProtoUtil.createMakeRequest(projectId,modules),handler);
}
