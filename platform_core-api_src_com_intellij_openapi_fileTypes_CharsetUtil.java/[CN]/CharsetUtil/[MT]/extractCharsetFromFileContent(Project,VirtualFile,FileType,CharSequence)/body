{
  if (fileType instanceof LanguageFileType && ourSupportsCharsetDetection.get(fileType)) {
    return ((LanguageFileType)fileType).extractCharsetFromFileContent(project,virtualFile,text);
  }
  return null;
}
