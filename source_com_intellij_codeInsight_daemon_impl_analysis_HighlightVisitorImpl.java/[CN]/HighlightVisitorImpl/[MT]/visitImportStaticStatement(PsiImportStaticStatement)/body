{
  if (statement.getManager().getEffectiveLanguageLevel().compareTo(LanguageLevel.JDK_1_5) < 0) {
    myHolder.add(HighlightInfo.createHighlightInfo(HighlightInfoType.ERROR,statement.getFirstChild(),JavaErrorMessages.message("static.imports.prior.15")));
  }
}
