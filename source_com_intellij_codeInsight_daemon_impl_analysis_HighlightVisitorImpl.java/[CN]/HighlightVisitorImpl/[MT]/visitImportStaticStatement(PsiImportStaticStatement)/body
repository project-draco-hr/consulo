{
  if (statement.getManager().getEffectiveLanguageLevel().compareTo(LanguageLevel.JDK_1_5) < 0) {
    myHolder.add(HighlightInfo.createHighlightInfo(HighlightInfoType.ERROR,statement.getFirstChild(),"Static imports are not supported at this language level"));
  }
}
