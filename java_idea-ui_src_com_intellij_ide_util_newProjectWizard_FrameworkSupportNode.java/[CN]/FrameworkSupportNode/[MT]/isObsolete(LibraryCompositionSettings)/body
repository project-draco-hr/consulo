{
  final LibraryInfo[] libraries=getLibraries();
  return !settings.getBaseDirectoryForDownloadedFiles().equals(myBaseDirForLibrariesGetter.compute()) || !Comparing.equal(settings.getLibraryInfos(),libraries);
}
