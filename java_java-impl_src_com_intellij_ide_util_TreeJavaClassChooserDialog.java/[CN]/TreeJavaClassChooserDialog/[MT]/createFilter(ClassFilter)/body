{
  if (classFilter == null) {
    return null;
  }
 else {
    return new Filter<PsiClass>(){
      @Override public boolean isAccepted(      PsiClass element){
        return classFilter.isAccepted(element);
      }
    }
;
  }
}
