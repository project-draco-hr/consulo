{
  if (classFilter == null) {
    return null;
  }
 else {
    return new Filter<Object>(){
      @Override public boolean isAccepted(      Object element){
        return element instanceof PsiClass && classFilter.isAccepted((PsiClass)element);
      }
    }
;
  }
}
