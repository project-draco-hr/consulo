{
  doTest("Test","TestSubclass",new String[]{"Method <b><code>Test.foo()</code></b> is private and will not be accessible from method <b><code>x()</code></b>.","Method <b><code>x()</code></b> uses method <b><code>Test.foo()</code></b>, which is not moved to the superclass"},new RefactoringTestUtil.MemberDescriptor("x",PsiMethod.class));
}
