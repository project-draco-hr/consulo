{
  if (!(otherState instanceof MyValidityState)) {
    return false;
  }
  final MyValidityState state=(MyValidityState)otherState;
  return state.myApkFileTimestamp == myApkFileTimestamp && state.mySigningMode.equals(mySigningMode) && state.myDebugKeyStorePath.equals(myDebugKeyStorePath);
}
