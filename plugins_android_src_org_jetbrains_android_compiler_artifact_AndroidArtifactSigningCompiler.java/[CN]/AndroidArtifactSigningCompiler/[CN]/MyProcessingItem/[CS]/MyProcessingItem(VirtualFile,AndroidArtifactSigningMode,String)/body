{
  myApkFile=apkFile;
  mySigningMode=signingMode;
  myDebugKeyStorePath=debugKeyStorePath;
  myValidityState=new MyValidityState(myApkFile.getModificationStamp(),mySigningMode.name(),myDebugKeyStorePath);
}
