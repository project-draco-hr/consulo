{
  if (!myExpression.isValid() || !myExpression.isWritable() || !myExpression.getManager().isInProject(myExpression)|| !myExpectedType.isValid()|| myExpression.getType() == null)   return false;
  return findWrapper(myExpression.getType(),myExpectedType) != null;
}
