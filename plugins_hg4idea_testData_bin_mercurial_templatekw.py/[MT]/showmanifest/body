def showmanifest(**args):
    (repo, ctx, templ) = (args['repo'], args['ctx'], args['templ'])
    args = args.copy()
    args.update(dict(rev=repo.manifest.rev(ctx.changeset()[0]), node=hex(ctx.changeset()[0])))
    return templ('manifest', **args)
