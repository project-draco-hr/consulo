{
  if (isInProcess) {
    super.getArtifact(artifact,remoteRepositories);
    return;
  }
  isInProcess=true;
  try {
    if (isOpen()) {
      try {
        super.getArtifact(artifact,remoteRepositories);
      }
 catch (      WagonException ignore) {
      }
    }
    postResolve(artifact);
  }
  finally {
    isInProcess=false;
  }
}
