{
  if (isInProcess) {
    super.getArtifact(artifact,repository,force);
    return;
  }
  isInProcess=true;
  try {
    if (isOpen()) {
      try {
        super.getArtifact(artifact,repository,force);
      }
 catch (      WagonException ignore) {
      }
    }
    postResolve(artifact);
  }
  finally {
    isInProcess=false;
  }
}
