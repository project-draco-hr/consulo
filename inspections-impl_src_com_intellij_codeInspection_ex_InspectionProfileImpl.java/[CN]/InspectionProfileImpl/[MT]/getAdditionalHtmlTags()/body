{
  if (myBaseProfile != null) {
    return myBaseProfile.getAdditionalHtmlTags().length() > 0 ? myBaseProfile.getAdditionalHtmlTags() + (myAdditionalHtmlTags.length() > 0 ? "," + myAdditionalHtmlTags : "") : myAdditionalHtmlTags;
  }
  return myAdditionalHtmlTags;
}
