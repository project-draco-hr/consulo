{
  if (nameHint == null) {
    return true;
  }
  if (nameHint.equals(lRefExpr.getName())) {
    final PsiElement target=lRefExpr.resolve();
    if (!(target instanceof PsiVariable) && !(target instanceof GrAccessorMethod)) {
      return true;
    }
  }
  return false;
}
