{
  Runnable task=new Runnable(){
    public void run(){
      Messages.showErrorDialog(project,message,title);
    }
  }
;
  if (ApplicationManager.getApplication().isDispatchThread())   task.run();
 else   ApplicationManager.getApplication().invokeLater(task);
}
