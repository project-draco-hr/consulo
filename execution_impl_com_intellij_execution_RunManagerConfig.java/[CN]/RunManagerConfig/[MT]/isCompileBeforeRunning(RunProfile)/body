{
  return isCompileBeforeRunning() && (!(runProfile instanceof RunConfiguration) || myManager.getCompileMethodBeforeRun((RunConfiguration)runProfile) == MAKE || myManager.getCompileMethodBeforeRun((RunConfiguration)runProfile) == MAKE_ANT);
}
