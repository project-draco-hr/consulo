{
  super(frame.myTimer);
  myFrame=frame;
  myVariableName=variable.name();
  myVariable=variable;
  try {
    myVariableType=variable.type();
  }
 catch (  ClassNotLoadedException e) {
    myVariableType=null;
  }
}
