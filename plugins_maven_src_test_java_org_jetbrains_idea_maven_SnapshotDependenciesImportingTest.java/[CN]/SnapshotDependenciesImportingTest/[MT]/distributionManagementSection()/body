{
  return "<distributionManagement>" + "  <snapshotRepository>" + "    <id>internal</id>"+ "    <url>file://" + remoteRepoDir.getPath() + "</url>"+ "  </snapshotRepository>"+ "</distributionManagement>";
}
