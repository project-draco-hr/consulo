{
  return "<repositories>" + "  <repository>" + "    <id>internal</id>"+ "    <url>file://" + remoteRepoDir.getPath() + "</url>"+ "    <snapshots>"+ "      <enabled>true</enabled>"+ "      <updatePolicy>always</updatePolicy>"+ "    </snapshots>"+ "  </repository>"+ "</repositories>";
}
