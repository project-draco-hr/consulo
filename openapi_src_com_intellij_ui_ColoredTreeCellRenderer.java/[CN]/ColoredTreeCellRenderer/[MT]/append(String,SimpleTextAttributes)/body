{
  if (mySelected && myFocused) {
    super.append(fragment,new SimpleTextAttributes(attributes.getStyle(),UIManager.getColor("Tree.selectionForeground")));
  }
 else {
    super.append(fragment,attributes);
  }
}
