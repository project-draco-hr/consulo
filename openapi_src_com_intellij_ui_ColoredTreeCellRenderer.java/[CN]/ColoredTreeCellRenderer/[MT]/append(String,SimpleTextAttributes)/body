{
  if (mySelected && myFocused) {
    super.append(fragment,new SimpleTextAttributes(attributes.getStyle(),UIUtil.getTreeSelectionForeground()));
  }
 else {
    super.append(fragment,attributes);
  }
}
