{
  moduleRootModel.iterateContentEntries(new Processor<ContentEntry>(){
    @Override public boolean process(    ContentEntry contentEntry){
      VirtualFile file=contentEntry.getFile();
      return file == null || processor.process(file);
    }
  }
);
}
