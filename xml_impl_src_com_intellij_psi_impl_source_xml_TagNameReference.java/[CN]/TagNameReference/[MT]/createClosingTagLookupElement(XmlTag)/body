{
  LookupElementBuilder builder=LookupElementBuilder.create(myNameElement.getText().contains(":") ? tag.getLocalName() : tag.getName());
  return TailTypeDecorator.withTail(AutoCompletionPolicy.GIVE_CHANCE_TO_OVERWRITE.applyPolicy(builder),TailType.createSimpleTailType('>'));
}
