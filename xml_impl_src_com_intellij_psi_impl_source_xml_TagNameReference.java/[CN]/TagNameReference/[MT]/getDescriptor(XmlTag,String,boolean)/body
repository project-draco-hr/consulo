{
  XmlNSDescriptor nsDescriptor=element.getNSDescriptor(namespace,strict);
  if (nsDescriptor == null && PsiUtil.isInJspFile(element))   nsDescriptor=PsiUtil.getJspFile(element).getDocument().getRootTag().getNSDescriptor(namespace,strict);
  return nsDescriptor;
}
