{
  expect(subject.load(2)).andReturn("content".getBytes()).times(1);
  replay(subject);
  assertEquals("content".getBytes(),s.load(2));
  assertEquals("content".getBytes(),s.load(2));
  verify(subject);
}
