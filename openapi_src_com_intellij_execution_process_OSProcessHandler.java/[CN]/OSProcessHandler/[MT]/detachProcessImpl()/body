{
  new Thread(new Runnable(){
    public void run(){
      try {
        myProcess.getOutputStream().close();
      }
 catch (      IOException e) {
        LOG.error(e);
      }
      myWaitFor.detach();
    }
  }
).start();
}
