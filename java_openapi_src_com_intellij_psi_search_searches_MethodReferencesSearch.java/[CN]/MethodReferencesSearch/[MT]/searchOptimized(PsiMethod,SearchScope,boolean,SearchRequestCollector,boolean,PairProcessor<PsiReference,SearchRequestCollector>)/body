{
  final SearchRequestCollector nested=new SearchRequestCollector();
  collector.searchQuery(new QuerySearchRequest(search(new SearchParameters(method,scope,strictSignatureSearch,nested)),nested,inReadAction,processor));
}
