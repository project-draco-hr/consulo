{
  if (tag == null)   return;
  tagStarted(tag,tag.getLocalName());
  if (processSubTags) {
    XmlTag[] subTags=tag.getSubTags();
    for (    XmlTag subTag : subTags) {
      processTag(subTag);
    }
  }
  tagFinished(tag);
}
