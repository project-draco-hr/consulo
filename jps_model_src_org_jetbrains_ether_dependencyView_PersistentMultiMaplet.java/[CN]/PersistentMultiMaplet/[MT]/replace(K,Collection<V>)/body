{
  try {
    myCache.remove(key);
    if (value == null) {
      myMap.remove(key);
    }
 else {
      myMap.put(key,value);
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
