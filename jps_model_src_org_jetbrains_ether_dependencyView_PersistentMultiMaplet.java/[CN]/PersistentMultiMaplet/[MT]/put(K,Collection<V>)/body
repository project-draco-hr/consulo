{
  try {
    final Collection<V> x=map.get(key);
    if (x == null) {
      map.put(key,value);
    }
 else {
      x.addAll(value);
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
