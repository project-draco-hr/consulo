{
  final String relativeFileName=FileUtils.ensureTrailingSlash(directoryObject.getPath()) + FileUtils.removeLeadingSlash(directoryPathRelativeToDirectoryObject);
  return createInstance(relativeFileName);
}
