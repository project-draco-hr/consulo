{
  if (myResult != null && !myResult.toString().equals(myTextField.getText())) {
    myResult=null;
  }
  if (myCancelled) {
    return;
  }
  if (isPopupShowing() || myAutoPopup) {
    showPopup(false);
  }
 else {
    hideCurrentPopup();
  }
}
