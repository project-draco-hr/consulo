{
  final LibraryTablesRegistrar registrar=LibraryTablesRegistrar.getInstance();
  final LibrariesModifiableModel provider=myContext.myLevel2Providers.get(myLevel);
  createLibrariesNode(registrar.getLibraryTableByLevel(myLevel,myProject),provider,myContext.createModifiableModelProvider(myLevel,false));
}
