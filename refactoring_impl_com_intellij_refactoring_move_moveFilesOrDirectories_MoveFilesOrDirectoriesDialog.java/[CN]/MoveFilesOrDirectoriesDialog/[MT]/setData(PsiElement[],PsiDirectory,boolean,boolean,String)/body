{
  if (psiElements.length == 1) {
    String text;
    if (psiElements[0] instanceof PsiFile) {
      text=RefactoringBundle.message("move.file.0",((PsiFile)psiElements[0]).getVirtualFile().getPresentableUrl());
    }
 else {
      text=RefactoringBundle.message("move.directory.0",((PsiDirectory)psiElements[0]).getVirtualFile().getPresentableUrl());
    }
    myNameLabel.setText(text);
  }
 else {
    myNameLabel.setText((psiElements[0] instanceof PsiFile) ? RefactoringBundle.message("move.specified.files") : RefactoringBundle.message("move.specified.directories"));
  }
  myTargetDirectoryField.setText(initialTargetDirectory == null ? "" : initialTargetDirectory.getVirtualFile().getPresentableUrl());
  validateOKButton();
  myHelpID=helpID;
}
