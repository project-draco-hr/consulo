{
  boolean isRngTag=ApplicationLoader.RNG_NAMESPACE.equals(nsURI);
  if (!isRNG) {
    if (!isRngTag) {
      stop();
    }
 else {
      isRNG=true;
    }
  }
  isInclude=isRngTag && "include".equals(name);
}
