{
  final CompilerManager compilerManager=CompilerManager.getInstance(myProject);
  compilerManager.addTranslatingCompiler(new JavaFxCompiler(),new HashSet<FileType>(Arrays.asList(JavaFxFileType.INSTANCE,StdFileTypes.CLASS)),new HashSet<FileType>(Arrays.asList(StdFileTypes.CLASS)));
}
