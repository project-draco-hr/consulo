{
  ApplicationManager.getApplication().invokeLater(new Runnable(){
    public void run(){
      Messages.showMessageDialog("Cannot save file:\n" + e.getMessage(),"Cannot Save File",Messages.getErrorIcon());
    }
  }
);
}
