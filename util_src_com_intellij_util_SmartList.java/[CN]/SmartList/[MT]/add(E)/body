{
  if (mySize < 2) {
    myArray2[mySize]=e;
  }
 else {
    if (mySize == 2) {
      if (myList == null) {
        myList=new ArrayList<E>(3);
      }
      myList.add(myArray2[0]);
      myList.add(myArray2[1]);
    }
    myList.add(e);
  }
  mySize++;
  return true;
}
