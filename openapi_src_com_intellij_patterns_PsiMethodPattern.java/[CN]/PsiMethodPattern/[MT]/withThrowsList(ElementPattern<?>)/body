{
  return with(new PatternCondition<PsiMethod>(){
    public boolean accepts(    @NotNull final PsiMethod method,    final MatchingContext matchingContext,    @NotNull final TraverseContext traverseContext){
      return pattern.accepts(method.getThrowsList());
    }
  }
);
}
