{
  if (ch == '*') {
    buf.append("(\\w|:)");
  }
 else   if ("{}[].+^$()?".indexOf(ch) != -1) {
    buf.append('\\');
  }
  if (Character.isUpperCase(ch)) {
    buf.append("[A-Za-z_]*");
  }
  buf.append(ch);
}
