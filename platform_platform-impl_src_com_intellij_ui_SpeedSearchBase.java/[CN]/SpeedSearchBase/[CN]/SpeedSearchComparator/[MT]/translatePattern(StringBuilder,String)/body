{
  if (myShouldMatchFromTheBeginning)   buf.append('^');
  final int len=pattern.length();
  for (int i=0; i < len; ++i) {
    translateCharacter(buf,pattern.charAt(i));
  }
  if (buf.length() > 0 && "*^".indexOf(buf.charAt(buf.length() - 1)) == -1)   buf.append(')');
}
