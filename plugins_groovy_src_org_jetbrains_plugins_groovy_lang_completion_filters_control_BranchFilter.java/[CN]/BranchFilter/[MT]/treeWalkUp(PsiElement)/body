{
  PsiElement parent=elem.getParent();
  while (parent != null && !(parent instanceof GrWhileStatement || parent instanceof GrForStatement || parent instanceof GrCaseSection)) {
    parent=parent.getParent();
  }
  return parent != null;
}
