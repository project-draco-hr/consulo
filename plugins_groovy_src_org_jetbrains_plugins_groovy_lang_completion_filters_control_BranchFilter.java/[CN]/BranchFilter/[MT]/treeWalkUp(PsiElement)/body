{
  PsiElement parent=elem.getParent();
  while (parent != null && !(parent instanceof GrWhileStatement || parent instanceof GrForStatement || parent instanceof GrCaseBlock)) {
    parent=parent.getParent();
  }
  return parent != null;
}
