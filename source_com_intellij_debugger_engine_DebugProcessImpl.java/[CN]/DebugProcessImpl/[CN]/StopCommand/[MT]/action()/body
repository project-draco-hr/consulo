{
  if (isAttached()) {
    if (myIsTerminateTargetVM) {
      getVirtualMachineProxy().exit(-1);
    }
 else {
      getVirtualMachineProxy().dispose();
    }
  }
 else {
    stopConnecting();
  }
}
