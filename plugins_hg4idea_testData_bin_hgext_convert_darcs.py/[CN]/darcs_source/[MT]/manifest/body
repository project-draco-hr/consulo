def manifest(self):
    man = []
    (output, status) = self.run('show', 'files', no_directories=True, repodir=self.tmppath)
    self.checkexit(status)
    for line in output.split('\n'):
        path = line[2:]
        if path:
            man.append(path)
    return man
