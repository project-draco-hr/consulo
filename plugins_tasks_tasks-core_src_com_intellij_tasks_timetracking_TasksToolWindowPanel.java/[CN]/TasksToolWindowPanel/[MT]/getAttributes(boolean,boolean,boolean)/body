{
  return new SimpleTextAttributes(isRunning ? SimpleTextAttributes.STYLE_BOLD : SimpleTextAttributes.STYLE_PLAIN,isSelected ? UIUtil.getTableSelectionForeground() : isClosed && !isRunning ? UIUtil.getLabelDisabledForeground() : UIUtil.getTableForeground());
}
