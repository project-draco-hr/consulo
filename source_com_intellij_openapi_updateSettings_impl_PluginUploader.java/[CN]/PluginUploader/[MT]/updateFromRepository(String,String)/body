{
  @NonNls final String url=RepositoryHelper.DOWNLOAD_URL + URLEncoder.encode(pluginId,"UTF8") + "&build="+ ApplicationInfo.getInstance().getBuildNumber();
  new PluginUploader(pluginId,url,pluginVersion).prepareToInstall();
}
