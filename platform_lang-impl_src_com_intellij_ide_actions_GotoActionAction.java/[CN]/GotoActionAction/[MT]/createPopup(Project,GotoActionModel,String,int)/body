{
  return ChooseByNamePopup.createPopup(project,model,new DefaultChooseByNameItemProvider(null){
    @Override protected void sortNamesList(    @NotNull String namePattern,    @NotNull List<MatchResult> namesList){
      Collections.sort(namesList,ELEMENTS_COMPARATOR);
    }
  }
,initialText,false,initialIndex);
}
