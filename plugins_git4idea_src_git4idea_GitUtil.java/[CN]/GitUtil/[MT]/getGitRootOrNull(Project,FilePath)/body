{
  VirtualFile root=VcsUtil.getVcsRootFor(project,filePath);
  while (root != null) {
    if (root.findFileByRelativePath(".git") != null) {
      break;
    }
    root=root.getParent();
  }
  return root;
}
