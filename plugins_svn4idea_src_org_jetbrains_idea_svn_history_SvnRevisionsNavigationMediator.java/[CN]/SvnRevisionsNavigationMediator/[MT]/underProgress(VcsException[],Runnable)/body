{
  final boolean succeeded=ProgressManager.getInstance().runProcessWithProgressSynchronously(process,"Getting latest repository revision",true,myProject);
  if (exception[0] != null) {
    throw exception[0];
  }
  if (!succeeded) {
    throw new ProcessCanceledException();
  }
}
