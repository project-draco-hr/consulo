{
  String result=count > 1024 * 1024 ? (count / 1024 / 1024) + "M" : count > 1024 ? (count / 1024) + "K" : String.valueOf(count);
  if (full) {
    return result + " characters";
  }
  return result;
}
