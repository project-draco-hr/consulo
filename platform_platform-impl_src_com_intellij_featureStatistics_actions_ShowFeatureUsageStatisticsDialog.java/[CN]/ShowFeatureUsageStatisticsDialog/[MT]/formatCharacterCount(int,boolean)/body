{
  String result=count > 1024 * 1024 ? (count / 1024 / 1024) + "MB " : count > 1024 ? (count / 1024) + "KB " : count + " characters";
  if (full && count > 1024) {
    return result + " code";
  }
  return result;
}
