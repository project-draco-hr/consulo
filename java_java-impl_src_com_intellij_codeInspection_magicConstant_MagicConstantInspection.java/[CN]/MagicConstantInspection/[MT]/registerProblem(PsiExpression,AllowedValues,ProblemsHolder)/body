{
  String values=StringUtil.join(allowedValues.values,new Function<PsiAnnotationMemberValue,String>(){
    @Override public String fun(    PsiAnnotationMemberValue value){
      return value.getText();
    }
  }
,", ");
  holder.registerProblem(argument,"Must be one of the: " + values);
}
