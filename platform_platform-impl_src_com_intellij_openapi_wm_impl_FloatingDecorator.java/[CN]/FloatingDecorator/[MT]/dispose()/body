{
  if (ScreenUtil.isStandardAddRemoveNotify(getParent()))   Disposer.dispose(myDelayAlarm);
 else {
    if (isShowing()) {
      SwingUtilities.invokeLater(new Runnable(){
        @Override public void run(){
          show();
        }
      }
);
    }
  }
  super.dispose();
}
