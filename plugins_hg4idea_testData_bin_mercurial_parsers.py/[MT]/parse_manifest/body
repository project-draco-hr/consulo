def parse_manifest(mfdict, fdict, lines):
    for l in lines.splitlines():
        (f, n) = l.split('\x00')
        if (len(n) > 40):
            fdict[f] = n[40:]
            mfdict[f] = bin(n[:40])
        else:
            mfdict[f] = bin(n)
