{
  if (location instanceof PsiBinaryExpression) {
    return "#ref can be simplified to " + calculateSimplifiedBinaryExpression((PsiBinaryExpression)location) + " #loc";
  }
 else {
    return "#ref can be simplified to " + calculateSimplifiedPrefixExpression((PsiPrefixExpression)location) + " #loc";
  }
}
