{
  if (myConstructor != null) {
    return "Replace constructor " + UsageViewUtil.getDescriptiveName(myConstructor) + " with a factory method";
  }
 else {
    return "Replace default constructor of " + UsageViewUtil.getDescriptiveName(myOriginalClass) + " with a factory method";
  }
}
