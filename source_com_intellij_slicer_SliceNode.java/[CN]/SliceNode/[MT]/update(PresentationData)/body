{
  if (!initialized) {
    initializeDuplicateFlag();
    initialized=true;
  }
  if (presentation != null) {
    presentation.setChanged(presentation.isChanged() || changed);
    changed=false;
  }
}
