{
  if (block == null) {
    return;
  }
  final PsiStatement[] statements=block.getStatements();
  for (int i=0; i < statements.length; i++) {
    calculateVariablesDeclared(statements[i],variablesDeclaredAtTopLevel,variablesDeclaredAtLowerLevels,isTopLevel);
  }
}
