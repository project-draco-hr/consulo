{
  if (now) {
    mySpotlightPainter.updateForCurrentConfigurable();
  }
 else {
    mySpotlightUpdate.queue(new Update(this){
      public void run(){
        mySpotlightPainter.updateForCurrentConfigurable();
      }
    }
);
  }
}
