{
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      if (myDialog != null) {
        GeneralSettings.getInstance().setSearchInBackground(true);
        myDialog.background();
        myDialog=null;
      }
    }
  }
);
}
