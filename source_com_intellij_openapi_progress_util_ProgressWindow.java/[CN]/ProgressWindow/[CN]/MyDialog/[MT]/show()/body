{
  if (myPopup != null) {
    myPopup.cancel();
  }
  myPopup=JBPopupFactory.getInstance().createComponentPopupBuilder(myPanel,myCancelButton).setForceHeavyweight(true).setRequestFocus(true).createPopup();
  myPopup.showInCenterOf(myParentWindow);
  StackingPopupDispatcher.onPopupHidden(myPopup);
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      myCancelButton.requestFocus();
    }
  }
);
}
