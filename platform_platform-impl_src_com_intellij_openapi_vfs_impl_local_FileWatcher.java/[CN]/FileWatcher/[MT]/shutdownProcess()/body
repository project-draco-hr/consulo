{
  if (notifierProcess != null) {
    if (isAlive()) {
      try {
        writeLine(EXIT_COMMAND);
      }
 catch (      IOException ignore) {
      }
    }
    notifierProcess=null;
    notifierReader=null;
    notifierWriter=null;
  }
}
