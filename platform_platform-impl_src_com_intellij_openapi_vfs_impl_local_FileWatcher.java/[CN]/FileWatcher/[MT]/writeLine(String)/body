{
  if (LOG.isDebugEnabled()) {
    LOG.debug("<< " + line);
  }
  final BufferedWriter writer=myNotifierWriter;
  if (writer != null) {
    writer.write(line);
    writer.newLine();
    writer.flush();
  }
}
