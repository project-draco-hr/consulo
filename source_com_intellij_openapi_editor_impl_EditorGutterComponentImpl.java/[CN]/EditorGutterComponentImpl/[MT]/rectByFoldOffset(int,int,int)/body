{
  VisualPosition foldStart=offsetToLineStartPosition(offset);
  int anchorY=myEditor.visibleLineNumberToYPosition(foldStart.line) + myEditor.getLineHeight() - myEditor.getDescent() - anchorWidth;
  Rectangle rect=new Rectangle(anchorX,anchorY,anchorWidth,anchorWidth);
  return rect;
}
