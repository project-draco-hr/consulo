{
  if (context == null)   return false;
  ASTNode node=context.getNode();
  return node != null && node.getElementType() == GroovyDocTokenTypes.mGDOC_TAG_NAME && context.getParent() instanceof GrDocInlinedTag;
}
