{
  final String className=StringUtil.getPackageName(val);
  final PsiLiteralExpression element=getElement();
  return StringUtil.isEmpty(className) ? PsiUtil.getTopLevelClass(element) : JavaPsiFacade.getInstance(element.getProject()).findClass(className,element.getResolveScope());
}
