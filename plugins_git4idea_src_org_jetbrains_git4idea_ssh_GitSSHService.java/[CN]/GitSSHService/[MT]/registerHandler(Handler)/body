{
  while (true) {
    int rnd=RANDOM.nextInt();
    if (rnd == Integer.MIN_VALUE) {
      continue;
    }
    rnd=Math.abs(rnd);
    if (handlers.containsKey(rnd)) {
      continue;
    }
    handlers.put(rnd,handler);
    return rnd;
  }
}
