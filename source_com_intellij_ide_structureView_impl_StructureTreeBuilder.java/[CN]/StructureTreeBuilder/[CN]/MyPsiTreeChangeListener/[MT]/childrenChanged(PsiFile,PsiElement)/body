{
  if (!myFile.isValid()) {
    myUpdater.cancelAllRequests();
    return;
  }
  if (!myFile.equals(file)) {
    myUpdater.addSubtreeToUpdate(myRootNode);
    return;
  }
  myUpdater.addSubtreeToUpdate(myRootNode);
  return;
}
