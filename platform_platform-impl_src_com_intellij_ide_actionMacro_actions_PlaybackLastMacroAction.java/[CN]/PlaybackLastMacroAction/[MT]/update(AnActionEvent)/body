{
  e.getPresentation().setEnabled(!ActionMacroManager.getInstance().isPlaying() && ActionMacroManager.getInstance().hasRecentMacro() && PlatformDataKeys.EDITOR.getData(e.getDataContext()) != null);
}
