{
  super(hash,Collections.<Hash>emptyList(),-1,root,"Loading...",STUB_USER,"",STUB_USER,-1,new ThrowableComputable<Collection<Change>,Exception>(){
    @Override public Collection<Change> compute() throws Exception {
      return Collections.emptyList();
    }
  }
);
  myLoadingTaskIndex=loadingTaskIndex;
}
