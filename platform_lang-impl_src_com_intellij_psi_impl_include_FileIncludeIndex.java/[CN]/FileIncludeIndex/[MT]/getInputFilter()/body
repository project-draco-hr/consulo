{
  return new InputFilter(){
    @Override public boolean acceptInput(    VirtualFile file){
      if (file.getFileSystem() == JarFileSystem.getInstance()) {
        return false;
      }
      for (      FileIncludeProvider provider : myProviders) {
        if (provider.acceptFile(file)) {
          return true;
        }
      }
      return false;
    }
  }
;
}
