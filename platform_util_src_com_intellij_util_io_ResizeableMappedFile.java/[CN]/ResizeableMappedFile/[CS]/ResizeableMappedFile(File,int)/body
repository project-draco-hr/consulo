{
  myStorage=new PagedFileStorage(file);
  if (!file.exists() || file.length() == 0) {
    writeLength(0);
  }
  myLogicalSize=readLength();
  if (myLogicalSize == 0) {
    resize(initialSize);
  }
}
