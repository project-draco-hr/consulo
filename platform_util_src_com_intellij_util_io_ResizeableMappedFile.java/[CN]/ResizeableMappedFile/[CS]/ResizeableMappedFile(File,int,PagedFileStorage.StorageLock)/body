{
  myStorage=new PagedFileStorage(file,lock);
  if (!file.exists() || file.length() == 0) {
    writeLength(0);
  }
  myLogicalSize=readLength();
  if (myLogicalSize == 0) {
synchronized (lock) {
      resize(initialSize);
    }
  }
}
