{
  if (myVisible == value) {
    return;
  }
  myVisible=value;
  if (myVisible) {
    GwtUIAccess gwtUIAccess=GwtUIAccess.get();
    gwtUIAccess.showModal(this);
  }
 else {
    disposeImpl();
  }
  markAsChanged();
}
