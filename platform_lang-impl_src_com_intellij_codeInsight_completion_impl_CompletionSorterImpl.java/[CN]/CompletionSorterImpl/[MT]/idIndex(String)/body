{
  return ContainerUtil.indexOf(myMembers,new Condition<ClassifierFactory<LookupElement>>(){
    @Override public boolean value(    ClassifierFactory<LookupElement> factory){
      return id.equals(factory.getId());
    }
  }
);
}
