{
  Font font=getSmallFont();
  FontMetrics fontMetrics=g.getFontMetrics(font);
  g.setColor(Color.BLACK);
  g.setFont(font);
  g.drawString(tc.getFileSizeText(),8,BLANK_ICON.getIconHeight() + 4 - fontMetrics.getHeight() + fontMetrics.getAscent());
}
