{
  Font labelFont=UIUtil.getLabelFont();
  FontMetrics fontMetrics=c.getFontMetrics(labelFont);
  return new Dimension(BLANK_ICON.getIconWidth() + 10,BLANK_ICON.getIconHeight() + fontMetrics.getHeight() + 15);
}
