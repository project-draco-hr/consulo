{
  CheckUtil.checkWritable(element);
  if (!SourceTreeToPsiMap.hasTreeElement(element))   return;
  new ReferenceAdjuster(getSettings()).processRange(SourceTreeToPsiMap.psiElementToTree(element),startOffset,endOffset);
}
