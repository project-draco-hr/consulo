{
  if (myMappingsLoaded)   return;
  for (  VirtualFile file : ModuleRootManager.getInstance(module).getContentRoots()) {
    setDirectoryMapping(file.getPath(),activeVcsName);
  }
  sortDirectoryMappings();
  removeRedundantMappings();
}
