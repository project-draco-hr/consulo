{
  while (bar())   ;
  while (bar()) {
  }
  for (int i=0; i < 4; i++)   ;
  for (int i=0; i < 4; i++) {
  }
  if (bar())   ;
  if (bar()) {
  }
  if (bar()) {
    return;
  }
 else {
  }
  if (bar()) {
    return;
  }
 else   ;
switch (j) {
  }
}
