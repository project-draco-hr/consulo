{
  while (nextLeaf != null && (nextLeaf instanceof PsiWhiteSpace || nextLeaf instanceof PsiComment || nextLeaf instanceof PsiErrorElement)) {
    nextLeaf=nextLeaf.getNextSibling();
  }
  return nextLeaf;
}
