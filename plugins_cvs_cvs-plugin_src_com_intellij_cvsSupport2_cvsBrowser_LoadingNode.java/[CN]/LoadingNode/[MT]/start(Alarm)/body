{
  myPeriodRequest=new Runnable(){
    public void run(){
      if (getParent() != null) {
        updatePeriod();
        if (!stopped) {
          alarm.addRequest(this,100);
        }
      }
    }
  }
;
  alarm.addRequest(myPeriodRequest,100);
}
