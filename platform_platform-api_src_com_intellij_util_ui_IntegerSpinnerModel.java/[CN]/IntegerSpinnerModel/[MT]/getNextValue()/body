{
  int result;
  if (myMaxValue >= 0 && myValue == myMaxValue) {
    if (myMinValue < 0)     return null;
    result=myMinValue;
  }
 else {
    result=myValue + 1;
  }
  return new Integer(result);
}
