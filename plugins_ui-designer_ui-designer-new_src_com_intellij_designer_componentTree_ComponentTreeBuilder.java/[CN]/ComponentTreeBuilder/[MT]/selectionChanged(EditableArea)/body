{
  try {
    removeListeners();
    if (mySurfaceArea == area) {
      selectFromSurface();
    }
 else {
      mySurfaceArea.setSelection(myTreeArea.getSelection());
    }
  }
  finally {
    addListeners();
  }
}
