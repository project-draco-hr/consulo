{
  try {
    if (isUnitTestingMode()) {
      acquireLock();
    }
 else {
      attemptLock();
    }
    myCancelRequest.set(requested);
  }
 catch (  InterruptedException e) {
    return;
  }
 finally {
    releaseLock();
  }
}
