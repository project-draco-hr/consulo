{
  try {
    if (isUnitTestingMode()) {
      acquireLock();
    }
 else {
      attemptLock();
    }
    myCancelRequest.set(requested);
  }
 catch (  InterruptedException ignored) {
  }
 finally {
    releaseLock();
  }
}
