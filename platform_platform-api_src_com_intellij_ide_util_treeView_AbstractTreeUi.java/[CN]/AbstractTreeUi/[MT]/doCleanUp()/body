{
  Runnable cleanup=new Runnable(){
    public void run(){
      if (!isReleased()) {
        cleanUpNow();
      }
    }
  }
;
  if (isPassthroughMode()) {
    cleanup.run();
  }
 else {
    UIUtil.invokeLaterIfNeeded(cleanup);
  }
}
