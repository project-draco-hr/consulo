{
synchronized (PsiLock.LOCK) {
    if (myTags == null) {
      PsiDocTag[] tags=new PsiDocTag[1];
      tags[0]=new ClsDocTagImpl(this,"@deprecated");
      myTags=tags;
    }
    return myTags;
  }
}
