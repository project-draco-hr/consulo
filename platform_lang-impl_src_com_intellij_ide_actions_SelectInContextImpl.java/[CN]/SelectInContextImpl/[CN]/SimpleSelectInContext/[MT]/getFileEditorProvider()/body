{
  return new FileEditorProvider(){
    public FileEditor openFileEditor(){
      final FileEditor[] fileEditors=FileEditorManager.getInstance(getProject()).openFile(myElementToSelect.getContainingFile().getVirtualFile(),false);
      return fileEditors.length > 0 ? fileEditors[0] : null;
    }
  }
;
}
