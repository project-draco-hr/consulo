{
  setChangesToDisplay(new ArrayList<VirtualFile>(Collections2.filter(getIncludedChanges(),new Predicate<VirtualFile>(){
    @Override public boolean apply(    @Nullable VirtualFile input){
      return input != null && input.isValid();
    }
  }
)));
}
