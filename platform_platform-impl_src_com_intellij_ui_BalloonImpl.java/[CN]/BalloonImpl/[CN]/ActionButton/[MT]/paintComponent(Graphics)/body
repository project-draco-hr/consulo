{
  super.paintComponent(g);
  if (hasPaint()) {
    if (myHoverIcon != null && myButton.isHovered()) {
      paintIcon(g,myHoverIcon);
      showHint(true);
    }
 else {
      paintIcon(g,myIcon);
      showHint(false);
    }
  }
}
