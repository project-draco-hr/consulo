def _readmeta(self, node):
    t = self.revision(node)
    if (not t.startswith('\x01\n')):
        return {}
    s = t.index('\x01\n', 2)
    mt = t[2:s]
    m = {}
    for l in mt.splitlines():
        (k, v) = l.split(': ', 1)
        m[k] = v
    return m
