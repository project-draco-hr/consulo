{
  myMeaningfulRows=0;
  for (  int row : rows) {
    myCommitI=tableModel.getCommitAt(row);
    if (!myCommitI.holdsDecoration()) {
      ++myMeaningfulRows;
      if (myMeaningfulRows > 1)       break;
    }
  }
}
