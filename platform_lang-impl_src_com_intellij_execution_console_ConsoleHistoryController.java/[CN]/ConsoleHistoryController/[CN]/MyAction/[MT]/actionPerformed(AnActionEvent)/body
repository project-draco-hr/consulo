{
  final String command;
  command=myNext ? getModel().getHistoryNext() : StringUtil.notNullize(getModel().getHistoryPrev(),myMultiline ? "" : StringUtil.notNullize(myHelper.getContent()));
  setConsoleText(command,myNext && getModel().getHistoryCursor() == 0,true);
}
