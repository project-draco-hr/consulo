{
  final SVNURL url=entryPath == null ? myRepositoryRoot.appendPath(myLastPath,true) : myRepositoryRoot.appendPath(entryPath.getPath(),true);
  return new SvnFileRevision(myVcs,SVNRevision.UNDEFINED,logEntry,url.toString(),copyPath,null);
}
