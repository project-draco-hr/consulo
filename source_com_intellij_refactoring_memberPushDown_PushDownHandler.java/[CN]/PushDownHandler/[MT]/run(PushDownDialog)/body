{
  new PushDownProcessor(myProject,dialog.getSelectedMemberInfos(),myClass,new JavaDocPolicy(dialog.getJavaDocPolicy()),dialog.isPreviewUsages(),new Runnable(){
    public void run(){
      dialog.close(DialogWrapper.CANCEL_EXIT_CODE);
    }
  }
).run(null);
}
