{
  new PushDownProcessor(myProject,dialog.getSelectedMemberInfos(),myClass,new JavaDocPolicy(dialog.getJavaDocPolicy()),new Runnable(){
    public void run(){
      dialog.close(DialogWrapper.CANCEL_EXIT_CODE);
    }
  }
).run(null);
}
