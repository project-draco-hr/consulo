{
  if (myExporter == null) {
    buf.append(FONT_OPENING);
    buf.append(CLOSE_TAG);
    buf.append("<a HREF=\"file://bred.txt#invoke:" + index);
    buf.append("\">");
    buf.append(text);
    buf.append("</a></font>");
  }
}
