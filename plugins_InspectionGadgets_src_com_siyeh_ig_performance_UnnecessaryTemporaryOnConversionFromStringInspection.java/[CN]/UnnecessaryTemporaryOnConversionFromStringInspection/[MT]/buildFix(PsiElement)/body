{
  final String replacementExpression=calculateReplacementExpression(location);
  if (replacementExpression == null) {
    return null;
  }
  final String name=InspectionGadgetsBundle.message("unnecessary.temporary.on.conversion.from.string.fix.name",replacementExpression);
  return new UnnecessaryTemporaryObjectFix(name);
}
