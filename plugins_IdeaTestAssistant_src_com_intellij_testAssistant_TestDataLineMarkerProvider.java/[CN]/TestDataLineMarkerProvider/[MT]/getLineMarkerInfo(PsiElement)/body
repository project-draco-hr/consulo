{
  if (ApplicationManager.getApplication().isUnitTestMode()) {
    return null;
  }
  if (element instanceof PsiMethod) {
    final PsiMethod method=(PsiMethod)element;
    if (isTestMethod(method)) {
      return new LineMarkerInfo<PsiMethod>(method,method.getModifierList().getTextRange(),AllIcons.Nodes.TestPackage,Pass.UPDATE_ALL,null,new TestDataNavigationHandler(),GutterIconRenderer.Alignment.LEFT);
    }
  }
  return null;
}
