{
  return ContainerUtil.find(textEditorHighlightingPasses,new Condition<TextEditorHighlightingPass>(){
    @Override public boolean value(    TextEditorHighlightingPass pass){
      return pass.getId() == id;
    }
  }
);
}
