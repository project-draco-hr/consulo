{
  buffer.append('{');
  for (int i=0; i < myInitializers.length; i++) {
    ((ClsElementImpl)myInitializers[i]).appendMirrorText(0,buffer);
    if (i > 0) {
      buffer.append(" ,");
    }
  }
  buffer.append('}');
}
