{
  super.setUp();
  initApplication(this);
  doSetup(new SimpleLightProjectDescriptor(getModuleType(),getProjectJDK()),configureLocalInspectionTools(),myAvailableInspectionTools,this);
  ((InjectedLanguageManagerImpl)InjectedLanguageManager.getInstance(getProject())).pushInjectors();
  storeSettings();
  myThreadTracker=new ThreadTracker();
}
