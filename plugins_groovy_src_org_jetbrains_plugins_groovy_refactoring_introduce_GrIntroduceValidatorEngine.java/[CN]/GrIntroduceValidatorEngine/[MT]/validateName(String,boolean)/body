{
  String result=name;
  if (isOKImpl(name,true).size() > 0 && !increaseNumber || name.length() == 0) {
    return "";
  }
  int i=1;
  while (isOKImpl(result,true).size() > 0) {
    result=name + i;
    i++;
  }
  return result;
}
