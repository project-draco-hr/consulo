{
  return new DescriptorComposer(this){
    @Override protected void composeAdditionalDescription(    final StringBuffer buf,    final RefEntity refEntity){
      getTool().compose(buf,refEntity,this);
    }
  }
;
}
