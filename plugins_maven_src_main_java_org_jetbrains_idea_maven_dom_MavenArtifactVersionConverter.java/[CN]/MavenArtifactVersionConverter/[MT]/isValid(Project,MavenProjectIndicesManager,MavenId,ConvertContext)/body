{
  if (StringUtil.isEmpty(id.groupId) || StringUtil.isEmpty(id.artifactId) || StringUtil.isEmpty(id.version)) {
    return false;
  }
  if (isVersionRange(id))   return true;
  return manager.hasVersion(id.groupId,id.artifactId,id.version);
}
