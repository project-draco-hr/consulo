{
  DirectoryInfo info=getInfoForFileOrDirectory(fileOrDir);
  return info != null && info.isInModuleSource() && myDirectoryIndex.getContentFolderType(info) == ProductionResourceContentFolderTypeProvider.getInstance();
}
