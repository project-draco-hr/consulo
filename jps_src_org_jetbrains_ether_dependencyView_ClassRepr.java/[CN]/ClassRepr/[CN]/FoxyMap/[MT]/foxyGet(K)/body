{
  final Object c=get(key);
  if (c == null) {
    return null;
  }
  if (c instanceof Collection) {
    return (Collection)c;
  }
  final List<V> l=new LinkedList<V>();
  l.add((V)c);
  return l;
}
