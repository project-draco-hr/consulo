{
  final TextAttributesKey key=getColorAttributesKey(fragment.getType());
  if (key != null) {
    final FragmentNumberGutterIconRenderer renderer=new FragmentNumberGutterIconRenderer(myCounter + 1,key,myAppender1.getEditor().getScrollPane());
    ++myCounter;
    return renderer;
  }
  return null;
}
