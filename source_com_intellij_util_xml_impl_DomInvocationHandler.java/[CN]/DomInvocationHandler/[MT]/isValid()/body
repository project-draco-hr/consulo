{
  if (!myInvalidated) {
    if (myXmlTag != null) {
      if (!myXmlTag.isValid()) {
        myInvalidated=true;
        return false;
      }
    }
    if (myParent != null && !myParent.isValid()) {
      myInvalidated=true;
      return false;
    }
  }
  return !myInvalidated;
}
