{
  return ContainerUtil.map(conflicts,new Function<MergeRange,MergeWordFragment>(){
    @Override public MergeWordFragment fun(    MergeRange ch){
      return new MergeWordFragmentImpl(ch);
    }
  }
);
}
