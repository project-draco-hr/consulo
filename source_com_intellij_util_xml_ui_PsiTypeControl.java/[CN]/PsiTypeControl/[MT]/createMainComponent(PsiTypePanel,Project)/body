{
  if (boundedComponent == null) {
    boundedComponent=new PsiTypePanel();
  }
  return PsiClassControl2.initReferenceEditorWithBrowseButton(boundedComponent,new ReferenceEditorWithBrowseButton(null,project,new Factory<Document>(){
    public Document create(){
      return ReferenceEditorWithBrowseButton.createTypeDocument("",PsiManager.getInstance(project));
    }
  }
),this);
}
