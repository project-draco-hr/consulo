{
  return ParserUtils.lookAhead(builder,mIDENT) || TokenSets.BUILT_IN_TYPE.contains(builder.getTokenType()) || TokenSets.MODIFIERS.contains(builder.getTokenType())|| ParserUtils.lookAhead(builder,kDEF)|| ParserUtils.lookAhead(builder,mAT)|| ParserUtils.lookAhead(builder,mSTRING_LITERAL);
}
