{
  return builder.getTokenType() == mIDENT || TokenSets.BUILT_IN_TYPE.contains(builder.getTokenType()) || TokenSets.MODIFIERS.contains(builder.getTokenType()) || builder.getTokenType() == kDEF || builder.getTokenType() == mAT || builder.getTokenType() == mASSIGN || builder.getTokenType() == mGSTRING_LITERAL || builder.getTokenType() == mSTRING_LITERAL;
}
