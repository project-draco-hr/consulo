{
  if (myClassName.equals(otherChecker.myClassName) && myMethodName.equals(otherChecker.myMethodName) && myParameterClassList.equals(otherChecker.myParameterClassList)&& myCheckedParameterIndex == otherChecker.myCheckedParameterIndex) {
    return true;
  }
  return false;
}
