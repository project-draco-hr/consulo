{
  T state=myMemCache.get(url);
  if (state != null) {
    return state;
  }
  try {
    return state=myMap.get(url);
  }
  finally {
    if (state != null) {
      myMemCache.put(url,state);
    }
  }
}
