{
  if (context != null && HtmlUtil.isHtml5Context(context) && attributeName.startsWith(HtmlUtil.HTML5_DATA_ATTR_PREFIX)) {
    return new AnyXmlAttributeDescriptor(attributeName);
  }
  return null;
}
