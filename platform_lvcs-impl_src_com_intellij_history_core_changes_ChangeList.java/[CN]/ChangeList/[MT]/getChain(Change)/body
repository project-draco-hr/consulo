{
  List<Change> result=new ArrayList<Change>();
  for (  Change c : myChanges) {
    if (c == initialChange) {
      result.add(c);
      continue;
    }
    if (c.affectsSameAs(result))     result.add(c);
  }
  return result;
}
