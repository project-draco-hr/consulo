{
  try {
    for (    MavenIndex data : myIndices) {
      try {
        data.close();
      }
 catch (      MavenIndexException e) {
        MavenLog.warn(e);
      }
    }
  }
  finally {
    myIndices.clear();
  }
}
