{
  FileTypeManager fileTypeManager=FileTypeManager.getInstance();
  if (!fileTypeManager.isFileIgnored(file) && file.getFileType() == StdFileTypes.JAVA) {
    ourJavaFilesCount+=increase;
    return true;
  }
  return false;
}
