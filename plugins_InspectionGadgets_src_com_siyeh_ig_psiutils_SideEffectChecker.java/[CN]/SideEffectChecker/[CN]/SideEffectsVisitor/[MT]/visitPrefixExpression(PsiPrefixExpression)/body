{
  super.visitPrefixExpression(expression);
  final PsiJavaToken sign=expression.getOperationSign();
  if (sign.getTokenType() == JavaTokenType.PLUSPLUS || sign.getTokenType() == JavaTokenType.PLUSPLUS) {
    m_mayHaveSideEffects=true;
  }
}
