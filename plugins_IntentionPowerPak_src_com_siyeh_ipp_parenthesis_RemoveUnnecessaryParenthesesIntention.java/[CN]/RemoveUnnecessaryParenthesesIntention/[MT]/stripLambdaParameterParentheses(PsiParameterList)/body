{
  final PsiLambdaExpression expression=(PsiLambdaExpression)JavaPsiFacade.getElementFactory(element.getProject()).createExpressionFromText(element.getParameters()[0].getName() + "->{}",element);
  element.replace(expression.getParameterList());
}
