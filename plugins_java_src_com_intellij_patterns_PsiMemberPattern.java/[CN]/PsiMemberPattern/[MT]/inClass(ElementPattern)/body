{
  return with(new PatternConditionPlus<T,PsiClass>("inClass",pattern){
    @Override public boolean processValues(    T t,    ProcessingContext context,    PairProcessor<PsiClass,ProcessingContext> processor){
      return processor.process(t.getContainingClass(),context);
    }
  }
);
}
