{
  final StringBuilder buffer=new StringBuilder();
  for (  String goal : list) {
    if (buffer.length() != 0) {
      buffer.append(delim);
    }
    buffer.append(goal);
  }
  return buffer.toString();
}
