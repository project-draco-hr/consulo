{
  RepositoryTreeNode node=getNotNullSelectedNode();
  SVNURL url=node.getURL();
  AbstractVcsHelper.getInstance(myProject).showChangesBrowser(myVCS.getCommittedChangesProvider(),new DefaultRepositoryLocation(url.toString()),"Changes in " + url.toString(),getContentPane());
}
