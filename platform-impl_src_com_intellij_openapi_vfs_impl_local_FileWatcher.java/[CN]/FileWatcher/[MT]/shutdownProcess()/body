{
  if (notifierProcess != null) {
    if (isAlive()) {
      try {
        writeLine(EXIT_COMMAND);
      }
 catch (      IOException e) {
      }
    }
    notifierProcess=null;
    notifierReader=null;
    notifierWriter=null;
  }
}
