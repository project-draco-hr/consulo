{
  boolean changed=false;
  if (newOptionValue) {
    changed=searchContext != option;
    searchContext=option;
  }
 else   if (searchContext == option) {
    changed=true;
    searchContext=SearchContext.ANY;
  }
  if (changed) {
    notifyObservers();
  }
}
