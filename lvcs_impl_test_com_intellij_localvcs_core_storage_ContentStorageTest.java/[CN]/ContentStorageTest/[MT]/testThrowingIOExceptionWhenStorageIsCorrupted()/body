{
  int id=s.store("abc".getBytes());
  s.close();
  corruptStorageFile();
  try {
    s.load(id);
    fail();
  }
 catch (  IOException e) {
  }
  try {
    s.store("abc".getBytes());
    fail();
  }
 catch (  IOException e) {
  }
}
