{
  if (!updateStructure) {
    return getUi().queueUpdate(element,false);
  }
 else {
    if (forceResort) {
      getUi().incComparatorStamp();
    }
    return getUi().queueUpdate(element,true);
  }
}
