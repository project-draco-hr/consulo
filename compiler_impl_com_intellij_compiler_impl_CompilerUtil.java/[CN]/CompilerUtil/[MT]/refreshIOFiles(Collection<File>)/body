{
  if (files.size() == 0) {
    return;
  }
  doRefresh(new Runnable(){
    public void run(){
      final LocalFileSystem fs=LocalFileSystem.getInstance();
      fs.refresh(false);
      for (      File file : files) {
        fs.findFileByIoFile(file);
      }
    }
  }
);
}
