{
  doRefresh(new Runnable(){
    public void run(){
      final LocalFileSystem fs=LocalFileSystem.getInstance();
      if (files.size() < 10) {
        fs.refreshFiles(files);
      }
 else {
        fs.refresh(false);
      }
    }
  }
);
}
