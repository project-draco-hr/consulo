{
  Pair<ArrangementRuleEditingModelImpl,TIntIntHashMap> pair=myBuilder.build(new StdArrangementMatchRule(new StdArrangementEntryMatcher(matchCondition)),myTree,myRoot,null,myGroupingRules);
  assertNotNull(pair);
  myRowMappings.put(pair.first.getRow(),pair.first);
}
