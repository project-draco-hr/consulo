{
  Pair<ArrangementRuleEditingModelImpl,TIntIntHashMap> pair=myBuilder.build(new StdArrangementRule(new StdArrangementEntryMatcher(matchCondition)),myTree,myRoot,null,myGrouper);
  myRowMappings.put(pair.first.getRow(),pair.first);
}
