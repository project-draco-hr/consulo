{
  for (  int row : rows) {
    assertTrue(String.format("Expected to find mappings for rows %s. Actual: %s",Arrays.toString(rows),Arrays.toString(myRowMappings.keys())),myRowMappings.containsKey(row));
  }
}
