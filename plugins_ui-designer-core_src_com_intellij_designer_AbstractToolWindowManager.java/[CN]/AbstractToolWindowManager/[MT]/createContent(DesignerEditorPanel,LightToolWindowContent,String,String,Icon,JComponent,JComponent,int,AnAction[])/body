{
  return new LightToolWindow(content,title,title2,icon,component,focusedComponent,designer.getContentSplitter(),myStyle,this,myProject,myPropertiesComponent,getComponentName(),defaultWidth,actions);
}
