{
  while (object instanceof TestDecorator) {
    object=((TestDecorator)object).getTest();
  }
  if (myKnownKeys.containsKey(object))   return (String)myKnownKeys.get(object);
  return sendObject(object);
}
