{
  if (myActiveVcss.contains(vcs)) {
    vcs.deactivate();
    myActiveVcss.remove(vcs);
  }
  try {
    vcs.shutdown();
  }
 catch (  VcsException e) {
    LOG.info(e);
  }
  myVcss.remove(vcs);
  myCachedVCSs=null;
}
