{
  if (event instanceof MouseWheelEvent) {
    MouseWheelEvent wheel=(MouseWheelEvent)event;
    return 0 < wheel.getWheelRotation() ? BUTTON_WHEEL_DOWN : BUTTON_WHEEL_UP;
  }
  return event.getButton();
}
