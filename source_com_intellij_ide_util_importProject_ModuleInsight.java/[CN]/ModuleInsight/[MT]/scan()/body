{
  myProgress.setIndeterminate(true);
  myProgress.pushState();
  try {
    scanLibraries();
    if (myProgress.isCanceled()) {
      return;
    }
    scanModules();
  }
  finally {
    myProgress.popState();
  }
}
