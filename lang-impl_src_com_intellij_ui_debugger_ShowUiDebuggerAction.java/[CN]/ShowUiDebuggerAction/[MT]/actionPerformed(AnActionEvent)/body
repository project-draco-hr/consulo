{
  if (myDebugger == null) {
    myDebugger=new UiDebugger(){
      @Override public void dispose(){
        super.dispose();
        myDebugger=null;
      }
    }
;
  }
 else {
    myDebugger.show();
  }
}
