{
  if (object == this) {
    return true;
  }
  if (!(object instanceof HgContentRevision)) {
    return false;
  }
  HgContentRevision that=(HgContentRevision)object;
  return new EqualsBuilder().append(hgFile,that.hgFile).append(revisionNumber,that.revisionNumber).isEquals();
}
