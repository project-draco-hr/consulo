{
  if (object == this) {
    return true;
  }
  if (!(object instanceof HgContentRevision)) {
    return false;
  }
  HgContentRevision that=(HgContentRevision)object;
  return new EqualsBuilder().append(myHgFile,that.myHgFile).append(myRevisionNumber,that.myRevisionNumber).isEquals();
}
