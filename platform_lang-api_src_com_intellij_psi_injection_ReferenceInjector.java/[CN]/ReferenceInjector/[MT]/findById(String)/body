{
  return ContainerUtil.find(EXTENSION_POINT_NAME.getExtensions(),new Condition<ReferenceInjector>(){
    @Override public boolean value(    ReferenceInjector injector){
      return id.equals(injector.getId());
    }
  }
);
}
