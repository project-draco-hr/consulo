{
  PsiManager manager=aClass.getManager();
  PsiClass externalizableClass=JavaPsiFacade.getInstance(manager.getProject()).findClass("java.io.Externalizable",aClass.getResolveScope());
  return externalizableClass != null && aClass.isInheritor(externalizableClass,true);
}
