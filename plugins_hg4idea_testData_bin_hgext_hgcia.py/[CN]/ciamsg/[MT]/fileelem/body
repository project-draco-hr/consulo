def fileelem(self, path, uri, action):
    if uri:
        uri = (' uri=%s' % saxutils.quoteattr(uri))
    return ('<file%s action=%s>%s</file>' % (uri, saxutils.quoteattr(action), saxutils.escape(path)))
