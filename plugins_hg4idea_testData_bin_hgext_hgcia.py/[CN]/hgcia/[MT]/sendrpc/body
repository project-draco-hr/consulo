def sendrpc(self, msg):
    srv = xmlrpclib.Server(self.ciaurl)
    res = srv.hub.deliver(msg)
    if (res is not True):
        raise util.Abort((_('%s returned an error: %s') % (self.ciaurl, res)))
