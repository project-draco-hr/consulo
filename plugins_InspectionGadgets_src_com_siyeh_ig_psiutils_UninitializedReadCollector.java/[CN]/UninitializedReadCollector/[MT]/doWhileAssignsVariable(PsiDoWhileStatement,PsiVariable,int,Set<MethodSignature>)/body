{
  final PsiExpression condition=doWhileStatement.getCondition();
  final PsiStatement body=doWhileStatement.getBody();
  return expressionAssignsVariable(condition,variable,stamp,checkedMethods) || statementAssignsVariable(body,variable,stamp,checkedMethods);
}
