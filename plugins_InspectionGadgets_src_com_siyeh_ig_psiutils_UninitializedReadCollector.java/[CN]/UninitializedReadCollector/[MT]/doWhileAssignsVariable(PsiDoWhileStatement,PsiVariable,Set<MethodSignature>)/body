{
  final PsiExpression condition=doWhileStatement.getCondition();
  final PsiStatement body=doWhileStatement.getBody();
  return expressionAssignsVariable(condition,variable,checkedMethods) || statementAssignsVariable(body,variable,checkedMethods);
}
