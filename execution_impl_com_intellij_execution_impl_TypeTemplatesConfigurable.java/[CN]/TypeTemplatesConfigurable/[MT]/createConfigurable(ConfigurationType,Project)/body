{
  final ConfigurationFactory[] factories=type.getConfigurationFactories();
  final RunManagerImpl runManager=RunManagerImpl.getInstanceImpl(project);
  return factories.length == 1 ? (Configurable)new TemplateConfigurable(runManager.getConfigurationTemplate(factories[0])) : new TypeTemplatesConfigurable(type,runManager);
}
