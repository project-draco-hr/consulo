{
  if (!isOKActionEnabled())   return;
  JavaRefactoringSettings.getInstance().PUSH_DOWN_PREVIEW_USAGES=isPreviewUsages();
  invokeRefactoring(new PushDownProcessor(getProject(),getSelectedMemberInfos(),myClass,new JavaDocPolicy(getJavaDocPolicy())));
}
