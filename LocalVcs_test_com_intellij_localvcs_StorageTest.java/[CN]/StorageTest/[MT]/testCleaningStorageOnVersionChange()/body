{
  s=new Storage(tempDir){
    @Override protected int getVersion(){
      return 123;
    }
  }
;
  s.storeCounter(111);
  s.close();
  s=new Storage(tempDir){
    @Override protected int getVersion(){
      return 666;
    }
  }
;
  assertEquals(0,s.loadCounter());
}
