{
  return new Condition<Object[]>(){
    @Override public boolean value(    Object[] objects){
      for (      Object object : objects) {
        if (!condition.value(object))         return false;
      }
      return true;
    }
  }
;
}
