{
  return new Comparator<MyNode>(){
    @Override public int compare(    final MyNode o1,    final MyNode o2){
      return o1.getDisplayName().compareToIgnoreCase(o2.getDisplayName());
    }
  }
;
}
