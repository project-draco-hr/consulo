{
  expect(subject.load(1)).andReturn("content".getBytes()).times(2);
  subject.remove(1);
  replay(subject);
  assertArrayEquals("content".getBytes(),s.load(1));
  s.remove(1);
  assertArrayEquals("content".getBytes(),s.load(1));
  verify(subject);
}
