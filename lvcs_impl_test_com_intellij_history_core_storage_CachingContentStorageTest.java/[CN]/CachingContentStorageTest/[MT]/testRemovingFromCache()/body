{
  expect(subject.load(1)).andReturn("content".getBytes()).times(2);
  subject.remove(1);
  replay(subject);
  assertEquals("content".getBytes(),s.load(1));
  s.remove(1);
  assertEquals("content".getBytes(),s.load(1));
  verify(subject);
}
