{
  try {
    if (file.isValid()) {
      final DataOutputStream stream=getAttribute(indexName).writeAttribute(file);
      try {
        stream.writeBoolean(true);
      }
  finally {
        stream.close();
      }
    }
  }
 catch (  IOException e) {
    LOG.info(e);
  }
}
