{
  final String url=getProject().getPresentableUrl();
  final String projectDynPath=getOrCreateDynamicDirectory() + File.separatorChar + DYNAMIC_PROPERTIES_PROJECT+ "_"+ getProject().getName()+ "_"+ (url != null ? url : getProject().getName()).hashCode();
  return getOrCreateDir(projectDynPath);
}
