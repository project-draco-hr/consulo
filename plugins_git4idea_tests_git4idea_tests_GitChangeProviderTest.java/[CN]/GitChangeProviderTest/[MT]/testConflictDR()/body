{
  modifyFileInBranches("a.txt",FileAction.DELETE,FileAction.RENAME);
  VirtualFile newFile=myRepo.getDir().findChild("a.txt_feature_new");
  assertChanges(newFile,FileStatus.MERGED_WITH_CONFLICTS);
}
