{
  final int[] result;
  if (myArraySize >= 0) {
    result=new int[myArraySize];
  }
 else {
    result=new int[in.readInt()];
  }
  for (int i=0; i < result.length; i++) {
    result[i]=in.readInt();
  }
  return result;
}
