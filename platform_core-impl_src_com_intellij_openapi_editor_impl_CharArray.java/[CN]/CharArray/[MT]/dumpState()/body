{
  return String.format("deferred changes mode: %b, length: %d (data array length: %d, deferred shift: %d); view offsets: [%d; %d]; deferred changes: %s",isDeferredChangeMode(),length(),myCount,myDeferredShift,myStart,myEnd,myDeferredChangesStorage);
}
