{
  if (canFlushIdleRequests()) {
    flushIdleRequests();
  }
 else {
    if (processFocusRevalidation()) {
      if (isFocusTransferReady()) {
        flushIdleRequests();
      }
    }
    restartIdleAlarm();
  }
}
