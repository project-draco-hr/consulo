{
  if (isFocusTransferReady()) {
    runnable.run();
    return;
  }
  final boolean needsRestart=isIdleQueueEmpty();
  myIdleRequests.add(runnable);
  if (needsRestart) {
    restartIdleAlarm();
  }
}
