{
  Component focusOwner=getFocusOwner();
  return isFocusTransferReady() && !isIdleQueueEmpty() && !IdeEventQueue.getInstance().isDispatchingFocusEvent()&& !(focusOwner == null && (myValidFurtherRequestors.size() > 0 || myFocusRevalidator != null && !myFocusRevalidator.isExpired()));
}
