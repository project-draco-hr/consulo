{
  final GenericRearranger outerClassRearranger=new GenericRearranger(settings.getClassOrderAttributeList(),outerClasses,0,settings){
    /** 
 * There are no items related to outer classes -- outer classes are completely disjoint
 * and independent.  Hence this routine does nothing.
 * @param entries
 * @param rearrangedEntries
 */
    public void rearrangeRelatedItems(    List<ClassContentsEntry> entries,    List<IRuleInstance> rearrangedEntries){
    }
  }
;
  return outerClassRearranger.rearrangeEntries();
}
