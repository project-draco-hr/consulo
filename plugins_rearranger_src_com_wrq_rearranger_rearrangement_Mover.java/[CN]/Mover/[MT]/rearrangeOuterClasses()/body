{
  final GenericRearranger outerClassRearranger=new GenericRearranger(mySettings.getClassOrderAttributeList(),myOuterClasses,0,mySettings){
    /** 
 * There are no items related to outer classes -- outer classes are completely disjoint
 * and independent.  Hence this routine does nothing.
 * @param entries
 * @param rearrangedEntries
 */
    public void rearrangeRelatedItems(    List<ClassContentsEntry> entries,    List<RuleInstance> rearrangedEntries){
    }
  }
;
  return outerClassRearranger.rearrangeEntries();
}
