{
  writeFile("-someOption");
  VMOptions.writeXmx(1024);
  VMOptions.writeMaxPermGen(256);
  VMOptions.writeCodeCache(256);
  assertEquals("-someOption -Xmx1024m -XX:MaxPermSize=256m -XX:ReservedCodeCacheSize=256m",readFile());
}
