{
  if (myOneSide) {
    if (myIsAddition) {
      if (myFile == null || !myFile.isValid()) {
        throw new ChangeOutdatedException();
      }
    }
  }
 else {
    if (myFile == null || !myFile.isValid()) {
      throw new ChangeOutdatedException();
    }
  }
}
