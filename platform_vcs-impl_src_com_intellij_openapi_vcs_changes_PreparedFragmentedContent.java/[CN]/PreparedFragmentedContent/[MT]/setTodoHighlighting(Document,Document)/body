{
  final List<Pair<TextRange,TextAttributes>> beforeTodoRanges=new TodoForRanges(myProject,myFileName,oldDocument.getText(),true,getBeforeFragments(),myFileType,1).execute();
  final List<Pair<TextRange,TextAttributes>> afterTodoRanges=new TodoForRanges(myProject,myFileName,document.getText(),false,getAfterFragments(),myFileType,1).execute();
  setBeforeTodoRanges(beforeTodoRanges);
  setAfterTodoRanges(afterTodoRanges);
}
