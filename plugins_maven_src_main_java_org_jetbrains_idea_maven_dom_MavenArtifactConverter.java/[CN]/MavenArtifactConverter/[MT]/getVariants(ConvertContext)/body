{
  try {
    return getVariants(context.getFile().getProject(),MavenIndicesManager.getInstance(),MavenArtifactConverterHelper.getId(context),context);
  }
 catch (  MavenIndexException e) {
    MavenLog.info(e);
    return Collections.emptyList();
  }
}
