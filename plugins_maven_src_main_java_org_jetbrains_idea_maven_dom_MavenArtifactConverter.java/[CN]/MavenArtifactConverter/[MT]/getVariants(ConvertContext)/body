{
  try {
    Project p=context.getFile().getProject();
    return getVariants(p,MavenProjectIndicesManager.getInstance(p),MavenArtifactConverterHelper.getId(context),context);
  }
 catch (  MavenIndexException e) {
    MavenLog.info(e);
    return Collections.emptyList();
  }
}
