{
  for (int i=0; i < model.getColumnCount(); i++)   if (model.getColumn(i).getModelIndex() == index)   return i;
  LOG.assertTrue(false,"Total: " + model.getColumnCount() + " index: "+ index);
  return index;
}
