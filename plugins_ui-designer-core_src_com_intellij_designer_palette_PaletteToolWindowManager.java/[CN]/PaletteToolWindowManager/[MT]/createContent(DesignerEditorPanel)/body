{
  PalettePanel palettePanel=new PalettePanel();
  palettePanel.loadPalette(designer);
  return createContent(designer,palettePanel,"Palette",AllIcons.Toolwindows.ToolWindowPalette,palettePanel,palettePanel,getAnchor() == ToolWindowAnchor.LEFT ? SideBorder.LEFT : SideBorder.RIGHT,180,null);
}
