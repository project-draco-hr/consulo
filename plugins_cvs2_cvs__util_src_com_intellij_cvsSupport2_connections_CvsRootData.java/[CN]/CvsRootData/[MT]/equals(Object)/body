{
  if (!(object instanceof CvsRootData))   return false;
  CvsRootData other=(CvsRootData)object;
  return Comparing.equal(METHOD,other.METHOD) && Comparing.equal(HOST,other.HOST) && Comparing.equal(USER,other.USER)&& Comparing.equal(REPOSITORY,other.REPOSITORY)&& PORT == other.PORT;
}
