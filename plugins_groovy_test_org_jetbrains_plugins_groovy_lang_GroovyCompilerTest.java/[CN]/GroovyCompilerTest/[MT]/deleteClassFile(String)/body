{
  new WriteCommandAction(getProject()){
    protected void run(    Result result) throws Throwable {
      ModuleRootManager.getInstance(myModule).getModuleExtension(CompilerModuleExtension.class).getCompilerOutputPath().findChild(className + ".class").delete(this);
    }
  }
.execute();
}
