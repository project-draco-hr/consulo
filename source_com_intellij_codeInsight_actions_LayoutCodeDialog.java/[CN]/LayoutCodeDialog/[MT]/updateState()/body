{
  myCbIncludeSubdirs.setEnabled(myRbDirectory.isSelected());
  myCbOptimizeImports.setEnabled(!myRbSelectedText.isSelected() && !(myFile != null && !(myFile instanceof PsiJavaFile) && myRbFile.isSelected()));
}
