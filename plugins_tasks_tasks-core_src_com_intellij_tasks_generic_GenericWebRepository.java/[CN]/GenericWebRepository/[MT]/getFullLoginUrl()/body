{
  return getLoginURL().replaceAll(placeholder2regexp(SERVER_URL_PLACEHOLDER),getUrl()).replaceAll(placeholder2regexp(USERNAME_PLACEHOLDER),getUsername()).replaceAll(placeholder2regexp(PASSWORD_PLACEHOLDER),getPassword());
}
