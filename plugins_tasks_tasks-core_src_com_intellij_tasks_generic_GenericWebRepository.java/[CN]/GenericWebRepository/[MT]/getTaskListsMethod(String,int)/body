{
  String requestUrl=getFullTasksUrl(query,max);
  final HttpMethod method=GenericWebRepositoryEditor.GET.equals(myTasksListMethodType) ? new GetMethod(requestUrl) : getPostMethodFromURL(requestUrl);
  configureHttpMethod(method);
  return method;
}
