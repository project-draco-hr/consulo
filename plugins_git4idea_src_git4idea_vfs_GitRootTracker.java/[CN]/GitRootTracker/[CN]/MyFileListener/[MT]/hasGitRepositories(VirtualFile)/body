{
  if (!file.isDirectory()) {
    return false;
  }
  if (file.getName().equals(".git")) {
    return true;
  }
  for (  VirtualFile child : file.getChildren()) {
    if (hasGitRepositories(child)) {
      return true;
    }
  }
  return false;
}
