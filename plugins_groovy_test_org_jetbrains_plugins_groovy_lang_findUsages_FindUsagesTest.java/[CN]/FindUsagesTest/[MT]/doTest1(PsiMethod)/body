{
  final ArrayList<PsiFile> filesList=new ArrayList<PsiFile>();
  PsiReference[] refs=MethodReferencesSearch.search(method,GlobalSearchScope.projectScope(myProject),false).toArray(PsiReference.EMPTY_ARRAY);
  for (  PsiReference ref : refs) {
    addReference(ref,filesList);
  }
}
