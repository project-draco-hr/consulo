{
  myAlarm.cancelAllRequests();
  myAlarm.addRequest(new Runnable(){
    public void run(){
      for (      Component component : myQueue) {
        component.repaint();
      }
      myQueue.clear();
    }
  }
,50);
  myQueue.add(c);
}
