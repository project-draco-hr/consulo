{
  return new DslPointcut<T>(){
    @Override boolean matches(    T t,    ProcessingContext context){
      return true;
    }
    @Override boolean operatesOn(    Class c){
      return true;
    }
  }
;
}
