{
  super.setUp();
  ApplicationManager.getApplication().runWriteAction(new Runnable(){
    public void run(){
      try {
        init();
      }
 catch (      Exception e) {
        throw new RuntimeException(e);
      }
    }
  }
);
}
