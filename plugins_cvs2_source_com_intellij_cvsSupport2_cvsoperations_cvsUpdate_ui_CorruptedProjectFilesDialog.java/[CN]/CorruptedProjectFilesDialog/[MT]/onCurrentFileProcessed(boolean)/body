{
  if (myCorruptedFiles.isEmpty()) {
    doCloseAction(isOk);
    return;
  }
  myCorruptedFiles.remove(0);
  if (myCorruptedFiles.isEmpty()) {
    doCloseAction(isOk);
  }
 else {
    showNextFileInfo();
  }
}
