{
  final String name=getReferenceName();
  return name != null && element instanceof PsiNamedElement && name.equals(((PsiNamedElement)element).getName()) && element.getManager().areElementsEquivalent(resolve(),element);
}
