{
  final RTestUnitTestProxy suite=createSuiteProxy();
  suite.setStarted();
  suite.setFinished();
  assertEquals("<NO TESTS>",myColumnDuration.valueOf(suite));
  suite.setFinished();
  assertEquals("<NO TESTS>",myColumnDuration.valueOf(suite));
}
