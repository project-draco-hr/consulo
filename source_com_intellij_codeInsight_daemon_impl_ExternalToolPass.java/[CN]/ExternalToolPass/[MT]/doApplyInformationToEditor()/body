{
  if (myAnnotationHolder.hasAnnotations()) {
    List<HighlightInfo> infos=getHighlights();
    UpdateHighlightersUtil.setHighlightersToEditor(myProject,myDocument,0,myFile.getTextLength(),infos,UpdateHighlightersUtil.EXTERNAL_TOOLS_HIGHLIGHTERS_GROUP);
  }
}
