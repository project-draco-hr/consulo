{
  myPanel=new JPanel(){
    @Override public void removeNotify(){
      super.removeNotify();
      if (mySaveOnRemove) {
        saveTo(myBreakpoint,new Runnable(){
          @Override public void run(){
          }
        }
);
      }
    }
  }
;
}
