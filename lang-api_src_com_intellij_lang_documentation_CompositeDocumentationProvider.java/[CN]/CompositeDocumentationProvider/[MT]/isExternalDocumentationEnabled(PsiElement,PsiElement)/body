{
  for (  DocumentationProvider provider : myProviders) {
    if (provider instanceof ExtensibleDocumentationProvider && ((ExtensibleDocumentationProvider)provider).isExternalDocumentationEnabled(element,originalElement))     return true;
    final String url=provider.getUrlFor(element,originalElement);
    if (url != null) {
      return true;
    }
  }
  return false;
}
