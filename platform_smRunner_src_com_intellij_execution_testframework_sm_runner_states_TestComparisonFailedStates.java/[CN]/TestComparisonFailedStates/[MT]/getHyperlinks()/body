{
  return ContainerUtil.map(myStates,new Function<TestComparisionFailedState,DiffHyperlink>(){
    @Override public DiffHyperlink fun(    TestComparisionFailedState state){
      return state.getHyperlink();
    }
  }
);
}
