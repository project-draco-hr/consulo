{
  return "Project" + (isDisposed() ? " (Disposed" + (temporarilyDisposed ? " temporarily" : "") + ")" : isDefault() ? "" : " '" + getLocation() + "'") + (isDefault() ? " (Default)" : "")+ " "+ myName;
}
