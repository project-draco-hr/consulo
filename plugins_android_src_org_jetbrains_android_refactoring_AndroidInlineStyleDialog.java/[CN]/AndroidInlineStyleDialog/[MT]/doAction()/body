{
  if (isInlineThisOnly()) {
    close(OK_EXIT_CODE);
  }
 else {
    invokeRefactoring(new AndroidInlineAllStyleUsagesProcessor(myProject,myElement,myStyleTag,myStyleName,myAttributeValues,myParentStyleRef,null));
  }
}
