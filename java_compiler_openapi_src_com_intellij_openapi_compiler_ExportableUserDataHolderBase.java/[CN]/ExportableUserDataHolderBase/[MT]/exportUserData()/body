{
  final Map<Key,Object> result=new HashMap<Key,Object>();
synchronized (myKeys) {
    for (    Key<?> k : myKeys) {
      final Object data=getUserData(k);
      if (data != null) {
        result.put(k,data);
      }
    }
  }
  return result;
}
