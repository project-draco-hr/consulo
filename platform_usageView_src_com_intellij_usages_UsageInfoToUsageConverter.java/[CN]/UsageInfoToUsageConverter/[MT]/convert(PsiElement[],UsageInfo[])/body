{
  Usage[] usages=ContainerUtil.map(usageInfos,new Function<UsageInfo,Usage>(){
    @Override public Usage fun(    UsageInfo info){
      return convert(primaryElements,info);
    }
  }
,new Usage[usageInfos.length]);
  return usages;
}
