{
  TestFrameworkRunningModel model=getModel();
  final List<? extends AbstractTestProxy> myAllTests=model != null ? model.getRoot().getAllTests() : Collections.<AbstractTestProxy>emptyList();
  return Filter.FAILED_OR_INTERRUPTED.and(JavaAwareFilter.METHOD(project)).select(myAllTests);
}
