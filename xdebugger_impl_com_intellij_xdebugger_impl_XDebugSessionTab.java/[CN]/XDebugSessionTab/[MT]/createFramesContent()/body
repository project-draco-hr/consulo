{
  final JPanel framesPanel=new JPanel();
  Content framesContent=myUi.createContent(DebuggerContentInfo.FRAME_CONTENT,framesPanel,XDebuggerBundle.message("debugger.session.tab.frames.title"),IconLoader.getIcon("/debugger/frame.png"),null);
  final DefaultActionGroup framesGroup=new DefaultActionGroup();
  return framesContent;
}
