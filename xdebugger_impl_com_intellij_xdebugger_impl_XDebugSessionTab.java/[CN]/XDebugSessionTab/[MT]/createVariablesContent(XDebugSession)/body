{
  final XDebugVariablesView variablesView=new XDebugVariablesView(session,this);
  return myUi.createContent(DebuggerContentInfo.VARIABLES_CONTENT,variablesView.getPanel(),XDebuggerBundle.message("debugger.session.tab.variables.title"),VARIABLES_ICON,null);
}
