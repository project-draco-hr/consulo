{
  final XDebugVariablesView variablesView=new XDebugVariablesView(session,this);
  Content variablesContent=myUi.createContent(DebuggerContentInfo.VARIABLES_CONTENT,variablesView.getPanel(),XDebuggerBundle.message("debugger.session.tab.variables.title"),IconLoader.getIcon("/debugger/value.png"),null);
  return variablesContent;
}
