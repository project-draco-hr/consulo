{
  myDelegate=delegate;
  myModule=module;
  final VirtualFile file=delegate.getFile();
  if (file == null) {
    throw new IllegalArgumentException(String.format("Detected attempt to create ModuleAwareContentRoot object for content root that points to the un-existing file - %s, module: %s",delegate,module));
  }
  myFile=file;
}
