{
  List<MessageTreeNode> messages=new ArrayList<MessageTreeNode>(1);
  final List<String> lines=StringUtil.split(errorMessage,"\n");
  for (int i=0; i < lines.size(); i++) {
    messages.add(new MessageTreeNode(tree,parent,lines.get(i),XDebuggerUIConstants.ERROR_MESSAGE_ATTRIBUTES,XDebuggerUIConstants.ERROR_MESSAGE_ICON,i == 0 ? link : null));
  }
  return messages;
}
