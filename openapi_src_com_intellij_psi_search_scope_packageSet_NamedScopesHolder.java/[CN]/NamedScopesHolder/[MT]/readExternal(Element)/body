{
  List sets=element.getChildren(SCOPE_TAG);
  for (  Object set : sets) {
    try {
      addScope(readScope((Element)set));
    }
 catch (    ParsingException e) {
    }
  }
  fireScopeListeners();
}
