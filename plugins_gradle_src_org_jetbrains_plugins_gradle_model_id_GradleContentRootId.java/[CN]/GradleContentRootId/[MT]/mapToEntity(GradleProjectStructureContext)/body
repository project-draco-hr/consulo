{
switch (getOwner()) {
case GRADLE:
    return context.getProjectStructureHelper().findGradleContentRoot(this);
case INTELLIJ:
  return context.getProjectStructureHelper().findIntellijContentRoot(this);
}
return null;
}
