{
switch (getOwner()) {
case GRADLE:
    return context.getProjectStructureHelper().findGradleContentRoot(this);
case IDE:
  return context.getProjectStructureHelper().findIdeContentRoot(this);
}
return null;
}
