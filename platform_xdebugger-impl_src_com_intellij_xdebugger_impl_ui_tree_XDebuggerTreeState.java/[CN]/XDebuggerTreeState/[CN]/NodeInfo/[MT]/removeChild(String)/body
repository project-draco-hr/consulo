{
  if (myChildren == null) {
    return null;
  }
  Collection<NodeInfo> infos=myChildren.get(name);
  NodeInfo item=ContainerUtil.getFirstItem(infos);
  if (item != null) {
    infos.remove(item);
  }
  return item;
}
