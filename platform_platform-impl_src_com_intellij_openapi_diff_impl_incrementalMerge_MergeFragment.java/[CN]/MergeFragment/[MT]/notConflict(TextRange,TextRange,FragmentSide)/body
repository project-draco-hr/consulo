{
  TextRange left;
  TextRange right;
  if (versionSide == FragmentSide.SIDE1) {
    left=versionChange;
    right=null;
  }
 else {
    left=null;
    right=versionChange;
  }
  return new MergeFragment(left,baseChange,right);
}
