{
  vcs.createFile(p("file"),"old");
  vcs.commit();
  vcs.delete(p("file"));
  vcs.createFile(p("file"),"new");
  vcs.commit();
  assertRevisionsContent(new String[]{"new"},vcs.getEntryHistory(p("file")));
}
