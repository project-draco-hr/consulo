{
  if (file != null && file.exists()) {
    VirtualFile virtualFile=LocalFileSystem.getInstance().findFileByIoFile(file);
    if (virtualFile != null)     FileEditorManager.getInstance(myProject).openFile(virtualFile,true);
  }
}
