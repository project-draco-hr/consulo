{
  TreeUI actualUI=ui;
  if (!isCustomUI()) {
    if (!(ui instanceof WideSelectionTreeUI) && (UIUtil.isUnderAquaBasedLookAndFeel() || SystemInfo.isWindows)) {
      actualUI=new WideSelectionTreeUI(isMacWideSelection(),!isFileColorsEnabled());
    }
  }
  super.setUI(actualUI);
}
