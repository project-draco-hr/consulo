{
  TreeUI actualUI=ui;
  if (!isCustomUI()) {
    if (!(ui instanceof WideSelectionTreeUI) && (UIUtil.isUnderAquaBasedLookAndFeel() || SystemInfo.isWindows) && isMacWideSelection()) {
      actualUI=new WideSelectionTreeUI(isMacWideSelection(),true);
    }
  }
  super.setUI(actualUI);
}
