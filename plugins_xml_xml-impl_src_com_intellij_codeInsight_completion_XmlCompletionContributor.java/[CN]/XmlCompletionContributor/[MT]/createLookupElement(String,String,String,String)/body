{
  LookupElementBuilder builder=LookupElementBuilder.create(Pair.create(name,namespace),name).withInsertHandler(new ExtendedTagInsertHandler(name,namespace,namespacePrefix));
  if (!StringUtil.isEmpty(namespace)) {
    builder=builder.withTypeText(tailText,true);
  }
  return builder;
}
