{
  final FileElement tree=getTreeElement();
  if (tree != null) {
    tree.clearCaches();
  }
 else {
synchronized (myStubLock) {
      myStub=null;
    }
  }
  clearCaches();
  getViewProvider().rootChanged(this);
}
