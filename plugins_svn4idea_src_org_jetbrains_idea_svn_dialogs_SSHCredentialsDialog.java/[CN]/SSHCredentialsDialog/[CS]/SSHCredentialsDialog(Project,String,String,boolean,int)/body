{
  super(project,true);
  myProject=project;
  myRealm=realm;
  myUserName=userName;
  myAllowSave=allowSave;
  setResizable(true);
  getHelpAction().setEnabled(true);
  init();
  myPortField.setText("" + port);
}
