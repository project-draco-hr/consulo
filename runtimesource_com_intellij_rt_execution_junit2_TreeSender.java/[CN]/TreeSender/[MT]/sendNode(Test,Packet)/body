{
  Vector testCases=getTestCasesOf(test);
  packet.addObject(test).addLong(testCases.size());
  for (Enumeration each=testCases.elements(); each.hasMoreElements(); )   sendNode((Test)each.nextElement(),packet);
}
