{
  String path=fileOrDirectory.getPath();
  if (fileOrDirectory.getParent() == null && path.startsWith("//"))   return true;
  if (StringUtil.isEmpty(path))   return true;
  return convertToIOFile(fileOrDirectory).exists();
}
