{
  if (!file.isSymLink())   return false;
  final VirtualFile realFile=file.getRealFile();
  return realFile == null || realFile == file || FileUtil.isAncestor(convertToIOFile(realFile),convertToIOFile(file),true);
}
