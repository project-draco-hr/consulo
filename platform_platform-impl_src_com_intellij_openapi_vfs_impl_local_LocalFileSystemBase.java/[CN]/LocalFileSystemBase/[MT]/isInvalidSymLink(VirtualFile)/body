{
  if (!file.isSymLink())   return false;
  final VirtualFile target=file.getCanonicalFile();
  return target == null || target == file || FileUtil.isAncestor(convertToIOFile(target),convertToIOFile(file),true);
}
