{
  if (!forced && !isUnforcedRequestAllowed()) {
    if (cmd.equals(getLastEffectiveForcedRequest())) {
      result.setDone();
    }
 else {
      result.setRejected();
    }
    return true;
  }
  return false;
}
