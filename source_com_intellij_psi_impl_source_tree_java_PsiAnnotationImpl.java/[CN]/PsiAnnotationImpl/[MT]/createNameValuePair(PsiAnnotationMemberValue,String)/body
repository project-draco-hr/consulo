{
  final PsiAnnotation newAnno=JavaPsiFacade.getInstance(getProject()).getElementFactory().createAnnotationFromText("@A(" + namePrefix + value.getText()+ ")",null);
  return newAnno.getParameterList().getAttributes()[0];
}
