{
  return doWhenDone(new Runnable(){
    @Override public void run(){
      child.setDone();
    }
  }
).doWhenRejected(new Runnable(){
    @Override public void run(){
      child.setRejected();
    }
  }
);
}
