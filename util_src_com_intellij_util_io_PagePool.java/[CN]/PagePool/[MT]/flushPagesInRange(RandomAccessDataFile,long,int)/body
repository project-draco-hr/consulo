{
synchronized (lock) {
    final Collection<Page> pages=new ArrayList<Page>(myPages.values());
    for (    Page page : pages) {
      if (page.intersects(owner,start,length)) {
        dropPage(page);
      }
    }
  }
}
