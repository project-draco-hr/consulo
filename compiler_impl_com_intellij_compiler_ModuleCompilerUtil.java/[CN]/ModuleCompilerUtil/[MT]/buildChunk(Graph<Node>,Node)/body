{
  final Set<Node> outs=new HashSet<Node>();
  final Set<Node> ins=new HashSet<Node>();
  addAllReachable(graph,outs,node,true);
  addAllReachable(graph,ins,node,false);
  outs.retainAll(ins);
  if (outs.isEmpty()) {
    return new Chunk<Node>(node);
  }
  return new Chunk<Node>(outs);
}
