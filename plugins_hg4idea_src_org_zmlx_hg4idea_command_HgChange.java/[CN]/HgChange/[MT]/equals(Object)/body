{
  if (object == this) {
    return true;
  }
  if (!(object instanceof HgChange)) {
    return false;
  }
  HgChange that=(HgChange)object;
  return new EqualsBuilder().append(beforeFile,that.beforeFile).append(afterFile,that.afterFile).append(status,that.status).isEquals();
}
