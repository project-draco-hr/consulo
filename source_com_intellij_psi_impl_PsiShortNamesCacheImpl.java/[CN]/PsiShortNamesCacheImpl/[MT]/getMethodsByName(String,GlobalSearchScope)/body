{
  final Collection<PsiMethod> methods=StubIndex.getInstance().get(JavaMethodNameIndex.KEY,name,myManager.getProject(),scope);
  if (methods.size() == 0)   return PsiMethod.EMPTY_ARRAY;
  List<PsiMethod> list=filterMembers(methods,scope);
  return list.toArray(new PsiMethod[list.size()]);
}
