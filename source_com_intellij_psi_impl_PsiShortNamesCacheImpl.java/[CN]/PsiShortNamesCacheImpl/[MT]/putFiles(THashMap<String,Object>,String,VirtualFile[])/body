{
  if (files.length == 0) {
    map.remove(key);
  }
 else   if (files.length == 1) {
    map.put(key,files[0]);
  }
 else   if (files.length == 2) {
    map.put(key,Pair.create(files[0],files[1]));
  }
 else {
    map.put(key,files);
  }
}
