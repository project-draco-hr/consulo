{
  try {
    DataOutputStream out=new DataOutputStream(socket.getOutputStream());
    for (    String path : myLockedPaths) {
      out.writeUTF(path);
    }
    out.close();
  }
 catch (  IOException e) {
    LOG.debug(e);
  }
}
