{
  final String text;
  if (expression == null) {
    text="";
  }
 else {
    text=expression.getText();
  }
  if (ParenthesesUtils.getPrecedence(expression) > precedence) {
    return '(' + text + ')';
  }
 else {
    return text;
  }
}
