{
  if (ApplicationManager.getApplication().isUnitTestMode()) {
    throw new RuntimeException("Files changed");
  }
  Messages.showMessageDialog(myManager.getProject(),"Cannot undo. Some files were changed","Undo",Messages.getErrorIcon());
}
