{
  if (ApplicationManager.getApplication().isUnitTestMode()) {
    throw new RuntimeException("Files changed");
  }
  Messages.showMessageDialog(myManager.getProject(),CommonBundle.message("cannot.undo.error.message"),CommonBundle.message("undo.dialog.title"),Messages.getErrorIcon());
}
