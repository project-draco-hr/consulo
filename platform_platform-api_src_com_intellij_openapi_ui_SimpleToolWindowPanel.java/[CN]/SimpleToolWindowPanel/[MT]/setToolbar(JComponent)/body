{
  myToolbar=c;
  if (myVertical) {
    add(c,BorderLayout.NORTH);
  }
 else {
    add(c,BorderLayout.WEST);
  }
  if (myBorderless) {
    UIUtil.removeScrollBorder(c);
  }
  revalidate();
  repaint();
}
