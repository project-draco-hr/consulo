{
  if (dirty) {
    if (myWriteMask != null) {
      if (myWriteMask.cardinality() < PAGE_SIZE) {
        ensureRead();
      }
      myWriteMask=null;
    }
    owner.flushPage(this);
    dirty=false;
  }
}
