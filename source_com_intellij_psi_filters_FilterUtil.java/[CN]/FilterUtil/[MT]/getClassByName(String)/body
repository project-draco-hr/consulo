{
  final String[] packs={"","com.intellij.psi.","com.intellij.psi.jsp.","com.intellij.psi.xml.","com.intellij.aspects.psi."};
  for (int i=0; i < packs.length; i++) {
    final Class aClass=tryClass(packs[i] + shortName);
    if (aClass != null) {
      return aClass;
    }
  }
  return null;
}
