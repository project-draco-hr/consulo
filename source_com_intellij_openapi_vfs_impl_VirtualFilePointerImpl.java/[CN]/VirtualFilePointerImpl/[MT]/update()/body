{
  myInitialized=true;
  if (!isValid()) {
    LOG.assertTrue(myUrl != null);
    myFile=myVirtualFileManager.findFileByUrl(myUrl);
    if (myFile != null) {
      myUrl=null;
    }
  }
  myWasRecentlyValid=isValid();
}
