{
  if (arg instanceof PsiWildcardType) {
    return captureWildcard ? arg : PsiType.NULL;
  }
 else {
    if (arg == null || arg.getDeepComponentType() instanceof PsiPrimitiveType || PsiUtil.resolveClassInType(arg) != null) {
      return arg;
    }
  }
  return PsiType.NULL;
}
