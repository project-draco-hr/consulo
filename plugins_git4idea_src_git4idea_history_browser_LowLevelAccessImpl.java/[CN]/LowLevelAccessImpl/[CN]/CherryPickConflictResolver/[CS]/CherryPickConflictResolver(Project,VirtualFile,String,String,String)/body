{
  super(project,ServiceManager.getService(Git.class),ServiceManager.getService(PlatformFacade.class),Collections.singleton(root),makeParams(commitHash,commitAuthor,commitMessage));
  myRoot=root;
  myCommitHash=commitHash;
  myCommitAuthor=commitAuthor;
  myCommitMessage=commitMessage;
}
