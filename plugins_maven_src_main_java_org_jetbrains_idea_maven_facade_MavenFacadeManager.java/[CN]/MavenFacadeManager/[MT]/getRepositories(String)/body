{
  return perform(new Retriable<List<MavenRepositoryInfo>>(){
    @Override public List<MavenRepositoryInfo> execute() throws RemoteException {
      return getOrCreateWrappee().getRepositories(nexusUrl);
    }
  }
);
}
