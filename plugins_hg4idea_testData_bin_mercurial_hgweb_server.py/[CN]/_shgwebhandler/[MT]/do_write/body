def do_write(self):
    from OpenSSL.SSL import SysCallError
    try:
        super(_shgwebhandler, self).do_write()
    except SysCallError as inst:
        if (inst.args[0] != errno.EPIPE):
            raise
