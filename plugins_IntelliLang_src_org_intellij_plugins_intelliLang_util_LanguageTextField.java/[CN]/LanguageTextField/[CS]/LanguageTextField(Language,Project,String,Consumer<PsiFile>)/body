{
  super(createDocument(value,language,project,tagger),project,language != null ? language.getAssociatedFileType() : StdFileTypes.PLAIN_TEXT,language == null);
  myLanguage=language;
  myProject=project;
  setEnabled(language != null);
  ShiftTabAction.attachTo(this);
}
