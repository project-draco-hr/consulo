{
  return new Runnable(){
    public void run(){
      try {
        task.run(indicator);
      }
  finally {
        if (indicator instanceof ProgressIndicatorEx) {
          ((ProgressIndicatorEx)indicator).finish(task);
        }
      }
    }
  }
;
}
