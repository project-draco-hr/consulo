{
  ServerSocket socket=null;
  try {
    socket=new ServerSocket(getPortNumber());
  }
 catch (  BindException e) {
    return false;
  }
catch (  IOException e) {
    LOG.error(e);
    return false;
  }
 finally {
    if (socket != null) {
      try {
        socket.close();
      }
 catch (      IOException e) {
        LOG.error(e);
        return false;
      }
    }
  }
  return true;
}
