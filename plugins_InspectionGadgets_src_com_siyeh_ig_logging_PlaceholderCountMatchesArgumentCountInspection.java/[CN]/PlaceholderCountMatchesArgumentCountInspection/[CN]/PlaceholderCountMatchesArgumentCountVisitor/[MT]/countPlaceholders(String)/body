{
  int count=0;
  int index=value.indexOf("{}");
  while (index >= 0) {
    if (index <= 0 || value.charAt(index - 1) != '\\') {
      count++;
    }
    index=value.indexOf("{}",index + 1);
  }
  return count;
}
