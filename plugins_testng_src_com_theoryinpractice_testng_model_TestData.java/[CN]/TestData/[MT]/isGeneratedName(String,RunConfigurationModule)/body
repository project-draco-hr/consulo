{
  if (TEST_OBJECT == null)   return true;
  if ((TestType.CLASS.getType().equals(TEST_OBJECT) || TestType.METHOD.getType().equals(TEST_OBJECT)) && getMainClassName().length() == 0)   return ExecutionUtil.isNewName(s);
  if (TestType.METHOD.getType().equals(TEST_OBJECT) && getMethodName().length() == 0)   return ExecutionUtil.isNewName(s);
 else   return Comparing.equal(s,getGeneratedName(config));
}
