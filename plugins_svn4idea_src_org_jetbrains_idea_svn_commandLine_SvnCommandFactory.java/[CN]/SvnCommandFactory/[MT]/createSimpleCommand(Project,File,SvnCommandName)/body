{
  final SvnSimpleCommand command=new SvnSimpleCommand(workingDirectory,commandName,SvnApplicationSettings.getInstance().getCommandLinePath());
  addStartFailedListener(project,command);
  return command;
}
