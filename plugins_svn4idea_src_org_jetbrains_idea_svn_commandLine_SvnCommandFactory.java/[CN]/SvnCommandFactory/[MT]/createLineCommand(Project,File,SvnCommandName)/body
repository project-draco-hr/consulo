{
  final SvnLineCommand command=new SvnLineCommand(workingDirectory,commandName,SvnApplicationSettings.getInstance().getCommandLinePath(),null);
  addStartFailedListener(project,command);
  return command;
}
