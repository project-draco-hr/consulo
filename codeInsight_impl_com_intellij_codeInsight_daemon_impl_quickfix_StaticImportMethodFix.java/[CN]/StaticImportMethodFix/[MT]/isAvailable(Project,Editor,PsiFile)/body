{
  return PsiUtil.isLanguageLevel5OrHigher(file) && myMethodCall != null && myMethodCall.isValid() && myMethodCall.getMethodExpression().getQualifierExpression() == null && file.getManager().isInProject(file) && !(candidates == null ? candidates=getMethodsToImport() : candidates).isEmpty();
}
