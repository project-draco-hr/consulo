{
  return LanguageLevel.JDK_1_5.compareTo(PsiUtil.getLanguageLevel(file)) <= 0 && myMethodCall != null && myMethodCall.isValid() && file.getManager().isInProject(file) && !(candidates == null ? candidates=getMethodsToImport() : candidates).isEmpty();
}
