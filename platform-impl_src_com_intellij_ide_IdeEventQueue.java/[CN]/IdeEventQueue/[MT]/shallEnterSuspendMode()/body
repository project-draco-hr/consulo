{
  if (peekEvent(WindowEvent.WINDOW_OPENED) != null)   return true;
  final IdeFocusManager ideFocusManager=getIdeFocusManager();
  if (ideFocusManager != null) {
    return ideFocusManager.isFocusTransferInProgress();
  }
  return false;
}
