{
  ReplacePathToMacroMap result=super.getReplacePathMap();
  if (!myModule.isDisposed()) {
    addFileHierarchyReplacements(result,PathMacrosImpl.MODULE_DIR_MACRO_NAME,getModuleDir(myModule.getModuleFilePath()),getUserHome());
  }
  return result;
}
