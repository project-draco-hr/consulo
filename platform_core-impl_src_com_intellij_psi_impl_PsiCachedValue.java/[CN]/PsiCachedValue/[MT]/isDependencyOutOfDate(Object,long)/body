{
  if (dependency instanceof PsiElement && myLastPsiTimeStamp == myManager.getModificationTracker().getModificationCount() && ((PsiElement)dependency).isPhysical()) {
    return false;
  }
  return super.isDependencyOutOfDate(dependency,oldTimeStamp);
}
