{
  return ContainerUtil.mapNotNull(myUsers,new Function<String,String>(){
    @Override public String fun(    String user){
      if (ME.equals(user)) {
        VcsUser vcsUser=myData.get(root);
        return vcsUser == null ? null : vcsUser.getName();
      }
      return user;
    }
  }
);
}
