{
  removeAll();
  for (  MyItemLabel item : myList) {
    add(item);
  }
  revalidate();
  repaint();
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      scrollSelectionToVisible();
    }
  }
);
}
