{
  GitConflictResolver gitConflictResolver=GitBranchOperationsProcessor.prepareConflictResolverForUnmergedFilesBeforeCheckout(project,GitUtil.getRoots(repositories));
  boolean res=gitConflictResolver.merge();
  if (res) {
    return GitBranchOperationResult.success();
  }
 else {
    return GitBranchOperationResult.error(errorTitle,errorDescription);
  }
}
