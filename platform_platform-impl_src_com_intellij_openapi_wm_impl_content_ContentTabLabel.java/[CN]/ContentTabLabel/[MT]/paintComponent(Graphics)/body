{
  if (!isSelected() && myLayout.isToDrawTabs()) {
    g.translate(0,TAB_SHIFT);
  }
  super.paintComponent(g);
  if (!isSelected() && myLayout.isToDrawTabs()) {
    g.translate(0,-TAB_SHIFT);
  }
}
