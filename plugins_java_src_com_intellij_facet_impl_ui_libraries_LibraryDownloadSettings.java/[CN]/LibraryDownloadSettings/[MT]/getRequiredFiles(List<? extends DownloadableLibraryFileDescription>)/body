{
  return ContainerUtil.filter(files,new Condition<DownloadableLibraryFileDescription>(){
    @Override public boolean value(    DownloadableLibraryFileDescription description){
      return !description.isOptional();
    }
  }
);
}
