{
  myProject=project;
  myLayout=RunnerLayoutSettings.getInstance().getLayout(runnerType);
  Disposer.register(parent,this);
  myContentUI=new RunnerContentUi(myProject,this,ActionManager.getInstance(),IdeFocusManager.getInstance(myProject),myLayout,runnerTitle + " - " + sessionName);
  myContentPanel=new JPanel(new BorderLayout());
  myViewsContentManager=getContentFactory().createContentManager(myContentUI.getContentUI(),false,myProject);
  Disposer.register(this,myViewsContentManager);
  myContentPanel.add(myViewsContentManager.getComponent(),BorderLayout.CENTER);
}
