{
  final String text=right.getText();
  final LeafElement merged=Factory.createSingleLeafElement(TokenType.WHITE_SPACE,text,0,text.length(),null,right.getPsi().getManager());
  right.getTreeParent().replaceChild(right,merged);
}
