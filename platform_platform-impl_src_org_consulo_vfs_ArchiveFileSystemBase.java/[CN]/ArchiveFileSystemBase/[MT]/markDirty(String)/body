{
  final ArchiveHandler handler;
synchronized (myLock) {
    handler=myHandlers.remove(path);
    jarPathsCache=null;
  }
  if (handler != null) {
    return handler.markDirty();
  }
  return null;
}
