{
  if (!aClass.isEnum())   return null;
  PsiElement parent=aClass.getParent();
  if (!(parent instanceof PsiClass || parent instanceof PsiFile)) {
    HighlightInfo highlightInfo=HighlightInfo.createHighlightInfo(HighlightInfoType.ERROR,ClassUtil.getClassDeclarationTextRange(aClass),"Enum must not be local");
    return highlightInfo;
  }
  return null;
}
