{
  if (editor != myEditor) {
    doInternalCleanUp();
  }
  if (myEditor != null) {
    myEditor.getDocument().removeDocumentListener(this);
  }
  myEditor=editor;
  if (myEditor != null) {
    myEditor.getDocument().addDocumentListener(this);
  }
}
