def add(self, fn):
    if (self.entries is None):
        self._load()
    if (fn not in self.entries):
        self.opener('fncache', 'ab').write((encodedir(fn) + '\n'))
        self.entries.add(fn)
