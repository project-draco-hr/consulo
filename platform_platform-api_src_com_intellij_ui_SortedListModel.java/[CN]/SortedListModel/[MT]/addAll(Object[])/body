{
  int[] indices=new int[items.length];
  for (int i=0; i < items.length; i++) {
    T item=(T)items[i];
    int newIndex=add(item);
    for (int j=0; j < i; j++)     if (indices[j] >= newIndex)     indices[j]++;
    indices[i]=newIndex;
  }
  return indices;
}
