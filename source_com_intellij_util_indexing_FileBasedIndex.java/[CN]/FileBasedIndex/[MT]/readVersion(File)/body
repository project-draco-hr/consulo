{
  try {
    final DataInputStream in=new DataInputStream(new FileInputStream(file));
    try {
      return in.readInt();
    }
  finally {
      in.close();
    }
  }
 catch (  IOException e) {
    return -1;
  }
}
