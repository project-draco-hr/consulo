{
  PsiFile file=descriptor.getPlaceFile();
  if (file instanceof GroovyFile && ((GroovyFile)file).isScript()) {
    return myScriptType.contains(GroovyScriptTypeDetector.getScriptType((GroovyFile)file).getId());
  }
  return false;
}
