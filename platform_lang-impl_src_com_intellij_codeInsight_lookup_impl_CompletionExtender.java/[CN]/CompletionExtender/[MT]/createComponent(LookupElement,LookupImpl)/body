{
  final LookupCellRenderer renderer=new LookupCellRenderer(lookup);
  renderer.setFullSize(true);
  final JComponent component=(JComponent)renderer.getListCellRendererComponent(lookup.getList(),element,lookup.getList().getSelectedIndex(),true,false);
  component.setSize(component.getPreferredSize());
  return component;
}
