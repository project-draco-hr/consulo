{
  return ContainerUtil.find(log.getLogProviders(),new Condition<VcsLogProvider>(){
    @Override public boolean value(    VcsLogProvider logProvider){
      return logProvider.getSupportedVcs().equals(vcsKey);
    }
  }
) != null;
}
