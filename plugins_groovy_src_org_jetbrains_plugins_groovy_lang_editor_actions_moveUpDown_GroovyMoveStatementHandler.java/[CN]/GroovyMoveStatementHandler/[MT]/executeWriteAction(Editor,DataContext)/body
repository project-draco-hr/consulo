{
  if (isEnabled(editor,dataContext) && executeWriteActionInner(editor,dataContext))   return;
  if (myOriginalHandler.isEnabled(editor,dataContext)) {
    myOriginalHandler.execute(editor,dataContext);
  }
}
