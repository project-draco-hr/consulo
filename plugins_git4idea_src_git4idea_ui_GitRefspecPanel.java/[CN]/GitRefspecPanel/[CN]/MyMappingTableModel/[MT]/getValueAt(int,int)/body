{
  RefMapping m=myMapping.get(rowIndex);
switch (columnIndex) {
case FORCE_COLUMN:
    return m.force;
case LOCAL_COLUMN:
  return m.local;
case REMOTE_COLUMN:
return m.remote;
default :
throw new IllegalStateException("Invalid column: " + columnIndex);
}
}
