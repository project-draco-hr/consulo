{
  PsiElement element=getElement(dataContext);
  if (element == null)   return false;
  return !(element instanceof JspClass) && !(element instanceof JspHolderMethod) && (!(element instanceof PsiJavaFile) || PsiUtil.isInJspFile(element));
}
