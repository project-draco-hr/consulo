{
  if (source.getRepo().equals(target.getRepo())) {
    HgCommandExecutor.getInstance(project).execute(source.getRepo(),"rename",Arrays.asList("--after",source.getRelativePath(),target.getRelativePath()));
  }
}
