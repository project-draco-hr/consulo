{
  MutationUtils.replaceExpression(delegateClass + '.' + expression.getReferenceName()+ (myEnumConstant ? "." + PropertyUtil.suggestGetterName("value",expression.getType()) + "()" : ""),expression);
}
