{
  try {
    f.getParentFile().mkdirs();
    f.createNewFile();
    FileWriter w=new FileWriter(f);
    try {
      w.write(string);
    }
  finally {
      w.close();
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
