{
  if (myDoSynchronize) {
    if (column == 0) {
      getToolWrapper(row).getTool().setEnabled(((Boolean)aValue).booleanValue());
    }
  }
  super.setValueAt(aValue,row,column);
  myTable.repaint();
}
