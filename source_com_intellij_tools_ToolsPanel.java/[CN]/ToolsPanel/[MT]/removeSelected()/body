{
  int result=Messages.showYesNoDialog(this,"Do you want to delete the external tool(s)?","Warning",Messages.getWarningIcon());
  if (result != 0) {
    return;
  }
  TableUtil.removeSelectedItems(myTable);
  update();
  myTable.requestFocus();
}
