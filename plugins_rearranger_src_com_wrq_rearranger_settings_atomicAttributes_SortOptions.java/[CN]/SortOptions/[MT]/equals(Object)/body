{
  if (!(obj instanceof SortOptions))   return false;
  SortOptions so=(SortOptions)obj;
  return byModifiers == so.byModifiers && byType == so.byType && isTypeCaseInsensitive == so.isTypeCaseInsensitive && byName == so.byName && isNameCaseInsensitive == so.isNameCaseInsensitive;
}
