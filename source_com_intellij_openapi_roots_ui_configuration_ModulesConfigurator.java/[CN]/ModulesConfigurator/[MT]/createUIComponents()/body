{
  myLanguageLevelCombo=new LanguageLevelCombo();
  final JTextField textField=new JTextField();
  final FileChooserDescriptor outputPathsChooserDescriptor=new FileChooserDescriptor(false,true,false,false,false,false);
  InsertPathAction.addTo(textField,outputPathsChooserDescriptor);
  outputPathsChooserDescriptor.setHideIgnored(false);
  myProjectCompilerOutput=new FieldPanel(textField,null,null,new BrowseFilesListener(textField,ProjectBundle.message("project.compiler.output"),"",outputPathsChooserDescriptor),new Runnable(){
    public void run(){
    }
  }
);
}
