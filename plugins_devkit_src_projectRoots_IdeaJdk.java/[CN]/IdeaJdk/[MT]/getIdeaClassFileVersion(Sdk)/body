{
  int result=-1;
  File apiJar=getOpenApiJar(ideaSdk.getHomePath());
  if (apiJar == null)   return -1;
  final VirtualFile mainClassFile=JarFileSystem.getInstance().findFileByPath(FileUtil.toSystemIndependentName(apiJar.getPath()) + "!/com/intellij/psi/PsiManager.class");
  if (mainClassFile != null) {
    final BytePointer ptr;
    try {
      ptr=new BytePointer(mainClassFile.contentsToByteArray(),6);
      result=ClsUtil.readU2(ptr);
    }
 catch (    IOException e) {
    }
catch (    ClsFormatException e) {
    }
  }
  return result;
}
