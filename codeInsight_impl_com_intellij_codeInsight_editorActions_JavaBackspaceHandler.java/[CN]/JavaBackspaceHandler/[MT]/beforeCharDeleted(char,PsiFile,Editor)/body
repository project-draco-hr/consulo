{
  int offset=editor.getCaretModel().getOffset() - 1;
  myToDeleteGt=c == '<' && file instanceof PsiJavaFile && PsiUtil.isLanguageLevel5OrHigher(file) && JavaTypedHandler.isAfterClassLikeIdentifierOrDot(offset,editor);
}
