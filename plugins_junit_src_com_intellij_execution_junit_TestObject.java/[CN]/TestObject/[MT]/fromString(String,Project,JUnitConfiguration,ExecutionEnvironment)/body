{
  if (JUnitConfiguration.TEST_METHOD.equals(id))   return new TestMethod(project,configuration,environment);
  if (JUnitConfiguration.TEST_CLASS.equals(id))   return new TestClass(project,configuration,environment);
  if (JUnitConfiguration.TEST_PACKAGE.equals(id))   return new TestPackage(project,configuration,environment);
 else   if (JUnitConfiguration.TEST_DIRECTORY.equals(id)) {
    return new TestDirectory(project,configuration,environment);
  }
  if (JUnitConfiguration.TEST_PATTERN.equals(id)) {
    return new TestsPattern(project,configuration,environment);
  }
  return NOT_CONFIGURED;
}
