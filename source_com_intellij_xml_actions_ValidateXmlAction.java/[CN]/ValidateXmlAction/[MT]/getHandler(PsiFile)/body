{
  ValidateXmlActionHandler handler=new ValidateXmlActionHandler(true);
  handler.setErrorReporter(handler.new StdErrorReporter(file.getProject(),new Runnable(){
    public void run(){
      doRunAction(file);
    }
  }
));
  return handler;
}
