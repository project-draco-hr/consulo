{
  SchemesToImportPopup<T,E> popup=new SchemesToImportPopup<T,E>(getPanel()){
    @Override protected void onSchemeSelected(    final E scheme){
      if (scheme != null) {
        importScheme(scheme);
      }
    }
  }
;
  popup.show(mySchemesManager,collectCurrentSchemes());
}
