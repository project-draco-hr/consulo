{
  super.setUp();
  doTest("@interface A { int i() default 42; }\n class C { void m(int p) throws E { } }",null);
  LanguageLevelProjectExtension.getInstance(getProject()).setLanguageLevel(LanguageLevel.HIGHEST);
}
