{
  String result=FileUtil.getRelativePath(new File(myVirtualFile.getParent().getPath()),new File(modulePom.getPath()));
  result=FileUtil.toSystemIndependentName(result);
  result=result.substring(0,result.length() - ("/" + MavenConstants.POM_XML).length());
  return result;
}
