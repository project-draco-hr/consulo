{
  return nextStart == name.length() && patternIndex + i == myPattern.length - 1 && ' ' == myPattern[patternIndex + i] && (i == 1 && isWordStart(myPattern[patternIndex]) || i + nameIndex == name.length());
}
