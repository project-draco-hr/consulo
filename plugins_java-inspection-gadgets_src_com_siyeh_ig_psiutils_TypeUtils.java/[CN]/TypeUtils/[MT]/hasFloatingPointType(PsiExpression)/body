{
  if (expression == null) {
    return false;
  }
  final PsiType type=expression.getType();
  if (type == null) {
    return false;
  }
  return PsiType.FLOAT.equals(type) || PsiType.DOUBLE.equals(type);
}
