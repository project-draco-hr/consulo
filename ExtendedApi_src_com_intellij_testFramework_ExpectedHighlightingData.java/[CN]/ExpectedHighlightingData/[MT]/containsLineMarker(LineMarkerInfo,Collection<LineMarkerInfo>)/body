{
  for (  LineMarkerInfo markerInfo : where) {
    if (markerInfo.startOffset == info.startOffset && markerInfo.endOffset == info.endOffset && Comparing.equal(info.getLineMarkerTooltip(),markerInfo.getLineMarkerTooltip()))     return true;
  }
  return false;
}
