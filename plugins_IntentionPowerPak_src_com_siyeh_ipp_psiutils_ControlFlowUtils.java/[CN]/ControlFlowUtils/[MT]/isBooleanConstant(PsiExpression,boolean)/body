{
  if (!PsiUtil.isConstantExpression(test)) {
    return false;
  }
  final boolean value=((Boolean)ConstantExpressionUtil.computeCastTo(test,PsiType.BOOLEAN)).booleanValue();
  return value == val;
}
