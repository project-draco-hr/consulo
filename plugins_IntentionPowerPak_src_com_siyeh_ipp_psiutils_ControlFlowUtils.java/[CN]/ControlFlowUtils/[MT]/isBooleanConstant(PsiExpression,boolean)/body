{
  if (!PsiUtil.isConstantExpression(test)) {
    return false;
  }
  final Boolean constantValue=(Boolean)ConstantExpressionUtil.computeCastTo(test,PsiType.BOOLEAN);
  return constantValue != null && constantValue.booleanValue() == value;
}
