{
  removeAllChildren();
  if (groupByPackages) {
    buildPackages();
  }
 else {
    buildFiles();
  }
  setTreeModel(myTreeModel);
  if (myTreeModel != null)   myTreeModel.nodeStructureChanged(this);
}
