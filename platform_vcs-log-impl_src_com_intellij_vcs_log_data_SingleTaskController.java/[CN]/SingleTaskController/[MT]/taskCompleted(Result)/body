{
  if (result != null) {
    myResultHandler.consume(result);
  }
synchronized (LOCK) {
    if (myAwaitingRequests.isEmpty()) {
      myActive=false;
    }
 else {
      startNewBackgroundTask();
    }
  }
}
