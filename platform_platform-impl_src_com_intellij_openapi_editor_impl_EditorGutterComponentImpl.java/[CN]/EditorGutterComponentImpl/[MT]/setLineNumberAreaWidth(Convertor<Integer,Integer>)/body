{
  final int lineNumberAreaWidth=calculator.convert(myLineNumberConvertor.execute(endLineNumber()));
  if (myLineNumberAreaWidth != lineNumberAreaWidth) {
    myLineNumberAreaWidth=lineNumberAreaWidth;
    fireResized();
  }
}
