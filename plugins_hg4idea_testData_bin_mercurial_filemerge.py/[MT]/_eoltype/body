def _eoltype(data):
    'Guess the EOL type of a file'
    if ('\x00' in data):
        return None
    if ('\r\n' in data):
        return '\r\n'
    if ('\r' in data):
        return '\r'
    if ('\n' in data):
        return '\n'
    return None
