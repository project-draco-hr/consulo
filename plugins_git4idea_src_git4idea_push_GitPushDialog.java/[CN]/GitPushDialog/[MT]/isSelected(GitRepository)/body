{
  Collection<GitRepository> selectedRepositories=myListPanel.getSelectedRepositories();
  return selectedRepositories != null ? selectedRepositories.contains(repository) : myInitialPushSpecs.getSelectedRepositories().contains(repository);
}
