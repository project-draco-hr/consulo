{
  if (ideEntity != null) {
    return ProjectSystemId.IDE;
  }
 else   if (externalEntity != null) {
    return externalEntity.getOwner();
  }
 else {
    throw new RuntimeException(String.format("Can't detect owner system for the given arguments: external=%s, ide=%s",externalEntity,ideEntity));
  }
}
