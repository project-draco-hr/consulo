{
  final Ref<String> errorMessageHolder=new Ref<String>(){
    @Override public void set(    @Nullable String value){
      if (value != null) {
        errorCallback.consume(value);
      }
    }
  }
;
  refreshProject(project,externalSystemId,errorMessageHolder);
}
