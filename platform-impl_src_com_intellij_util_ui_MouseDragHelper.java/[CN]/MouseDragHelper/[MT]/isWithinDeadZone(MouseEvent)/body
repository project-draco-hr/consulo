{
  final Point screen=new RelativePoint(e).getScreenPoint();
  return Math.abs(myPressPointScreen.x - screen.x) < DRAG_START_DEADZONE && Math.abs(myPressPointScreen.y - screen.y) < DRAG_START_DEADZONE;
}
