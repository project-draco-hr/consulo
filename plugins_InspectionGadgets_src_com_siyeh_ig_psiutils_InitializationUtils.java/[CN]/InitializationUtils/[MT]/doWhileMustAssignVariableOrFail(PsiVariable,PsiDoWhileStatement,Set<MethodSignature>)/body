{
  final PsiExpression condition=doWhileStatement.getCondition();
  final PsiStatement body=doWhileStatement.getBody();
  return expressionMustAssignVariableOrFail(field,condition,checkedMethods) || statementMustAssignVariableOrFail(field,body,checkedMethods);
}
