{
  final int b1=di.readUnsignedByte();
  final int b2=di.readUnsignedByte();
  if (b1 == 0x47 && b2 == 0x49) {
    return readGif(di);
  }
  if (b1 == 0x89 && b2 == 0x50) {
    return readPng(di);
  }
  if (b1 == 0xff && b2 == 0xd8) {
    return readJpeg(di);
  }
  return null;
}
