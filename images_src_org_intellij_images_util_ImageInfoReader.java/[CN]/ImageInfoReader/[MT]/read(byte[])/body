{
  final DataInputStream is=new DataInputStream(new ByteArrayInputStream(data));
  try {
    return readFileData(is);
  }
 catch (  IOException e) {
    return null;
  }
 finally {
    try {
      is.close();
    }
 catch (    IOException e) {
    }
  }
}
