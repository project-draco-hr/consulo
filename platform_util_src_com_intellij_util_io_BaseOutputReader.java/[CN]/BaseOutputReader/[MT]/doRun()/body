{
  try {
    while (true) {
      boolean read=readAvailable();
      if (isStopped) {
        break;
      }
      TimeoutUtil.sleep(read ? 1 : 5);
    }
  }
 catch (  IOException e) {
    LOG.info(e);
  }
catch (  Exception e) {
    LOG.error(e);
  }
 finally {
    try {
      myReader.close();
    }
 catch (    IOException e) {
      LOG.error("Can't close stream",e);
    }
  }
}
