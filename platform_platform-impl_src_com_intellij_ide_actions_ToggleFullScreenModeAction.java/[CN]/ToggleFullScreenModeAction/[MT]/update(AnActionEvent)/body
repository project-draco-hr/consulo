{
  super.update(e);
  IdeFrameImpl frame=getFrame(e);
  final boolean operational=!SystemInfo.isMac && frame != null && frame.getGraphicsConfiguration().getDevice().isFullScreenSupported();
  e.getPresentation().setVisible(operational);
  e.getPresentation().setEnabled(operational);
}
