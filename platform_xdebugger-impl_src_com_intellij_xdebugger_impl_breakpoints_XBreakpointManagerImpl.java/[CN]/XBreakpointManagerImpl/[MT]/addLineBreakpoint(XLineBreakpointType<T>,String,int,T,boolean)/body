{
  ApplicationManager.getApplication().assertWriteAccessAllowed();
  XLineBreakpointImpl<T> breakpoint=new XLineBreakpointImpl<T>(type,this,properties,new LineBreakpointState<T>(true,type.getId(),fileUrl,line,temporary,myTime++));
  addBreakpoint(breakpoint,false,true);
  return breakpoint;
}
