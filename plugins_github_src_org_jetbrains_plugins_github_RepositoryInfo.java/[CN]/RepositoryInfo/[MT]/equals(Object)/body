{
  if (!(obj instanceof RepositoryInfo)) {
    return false;
  }
  final RepositoryInfo repositoryInfo=(RepositoryInfo)obj;
  return Comparing.equal(getName(),repositoryInfo.getName()) && Comparing.equal(getOwner(),repositoryInfo.getOwner());
}
