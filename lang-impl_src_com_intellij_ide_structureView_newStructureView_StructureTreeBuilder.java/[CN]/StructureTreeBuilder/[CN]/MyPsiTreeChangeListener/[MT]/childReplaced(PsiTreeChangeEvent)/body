{
  PsiElement oldChild=event.getOldChild();
  PsiElement newChild=event.getNewChild();
  if (oldChild instanceof PsiWhiteSpace && newChild instanceof PsiWhiteSpace)   return;
  if (oldChild instanceof PsiCodeBlock && newChild instanceof PsiCodeBlock)   return;
  childrenChanged();
}
