{
  myRangeMarker=persistent ? new PersistentLineMarker(model.getDocument(),start) : new RangeMarkerImpl(model.getDocument(),start,end);
  myTextAttributes=textAttributes;
  myTargetArea=target;
  myLayer=layer;
  myModel=(MarkupModelImpl)model;
}
