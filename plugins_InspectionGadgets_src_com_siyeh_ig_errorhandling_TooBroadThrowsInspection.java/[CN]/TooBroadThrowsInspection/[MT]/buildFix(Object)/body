{
  final Collection<PsiClass> maskedExceptions=(Collection<PsiClass>)infos[0];
  final Boolean originalNeeded=(Boolean)infos[1];
  return new AddThrowsClauseFix(maskedExceptions,originalNeeded.booleanValue());
}
