{
  final ActionCallback callback=new ActionCallback();
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      removeDeferredNow();
      callback.setDone();
    }
  }
);
  return callback;
}
