{
  final Insets insets=getLayoutInsets();
  comp.setBounds(insets.left + INNER + xAddin,yComp + 1,getWidth() - insets.left - insets.right- INNER * 2 - xAddin,getHeight() - insets.bottom - yComp- 2);
}
