def domain(author):
    'get domain of author, or empty string if none.'
    f = author.find('@')
    if (f == (-1)):
        return ''
    author = author[(f + 1):]
    f = author.find('>')
    if (f >= 0):
        author = author[:f]
    return author
