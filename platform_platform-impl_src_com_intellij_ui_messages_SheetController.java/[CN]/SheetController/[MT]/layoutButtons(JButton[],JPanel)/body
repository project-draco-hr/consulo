{
  for (int i=0; i < buttons.length; i++) {
    panel.add(buttons[i]);
    buttons[i].repaint();
    Dimension size=buttons[i].getPreferredSize();
    buttons[i].setBounds(SHEET_WIDTH - (size.width + 10) * (buttons.length - i) - 15,SHEET_HEIGHT - 45,size.width,size.height);
  }
}
