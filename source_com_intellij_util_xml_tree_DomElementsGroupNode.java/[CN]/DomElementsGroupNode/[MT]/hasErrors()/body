{
  for (  DomElement domElement : myChildDescription.getValues(myParentElement)) {
    if (DomElementAnnotationsManager.getInstance(myParentElement.getManager().getProject()).getCachedProblemHolder(domElement).getProblems(domElement,true).size() > 0)     return true;
  }
  return false;
}
