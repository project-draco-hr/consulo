{
  return ourThreadExecutorsService.submit(new Runnable(){
    public void run(){
      try {
        action.run();
      }
 catch (      Throwable t) {
        LOG.error(t);
      }
 finally {
        Thread.interrupted();
      }
    }
  }
);
}
