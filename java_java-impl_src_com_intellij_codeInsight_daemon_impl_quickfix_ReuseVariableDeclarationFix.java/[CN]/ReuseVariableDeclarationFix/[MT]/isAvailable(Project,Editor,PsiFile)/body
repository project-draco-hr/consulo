{
  final PsiVariable previousVariable=findPreviousVariable();
  return myVariable != null && myVariable.isValid() && myVariable instanceof PsiLocalVariable && previousVariable != null && Comparing.equal(previousVariable.getType(),myVariable.getType()) && myIdentifier != null && myIdentifier.isValid() && myVariable.getManager().isInProject(myVariable);
}
