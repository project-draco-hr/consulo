{
  return mySettingsPane != null && (!mySettings.getLogin().equals(mySettingsPane.getLogin()) || !Comparing.strEqual(mySettings.getPassword(),mySettingsPane.getPassword()) || !mySettings.getHost().equals(mySettingsPane.getHost()));
}
