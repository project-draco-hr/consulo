{
  return mySettingsPane == null || !mySettings.getLogin().equals(mySettingsPane.getLogin()) || !mySettings.getPassword().equals(mySettingsPane.getPassword()) || !mySettings.getHost().equals(mySettingsPane.getHost());
}
