{
  SdkType sdkType=SdkType.findInstance(sdkTypeClass);
  if (sdkType == null) {
    return null;
  }
  for (  Sdk sdk : mySdks) {
    if (sdk.isBundled() && sdk.getSdkType() == sdkType) {
      return sdk;
    }
  }
  return null;
}
