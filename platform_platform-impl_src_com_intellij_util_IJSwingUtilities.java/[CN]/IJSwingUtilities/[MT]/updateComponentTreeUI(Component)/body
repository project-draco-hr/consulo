{
  if (c == null)   return;
  for (  Component component : uiTraverser().postOrderDfsTraversal(c)) {
    if (component instanceof JComponent)     ((JComponent)component).updateUI();
  }
  c.invalidate();
  c.validate();
  c.repaint();
}
