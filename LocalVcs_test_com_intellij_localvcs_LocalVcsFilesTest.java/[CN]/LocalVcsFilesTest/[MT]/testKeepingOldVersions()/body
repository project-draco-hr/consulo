{
  myVcs.createFile(p("file"),"content");
  myVcs.commit();
  myVcs.changeFile(p("file"),"new content");
  myVcs.commit();
  assertRevisionsContent(new String[]{"new content","content"},myVcs.getEntryHistory(p("file")));
}
