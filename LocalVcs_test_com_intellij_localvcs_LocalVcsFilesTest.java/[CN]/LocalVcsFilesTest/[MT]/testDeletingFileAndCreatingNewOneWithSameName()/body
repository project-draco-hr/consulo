{
  myVcs.createFile(fn("file"),"old");
  myVcs.commit();
  myVcs.deleteFile(fn("file"));
  myVcs.createFile(fn("file"),"new");
  myVcs.commit();
  assertRevisionsContent(new String[]{"new"},myVcs.getRevisions(fn("file")));
}
