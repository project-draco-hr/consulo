{
  myVcs.createFile(p("file"),"old");
  myVcs.commit();
  myVcs.deleteFile(p("file"));
  myVcs.createFile(p("file"),"new");
  myVcs.commit();
  assertRevisionsContent(new String[]{"new"},myVcs.getEntries(p("file")));
}
