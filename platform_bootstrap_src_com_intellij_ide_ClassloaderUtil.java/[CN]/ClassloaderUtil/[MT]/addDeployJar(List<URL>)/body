{
  File deployJar=new File("/System/Library/Java/Support/Deploy.bundle/Contents/Home/lib/deploy.jar");
  if (deployJar.exists()) {
    try {
      classpathElements.add(deployJar.toURI().toURL());
    }
 catch (    MalformedURLException ignore) {
    }
  }
}
