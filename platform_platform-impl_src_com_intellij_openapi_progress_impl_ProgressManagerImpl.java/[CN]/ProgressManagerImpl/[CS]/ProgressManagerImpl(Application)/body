{
  if (!application.isUnitTestMode() && !DISABLED) {
    new Thread(NAME){
      public void run(){
        while (true) {
          try {
            sleep(10);
          }
 catch (          InterruptedException ignored) {
          }
          ourNeedToCheckCancel=true;
        }
      }
    }
.start();
  }
}
