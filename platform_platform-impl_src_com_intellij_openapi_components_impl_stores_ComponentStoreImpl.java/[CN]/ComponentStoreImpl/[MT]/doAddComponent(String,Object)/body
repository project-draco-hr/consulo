{
  if (myComponents.containsKey(componentName)) {
    LOG.error("Conflicting component name '" + componentName + "': "+ myComponents.get(componentName).getClass()+ " and "+ component.getClass());
  }
  myComponents.put(componentName,component);
}
