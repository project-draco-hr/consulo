{
  if (ReflectionCache.isAssignable(myFilter,hintClass))   return true;
  if (hintClass == PsiClass.class) {
    return ReflectionCache.isAssignable(myFilter,JspClassLevelDeclarationStatement.class);
  }
  return false;
}
