def loaditer(f):
    'Yield the dictionary objects generated by p4'
    try:
        while True:
            d = marshal.load(f)
            if (not d):
                break
            yield d
    except EOFError:
        pass
