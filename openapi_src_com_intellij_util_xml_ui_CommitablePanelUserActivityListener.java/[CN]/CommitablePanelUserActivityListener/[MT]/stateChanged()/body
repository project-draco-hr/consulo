{
  if (!myResetting) {
    ApplicationManager.getApplication().invokeLater(new Runnable(){
      public void run(){
        myResetting=true;
        try {
          applyChanges();
        }
  finally {
          myResetting=false;
        }
        doAfterApply();
      }
    }
);
  }
}
