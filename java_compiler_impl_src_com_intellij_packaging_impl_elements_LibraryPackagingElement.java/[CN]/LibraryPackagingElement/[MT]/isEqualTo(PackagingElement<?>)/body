{
  if (!(element instanceof LibraryPackagingElement)) {
    return false;
  }
  LibraryPackagingElement packagingElement=(LibraryPackagingElement)element;
  return myLevel != null && myName != null && myLevel.equals(packagingElement.getLevel()) && myName.equals(packagingElement.getName());
}
