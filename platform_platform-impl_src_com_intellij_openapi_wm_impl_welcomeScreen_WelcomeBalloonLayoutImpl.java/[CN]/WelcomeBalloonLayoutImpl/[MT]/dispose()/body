{
  if (myPopupBalloon != null) {
    for (    Disposable disposable : new ArrayList<Disposable>(myDisposableList)) {
      Disposer.dispose(disposable);
    }
    Disposer.dispose(myPopupBalloon);
    myPopupBalloon=null;
  }
}
