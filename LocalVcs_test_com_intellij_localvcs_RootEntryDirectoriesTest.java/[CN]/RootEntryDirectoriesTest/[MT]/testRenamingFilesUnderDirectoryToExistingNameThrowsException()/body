{
  root.doCreateDirectory(p("dir"));
  root.doCreateFile(p("dir/file1"),null);
  root.doCreateFile(p("dir/file2"),null);
  try {
    root.doRename(p("dir/file1"),"file2");
    fail();
  }
 catch (  LocalVcsException e) {
  }
}
