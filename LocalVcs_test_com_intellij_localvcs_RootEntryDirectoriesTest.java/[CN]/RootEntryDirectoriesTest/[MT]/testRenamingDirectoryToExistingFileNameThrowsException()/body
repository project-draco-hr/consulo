{
  root.doCreateDirectory(p("dir1"));
  root.doCreateDirectory(p("dir1/dir2"));
  root.doCreateFile(p("dir1/file"),null);
  try {
    root.doRename(p("dir1/dir2"),"file");
    fail();
  }
 catch (  LocalVcsException e) {
  }
}
