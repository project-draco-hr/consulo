{
  final CachedValue<DomFileElementImpl> value=xmlFile.getManager().getCachedValuesManager().createCachedValue(getOrCreateCachedValueProvider(xmlFile),false);
  xmlFile.putUserData(CACHED_FILE_ELEMENT_VALUE,value);
  return value;
}
