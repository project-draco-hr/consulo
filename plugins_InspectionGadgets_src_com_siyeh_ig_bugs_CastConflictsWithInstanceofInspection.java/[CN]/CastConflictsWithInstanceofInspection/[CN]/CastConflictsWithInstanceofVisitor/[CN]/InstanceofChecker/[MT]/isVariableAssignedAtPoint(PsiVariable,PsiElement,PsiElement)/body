{
  if (context == null) {
    return false;
  }
  final PsiElement directChild=getDirectChildWhichContainsElement(context,point);
  if (directChild == null) {
    return false;
  }
  final PsiElement[] children=context.getChildren();
  for (  PsiElement child : children) {
    if (child == directChild) {
      return isVariableAssignedAtPoint(variable,directChild,point);
    }
    if (VariableAccessUtils.variableIsAssigned(variable,child)) {
      return true;
    }
  }
  return false;
}
