{
  final PsiElement target=referenceExpression.resolve();
  if (!(target instanceof PsiVariable)) {
    return false;
  }
  final PsiVariable variable=(PsiVariable)target;
  return isVariableAssignedAtPoint(variable,element,referenceExpression);
}
