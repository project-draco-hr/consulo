{
  final VcsFileRevision revision=((VcsFileRevision)value);
  append(revision.getRevisionNumber().asString() + " " + DATE_FORMAT.format(revision.getRevisionDate())+ " "+ revision.getAuthor(),SimpleTextAttributes.SIMPLE_CELL_ATTRIBUTES);
}
