{
  if (!myRef.isValid()) {
    return false;
  }
  PsiManager manager=file.getManager();
  if (!manager.isInProject(file)) {
    return false;
  }
  if (getClassesToImport().isEmpty()) {
    return false;
  }
  return true;
}
