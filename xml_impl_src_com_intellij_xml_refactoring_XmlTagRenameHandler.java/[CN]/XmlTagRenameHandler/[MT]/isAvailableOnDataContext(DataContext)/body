{
  final PsiElement element=getElement(dataContext);
  if (PsiElementRenameHandler.isVetoed(element))   return false;
  return PlatformPatterns.psiElement().withParent(XmlTag.class).accepts(element) && isDeclarationOutOfProjectOrAbsent(element.getProject(),dataContext);
}
