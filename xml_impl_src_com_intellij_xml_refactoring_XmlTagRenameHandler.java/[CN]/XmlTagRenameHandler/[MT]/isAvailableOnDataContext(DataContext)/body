{
  final PsiElement element=getElement(dataContext);
  return StandardPatterns.psiElement().withParent(XmlTag.class).accepts(element) && isDeclarationOutOfProjectOrAbsent(element.getProject(),dataContext);
}
