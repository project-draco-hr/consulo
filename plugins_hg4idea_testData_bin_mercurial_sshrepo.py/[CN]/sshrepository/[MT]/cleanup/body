def cleanup(self):
    try:
        self.pipeo.close()
        self.pipei.close()
        for l in self.pipee:
            self.ui.status(_('remote: '), l)
        self.pipee.close()
    except:
        pass
