def readerr(self):
    while 1:
        size = util.fstat(self.pipee).st_size
        if (size == 0):
            break
        l = self.pipee.readline()
        if (not l):
            break
        self.ui.status(_('remote: '), l)
