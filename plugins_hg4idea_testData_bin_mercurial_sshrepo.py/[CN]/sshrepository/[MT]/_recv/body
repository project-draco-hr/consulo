def _recv(self):
    l = self.pipei.readline()
    self.readerr()
    try:
        l = int(l)
    except:
        self.abort(error.ResponseError(_('unexpected response:'), l))
    return self.pipei.read(l)
