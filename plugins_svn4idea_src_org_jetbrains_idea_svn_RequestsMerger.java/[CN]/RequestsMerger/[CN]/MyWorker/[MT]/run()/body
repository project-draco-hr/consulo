{
  LOG.debug("worker: started refresh");
  try {
    doAction(MyAction.start);
    myRunnable.run();
synchronized (myLock) {
      myInitialized=true;
    }
  }
  finally {
    doAction(MyAction.finish);
  }
}
