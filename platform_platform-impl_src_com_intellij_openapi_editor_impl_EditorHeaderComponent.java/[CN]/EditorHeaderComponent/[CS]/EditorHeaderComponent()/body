{
  super(new BorderLayout(0,0));
  GRADIENT_C1=new JBColor(getBackground(),JBColor.background());
  GRADIENT_C2=new JBColor(new Color(Math.max(0,GRADIENT_C1.getRed() - 0x18),Math.max(0,GRADIENT_C1.getGreen() - 0x18),Math.max(0,GRADIENT_C1.getBlue() - 0x18)),Gray._75);
}
