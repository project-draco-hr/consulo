{
  if (myBuffers != null) {
    for (int i=0; i < myBuffers.length; i++) {
      MappedBufferWrapper buffer=myBuffers[i];
      if (buffer != null) {
        buffer.dispose();
        myBuffers[i]=null;
      }
    }
  }
}
