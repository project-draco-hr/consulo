{
  if (myUpToDateHash == null) {
    if (hash != null) {
      if (!phisicalContentNeedsSave()) {
        myUpToDateHash=hash;
        return false;
      }
 else {
        return true;
      }
    }
 else {
      return true;
    }
  }
 else {
    return !myUpToDateHash.equals(hash);
  }
}
