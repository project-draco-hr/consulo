def getcommit(self, rev):
    certs = self.mtngetcerts(rev)
    return commit(author=certs['author'], date=util.datestr(util.strdate(certs['date'], '%Y-%m-%dT%H:%M:%S')), desc=certs['changelog'], rev=rev, parents=self.mtnrun('parents', rev).splitlines(), branch=certs['branch'])
