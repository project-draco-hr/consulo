{
  return new FileContent(f){
    @Override public byte[] getPhysicalBytes() throws IOException {
      return f.contentsToByteArray();
    }
    @Override public long getPhysicalLength() throws IOException {
      return getPhysicalBytes().length;
    }
  }
;
}
