{
  if (onNewLine) {
    final int spaces=space ? 1 : 0;
    myResult=SpaceProperty.createDependentLFProperty(spaces,spaces,dependance,mySettings.KEEP_LINE_BREAKS,mySettings.KEEP_BLANK_LINES_IN_CODE);
  }
 else {
    createSpaceInCode(space);
  }
}
