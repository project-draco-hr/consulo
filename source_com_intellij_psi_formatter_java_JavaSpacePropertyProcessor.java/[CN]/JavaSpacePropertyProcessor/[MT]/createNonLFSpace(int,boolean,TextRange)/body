{
  final ASTNode prev=getPrevElementType(myChild2);
  if (prev != null && prev.getElementType() == ElementType.END_OF_LINE_COMMENT) {
    return SpaceProperty.createSpaceProperty(0,Integer.MAX_VALUE,1,mySettings.KEEP_LINE_BREAKS,mySettings.KEEP_BLANK_LINES_IN_CODE);
  }
 else   if (dependantRange != null) {
    return SpaceProperty.createDependentLFProperty(spaces,spaces,dependantRange,mySettings.KEEP_LINE_BREAKS,mySettings.KEEP_BLANK_LINES_IN_CODE);
  }
 else {
    return SpaceProperty.createSpaceProperty(spaces,spaces,0,keepLineBreaks,mySettings.KEEP_BLANK_LINES_IN_CODE);
  }
}
