def _path(self, loc):
    p = self.config('paths', loc)
    if p:
        if ('%%' in p):
            self.warn(("(deprecated '%%' in path %s=%s from %s)\n" % (loc, p, self.configsource('paths', loc))))
            p = p.replace('%%', '%')
        p = util.expandpath(p)
    return p
