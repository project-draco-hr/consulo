def _readline(self, prompt=''):
    if sys.stdin.isatty():
        try:
            import readline
            readline.read_history_file
        except Exception:
            pass
    line = raw_input(prompt)
    if ((os.linesep == '\r\n') and line and (line[(-1)] == '\r')):
        line = line[:(-1)]
    return line
