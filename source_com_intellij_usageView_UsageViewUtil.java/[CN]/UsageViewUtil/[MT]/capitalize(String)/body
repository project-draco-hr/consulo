{
  if (s == null || s.length() == 0) {
    return s;
  }
  char c=Character.toUpperCase(s.charAt(0));
  if (s.length() == 1) {
    return "" + c;
  }
 else {
    return "" + c + s.substring(1);
  }
}
