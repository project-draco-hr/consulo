{
  if (file.isDirectory() || !file.isWritable() || FileTypeManager.getInstance().isFileIgnored(file)|| file.getFileType().isBinary()|| file.equals(project.getProjectFile())|| file.equals(project.getWorkspaceFile())) {
    return false;
  }
  return true;
}
