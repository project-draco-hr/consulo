{
  final MergeInfoHolder.ListMergeStatus status=getStatus(list,false);
  return ((status == null) || MergeInfoHolder.ListMergeStatus.ALIEN.equals(status)) ? null : Collections.singletonList(new Pair<String,SimpleTextAttributes>(status.getString(),status.getAttributes()));
}
