{
  myPanelWrapper=new JPanel(new GridBagLayout());
  myPanelWrapper.setPreferredSize(new Dimension(200,800));
  final GridBagConstraints gb=new GridBagConstraints(0,0,1,1,1,1,GridBagConstraints.NORTHEAST,GridBagConstraints.BOTH,new Insets(0,0,0,0),0,0);
  myPanelWrapper.add(myPanel,gb);
  final JScrollPane scrollPane=new JScrollPane(myPanelWrapper,JScrollPane.VERTICAL_SCROLLBAR_AS_NEEDED,JScrollPane.HORIZONTAL_SCROLLBAR_AS_NEEDED);
  final JPanel wrapper=new JPanel(new GridBagLayout());
  wrapper.add(scrollPane,gb);
  return wrapper;
}
