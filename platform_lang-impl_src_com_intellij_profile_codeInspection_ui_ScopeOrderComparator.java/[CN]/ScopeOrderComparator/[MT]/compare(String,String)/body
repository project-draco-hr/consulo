{
  final int key=getKey(scope1);
  final int key1=getKey(scope2);
  if (key >= 0) {
    if (key1 >= 0) {
      return key - key1;
    }
 else {
      return -1;
    }
  }
 else {
    if (key1 >= 0) {
      return 1;
    }
 else {
      return scope1.compareTo(scope2);
    }
  }
}
