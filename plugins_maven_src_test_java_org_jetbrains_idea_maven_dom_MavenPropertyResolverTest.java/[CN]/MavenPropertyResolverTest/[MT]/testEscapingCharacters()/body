{
  importProject("<groupId>test</groupId>" + "<artifactId>project</artifactId>" + "<version>1</version>"+ ""+ "<properties>"+ "  <foo>abc:def\\ghi</foo>"+ "</properties>");
  assertEquals("abc\\:def\\\\ghi",resolve(getModule("project"),"${foo}",new Properties(),null,":\\"));
}
