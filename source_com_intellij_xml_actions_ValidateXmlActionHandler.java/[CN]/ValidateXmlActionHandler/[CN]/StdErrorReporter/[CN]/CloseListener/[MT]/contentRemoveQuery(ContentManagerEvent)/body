{
  if (event.getContent() == myContent) {
    if (!myErrorsView.isProcessStopped()) {
      int result=Messages.showYesNoDialog("Validation is running. Terminate it?","Validation Is Running",Messages.getQuestionIcon());
      if (result != 0) {
        event.consume();
      }
    }
  }
}
