{
  return ApplicationManager.getApplication().runReadAction(new Computable<VcsRoot>(){
    @Nullable public VcsRoot compute(){
      VirtualFile vFile=ChangesUtil.findValidParent(file);
      if (vFile != null) {
        return getVcsRootObjectFor(vFile);
      }
      return null;
    }
  }
);
}
