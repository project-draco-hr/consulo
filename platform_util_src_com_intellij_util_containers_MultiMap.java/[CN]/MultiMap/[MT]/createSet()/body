{
  return new MultiMap<K,V>(){
    @Override protected Collection<V> createCollection(){
      return new SmartHashSet<V>();
    }
    @Override protected Collection<V> createEmptyCollection(){
      return Collections.emptySet();
    }
    @Override protected Map<K,Collection<V>> createMap(){
      return new THashMap<K,Collection<V>>();
    }
  }
;
}
