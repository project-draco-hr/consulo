{
  return new MultiMap<K,V>(){
    @NotNull @Override protected Map<K,Collection<V>> createMap(){
      return new THashMap<K,Collection<V>>(strategy);
    }
    @NotNull @Override protected Collection<V> createCollection(){
      return new SmartList<V>();
    }
  }
;
}
