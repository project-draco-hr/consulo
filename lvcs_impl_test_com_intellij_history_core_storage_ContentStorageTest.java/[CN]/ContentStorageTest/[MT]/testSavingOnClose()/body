{
  byte[] c=new byte[]{1,2,3};
  int id=s.store(c);
  s.close();
  IContentStorage another=createStorage();
  try {
    assertArrayEquals(c,another.load(id));
  }
  finally {
    another.close();
  }
}
