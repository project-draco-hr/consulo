{
  ConditionCheckManager.getInstance(myModule.getProject()).getAssertTrueMethods().add(buildMethodConditionCheck("Assertions","assertTrue",ConditionChecker.Type.ASSERT_TRUE_METHOD,"public class Assertions { public static boolean assertTrue(boolean b) {if(!b) throw new Exception();} }"));
  doTest();
}
