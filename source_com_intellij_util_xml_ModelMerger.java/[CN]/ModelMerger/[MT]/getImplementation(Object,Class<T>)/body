{
  return element instanceof ModelMerger.MergedObject ? ((ModelMerger.MergedObject)element).findImplementation(clazz) : element != null && clazz.isAssignableFrom(element.getClass()) ? (T)element : null;
}
