{
  String s=usercode.toString().trim();
  if (!s.endsWith("*/"))   return false;
  int i=s.lastIndexOf("/**");
  if (i < 0)   return false;
  return s.substring(i,s.length() - 2).indexOf("*/") < 0;
}
