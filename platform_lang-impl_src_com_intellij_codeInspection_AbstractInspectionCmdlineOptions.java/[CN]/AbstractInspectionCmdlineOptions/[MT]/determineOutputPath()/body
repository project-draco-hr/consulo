{
  final String outputPathProperty=getOutputPathProperty();
  return outputPathProperty != null ? outputPathProperty : getOutputFormatProperty() == PlainTextFormatter.NAME ? null : getDefaultOutputPath();
}
