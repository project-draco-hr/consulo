def system_rcpath():
    path = []
    if (len(getattr(sys, 'argv', [])) > 0):
        path.extend(rcfiles((os.path.dirname(sys.argv[0]) + '/../etc/mercurial')))
    path.extend(rcfiles('/etc/mercurial'))
    return path
