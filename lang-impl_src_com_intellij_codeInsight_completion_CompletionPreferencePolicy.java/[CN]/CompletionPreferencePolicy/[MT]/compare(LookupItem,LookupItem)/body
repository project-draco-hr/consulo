{
  if (item1 == item2)   return 0;
  double priority1=item1.getPriority();
  double priority2=item2.getPriority();
  if (priority1 > priority2)   return -1;
  if (priority1 < priority2)   return 1;
  final WeighingComparable<LookupElement<?>,CompletionLocation> w1=WeighingService.weigh(CompletionRegistrar.PRESELECT_KEY,(LookupElement<?>)item2,myLocation);
  final WeighingComparable<LookupElement<?>,CompletionLocation> w2=WeighingService.weigh(CompletionRegistrar.PRESELECT_KEY,(LookupElement<?>)item1,myLocation);
  return w1.compareTo(w2);
}
