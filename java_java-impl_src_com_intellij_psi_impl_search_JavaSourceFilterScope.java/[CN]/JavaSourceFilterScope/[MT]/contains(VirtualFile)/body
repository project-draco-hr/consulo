{
  if (myDelegate != null && !myDelegate.contains(file)) {
    return false;
  }
  final FileType fileType=file.getFileType();
  return StdFileTypes.JAVA == fileType && myIndex.isInSourceContent(file) || StdFileTypes.CLASS == fileType && myIndex.isInLibraryClasses(file);
}
