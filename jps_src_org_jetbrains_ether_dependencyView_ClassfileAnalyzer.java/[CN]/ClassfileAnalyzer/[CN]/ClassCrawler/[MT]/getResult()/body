{
  final ClassRepr repr=takeIntoAccount ? new ClassRepr(access,sourceFile,fileName,name,signature,superClass,interfaces,nestedClasses,fields,methods) : null;
  if (repr != null) {
    repr.updateClassUsages(usages);
  }
  return new Pair<ClassRepr,Set<UsageRepr.Usage>>(repr,usages);
}
