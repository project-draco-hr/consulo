{
  if (myExecuteEnabled) {
    myToolProvider.execute(Collections.singletonList(myTargetOperation),DesignerBundle.message("command.tool_operation",myContext.getType()));
    if (myContext.isCreate()) {
      myArea.setSelection(myContext.getComponents());
    }
  }
}
