{
  return new PatternCondition<T>("inFxmlFile"){
    @Override public boolean accepts(    @NotNull T value,    ProcessingContext context){
      return JavaFxFileTypeFactory.isFxml(value.getContainingFile());
    }
  }
;
}
