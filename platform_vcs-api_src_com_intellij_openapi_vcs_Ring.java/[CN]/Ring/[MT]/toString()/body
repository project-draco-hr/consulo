{
  return "Ring{" + "myFreeNumbers=" + StringUtil.join(myFreeNumbers,new Function<T,String>(){
    @Override public String fun(    T t){
      return t.toString();
    }
  }
,",") + ", myFirst="+ myFirst+ ", myNextAvailable="+ myNextAvailable+ '}';
}
