{
  return ((PsiManagerEx)getElement().getManager()).getResolveCache().resolveWithCaching(this,new ResolveCache.PolyVariantResolver<MyReference>(){
    @Override public ResolveResult[] resolve(    MyReference myReference,    boolean incompleteCode){
      return resolveInner();
    }
  }
,false,incompleteCode);
}
