{
  StringBuilder builder=new StringBuilder();
  builder.append("PsiParameterStub[").append(myName).append(':').append(TypeInfo.createTypeText(getType(false))).append(']');
  return builder.toString();
}
