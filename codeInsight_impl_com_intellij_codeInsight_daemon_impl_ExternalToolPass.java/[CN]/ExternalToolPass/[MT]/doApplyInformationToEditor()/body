{
  List<HighlightInfo> infos=getHighlights();
  UpdateHighlightersUtil.setHighlightersToEditor(myProject,myDocument,myStartOffset,myEndOffset,infos,UpdateHighlightersUtil.EXTERNAL_TOOLS_HIGHLIGHTERS_GROUP);
  HighlightUtil.addErrorsToWolf(infos,myFile);
}
