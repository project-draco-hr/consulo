{
  includeScopeClass(scopeClass);
  if (scopeClass == PsiClass.class) {
    includeScopeClass(JspClassLevelDeclarationStatement.class);
  }
  myPosition=position;
  myPeer=new CompletionVariantPeerImpl(this);
}
