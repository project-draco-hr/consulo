{
  if (widthInColumns < 0) {
    Document document=myEditor.getDocument();
    widthInColumns=myRepresentationHelper.toVisualColumnSymbolsNumber(document.getCharsSequence(),myFoldRegion.getStartOffset(),myFoldRegion.getEndOffset(),startX);
  }
  return widthInColumns;
}
