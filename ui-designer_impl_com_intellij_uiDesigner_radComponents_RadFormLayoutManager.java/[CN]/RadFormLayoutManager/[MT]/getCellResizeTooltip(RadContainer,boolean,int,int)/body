{
  final String size=getUpdatedSize(container,isRow,cell,newSize).toString();
  return isRow ? UIDesignerBundle.message("tooltip.resize.row",cell,size) : UIDesignerBundle.message("tooltip.resize.column",cell,size);
}
