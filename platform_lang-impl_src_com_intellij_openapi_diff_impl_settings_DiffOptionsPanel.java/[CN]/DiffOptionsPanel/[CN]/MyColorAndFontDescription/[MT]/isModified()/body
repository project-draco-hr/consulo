{
  TextAttributes attrs=myDiffType.getTextAttributes(myScheme);
  return !Comparing.equal(myOriginalBackground,attrs.getBackgroundColor()) || !Comparing.equal(myOriginalStripebar,attrs.getErrorStripeColor());
}
