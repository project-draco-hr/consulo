{
  super.visitBinaryExpression(exp);
  if (!isSubtraction(exp)) {
    return;
  }
  final PsiMethod method=(PsiMethod)PsiTreeUtil.getParentOfType(exp,PsiMethod.class);
  if (!isCompareTo(method)) {
    return;
  }
  registerError(exp);
}
