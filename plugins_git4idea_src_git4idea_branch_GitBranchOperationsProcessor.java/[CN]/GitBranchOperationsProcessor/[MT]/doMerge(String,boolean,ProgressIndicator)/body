{
  Map<GitRepository,String> revisions=new HashMap<GitRepository,String>();
  for (  GitRepository repository : myRepositories) {
    revisions.put(repository,repository.getCurrentRevision());
  }
  new GitMergeOperation(myProject,myRepositories,branchName,localBranch,getCurrentBranchOrRev(),mySelectedRepository,revisions,indicator).execute();
}
