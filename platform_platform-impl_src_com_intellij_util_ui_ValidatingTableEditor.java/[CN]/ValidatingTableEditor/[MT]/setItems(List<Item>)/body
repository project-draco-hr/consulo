{
  if (items.isEmpty()) {
    getTableModel().setItems(Collections.<Item>emptyList());
    myWarnings.clear();
  }
 else {
    getTableModel().setItems(new ArrayList<Item>(items));
    for (    Item item : items) {
      myWarnings.add(null);
    }
  }
  updateButtons();
}
