{
  if (items.isEmpty()) {
    getTableModel().setItems(Collections.<Item>emptyList());
    myWarnings.clear();
  }
 else {
    myWarnings.clear();
    for (    Item item : items) {
      myWarnings.add(null);
    }
    getTableModel().setItems(new ArrayList<Item>(items));
  }
  updateButtons();
}
