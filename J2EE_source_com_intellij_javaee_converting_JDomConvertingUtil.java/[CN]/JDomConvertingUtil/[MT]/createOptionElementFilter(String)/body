{
  return new Condition<Element>(){
    public boolean value(    final Element element){
      return OPTION_ELEMENT.equals(element.getName()) && optionName.equals(element.getAttributeValue(NAME_ATTRIBUTE));
    }
  }
;
}
