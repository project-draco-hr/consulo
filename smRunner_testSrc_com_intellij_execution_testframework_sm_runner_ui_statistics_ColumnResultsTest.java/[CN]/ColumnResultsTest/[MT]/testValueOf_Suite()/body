{
  assertEquals("<underfined>",myColumn.valueOf(mySuite));
  mySuite.setStarted();
  assertEquals("<underfined>",myColumn.valueOf(mySuite));
  createTestProxy(mySuite);
  assertEquals("<underfined>",myColumn.valueOf(mySuite));
  mySuite.setFinished();
  assertEquals("<underfined>",myColumn.valueOf(mySuite));
}
