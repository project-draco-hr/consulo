{
  StringBuffer sb=new StringBuffer(s);
  for (int i=0; i < sb.length(); i++) {
switch (sb.charAt(i)) {
case '\\':
case ' ':
      sb.insert(i,'\\');
    i++;
  break;
case '\n':
sb.replace(i,i + 1,"\\n");
i++;
break;
case '\t':
sb.replace(i,i + 1,"\\t");
i++;
break;
}
}
return sb.toString();
}
