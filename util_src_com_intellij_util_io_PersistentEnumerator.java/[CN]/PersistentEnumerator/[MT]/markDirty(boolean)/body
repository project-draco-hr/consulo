{
  if (myDirty) {
    if (!dirty) {
      myStorage.putInt(0,CORRECTLY_CLOSED_MAGIC);
      myDirty=false;
    }
  }
 else {
    if (dirty) {
      myStorage.putInt(0,DIRTY_MAGIC);
      myDirty=true;
    }
  }
}
