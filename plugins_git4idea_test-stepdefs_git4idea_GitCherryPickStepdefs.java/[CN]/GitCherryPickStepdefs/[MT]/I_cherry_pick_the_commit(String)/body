{
  CommitDetails realCommit=virtualCommits.getRealCommit(hash);
  new GitCherryPicker(myProject,myGit,myPlatformFacade,mySettings.isAutoCommitOnCherryPick()).cherryPick(Collections.singletonMap(myRepository,Collections.singletonList(mockCommit(realCommit.getHash(),realCommit.getMessage()))));
}
