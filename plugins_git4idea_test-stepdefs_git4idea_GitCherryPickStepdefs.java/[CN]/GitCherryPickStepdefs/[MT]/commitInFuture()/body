{
  myVcsHelper.registerHandler(new MockVcsHelper.CommitHandler(){
    @Override public boolean commit(    String commitMessage){
      git(String.format("commit -am '%s'",commitMessage));
      return true;
    }
  }
);
}
