{
  final TestConsoleProperties consoleProperties=new SMTRunnerConsoleProperties(config,testFrameworkName);
  return attachRunner(testFrameworkName,processHandler,consoleProperties,commandLineState.getRunnerSettings(),commandLineState.getConfigurationSettings());
}
