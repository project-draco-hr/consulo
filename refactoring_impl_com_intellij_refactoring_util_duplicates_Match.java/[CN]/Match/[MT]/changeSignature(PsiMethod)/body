{
  final ArrayList<ParameterInfo> newParameters=patchParams(psiMethod);
  final ChangeSignatureProcessor csp=new ChangeSignatureProcessor(psiMethod.getProject(),psiMethod,false,null,psiMethod.getName(),psiMethod.getReturnType(),newParameters.toArray(new ParameterInfo[newParameters.size()]));
  csp.run();
}
