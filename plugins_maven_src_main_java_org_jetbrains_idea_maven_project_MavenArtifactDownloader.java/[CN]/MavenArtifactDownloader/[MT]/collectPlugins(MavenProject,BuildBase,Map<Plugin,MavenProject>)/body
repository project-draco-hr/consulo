{
  if (build != null) {
    final List<Plugin> list=(List<Plugin>)build.getPlugins();
    if (list != null) {
      for (      Plugin plugin : list) {
        plugins.put(plugin,mavenProject);
      }
    }
  }
}
