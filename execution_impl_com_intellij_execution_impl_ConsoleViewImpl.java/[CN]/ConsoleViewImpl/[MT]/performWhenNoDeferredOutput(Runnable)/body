{
  if (!hasDeferredOutput()) {
    runnable.run();
  }
 else {
    mySpareTimeAlarm.addRequest(new Runnable(){
      public void run(){
        performWhenNoDeferredOutput(runnable);
      }
    }
,100);
  }
}
