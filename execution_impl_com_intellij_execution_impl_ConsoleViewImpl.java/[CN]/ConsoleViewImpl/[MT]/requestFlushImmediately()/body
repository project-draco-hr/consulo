{
  if (myEditor != null) {
    myFlushAlarm.addRequest(new Runnable(){
      public void run(){
        flushDeferredText();
      }
    }
,0,ModalityState.stateForComponent(myEditor.getComponent()));
  }
}
