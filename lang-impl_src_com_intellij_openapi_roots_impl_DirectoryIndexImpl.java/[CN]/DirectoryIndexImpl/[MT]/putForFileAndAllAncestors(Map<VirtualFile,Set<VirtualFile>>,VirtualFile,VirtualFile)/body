{
  while (true) {
    Set<VirtualFile> set=map.get(file);
    if (set == null) {
      set=new HashSet<VirtualFile>();
      map.put(file,set);
    }
    set.add(value);
    file=file.getParent();
    if (file == null)     break;
  }
}
