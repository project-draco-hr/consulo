{
  myConnection.deliverImmediately();
  if (myInitialized && PendingEventDispatcher.isDispatchingAnyEvent()) {
    VirtualFileManager.getInstance().dispatchPendingEvent(myVirtualFileListener);
  }
}
