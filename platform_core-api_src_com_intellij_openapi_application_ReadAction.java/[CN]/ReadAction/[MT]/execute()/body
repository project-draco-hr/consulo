{
  final RunResult<T> result=new RunResult<T>(this);
  return ApplicationManager.getApplication().runReadAction(new Computable<RunResult<T>>(){
    @Override public RunResult<T> compute(){
      return result.run();
    }
  }
);
}
