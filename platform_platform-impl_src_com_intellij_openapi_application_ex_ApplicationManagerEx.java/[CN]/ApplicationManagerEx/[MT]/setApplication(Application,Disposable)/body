{
  final Application old=ourApplication;
  Disposer.register(parent,new Disposable(){
    public void dispose(){
      if (old != null) {
        setApplication(old);
      }
    }
  }
);
  setApplication(instance);
}
