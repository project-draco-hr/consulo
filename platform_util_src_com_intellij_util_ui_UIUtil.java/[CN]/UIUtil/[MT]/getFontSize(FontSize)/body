{
  int defSize=getLabelFont().getSize();
switch (size) {
case TREE:
    return Math.max(defSize - 2f,12f);
case SMALL:
  return Math.max(defSize - 2f,11f);
case MINI:
return defSize - 4f;
default :
return defSize;
}
}
