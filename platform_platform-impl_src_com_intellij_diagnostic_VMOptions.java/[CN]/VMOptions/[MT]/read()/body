{
  File file=getFile();
  if (file == null)   return null;
  try {
    String content=FileUtil.loadFile(file);
    if (isMacOs()) {
      content=extractMacOsVMOptions(content);
    }
    return content;
  }
 catch (  IOException e) {
    LOG.info(e);
    return null;
  }
}
