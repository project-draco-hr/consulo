{
  int c=0;
  while (c != -1) {
    skipToChar(s.charAt(0));
    int i=1;
    do {
      if (i == s.length())       return;
      c=read();
      if (c != s.charAt(i++)) {
        break;
      }
    }
 while (c != -1);
    peekc=NEED_CHAR;
  }
}
