{
  final Element parent=attribute.getParent();
  final String attrName=attribute.getName();
  if (parent.getName().equals(EnvironmentVariablesComponent.ENV) && (attrName.equals(EnvironmentVariablesComponent.NAME) || attrName.equals(EnvironmentVariablesComponent.VALUE))) {
    return true;
  }
  return false;
}
