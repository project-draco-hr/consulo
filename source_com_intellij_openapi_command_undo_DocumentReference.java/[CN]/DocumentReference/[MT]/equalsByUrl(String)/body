{
  VirtualFile file=getFile();
  if (file == null)   return false;
  if (file.isValid())   return false;
  String url1=getUrl();
  if ((url1 == null) || (url == null))   return false;
  if (SystemInfo.isFileSystemCaseSensitive)   return url.equals(url1);
 else   return url.compareToIgnoreCase(url1) == 0;
}
