{
  if (!myCanPastThreshold) {
    myCanPastThreshold=Math.abs(myStartScreenX - myCurrentScreenX) > DRAG_THRESHOLD || Math.abs(myStartScreenY - myCurrentScreenY) > DRAG_THRESHOLD;
  }
  return myCanPastThreshold;
}
