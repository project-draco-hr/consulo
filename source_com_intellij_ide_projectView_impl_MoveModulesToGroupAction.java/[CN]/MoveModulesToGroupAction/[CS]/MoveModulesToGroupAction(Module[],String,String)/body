{
  super(title);
  myModules=modules;
  myGroupName=groupName;
  Presentation presentation=getTemplatePresentation();
  String description=groupName == null ? "Create new module group" : "Move " + whatToMove() + " to the group '"+ groupName+ "'";
  presentation.setDescription(description);
}
