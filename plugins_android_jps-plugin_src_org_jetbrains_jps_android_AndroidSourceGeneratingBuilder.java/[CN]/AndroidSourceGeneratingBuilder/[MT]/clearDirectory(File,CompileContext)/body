{
  if (!deleteAndMarkRecursively(dir,context)) {
    return false;
  }
  if (!dir.mkdirs()) {
    context.processMessage(new CompilerMessage(BUILDER_NAME,BuildMessage.Kind.ERROR,"Cannot create directory " + dir.getPath()));
    return false;
  }
  return true;
}
