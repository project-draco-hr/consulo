{
  int size=memoizedSerializedSize;
  if (size != -1)   return size;
  size=0;
  for (int i=0; i < toUpdate_.size(); i++) {
    size+=com.google.protobuf.CodedOutputStream.computeMessageSize(1,toUpdate_.get(i));
  }
{
    int dataSize=0;
    for (int i=0; i < toRemove_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeBytesSizeNoTag(toRemove_.getByteString(i));
    }
    size+=dataSize;
    size+=1 * getToRemoveList().size();
  }
  memoizedSerializedSize=size;
  return size;
}
