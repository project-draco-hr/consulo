{
  if (!hasGlobalOptionsPath()) {
    return false;
  }
  for (int i=0; i < getPathVariableCount(); i++) {
    if (!getPathVariable(i).isInitialized()) {
      return false;
    }
  }
  return true;
}
