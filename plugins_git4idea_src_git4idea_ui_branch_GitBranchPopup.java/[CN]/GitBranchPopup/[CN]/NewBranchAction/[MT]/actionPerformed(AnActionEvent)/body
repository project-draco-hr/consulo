{
  final String name=Messages.showInputDialog(myProject,"Enter name of new branch","Checkout New Branch",Messages.getQuestionIcon(),"",GitNewBranchNameValidator.newInstance(myRepository));
  if (name != null) {
    new GitBranchOperationsProcessor(myProject,myRepository).checkoutNewBranch(name);
  }
}
