{
  final String name=Messages.showInputDialog(myProject,"Enter name of new branch","Checkout new branch",Messages.getQuestionIcon(),"",GitRefNameValidator.getInstance());
  if (name != null) {
    new GitBranchOperationsProcessor(myProject,myRepository).checkoutNewBranch(name);
  }
}
