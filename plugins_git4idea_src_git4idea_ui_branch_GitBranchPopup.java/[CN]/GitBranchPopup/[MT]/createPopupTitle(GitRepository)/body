{
  String title="Git Branches";
  if (myRepositoryManager.moreThanOneRoot() && (myMultiRootBranchConfig.diverged() || myVcsSettings.getSyncSetting() == GitSyncRepoSetting.DONT)) {
    title+=" in " + GitUIUtil.getShortRepositoryName(currentRepository);
  }
  return title;
}
