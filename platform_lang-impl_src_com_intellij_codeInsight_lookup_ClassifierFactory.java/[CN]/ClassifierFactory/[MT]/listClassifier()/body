{
  return new Classifier<T>(){
    @Override public void addElement(    T t){
    }
    @Override public Iterable<T> classify(    List<T> source){
      return source;
    }
    @Override public void describeItems(    LinkedHashMap<T,StringBuilder> map){
    }
  }
;
}
