{
  if (importerPreferences == null) {
    importerPreferences=getProject().getComponent(MavenWorkspacePreferencesComponent.class).getState().myImporterPreferences.clone();
  }
  return importerPreferences;
}
