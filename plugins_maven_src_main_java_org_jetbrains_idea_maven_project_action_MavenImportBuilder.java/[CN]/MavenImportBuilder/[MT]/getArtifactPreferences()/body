{
  if (artifactPreferences == null) {
    artifactPreferences=getProject().getComponent(MavenWorkspacePreferencesComponent.class).getState().myArtifactPreferences.clone();
  }
  return artifactPreferences;
}
