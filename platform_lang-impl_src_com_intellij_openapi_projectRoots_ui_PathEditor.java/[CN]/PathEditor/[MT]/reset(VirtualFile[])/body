{
  keepSelectionState();
  clearList();
  myEnabled=files != null;
  if (myEnabled) {
    for (    VirtualFile file : files) {
      addElement(file);
    }
  }
  setModified(false);
  updateButtons();
}
