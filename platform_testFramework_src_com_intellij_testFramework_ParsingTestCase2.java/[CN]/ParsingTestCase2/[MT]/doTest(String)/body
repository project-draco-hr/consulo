{
  String name=getTestName(false);
  String text=loadFile(name + "." + myFileType.getDefaultExtension());
  myFile=createPsiFile(name,text);
  ensureParsed(myFile);
  assertEquals(text,myFile.getText());
  checkResult(name + suffix,myFile);
}
