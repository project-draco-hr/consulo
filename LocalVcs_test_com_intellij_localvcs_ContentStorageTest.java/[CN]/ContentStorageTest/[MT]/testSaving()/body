{
  byte[] c=new byte[]{1,2,3};
  s.storeContent(1,c);
  s.save();
  ContentStorage another=createStorage();
  try {
    assertEquals(c,another.loadContent(1));
  }
  finally {
    another.close();
  }
}
