{
  if (bounds.x < target.x) {
    feedback.addHorizontalLine(bounds.x - SNAP_LINE_BORDER,y,target.x + target.width - bounds.x + 2 * SNAP_LINE_BORDER);
  }
 else {
    feedback.addHorizontalLine(target.x - SNAP_LINE_BORDER,y,bounds.x + bounds.width - target.x + 2 * SNAP_LINE_BORDER);
  }
}
