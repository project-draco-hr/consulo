{
  VirtualFile[] files=library.getFiles(OrderRootType.CLASSES);
  if (files.length == 1 && (LibraryUtil.isClassAvailableInLibrary(files,"junit.framework.TestCase") || LibraryUtil.isClassAvailableInLibrary(library,"org.junit.Test"))) {
    return DependencyScope.TEST;
  }
  return null;
}
