{
  return new FileBasedIndex.InputFilter(){
    @Override public boolean acceptInput(    Project project,    VirtualFile file){
      if (file.getFileSystem() instanceof JarFileSystem) {
        return false;
      }
      return file.getFileType() == StdFileTypes.XML;
    }
  }
;
}
