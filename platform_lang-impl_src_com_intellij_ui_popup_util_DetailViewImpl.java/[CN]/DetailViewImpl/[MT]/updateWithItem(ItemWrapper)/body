{
  if (myWrapper != wrapper) {
    myWrapper=wrapper;
    if (wrapper != null) {
      wrapper.updateDetailView(this);
    }
 else {
      clearEditor();
      repaint();
    }
    revalidate();
  }
}
