{
  if ((myK == null || myK.equals(key)) && myAll == null) {
    myK=key;
    myV=value;
  }
 else {
    if (myAll == null) {
      myAll=new HashMap<String,V>();
      myAll.put(myK,myV);
    }
    myAll.put(key,value);
  }
}
