{
  updateState(state);
  if (isReloadNeeded()) {
    if (isFullReload()) {
      getModel().reloadModel(isFullReload());
    }
 else {
      if (state) {
        getModel().applySettings();
      }
 else {
        getModel().applyRemove();
      }
    }
  }
}
