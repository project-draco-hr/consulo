{
  updateState(state);
  if (isReloadNeeded()) {
    if (isFullReload()) {
      getModel().reloadModel();
    }
 else {
      if (state) {
        getModel().applySettings();
      }
 else {
        getModel().applyRemove();
      }
    }
  }
}
