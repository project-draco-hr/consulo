{
  String returnTypeText=StringUtil.notNullize(myMethod.getReturnTypeText());
  return JavaPsiFacade.getInstance(myProject).getElementFactory().createTypeCodeFragment(returnTypeText,myMethod.getMethod(),true,true);
}
