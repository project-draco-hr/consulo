{
  if (anonymousClass) {
    return "Method <code>#ref()</code>#loc is only used from an " + "anonymous class" + text;
  }
  return "Method <code>#ref()</code>#loc is only used from inner" + " class <code>" + text + "</code>";
}
