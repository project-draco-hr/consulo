{
  TextRange elem=e.getTextRange();
  if (elem.getLength() > 2) {
    return new SmartList<TextRange>(new TextRange(elem.getStartOffset() + 1,elem.getEndOffset() - 1));
  }
  return Collections.emptyList();
}
