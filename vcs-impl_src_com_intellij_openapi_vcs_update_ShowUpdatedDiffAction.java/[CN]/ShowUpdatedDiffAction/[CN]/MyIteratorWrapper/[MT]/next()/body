{
  final VirtualFilePointer pointer=myVfIterator.next();
  return new Change(new MyCheckpointContentRevision(pointer,myBeforeLoader,true),new MyCheckpointContentRevision(pointer,myAfterLoader,false));
}
