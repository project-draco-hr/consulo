{
  myIndicator.checkCanceled();
  final FileType fileType=t.getFileType();
  if (fileType == StdFileTypes.XML || fileType == StdFileTypes.XHTML || fileType == StdFileTypes.HTML || fileType == StdFileTypes.JSP || fileType == StdFileTypes.JSPX) {
    processXmlFile(t);
  }
  return true;
}
