{
  if (!isLastStep()) {
    final WizardStep nextStep=mySteps.get(getNextStep());
    if (!nextStep.preNextCheck()) {
      return;
    }
    nextStep.activate();
    nextStep.focus();
  }
  super.doNextAction();
}
