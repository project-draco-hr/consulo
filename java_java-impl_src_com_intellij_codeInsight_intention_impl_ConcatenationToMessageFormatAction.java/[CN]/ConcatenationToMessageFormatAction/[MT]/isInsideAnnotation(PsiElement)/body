{
  for (int i=0; i < 20 && element instanceof PsiBinaryExpression; i++) {
    element=element.getParent();
    if (element instanceof PsiNameValuePair || element instanceof PsiArrayInitializerMemberValue) {
      return true;
    }
  }
  return false;
}
