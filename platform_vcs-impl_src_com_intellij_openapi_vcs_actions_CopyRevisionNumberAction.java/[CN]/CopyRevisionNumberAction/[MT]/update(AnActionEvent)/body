{
  int corrected=myLineNumber;
  final boolean enabled=myLineNumber >= 0 && (corrected=myGetUpToDateLineNumber.getLineNumber(myLineNumber)) >= 0 && myAnnotation.getLineRevisionNumber(corrected) != null;
  e.getPresentation().setEnabled(enabled);
  e.getPresentation().setVisible(enabled);
}
