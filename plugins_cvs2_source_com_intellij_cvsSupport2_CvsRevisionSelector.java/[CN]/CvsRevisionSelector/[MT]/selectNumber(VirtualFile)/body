{
  final SelectFileVersionDialog selector=new SelectFileVersionDialog(PeerFactory.getInstance().getVcsContextFactory().createFilePathOn(file),myProject);
  selector.show();
  if (selector.isOK()) {
    return new CvsRevisionNumber(selector.getRevisionOrDate());
  }
 else {
    return null;
  }
}
