{
  final SelectFileVersionDialog selector=new SelectFileVersionDialog(VcsContextFactory.SERVICE.getInstance().createFilePathOn(file),myProject);
  selector.show();
  if (selector.isOK()) {
    return new CvsRevisionNumber(selector.getRevisionOrDate());
  }
 else {
    return null;
  }
}
