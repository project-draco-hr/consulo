{
  super.onActionPerformed(context,tabbedWindow,successfully,handler);
  CvsVcs2.getInstance(context.getProject()).getCheckoutProvider().refreshAfterCheckout(ProjectLevelVcsManager.getInstance(context.getProject()).getCompositeCheckoutListener(),mySelectedElements,myCheckoutDirectory,myUseAlternativeCheckoutPath);
}
