{
  if (myError != null) {
    return false;
  }
  while (myPaths.size() == 0 && !myStopped) {
    try {
      wait();
    }
 catch (    InterruptedException ignored) {
    }
  }
  return myPaths.size() > 0 || !myStopped;
}
