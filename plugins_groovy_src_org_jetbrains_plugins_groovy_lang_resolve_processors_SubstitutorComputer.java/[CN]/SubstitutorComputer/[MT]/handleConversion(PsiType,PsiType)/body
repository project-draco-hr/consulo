{
  final GroovyPsiElement context=myPlace;
  if (!TypesUtil.isAssignable(TypeConversionUtil.erasure(paramType),argType,context) && TypesUtil.isAssignableByMethodCallConversion(paramType,argType,context)) {
    return paramType;
  }
  return argType;
}
