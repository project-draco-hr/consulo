{
  final GroovyFilterLexer groovyFilterLexer=new GroovyFilterLexer(new GroovyLexer(),consumer);
  return new FilterLexer(groovyFilterLexer,new FilterLexer.SetFilter(GroovyTokenTypes.WHITE_SPACES_OR_COMMENTS));
}
