def _lookup(path):
    scheme = 'file'
    if path:
        c = path.find(':')
        if (c > 0):
            scheme = path[:c]
    thing = (schemes.get(scheme) or schemes['file'])
    try:
        return thing(path)
    except TypeError:
        return thing
