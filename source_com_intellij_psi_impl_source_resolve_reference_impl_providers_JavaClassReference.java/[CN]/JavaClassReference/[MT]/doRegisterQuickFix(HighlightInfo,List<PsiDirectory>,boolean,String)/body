{
  final CreateClassOrPackageIntentionAction action=new CreateClassOrPackageIntentionAction(writableDirectoryList,this,createJavaClass,extendClass);
  QuickFixAction.registerQuickFixAction(info,action);
  return action;
}
