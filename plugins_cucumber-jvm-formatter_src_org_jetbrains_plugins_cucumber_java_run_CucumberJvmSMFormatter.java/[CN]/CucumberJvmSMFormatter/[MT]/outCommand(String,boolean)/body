{
  if (currentScenario == null && waitForScenario) {
    queue.add(s);
  }
 else {
    try {
      appendable.append("\n");
      appendable.append(s);
      appendable.append("\n");
    }
 catch (    IOException ignored) {
    }
  }
}
