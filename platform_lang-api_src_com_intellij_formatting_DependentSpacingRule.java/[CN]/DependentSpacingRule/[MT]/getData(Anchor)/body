{
  if (!myData.containsKey(anchor)) {
    throw new IllegalArgumentException(String.format("No data is registered for the dependent spacing rule %s. Registered: %s",anchor,myData));
  }
  return (T)myData.get(anchor);
}
