{
  boolean computed=myComputed;
  T value=myValue;
  if (computed) {
    return value;
  }
synchronized (this) {
    computed=myComputed;
    value=myValue;
    if (!computed) {
      myValue=value=compute();
      myComputed=true;
    }
  }
  return value;
}
