{
  String name=file.getName();
  if (!name.isEmpty()) {
    if (namesEqual(fs,name,FSRecords.getNameSequence(id)))     return false;
  }
 else {
    if (areChildrenLoaded(id))     return false;
  }
  FSRecords.writeAttributesToRecord(id,parentId,attributes,name);
  return true;
}
