{
  PsiNamedElement variable=getVariable();
  if (variable == null) {
    LOG.error(myElementToRename);
    return "Rename";
  }
  return RefactoringBundle.message("renaming.0.1.to.2",UsageViewUtil.getType(variable),UsageViewUtil.getDescriptiveName(variable),variable.getName());
}
