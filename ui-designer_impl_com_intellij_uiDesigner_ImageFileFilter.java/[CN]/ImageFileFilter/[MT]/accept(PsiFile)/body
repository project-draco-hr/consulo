{
  final VirtualFile virtualFile=file.getVirtualFile();
  return virtualFile != null && myExtensions.contains(virtualFile.getExtension()) && (myModuleScope == null || myModuleScope.contains(virtualFile));
}
