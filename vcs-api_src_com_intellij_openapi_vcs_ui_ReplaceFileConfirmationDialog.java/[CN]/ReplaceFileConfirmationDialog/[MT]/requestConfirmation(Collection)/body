{
  if (modifiedFiles.isEmpty())   return true;
  return Messages.showDialog(createMessage(modifiedFiles),myActionName,new String[]{createOwerriteButtonName(modifiedFiles),getCancelButtonText()},0,Messages.getWarningIcon()) == DialogWrapper.OK_EXIT_CODE;
}
