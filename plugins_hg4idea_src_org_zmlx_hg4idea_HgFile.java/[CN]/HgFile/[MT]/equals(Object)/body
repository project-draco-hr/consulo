{
  if (object == this) {
    return true;
  }
  if (!(object instanceof HgFile)) {
    return false;
  }
  HgFile that=(HgFile)object;
  return new EqualsBuilder().append(vcsRoot,that.vcsRoot).append(file,that.file).isEquals();
}
