{
  if (InspectionGadgetsPlugin.TELEMETRY_ENABLED && listener == null) {
    final Application application=ApplicationManager.getApplication();
    final InspectionGadgetsPlugin plugin=(InspectionGadgetsPlugin)application.getComponent(INSPECTION_GADGETS_COMPONENT_NAME);
    listener=plugin.getTelemetry();
  }
}
