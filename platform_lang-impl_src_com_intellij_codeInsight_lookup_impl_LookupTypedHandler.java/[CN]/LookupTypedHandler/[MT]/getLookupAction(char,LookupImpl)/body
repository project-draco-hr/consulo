{
  final CharFilter.Result filtersDecision=getFiltersDecision(charTyped,lookup);
  if (filtersDecision != null) {
    return filtersDecision;
  }
  throw new AssertionError("Typed char not handler by char filter: c=" + charTyped + "; prefix="+ lookup.getCurrentItem()+ "; filters="+ Arrays.toString(getFilters()));
}
