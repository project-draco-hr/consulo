{
  myErrorViewStructure.addNavigatableMessage(groupName,navigatable,ErrorTreeElementKind.convertMessageFromCompilerErrorType(type),text,data,exportTextPrefix,rendererTextPrefix,data instanceof VirtualFile ? (VirtualFile)data : null);
  myBuilder.updateTree();
}
