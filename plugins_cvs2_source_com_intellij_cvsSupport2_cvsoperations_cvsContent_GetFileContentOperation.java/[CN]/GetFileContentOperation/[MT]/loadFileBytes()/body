{
  if (myState != LOADING) {
    LOG.error("state = " + String.valueOf(myState));
  }
  if (myReader.isEmpty()) {
    myState=DELETED;
    return null;
  }
 else {
    myState=SUCCESSFULLY_LOADED;
    return myReader.getReadContent();
  }
}
