{
  if (horizontal) {
    if (myCaptionColumnLayout == null) {
      myCaptionColumnLayout=new RadViewLayout(){
        @Override public EditOperation processChildOperation(        OperationContext context){
          if (context.isMove()) {
            return new HorizontalCaptionFlowBaseOperation((RadTableLayoutComponent)RadTableLayout.this.myContainer,myContainer,context,mainArea);
          }
          return null;
        }
      }
;
    }
    return myCaptionColumnLayout;
  }
  if (myCaptionRowLayout == null) {
    myCaptionRowLayout=new RadViewLayout(){
      @Override public EditOperation processChildOperation(      OperationContext context){
        if (context.isMove()) {
          return new VerticalCaptionFlowBaseOperation((RadTableLayoutComponent)RadTableLayout.this.myContainer,myContainer,context,mainArea);
        }
        return null;
      }
    }
;
  }
  return myCaptionRowLayout;
}
