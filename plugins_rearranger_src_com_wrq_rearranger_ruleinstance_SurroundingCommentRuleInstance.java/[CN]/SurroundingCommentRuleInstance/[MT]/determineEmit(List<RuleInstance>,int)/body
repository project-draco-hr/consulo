{
  if (match(resultRuleInstances,commentRule.getnPrecedingRulesToMatch(),-1,startIndex,commentRule.isAllPrecedingRules()) && match(resultRuleInstances,commentRule.getnSubsequentRulesToMatch(),+1,startIndex,commentRule.isAllSubsequentRules())) {
    setEmit(true);
  }
}
