{
  if (match(resultRuleInstances,myCommentRule.getnPrecedingRulesToMatch(),-1,startIndex,myCommentRule.isAllPrecedingRules()) && match(resultRuleInstances,myCommentRule.getnSubsequentRulesToMatch(),+1,startIndex,myCommentRule.isAllSubsequentRules())) {
    setEmit(true);
  }
}
