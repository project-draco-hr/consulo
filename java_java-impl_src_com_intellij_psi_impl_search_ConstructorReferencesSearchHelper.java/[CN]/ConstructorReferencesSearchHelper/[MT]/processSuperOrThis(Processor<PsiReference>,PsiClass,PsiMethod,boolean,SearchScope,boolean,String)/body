{
  return ApplicationManager.getApplication().runReadAction(new Computable<Boolean>(){
    public Boolean compute(){
      return processSuperOrThisInReadAction(inheritor,searchScope,superOrThisKeyword,isStrictSignatureSearch,constructor,constructorCanBeCalledImplicitly,processor);
    }
  }
);
}
