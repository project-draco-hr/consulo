{
  if (myEditorRow < 0) {
    return;
  }
  repaintRows(0,getModel().size() - 1,false);
  mySkipSelectionChange=true;
  try {
    getModel().removeElementAt(myEditorRow);
  }
  finally {
    mySkipSelectionChange=false;
  }
  myEditorRow=-1;
}
