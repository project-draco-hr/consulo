{
synchronized (myLock) {
    checkNotStarted();
    try {
      myProcess=startProcess();
      startHandlingStreams();
    }
 catch (    Throwable t) {
      myListeners.getMulticaster().startFailed(t);
    }
  }
}
