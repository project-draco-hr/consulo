{
  TextAttributes attributes=HighlightInfo.getAttributesByType(type);
  if (attributes == null || attributes.isEmpty() && type.getSeverity() == HighlightInfo.INFORMATION)   return false;
  return true;
}
