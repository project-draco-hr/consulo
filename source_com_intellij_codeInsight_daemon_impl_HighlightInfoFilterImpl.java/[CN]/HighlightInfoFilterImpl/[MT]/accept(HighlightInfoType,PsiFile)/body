{
  TextAttributes attributes=HighlightInfo.getAttributesByType(type);
  if (attributes == null || attributes.isEmpty() && type.getSeverity() == HighlightSeverity.INFORMATION)   return false;
  return true;
}
