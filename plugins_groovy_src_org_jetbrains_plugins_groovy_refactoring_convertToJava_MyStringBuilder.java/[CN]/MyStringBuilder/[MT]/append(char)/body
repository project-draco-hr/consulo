{
  if (innerLen == arr.length) {
    final char[] chars=new char[innerLen * 2];
    System.arraycopy(arr,0,chars,0,arr.length);
    arr=chars;
  }
  arr[innerLen]=c;
  innerLen++;
  realLength++;
  return this;
}
