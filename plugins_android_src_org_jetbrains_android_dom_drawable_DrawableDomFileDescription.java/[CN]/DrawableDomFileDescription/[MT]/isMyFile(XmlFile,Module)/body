{
  if (!super.isMyFile(file,module)) {
    return false;
  }
  final XmlTag rootTag=file.getRootTag();
  if (rootTag == null) {
    return false;
  }
  final String rootTagName=rootTag.getName();
  return !DrawableStateListDomFileDescription.SELECTOR_TAG_NAME.equals(rootTagName) && ArrayUtil.find(BitmapOrNinePatchDomFileDescription.ROOT_TAG_NAMES,rootTag) < 0;
}
