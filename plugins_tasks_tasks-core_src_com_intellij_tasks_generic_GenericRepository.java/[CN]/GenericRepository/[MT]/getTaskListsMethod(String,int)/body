{
  String requestUrl=getFullTasksUrl(query,max);
  final HttpMethod method=GenericRepositoryEditor.GET.equals(getTasksListMethodType()) ? new GetMethod(requestUrl) : getPostMethodFromURL(requestUrl);
  configureHttpMethod(method);
  return method;
}
